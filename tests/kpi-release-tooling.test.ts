import assert from 'node:assert/strict';import test from 'node:test';
import { authorRelease,backfillRelease } from '../scripts/kpi-release.mjs';
const latest:any={version:'old',release:{approved_head:'x',merge_commit:'y'},metrics:{}};const history:any[]=[];
const input:any={snapshot_date:'2026-08-21',version:'GOV',block:'GOV',title:'Gate',summary:'real',backend_tests:'10/10',frontend_smoke_tests:'20/20',flutter_tests:'30/30',blocks_completed:151,pr:null,merge_commit:null,approved_head:null};
test('author escreve snapshot+append com os dois SHAs nulos',()=>{const out=authorRelease(latest,history,input);assert.equal(out.latest.release.approved_head,null);assert.equal(out.history.length,1);assert.equal(out.history[0].merge_commit,null);assert.equal(out.latest.metrics.backend_tests.display,'10/10');assert.equal(out.latest.metrics.blocks_completed.value,151);});
test('author falha se tentar preencher approved_head ou omitir número executado',()=>{assert.throws(()=>authorRelease(latest,history,{...input,approved_head:'a'.repeat(40)}),/null/);const bad={...input};delete bad.backend_tests;assert.throws(()=>authorRelease(latest,history,bad),/backend_tests/);});
test('backfill preserva head aprovado e merge factual distintos e atualiza o append correspondente',()=>{const authored=authorRelease(latest,history,input);const out=backfillRelease(authored.latest,authored.history,{pr:12,approvedHead:'a'.repeat(40),mergeCommit:'b'.repeat(40)});assert.equal(out.latest.release.approved_head,'a'.repeat(40));assert.equal(out.history[0].merge_commit,'b'.repeat(40));assert.throws(()=>backfillRelease(out.latest,out.history,{pr:12,approvedHead:'a'.repeat(40),mergeCommit:'a'.repeat(40)}));});
