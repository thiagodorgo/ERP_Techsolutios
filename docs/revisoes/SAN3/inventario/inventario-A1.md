# Inventário balde A — fatia A1 (linhas 1–32) · head medido `ca5fd19a` (worktree b06, `fix/billing-durability`, árvore limpa)

| ID | linha | descrição | status_comprovado | prova | gravidade | origem | escopo | risco | dependências | bloco_dono | impacto_vendavel | teste_de_encerramento |
|---|--:|---|---|---|---|---|---|---|---|---|---|---|
| P-029 | 339 | Modal de edição de tarifa deixava os 3 selects de referência editáveis; backend os ignorava | FECHADA (texto diz ABERTA) | `grep -n 'disabled={isEdit}' frontend/src/modules/registry/tariffs/components/TariffFormModal.tsx` → l.157/181/194 (hint "Fixa após a criação" l.166/189/202; l.115 remove os 3 ids do PATCH); `git log -S'disabled={isEdit}'` → c26467e3 2026-07-12 (#165) | MÉDIA (declarada) → nula: corrigida | 2026-07-12 · gate Ω2-a.2 (validador-mestre) | dentro-do-bloco Ω2-a.2 (#165) | 8-acabamento-web/mobile | — | fechada em #165; só falta linha de status | NAO — corrigido no mesmo PR | Asserção que os 3 selects têm `disabled` em modo edição e o PATCH omite priceTableId/serviceCatalogId/customerId (hoje verde; não há teste dedicado: `grep -rl tariff-field-price-table frontend/src` só acha o componente) |
| P-Ω3F3B-UPDATE-VALIDA4 | 609 | Tripwire: validação #4 só no create; update não pode ganhar customer/service | ABERTA (invariante vale; teste de ordem recomendado ausente) | `src/modules/work-orders/work-order.types.ts:309-346` `UpdateWorkOrderInput` sem customerId/serviceCatalogId; `tests/work-order-registry-snapshot.test.ts:199-224` cobre só 400 isolado (randomUUID, OS sem cliente) — nenhum caso de precedência 400-antes-de-422 (`grep -rn tariff_not_found_for_service tests` → só casos isolados) | MÉDIA (mantida) | 2026-07-15 · J-OMEGA3F-3B (critico-adversarial) | pre-existente (guarda, não defeito) | 4-faturamento/dinheiro (tripwire de cobrança errada) | — | a atribuir → proposta `B-wo-tarifa-tripwire-ordem` | NAO — bypass não existe hoje (update é incapaz de trocar o par) | Caso novo: OS com cliente sem tarifa + `service_catalog_id` bem-formado inexistente → `400 invalid_service_catalog_reference` (não 422); + guard de tipo que quebra se `UpdateWorkOrderInput` ganhar os 2 campos |
| P-Ω4-3-REFATURAR-DELTA | 941 | Item lançado após o 1º faturamento nunca é faturado (2º POST → 409) | ABERTA | `src/modules/work-order-financials/work-order-financial.service.ts:354` "409 already_invoiced — mesmo que todos os itens já estejam carimbados" (+ l.320/391); `grep -rniE 'delta\|refatur' src/modules/work-order-financials` → 0 caminho de delta | MÉDIA (reclassificada 2026-08-29) | Ω4-3 (jul/2026); retirada do balde C em 2026-08-29 (A-5) | pre-existente (desenho v1 do faturamento) | 4-faturamento/dinheiro | — (P-Ω4-3-INVOICE-ATOMIC é irmã, não pré-requisito) | a atribuir → proposta `B-fin-faturar-delta` | NAO — 1º faturamento funciona e nenhum valor sai errado; receita do item tardio fica "a faturar" (contorno: título manual) | 2º `POST /work-orders/:id/invoice` após item novo → 201 com título do delta (hoje 409 `already_invoiced`) |
| P-Ω4-4-REVERSE-IDEM | 1056 | Estorno concorrente duplicava: faltava índice único em reversal_of e tx | FECHADA (texto diz ABERTA) | `prisma/migrations/20260869000000_add_financial_invariants/migration.sql:38-40` `CREATE UNIQUE INDEX financial_entries_reversal_of_active_key ON (tenant_id, reversal_of) WHERE reversal_of IS NOT NULL AND deleted_at IS NULL`; `src/modules/financial-entries/financial-entry.service.ts:312-330` `uow.run` + `findByIdForUpdate` + re-check na tx; `git log --diff-filter=A` → 99f18403 2026-09-04 (#371, B-O6R-02 c5); teste `tests/financial-entry-delete-reverse-race-db.test.ts` presente | MÉDIA (declarada) → nula: corrigida. Ressalva: índice é condicional (WARNING e migração segue SEM índice se houver duplicata legada) | Ω4-4 (jul/2026) | pre-existente, fechado dentro de B-O6R-02 | 4-faturamento/dinheiro | — | B-O6R-02 (#371) — entregue | NAO — corrigido | `financial-entry-delete-reverse-race-db.test.ts`: 2×reverse(A) concorrentes → 1 contra-lançamento (verde no head; DB-gated) |
| P-Ω4-7-CLEAR-ATOMIC | 1235 | Compensar/devolver cheque: lançamento e vínculo fora de uma transação | FECHADA (texto diz ABERTA) | `src/modules/cheques/cheque.service.ts:165-186` (clear) e `:227-232` (bounce após clear) rodam em `uow.run`; `:253-291 moveMoneyInUnit` = CAS + create + attach na mesma unidade, `if (!linked) throw` (fail-closed); `tests/cheque-clear-bounce-atomic-db.test.ts` adicionado em 99f18403 2026-09-04 (#371 F5) | MÉDIA (declarada) → nula: corrigida | Ω4-7 (jul/2026); retirada do balde C 2026-08-29 | pre-existente, fechado dentro de B-O6R-02 F5 | 1-perda/corrupcao-de-dados | — | B-O6R-02 (#371) — entregue | NAO — corrigido | `cheque-clear-bounce-atomic-db.test.ts`: crash entre create e attach não deixa cheque `cleared` sem `cleared_entry_id` (verde no head; DB-gated) |
| P-GOLIVE-VALIDATE-CONSTRAINT | 1355 | CHECK do cancelamento segue NOT VALID; VALIDATE nunca operacionalizado | ABERTA | `grep -rn 'VALIDATE CONSTRAINT' prisma/migrations/*/migration.sql` → 5 hits, nenhum para `work_orders_cancelled_decision_check`; `20260816000000_add_cancel_decision_check/migration.sql:14` "rode VALIDATE CONSTRAINT num bloco posterior" | MÉDIA | 2026-07-19 · go-live readiness (#229) | pre-existente | 5-confiabilidade | P-Ω3F6-LEGACY-NULL (consumidor trata NULL); migrations 13..16 aplicadas em produção | a atribuir → proposta `B-golive-validate-cancel-check` (migration aditiva, só VALIDATE) | NAO — CHECK já vale para linhas novas; texto diz 0 legadas na base do dono (NAO MEDI a base viva) | DB-gated: `SELECT convalidated FROM pg_constraint WHERE conname='work_orders_cancelled_decision_check'` → hoje `false`, depois `true` |
| P-AUDIT-FOLLOWUPS | 1535 | 4 follow-ups da tela Auditoria: ator cru, tenant_id no corpo, guard×backend, acentos | ABERTA (4/4 persistem) | (1) `src/modules/core-saas/routes/audit.routes.ts:74` `actor_user_id` cru, `grep createUserNameResolver` no arquivo → 0; (2) `:75 tenant_id: event.tenant_id` no corpo; (3) `frontend/src/App.tsx:562/572/580` guard aceita `audit:read/audit.read/audit:view` × backend `audit.routes.ts:45 requirePermission("audit.read")`; (4) `frontend/src/guards/PermissionGuard.tsx:29-30` "Acesso nao autorizado"/"usuario"/"permissao" | MÉDIA (item 2 é §2.8, mas tenant do próprio ator) | 2026-07-20 · PR-SCALE-3 | pre-existente | 8-acabamento-web/mobile (item 2 tangencia 3-seguranca, risco baixo) | — | a atribuir → proposta `B-audit-followups` | NAO — nenhuma regressão; tenant_id exposto é o do próprio ator | (2) `GET /audit-events` corpo sem `tenant_id` (hoje vermelho); (4) grep de acentos em PermissionGuard (hoje vermelho) |
| P-CHK-PRISMA-CLIENT-TYPING | 1693 | Repositório Prisma de checklist descarta tipos gerados (args: unknown, cast duplo) | ABERTA | `src/modules/checklists/checklist-prisma.repository.ts:63-69` delegates `(args: unknown)`; `:1111 tx as unknown as PrismaChecklistClient`. O gap original (V7 sem teste DB) já tem `tests/checklist-template-prisma-db.test.ts` | MÉDIA | 2026-08-02 · junta dba do fix P-CHK-TEMPLATE-PRISMA-V7 | pre-existente | 6-contratos/testes | — | a atribuir → proposta `B-chk-prisma-typing` | NAO — rede runtime + teste DB-gated existem; é refactor de tipagem | `tsc` deve falhar ao passar relation-scalar duplicado num nested-create do checklist (hoje compila) — delegates tipados com `Prisma.*CreateArgs` |
| P-MOBILE-BANNER-INTEGRACAO | 1898 | Banner "Integração remota ainda não ativa" renderizado sem gate; mente e sem acento | ABERTA | `mobile/flutter_app/lib/shared/ui/sync_screen.dart:92` `_BackendPendingNotice()` direto em `children` sem condição; `:269 'Integracao remota ainda nao ativa'` | MÉDIA | 2026-08-06 · validação em device (AVD) | pre-existente (B-077/B-090b) | 8-acabamento-web/mobile | — | a atribuir → proposta `B-mobile-reconciliacao-sync` (o "PR-08" do texto) | NAO — só UX/honestidade | Widget test: com feature_flags habilitados o texto do banner não é encontrado (hoje vermelho) |
| P-MOBILE-OS-SEEDS | 1912 | App de campo mostra OS-1042/43/44 fabricadas em vez de vazio honesto | ABERTA | `mobile/flutter_app/lib/features/work_orders/data/work_order_repository.dart:750-752` seeds vivos; `:118-121` semeia quando `_remoteApi == null` (guard de 3b56da1a 2026-06-14 — ANTERIOR ao achado, logo não o resolveu) e seeds já persistidos no store nunca são expurgados ao entrar em modo remoto; único commit pós-achado no diretório: 7e60b904 2026-08-12 (B-127 timeline), não toca isso | ALTA (mantida — D-007) | 2026-08-06 · validação em device | pre-existente (B-077) | 8-acabamento-web/mobile | — | a atribuir → proposta `B-mobile-reconciliacao-sync` (junto do banner) | NAO — só semeia com `_remoteApi == null` (build local/dev ou sem token); build remoto com store limpo não fabrica; HIPÓTESE: o device tinha store semeado de sessão local anterior | Unit test do repository: store pré-semeado + remoteApi presente + pull vazio → lista vazia sem `OS-1042` (hoje vermelho: `_orders = tenantOrders` devolve os seeds persistidos) |
| P-CHK-PATCH-SEM-LOCK | 1980 | PATCH de modelo de checklist é last-write-wins; sem If-Match/versão no backend | ABERTA (mitigada no cliente) | `grep -rniE 'If-Match\|expectedUpdatedAt\|optimistic' src/modules/checklists` → 0; `src/modules/checklists/checklist.service.ts:119 updateTemplate` sem comparação de versão; mitigação presente em `frontend/src/modules/checklists/pages/ChecklistEditorPage.tsx` (ConcurrentEditDialog) | MÉDIA | 2026-08-07 · junta PR-02b (critico) | pre-existente | 1-perda/corrupcao-de-dados | junta prévia (muda contrato REST público) | a atribuir → proposta `B-chk-optimistic-lock` | NAO — janela de 1 requisição, mitigada no editor web | `PATCH /tenant/checklists/:id` com `expectedUpdatedAt` defasado → 409 (hoje 200 sobrescreve) |
| P-CHK-CHIPS-SEM-CONSUMIDOR | 2000 | Inspector grava vehicleTypes/markerTypes que render, Flutter e snapshot ignoram | ABERTA | Em `src/` as chaves só existem como defaults em `src/modules/checklists/checklist.components.ts:16,25` (nenhum leitor no DTO de render); `grep -rnE 'vehicleTypes\|markerTypes' mobile/flutter_app/lib` → 0; inspector segue oferecendo os toggles (`frontend/.../ChecklistFieldInspector.tsx:216-247`) sem selo "Em breve"; único leitor é o preview web (`checklist-preview.model.ts:140,147`), não o campo | MÉDIA | 2026-08-08 · junta PR-02c | pre-existente | 8-acabamento-web/mobile | — | a atribuir → proposta `B-chk-plumb-config-render` (o "PR-04" do texto) | NAO — não quebra; mente sobre ter efeito | DTO de render e `checklist_remote_api.dart` expõem `config.vehicleTypes/markerTypes` e o renderizador os usa (hoje ausente nos dois) |
| P-JUNTA-LIMPEZA-BASE-VIVA | 2039 | Regra de teardown escopado para QUALQUER agente na base viva não está escrita | PARCIAL | `CLAUDE.md:394-395` (§C7.1-bis, D-INSPETOR-TERRENO-JUNTA 2026-08-24): cluster descartável por jurado, "a base viva não é alvo de ninguém" — cobre juntas por desenho mais forte; mas a regra literal pedida (só o que criou, por id; nunca listagem/wildcard/desligar trigger) não existe: `grep -inE 'listagem\|wildcard\|teardown' CLAUDE.md` → 0; `grep -rli listagem .claude/agents` → 0 | MÉDIA | 2026-08-08 · junta PR-02d (incidente 2) | pre-existente (processo) | 1-perda/corrupcao-de-dados (base do dono) | — | a atribuir → próximo bloco de governança (proposta `B-gov-teardown-escopado`) | NAO — processo, não produto | Guard documental: preâmbulo padrão de junta/agentes contém a regra (grep hoje 0 hits) |
| P-O6R-B07-APPROVAL-BY-POLICY | 2996 | finance/inventory sem `work_orders:approve` por falta de alçada monetária no agregado | ABERTA | `src/modules/core-saas/permissions/catalog.ts:40-42` comentário + varredura de `ROLE_PERMISSIONS`: só `manager` lista `work_orders:approve` (finance/inventory/technician não); `grep -niE 'amount\|limit\|alcada' src/modules/work-orders/approval.types.ts` → 0 | MÉDIA | 2026-09-02 · B-O6R-07a §3.1 | pre-existente (modelo ausente) | 3-seguranca/permissoes | modelo de alçada por valor (APPROVAL_LIMITS.md) ancorado no agregado | "bloco que introduzir alçada por valor" → proposta `B-wo-alcada-por-valor` | NAO — manager/admin aprovam; conceder sem alçada seria a classe do SEC-002 | finance aprova dentro do limite → 200 e acima → 403 (hoje 403 sempre) |
| P-ARNES-RLS-TEST-FORA-DO-SWEEP | 3770 | Família rls_test_ já entrou no sweep; destino das 68 órfãs da base viva indecidido | ABERTA | Código do apenso feito: `tests/helpers/auth-identity-fixture.ts:123 "rls_test"` em `SWEPT_ROLE_FAMILIES`; as 68 na base viva: NAO MEDI (proibido tocar `erp-postgres`) | MÉDIA | 2026-08-28 · B-O6R-ARNES C-C | pre-existente | 6-contratos/testes | P-ARNES-SWEEP-DEPENDE-DA-DISCIPLINA-DO-OPERADOR (mesma junta, mesmo dado) | junta desta pendência (nomeado) | NAO — roles de arnês, sem objeto de produto | Recontagem supervisionada só-SELECT em `pg_roles WHERE rolname LIKE 'rls\_test\_%'` com N registrado; não há teste automatizável sem tocar a base viva |
| P-GOV-MAIN-SEM-PROTECAO | 4772 | Ruleset da main vive só no servidor; `.github/rulesets` não versionado; branch de governança inchada | ABERTA | `git ls-files .github` → só 5 workflows; `ls .github/rulesets` → inexistente; estado do ruleset no GitHub: NAO MEDI (`gh api` fora das ferramentas permitidas) | MÉDIA | 2026-08-25 · ciclo 3 de governança (D-GOV-AMEACA-DESCUIDO) | pre-existente | 9-documentacao/rastreabilidade | — | workstream de governança (nomeado) | NAO — proteção efetiva declarada no servidor; o que falta é versionamento | `git ls-files .github/rulesets/*.json` ≥ 1 (hoje 0) |
| P-SAN2-LEITURA-DAS-79 | 4805 | Leitura item a item das diferidas do balde C ainda não feita | ABERTA | `grep -c 'NÃO verificada item a item' agent-orchestration/controle/pendencias.md` → 80; índice: balde C = 76 entradas | MÉDIA | 2026-08-29 · SAN2-1 (opção C, decisão do dono) | pre-existente | 9-documentacao/rastreabilidade | ciclo 5 do financeiro (#371 mergeado — satisfeita) | bloco próprio (nomeado; este inventário é a execução dele) | NAO — registro; HIPÓTESE do texto: ~40% de materiais escondidos | 0 etiquetas "NÃO verificada item a item" + índice regenerado |
| P-SAN2-2-INDICE-DONO-SEMPRE-SIM | 4983 | Gerador do índice marca dono=sim para entradas cujo campo diz "a atribuir" | ABERTA | `agent-orchestration/controle/gerar-indice-pendencias.py:98` regex inalterada (`\*\*dono:\*\*\s*(?!a atribuir)`); efeito vivo: `pendencias-indice.md:63` mostra `sim` para P-Ω3F3B cujo campo (`pendencias.md:621`) é `**dono:** a atribuir` | MÉDIA | 2026-08-30 · SAN2-2 Fase 4 (emenda 2026-09-05) | pre-existente | 9-documentacao/rastreabilidade | — | bloco de ferramentas de registro (re-atribuído 2026-09-05, sem número — dono anterior SAN2-5 mergeou sem fazer) | NAO — artefato de controle | Mutação: entrada com `**dono:** a atribuir` → coluna `a atribuir` (hoje `sim`) |
| P-KPI-PAINEL-NAO-RENDERIZA-SUMMARY | 5086 | Painel de KPI não renderiza `release.summary` nem `description` do history | ABERTA | `grep -nE 'release\.summary\|\.summary\b\|\.description\b' Kpis/app.js \| grep -v 'var FROZEN'` → 0; `grep -niE 'summary\|ressalva\|release' Kpis/index.html` → 0 | MÉDIA | 2026-08-30 · junta SAN2-2, C4 | pre-existente | 9-documentacao/rastreabilidade | — | SAN2-5 parte 1 (nomeado; ÓRFÃO — #367 mergeou sem tocar `Kpis/app.js`, ver emenda de P-SAN2-2) | NAO — números hidratam; só as ressalvas ficam fora do painel | Guard em `tests/kpi-dashboard-charts.test.ts` que falha se a seção do summary sumir/defasar do JSON (hoje seção inexistente) |
| P-OBITUARIO-DERIVADO-DO-DIRETORIO | 5187 | Obituário de identidades cobre só quem tinha arquivo; ≥15 identidades votantes fora | ABERTA | `grep -c` de 4 dos 15 nomes (auditor-do-kpi-honesto, jurado-reg-diff-escopo, provador-de-mutacao-do-espelho, jurado-san2r-kpi-registro) em `agent-orchestration/omega/juntas/OBITUARIO-IDENTIDADES.md` → 0/4 | MÉDIA | 2026-08-31 · junta SAN2-3, C1 | pre-existente | 9-documentacao/rastreabilidade | — | SAN2-5 parte 3 (nomeado; órfão como a irmã) | NAO — §1.4 fail-closed impede passe livre | Script que deriva o obituário de `omega/juntas/votos/**/*.json` e compara com o `.md` (hoje divergiria em ≥15 nomes) |
| P-Ω4-3-CURRENCY-BRL | 970 | Item da OS aceita qualquer ISO de 3 letras; faturar só aceita BRL → beco 400 | ABERTA | `src/modules/tariffs/financial-item.shape.ts:81-88` `parseCurrency` aceita `/^[A-Za-z]{3}$/`; `work-order-financial.validators.ts:28` o reexporta; `work-order-financial.service.ts:88` item manual o usa e `:132` só exige homogeneidade; faturar: `:372` `currency = items[0].currency` → `titleService.createForWorkOrder` → `financial-title.service.ts:134` `parseCurrency` de `financial-title.validators.ts:136-143` (`CURRENCY_ALLOWLIST={BRL}` → 400 `invalid_currency`). Caminho via tarifa (`:119` `tariff.currency`): NAO MEDI (`price-table.validators.ts:35` tem parser próprio, não lido) | MÉDIA-BAIXA (título) / MEDIA (campo) — mantida | Ω4-3 (jul/2026) — interação Ω3F×Ω4-3 | pre-existente (desenho v1) | 4-faturamento/dinheiro | — | a atribuir → proposta `B-wof-moeda-brl-v1` (allowlist {BRL} no item, igual ao título) | NAO — nenhum valor sai errado: o título é RECUSADO (400), não emitido em moeda errada; a OS fica sem faturar | `POST` de item manual com `currency:"USD"` → 400 `invalid_currency` (hoje aceita); faturar OS nunca chega a 400 vindo de outro módulo |
| P-O6R-B07 | 2917 | Guarda-chuva 1 P0 + 2 P1: aprovação por update, lockout morto, scanner sempre limpo | PARCIAL (texto diz FECHADA — l.2974; a l.2973 ainda diz ABERTA; o índice publica ABERTA) | SEC-002 superado: `src/modules/work-orders/work-order.routes.ts:86-99` approve/reject com `requirePermission(WORK_ORDER_PERMISSIONS.approve)` (#369 dc8168b9). SEC-003 fechado: `src/modules/auth/repositories/local-auth-credential.repository.ts:108-118` `locked_until = CASE WHEN failed_attempts + 1 >= 5 THEN now() + 15 min`. SEC-004 parcial: `src/config/env.ts:637-638` default `unavailable` em production e `noop` fora dela; `:546-551` recusa `noop` no boot de produção; `evidence-scanner.factory.ts:34` (#380 fe2748c8). Residual SEC-002: as 10 vias (linha SUBRECURSO abaixo); residual SEC-004: noop em dev/test por desenho + 503 em produção até haver AV real | ALTA (índice) / P0 + P1 — a gravidade viva está nas residuais | 2026-08-14 · auditoria Ω6R (plano, bloco 7) | pre-existente (achados de auditoria); 07a #369 e 07b #380 entregues | 3-seguranca/permissoes | P-O6R-SUBRECURSO-OBJECT-SCOPE (residual SEC-002) · P-O6R-B07B-SCANNER-AV-REAL (residual SEC-004, fora desta fatia) | B-O6R-07 — 07a (#369) e 07b (#380) mergeados; residuais com B-O6R-07c (não começou) e com o dono do SCANNER-AV-REAL | NAO por si (para não contar em dobro) — o que bloqueia vive nas residuais: SUBRECURSO (nesta fatia, BLOQUEIA) e SCANNER-AV-REAL (fora da fatia: produção responde 503 a todo upload) | Fecha quando as duas residuais fecharem: técnico não atribuído → 403 nas 10 vias; upload em produção com scanner real → 201 se limpo / 422 se infectado |
| P-O6R-B01-RELIGACAO-SEM-REMEDIO | 3651 | Titular da conta provada só desfaz a religação com credencial de outra organização | ABERTA | `src/modules/auth/services/identity-link.service.ts:540-551` `reauthenticate`: `reauthTenantId === removedLink.tenant_id` → 403 `reauth_credential_unavailable` ("must use a credential from another linked organization", S7); única rota de saída `identity-links.routes.ts:164` `DELETE` → `service.unlink` (`:352` chama `reauthenticate`). Saída alternativa (troca de senha que desvincula `religacao`, `identity-link.service.ts:390-397`) ARMADA E INERTE: `changePasswordWithIdentityHook` (`local-auth-credential.service.ts:136`) só é chamado por `tests/auth-identity-revocation-db.test.ts:504,534` e `tests/auth-identity-role-real-db.test.ts:457,517`; nenhum `*.routes.ts` o alcança. Único commit pós-achado nesses arquivos: dc8168b9 (#369), que não mexe no S7 | ALTA (declarada) — minha leitura: MÉDIA (ergonomia de saída; religar exige prova de credencial) | 2026-08-19 · agente-secops, B-O6R-01 (#357) | pre-existente (desenho S7 do B-O6R-01) | 3-seguranca/permissoes | rota de troca de senha (P-O6R-B01-TROCA-SENHA, citada no código) — é ela que ativaria a saída já armada | corpo nomeia B-O6R-07 × status diz "a atribuir"; 07a/07b mergearam sem tocar o S7 → ÓRFÃ; proposta `B-auth-troca-senha-e-saida-religacao` | NAO — só identidade multi-org religada; não é tomada de conta | Titular do vínculo `religacao` desfaz o vínculo sem credencial de outra org (ex.: troca de senha por rota → vínculo removido + sessões revogadas); hoje 403 `reauth_credential_unavailable` e nenhuma rota de troca |
| P-O6R-B01-LOGERROR-MORTO | 3672 | Falha da fonte de candidatos do login sem organização não é logada em produção | ABERTA | `src/modules/auth/services/anonymous-login.service.ts:90` `logError?` opcional; `:156` `this.deps.logError?.(…)`; única composição `src/modules/auth/routes/auth.routes.ts:90-106` `new AnonymousLoginService({ listCandidates, verifyCandidate, finalizeSuccess, registerFailure })`, sem `logError` ⇒ a chamada opcional vira no-op; `grep -rn logError src` → as 2 linhas do serviço + falsos-positivos por substring em `ServiceCatalogError` | ALTA (observabilidade, declarada) — mantida para operação; sem efeito de dado | 2026-08-19 · agente-secops, B-O6R-01 | pre-existente | 5-confiabilidade | B-O6R-08 (canal de observabilidade) | B-O6R-08 (corpo) — NÃO começou (inventário O6R: sem branch/PR/comando, KPI `a_fazer`) | NAO — login sem organização falha fechado (401 uniforme); só a causa fica invisível | A composição de produção de `createAuthRouter` passa `logError` ligado a logger/métrica; fonte que lança → 1 registro observável (hoje 0) |
| P-KPI-RECENT-CONGELADO | 5388 | Seção "Últimas demandas" do painel parada em 28/08, com PR-topo 359 | ABERTA (agravada: 13 PRs de atraso, eram 2 no registro) | `node` sobre `Kpis/kpis-latest.json` → `recent.as_of = "2026-08-28"`, `recent.itens` = 8, PRs `359,(sem pr),355,354,353,352,347,351`; `Kpis/kpis-history.json` = 158 entradas, com 13 PRs >359 (`360…369,371,380,381`); consumo vivo `Kpis/app.js:1195` `var rec = latest.recent`, `:1233-1234` render + reveal; guard: `grep -rn recent tests/kpi-*.test.ts` → 0 | MÉDIA | 2026-08-31 · junta SAN2-4b, C3 (A-3) | pre-existente | 9-documentacao/rastreabilidade | P-KPI-PAINEL-NAO-RENDERIZA-SUMMARY (mesmos arquivos) | SAN2-5 parte 2 (nomeado; ÓRFÃO — mesma situação da irmã acima) | NAO — artefato de controle | Guard em `tests/kpi-dashboard-charts.test.ts` vermelho quando o PR-topo de `recent` < PR-topo do history (hoje 359 < 381), ou seção derivada do history |
| P-ARNES-SWEEP-DEPENDE-DA-DISCIPLINA-DO-OPERADOR | 5520 | Sweep de roles órfãs roda contra qualquer DATABASE_URL; só disciplina protege a base viva | ABERTA | `tests/helpers/auth-identity-fixture.ts:163-199` `sweepOrphanEphemeralRoles` = `SELECT … FROM pg_roles WHERE rolname LIKE ANY(…)` + `DROP OWNED BY`/`DROP ROLE`, sem nenhuma checagem de alvo; famílias `:117-124` incluem `rls_test`; chamadores `:335` (`createEphemeralRole`) e `tests/rls-tenant-isolation.test.ts:42`, que abre com `import "dotenv/config"` (l.1); o único uso de `DATABASE_URL` no runner (`scripts/run-backend-tests.mjs:90-94`) é orçamento de skip, não recusa de alvo; base viva: NAO MEDI (proibido) | MÉDIA | 2026-08-31 · junta SAN2-4b, C2 (A-C2-1) | pre-existente (mecanismo 0a398246 #357 → f081b5d #359 → df496d22 #366) | 1-perda/corrupcao-de-dados (dado forense da base do dono) | P-ARNES-RLS-TEST-FORA-DO-SWEEP | junta de P-ARNES-RLS-TEST-FORA-DO-SWEEP (nomeado) | NAO — roles de arnês, sem objeto de produto | Mutação: `DATABASE_URL` apontando para banco não marcado como descartável → o sweep recusa com erro nomeado em vez de dropar (hoje dropa) |
| P-O6R-B02-CRASH-NO-LOAD-SEM-SKIP | 6017 | Teste core-saas-role-authority morre no import sem DATABASE_URL em vez de declarar skip | ABERTA | `src/database/prisma.ts:11-13` `if (!connectionString) throw new Error("DATABASE_URL is required…")` em escopo de módulo; `src/modules/core-saas/store/prisma-core-saas.store.ts:3` importa `../../../database/prisma.js`; `tests/core-saas-role-authority.test.ts:25` importa `PrismaCoreSaasStore` estaticamente, sem guarda de topo; `git log --since=2026-09-01` nos 3 arquivos → 0 commits | MÉDIA | 2026-09-02 · carve-out do CP-3, B-O6R-02 c5 | pre-existente | 6-contratos/testes | — | trilha do arnês (sucessora do B-O6R-ARNES #359), atribuída em 2026-09-05; sem branch/PR | NAO — só a bateria sem banco | `npm test` sem `DATABASE_URL` → o arquivo registra os testes e pula DECLARADO; runner com 0 fail ambiental (hoje o piso o nomeia, ec≠0) |
| P-JUNTA-RECURSO-EFEMERO-POR-BLOCO | 6102 | Remoção de recurso efêmero só pelo identificador do bloco; regra fora do contrato | PARCIAL | Regra viva em mandatos: `git grep -l -i 'identificador do BLOCO' 15ef3fbe -- '*.md'` → 27 arquivos fora de `pendencias.md` (amostra lida: 10 corpos `jurado-06*`/`jurado-06d*` em `.claude/agents/especialistas` + espelho `.agents`); fora do contrato: `CLAUDE.md:382-388` §C7.1-ter(c) = só junction + `git archive`/autocrlf; `git grep … -- CLAUDE.md AGENTS.md '.claude/agents/*.md'` → 0; `inspetor-de-terreno-da-junta.md:62-64` só procura resíduo `jur-*`/`crit-*`, sem critério de propriedade | MÉDIA | 2026-09-04 · incidente entre sessões (B-O6R-02 c5 × B-O6R-07a) | dentro-do-bloco (mandato do orquestrador) | 5-confiabilidade (terreno de junta) | — | "próximo bloco de governança": #381 (B-GOV-ELENCO-ENXUTO) e #383 passaram sem consolidar → proposta `B-gov-recurso-efemero-por-bloco` | NAO — processo | Guard documental: §C7.1-ter(c) do `CLAUDE.md` e o corpo do inspetor contêm a regra (prefixo do bloco + "resíduo alheio se reporta"); hoje 0 |
| P-METODO-FERRAMENTA-SINTATICA-COMO-PROVA | 6238 | Série de método: grep/leitura publicado como execução; 5 casos, denominador pendente | ABERTA (registro; a metade prometida existe nos pareceres dos porteiros e não foi consolidada) | A entrada termina em `pendencias.md:6370-6372` "Não conclui … até ter as duas metades"; a outra metade existe: `agent-orchestration/omega/juntas/votos/O6R-07a/00c-porteiro-pos-merge-369.md:158-166` tabela por cadeira (≈92 re-execuções · 78 confirmaram · 14 refutaram; C3 do c2 = 25/24/1, logo não houve caso negativo puro) e `…/B-O6R-02-ciclo5/00c-porteiro-pos-merge-371.md:289` (60 · 52 · 8); `grep -rn P-METODO-FERRAMENTA` → só `pendencias.md:6238,6692` + o índice (ninguém consolidou) | informativa (campo, l.6289) — o índice publica ALTA por ARTEFATO DO GERADOR: `gerar-indice-pendencias.py:75-80` casa `\bALTA\b` com `re.I` em qualquer ponto do corpo e pegou "caixa alta" (`pendencias.md:6267`) | 2026-09-04 · sessões B-O6R-02 c5 × B-O6R-07a | pre-existente (registro de método) | 9-documentacao/rastreabilidade | — | nenhum por desenho (texto); compilador nomeado: porteiro-pos-merge → proposta de fecho num bloco de registro que consolide as séries do #369/#371 | NAO — dado de método | A entrada apensa o denominador por cadeira dos porteiros #369 e #371 e declara o inventário fechado; o gerador corrigido lê a severidade do campo (hoje ALTA) |
| P-O6R-B07-RATE-LIMIT-DISTRIBUIDO | 6373 | Freio de login por IP vive na memória do processo; sem Redis nem trust proxy | ABERTA (itens 1–3); o sub-residual `P-O6R-B07A-RASTRO-ANONIMO-SEM-IP` está FECHADO (o texto ainda o dá como aberto) | `src/modules/auth/routes/auth.routes.ts:115-117` `options.loginIpRateLimit ?? {}` → `store ?? new InMemoryTokenBucketStore()`; composição `src/app.ts:121` `createAuthRouter({ getCoreSaasService })`, sem `loginIpRateLimit`; única implementação `src/modules/portal-shared/token-bucket.ts:18` `InMemoryTokenBucketStore implements TokenBucketStore`; IP do socket `auth.routes.ts:554-557`. Sub-residual fechado: `anonymous-login.service.ts:215-221` `registerFailure(…, { ipAddress, userAgent })` → `local-auth-login.service.ts:327-345` → `recordLoginFailure` espalha `...auditContext` no registro (`:361-371`); comentário `:334` "fecha P-O6R-B07A-RASTRO-ANONIMO-SEM-IP" (07a ciclo 2, #369) | MÉDIA | 2026-09-02 · B-O6R-07a §3.5 | dentro-do-bloco (nasce com o §3.5); o fecho é infra, fora do §5 | 3-seguranca/permissoes | decisão de infra (trust proxy + allowlist); Redis de produção | "B-O6R-07a → trilha de infra" (nomeado, sem bloco numerado) → proposta `B-infra-rate-limit-redis` | NAO — o lockout por conta (5 falhas/15 min, `local-auth-credential.repository.ts:108-118`) limita a força bruta por conta independentemente do balde por IP; o teto por IP só se multiplica com N réplicas | 2 instâncias com store compartilhado → a 31ª tentativa do mesmo IP em 5 min dá 429 na outra instância (hoje 401 até 30×N) |
| P-O6R-SUBRECURSO-OBJECT-SCOPE | 6501 | Técnico não atribuído muta OS alheia por 10 vias: anexos, comentários, geocode, km | ABERTA | As 10 vias de `inventario-O6R.md` §Ω6R-SEC-002 (l.143-148), conferidas por amostragem 2/2 no head: via 2 `src/modules/work-orders/work-order.routes.ts:237-242` `DELETE /work-orders/:workOrderId/attachments/:attachmentId` só com `requirePermission(update)`; via 10 `src/modules/mobile/mobile-work-order-sync.ts:245-255` `work_order.mileage` exige `work_orders:status` e chama `service.setMileage` (`work-order.service.ts:1247`: lê `this.get`, grava `repository.update`, sem assert); `assertMutationObjectScope` só é chamado em `:852` (update) e `:1319` (changeStatus); único emissor de `not_assigned_to_actor` em `src` é `work-order.service.ts:836` | ALTA (registro) / residual de P0 — minha leitura: P0 nas vias destrutivas (2 e 10), P1 de produto nos comentários | 2026-09-03 · juntas c1/c2 do B-O6R-07a (C1-A1, S-A1); origens bf456b0 #173, eed6240 #197, D-Ω3F-5-COMMENT | pre-existente | 3-seguranca/permissoes | decisão de produto D-Ω3F-5-COMMENT (vias 4-7) antes de codar; censo da superfície de sync | B-O6R-07c (`fix/o6r07c-subresource-scope`) — NÃO começou (sem branch/PR/comando) | BLOQUEIA — ator de campo apaga evidência (blob) e escreve km em OS que não é dele; piso mínimo: vias 1, 2 e 10 | Técnico A × OS atribuída a B: POST/DELETE attachments, POST/PATCH/DELETE comments e tags, POST geocode*, sync `work_order.mileage` → 403 `not_assigned_to_actor`; B legítimo segue 201/204/200; censo de sync com vermelho-controle |
| P-AUTH-KDF-ROTACAO-V2 | 6587 | Rotação de KDF v=2 prometida em comentário; sem coexistência, re-hash nem try/catch | ABERTA | `src/modules/auth/services/password.service.ts:5` `SCRYPT_VERSION = 1`; `:99` recusa qualquer `version !== v=1` (não há ramo v=2); `:122-137` pino N/r/p + comentário "Rotacionar o custo exige uma VERSÃO NOVA do formato (v=2)"; `:45-49` `await deriveScryptHash(...)` sem try/catch próprio; `grep -rnE 'rehash\|needsRehash\|v=2' src/modules/auth` → só o comentário `:125`. Nota: o texto cita `scryptSync`; o head usa `scrypt` assíncrono com `reject` (`:68-93`), e a porta do 500 segue fechada só pelo pino. Proteção nos chamadores: NAO MEDI | MÉDIA | 2026-09-03 · junta c1 do B-O6R-07a (C2-A2, C2-A3) | pre-existente | 3-seguranca/permissoes | — | B-AUTH-KDF-V2 (nomeado, a agendar pós-O6R) — não começou | NAO — hoje o custo é fixo e seguro (N=16384); o risco só nasce quando alguém tentar rotacionar | Stored v=1 autentica e é re-hasheado para v=2 no login; stored v=2 autentica; trio fora do pino em v=2 → `invalid_credentials` sem 500 (try/catch próprio em `verifyPassword`) |

> **Heads medidos.** Linhas 1–20 (as 20 primeiras da tabela) foram medidas em `ca5fd19a`; as linhas 21–32 (as 12 últimas) em `15ef3fbe` (`main`, já com o B-O6R-06 #385), no worktree `gov-elenco`, somente leitura. **As árvores são idênticas**: `git rev-parse ca5fd19a^{tree}` = `git rev-parse 15ef3fbe^{tree}` = `7883a59e1834c2df7b03338a8114b9c2664e5f82`.

## FLIPS — status comprovado pelo código × status do texto (fatia A1 inteira, 32 linhas)

| ID | o texto diz | o código diz | prova curta | re-conferido em `15ef3fbe` |
|---|---|---|---|---|
| P-029 | ABERTA (`status: aberto`, l.339+) | FECHADA | `TariffFormModal.tsx:157/181/194` `disabled={isEdit}` (c26467e3, #165) | sim |
| P-Ω4-4-REVERSE-IDEM | ABERTA (`status: ABERTA`, l.1056+) | FECHADA | `prisma/migrations/20260869000000_add_financial_invariants/migration.sql:38` índice único parcial em `reversal_of`; `tests/financial-entry-delete-reverse-race-db.test.ts` no head (99f18403, #371) | sim |
| P-Ω4-7-CLEAR-ATOMIC | ABERTA (`status: ABERTA`, l.1235+) | FECHADA | `src/modules/cheques/cheque.service.ts:173/218` `uow.run` → `moveMoneyInUnit` (`:242`), `:288` `if (!linked) throw`; `tests/cheque-clear-bounce-atomic-db.test.ts` no head (#371) | sim |
| P-O6R-B07 | FECHADA (l.2974, a linha mais recente; a l.2973 ainda diz ABERTA) | PARCIAL | SEC-003 fechado; SEC-002 e SEC-004 só parcialmente superados, com residuais vivos (SUBRECURSO: 10 vias; SCANNER-AV-REAL: 503 em produção) | medido nesta passada |

**Flip de sub-item (não muda o status da entrada):** `P-O6R-B07-RATE-LIMIT-DISTRIBUIDO` dá como aberto o residual irmão `P-O6R-B07A-RASTRO-ANONIMO-SEM-IP`, e o código o fecha: `anonymous-login.service.ts:215-221` → `recordLoginFailure` com `...auditContext` (#369). Os itens 1–3 da entrada seguem abertos.

**Total: 4 flips de status em 32**, mais 1 de sub-item. São 3 entradas fechadas que o texto dá como abertas e 1 guarda-chuva que o texto fecha enquanto o código mostra parcial. Direção: em 3 dos 4 o registro está atrasado em relação ao código (conserto feito, linha de status não atualizada); no 4º é o inverso, o registro fecha mais do que o código fechou.

**PARCIAIS que NÃO são flip**, porque o texto já descreve a parcialidade: `P-JUNTA-LIMPEZA-BASE-VIVA`, `P-JUNTA-RECURSO-EFEMERO-POR-BLOCO` e `P-CHK-PATCH-SEM-LOCK` (aberta, mitigada no cliente).

### Defeitos de registro achados de passagem (não são flips, mas mudam o que o índice publica)

1. **A coluna de severidade do índice lê menção, não o campo.** `gerar-indice-pendencias.py:75-80` devolve a primeira de CRÍTICA/ALTA/MÉDIA/BAIXA que casar `\b…\b` com `re.I` em QUALQUER ponto do corpo. `P-METODO-FERRAMENTA-SINTATICA-COMO-PROVA` sai **ALTA** por causa de "caixa alta" (`pendencias.md:6267`), e o campo diz "informativa" (l.6289). É a mesma classe de `P-SAN2-2-INDICE-DONO-SEMPRE-SIM`: o mesmo gerador lendo menção como se fosse declaração. Não medi quantas outras linhas do índice têm severidade fabricada assim.
2. **Status contraditório sem `CONTRADITORIA`.** `P-O6R-B07` tem `status: ABERTA` (l.2973) e `status: FECHADA` (l.2974). A regra 2 ("parcialidade nunca fecha") desarma o FECHADA, que diz "parcialmente superado", antes de a regra 4 ver o conflito; o índice publica ABERTA sem sinalizar nada. Acerta por acaso (o código é PARCIAL), não porque leu a residual.
3. **Dono em conflito dentro da entrada.** Em `P-O6R-B01-RELIGACAO-SEM-REMEDIO` o corpo nomeia `B-O6R-07` e a linha de status diz "a atribuir". O 07a e o 07b mergearam sem tocar o S7, então a entrada está órfã de fato.
4. **`P-KPI-RECENT-CONGELADO` piorou:** no registro eram 2 PRs de atraso (#364, #365); hoje são 13 (`360…381`).

## BLOQUEIAM VENDAVEL — fatia A1 inteira (32 linhas)

| ID | por quê | piso mínimo para destravar | bloco_dono |
|---|---|---|---|
| P-O6R-SUBRECURSO-OBJECT-SCOPE | P0 residual do Ω6R-SEC-002: técnico não atribuído apaga anexo/blob (`DELETE …/attachments/:id`) e escreve km (`sync work_order.mileage` → 200) em OS que não é dele | vias 1, 2 e 10 (destrutivas/dado); os comentários (vias 3-7) podem ficar como risco declarado até a decisão do `D-Ω3F-5-COMMENT` | B-O6R-07c — não começou |

**1 em 32.** As outras 31 são NAO (o motivo está em cada linha).

**Fora da fatia, mas alcançado por ela:** o residual SEC-004 de `P-O6R-B07` é `P-O6R-B07B-SCANNER-AV-REAL` (índice l.98). Com `EVIDENCE_SCANNER` resolvido para `unavailable` em produção (`src/config/env.ts:637-638`), **produção responde 503 a todo upload**, nas 5 vias listadas em `pendencias.md:2968-2969`. Para um produto de campo isso tende a BLOQUEAR a venda, mas a linha é de outra fatia e não a medi aqui.
