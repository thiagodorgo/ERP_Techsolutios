# Inventário balde A (material) — fatia A2 (linhas 33ª–63ª da tabela ABERTAS · balde A)

- head medido: `git -C .claude/worktrees/gov-elenco rev-parse --short HEAD` → `15ef3fbe` (branch `main`, com B-O6R-06 #385 mergeado)
- terreno: somente leitura (git show / git log / git ls-tree / grep / leitura)
- inventariante: agente A2 (retomada; rodada anterior morreu sem medir)

| ID | linha | descrição (≤15 palavras) | status_comprovado | prova (comando → resultado) | gravidade | origem | escopo | risco | dependências | bloco_dono | impacto_vendavel | teste_de_encerramento |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| P-DERIVADO-ESQUECIDO | 6727 | autor muda a fonte e não regenera o derivado a jusante (índice, KPI, campos do painel) | PARCIAL | `git show 15ef3fbe:Kpis/kpis-latest.json \| node` → `fechados 13 · sem_por 0 · sem_em 0` (inst. 3 corrigida); linhas `status:` de P-O6R-B02-SUITES-LIST-CI → 2× `FECHADA` (inst. 4 corrigida); `git grep -l "pendencias-indice\|gerar-indice-pendencias" 15ef3fbe -- tests .github scripts` → nenhum guard. Instância VIVA da família, presente: P-GOV-CAMINHO-REPO-SESSAO com cabeçalho "FECHADA" (l.7961) × `status: ABERTA` (l.7999), invisível ao CABEC (l.48 exige `**FECHADA`) | MÉDIA | #370–#372 (2026-09-05) | dentro-do-bloco (declarado) | 9-documentacao/rastreabilidade | P-STATUS-NEGRITO-INVISIVEL-AO-GERADOR | a atribuir → `B-GOV-GUARD-DERIVADOS` | NAO — processo de registro; nenhum efeito no produto | guard de CI que roda o gerador e `kpi-freeze --check` e fica vermelho se o derivado divergir da fonte (mutação: editar pendencias.md sem regenerar → ec≠0) |
| P-GOV-REGISTRO-PURO-QUORUM | 6827 | não há regra escrita de quórum nem de quem grava a aprovação em PR de registro | ABERTA | `git show 15ef3fbe:…/decisoes.md \| grep -niE "registro puro\|PR de registro"` e o mesmo em CLAUDE.md → 0. A prática dupla está PRESENTE depois do registro: #379 (`e55245a7`) aprovado só por 5 pareceres em `votos/B-O6R-02-ciclo5-consolidado/`; #382 (`1b8319f9`) só por `PORTEIRO-381.md` + evidência; nenhum dos dois diffs tem `J-*.md` | MÉDIA | #372–#378 (2026-09-05); §C7.1 de 2026-07-13 | pre-existente | 9-documentacao/rastreabilidade | — | decisão do dono / junta de governança (proposta `B-GOV-QUORUM-REGISTRO`) | NAO — governança de registro, sem efeito no produto | decisão em decisoes.md + CLAUDE.md/AGENTS.md que fixa o quórum de PR sem src/tests/prisma e quem grava o parecer; o próximo PR de registro traz esse artefato |
| P-GOV-D-DURABILIDADE-FORA-DA-MAIN | 6883 | `D-DURABILIDADE-BRANCHES-LOCAIS` é definida só em demo/investidor; a main apenas a cita | ABERTA (pior: 6→12 arquivos citam) | `git show d1fab3bc:…/decisoes.md` → definição na l.1749 (presença fora da main); `git merge-base --is-ancestor d1fab3bc 15ef3fbe` → NAO; `git grep -l D-DURABILIDADE-BRANCHES-LOCAIS 15ef3fbe \| wc -l` → **12**; `git show 15ef3fbe:…/decisoes.md \| grep -c` → 0 | MÉDIA | `cae6086` (#372, 2026-09-05) | pre-existente | 9-documentacao/rastreabilidade | — | decisão do dono (proposta `B-GOV-PORTAR-DECISOES-DEMO`) | NAO — rastreabilidade de governança | `git show main:…/decisoes.md \| grep -c "^## D-DURABILIDADE-BRANCHES-LOCAIS"` = 1, ou `git grep` das citações na main = 0 |
| P-C3-DOIS-PRS-SEM-KPI | 6695 | #370 e #371 mergearam teste ou código sem o trio de KPI do §C3.1 | ABERTA (lacuna irreparável por construção; o texto erra sobre o #371) | `git show --name-only 54a4194` → 3 arquivos (pendencias.md + 2 testes); `git diff --name-only dc8168b 54a4194 -- Kpis/` → 0; o history não tem entrada `"pr": 370` (o grep acha só `"pr": 371`, l.2344, lançada depois por PR de registro, backfill_note l.2352). **CORREÇÃO:** o texto diz que o #371 não tocou `Kpis/*`, mas `git show --name-only 99f1840 \| grep ^Kpis/` → `Kpis/app.js` + `Kpis/kpis-latest.json` (tocou 2 dos 3; faltou `kpis-history.*`) | MÉDIA | #370 `54a4194` (2026-09-04) · #371 `99f1840` | dentro-do-bloco (declarado) | 9-documentacao/rastreabilidade | P-DERIVADO-ESQUECIDO | registro sem conserto de código → encerrar por decisão (dono ou `B-GOV-QUORUM-REGISTRO`) | NAO — lacuna do registro histórico; o número vigente vem de execução posterior | nenhum teste de código cabe: fecha por decisão escrita de que fica nomeada, ou por entrada retroativa `pr: 370` com nota no history |
| P-O6R-B07B-SCANNER-AV-REAL | 6920 | produção e staging recusam todo upload (503) até haver antivírus real | ABERTA | `git show 15ef3fbe:src/modules/evidence/evidence-scanner.factory.ts` l.34 → `env.EVIDENCE_SCANNER === "noop" ? new NoopEvidenceScanner() : new UnavailableEvidenceScanner()` (só dois desfechos, nenhum scanner real) e l.18 `production → unavailable → todo upload responde 503`; `fly.staging.toml:31` e `fly.production.toml:40` → `NODE_ENV = "production"` | ALTA | `fe2748c8` (#380, 2026-09-06) | dentro-do-bloco (consequência declarada do fail-closed) | 7-fluxo-necessario-para-venda | PD-O6R-B07B-CLAMD-INSTREAM + junta-5 (serviço externo); pré-requisito para ligar `STAGING_DEPLOY_ENABLED` | `B-AV-REAL` (o dono encaixa) | **BLOQUEIA** — upload impossível em produção: 503 nas 5 vias (evidência, anexo, OS, checklist, dano) | em NODE_ENV=production com scanner real: arquivo limpo → 201 stored; EICAR → 422; scanner fora do ar → 503; a factory ganha 3º desfecho, com vermelho-controle |
| P-O6R-B07B-ATTACHMENT-STORED-DO-CLIENTE | 6984 | duas rotas de pátio gravam anexo `stored` com chave e URL vindas do corpo | ABERTA (a metade READ fechou no #380; a WRITE segue aberta) | `git grep -n "attachment\.create(" 15ef3fbe -- src` → **3** sítios: `attachment-prisma.repository.ts:16` (V2), `impound-prisma.repository.ts:533` (M2), `impound.notifications-prisma.repository.ts:117` (M5); o critério 3→1 não foi cumprido. `parseFileUrl` (intake) aceita qualquer string ≤2048, sem esquema; `parseOptionalAttachment` aceita file_url ≤2000 + storage_key/content_type/provider do corpo; o DTO devolve `fileUrl: photo.fileUrl` (`impound.intake.dto.ts:48`) e o web o põe cru em `<a href>`/`<img src>` (`InspectionSection.tsx:89-90`; `adaptPhoto` não checa esquema). Um `javascript:` seria neutralizado pelo React ^19.1.0 (`frontend/package.json:17`; comportamento do React 19, NAO MEDI por execução), mas URL externa arbitrária carrega no console | ALTA | `574a1d2` (2026-07-26, #285) e `398a19d` (2026-07-27, #290) | pre-existente | 3-seguranca/permissoes | P-O6R-B07B-CHECKLIST-JSON-FILEURL (mesma classe); `src/modules/impound/**` era proibido no 07b | `B-O6R-07c` ou bloco de pátio (o dono decide) | NAO — o guard de prefixo de tenant no READ impede leitura entre organizações; resta integridade e URL externa dentro da mesma organização | `git grep -c "attachment\.create(" -- src` = 1; a rota de pátio só aceita attachmentId do tenant/processo; POST com storage_key/file_url no corpo → 400 e zero linha criada |
| P-O6R-B07B-MOBILE-RETRY-PERMANENTE | 7054 | o app re-tenta sem fim um upload recusado; 503 no checklist vira falha genérica | ABERTA | `git grep -n 503 15ef3fbe -- mobile/flutter_app/lib` → uma linha só, `evidence_upload.dart:261` (`503 → SCAN_FAILED`); `checklist_attachment_upload.dart` l.245-255 → braços unauthorized/rede/timeout/400/413/422 e `_ => 'UPLOAD_FAILED'`, sem 503; `sync_models.dart:3` → `enum SyncStatus { local, pending, syncing, failed, conflict, synced }`, sem estado terminal de revisão; o blob só é apagado dentro de `_isStoredStatus` (l.172/182), logo o invariante do B-108 está intacto | MÉDIA | `c0630fa` (2026-08-01, #321) | pre-existente | 8-acabamento-web/mobile | P-O6R-B07B-SCANNER-AV-REAL (enquanto o scanner der 503, todo anexo de checklist aparece como falha genérica) | `B-O6R-11` / QUA-004 (trilha mobile) | NAO — nenhuma perda de dado; custa banda e bateria e dá mensagem genérica | suíte Dart de mapeamento: 503 → SCAN_FAILED nos dois arquivos; 415/422 → estado terminal sem re-tentativa; blob preservado |
| P-O6R-B06-RECONCILE-BLOQUEADO | 7164 | o script que repara o subfaturamento legado não foi entregue; o predicado observável segue sem decisão | ABERTA | `git ls-tree -r --name-only 15ef3fbe -- scripts \| grep -iE "reconcile\|usage"` → vazio; voto C3 (`votos/B-O6R-06/C3-contrato-kpi-voto.json` l.5): "`scripts/reconcile-checklist-usage.ts` AUSENTE … CONFORMIDADE com o R2-A"; ata `J-B-O6R-06.md` §6.1 (l.109-111): "A junta decide o predicado observável, e exige aceite da série K com run de trilha C semeada antes de qualquer `--apply`" = adiado, NÃO decidido | ALTA | `15ef3fbe` (#385) | dentro-do-bloco | 4-faturamento/dinheiro | P-O6R-B06-DIVERGENCIA-MOBILE-NAO-FATURADA (preço da trilha C); suspende P-O6R-B06-RECONCILIACAO-NA-DEMO | a junta do B-O6R-06 decide → proposta `B-O6R-06-RECONCILE` | NAO — só atinge vistorias legadas, anteriores ao #385; as runs novas já nascem com a unidade na transação | série K com run de trilha C semeada: o dry-run não conta a run C; `--apply` insere exatamente N (runs de completeRun sem métrica) e a reexecução insere 0 |
| P-O6R-B06-DIVERGENCIA-MOBILE-NAO-FATURADA | 7216 | vistoria com divergência e ciência não é faturada; falta a decisão de produto | ABERTA | `git show 15ef3fbe:src/modules/checklists/checklist.service.ts` → `completeRun` (l.531) passa `meterCompletion: true` (l.548); `registerDivergence` (l.672) passa `false` (l.706); `acknowledgeRun` (l.735) passa `false` (l.760) | MÉDIA | caminho de `mobile-checklist-sync.ts`/`checklist.service.ts`, anterior ao bloco | pre-existente | 4-faturamento/dinheiro | P-O6R-B06-RECONCILE-BLOQUEADO (o predicado do reconcile depende desta decisão) | produto / dono | NAO — a regra de preço é determinística e declarada em cada sítio: não decidida, mas não aleatória | decisão escrita do dono; se for cobrar, `registerDivergence` → `meterCompletion: true` e A10/C4 passam a esperar 1 (a mutação M-16 já existe) |
| P-O6R-B06-RATEIO-CURSOR-100K | 7336 | o rateio recusa período acima de 100 mil linhas; falta paginação por cursor | ABERTA | `…/cloud-cost-allocation-prisma.repository.ts` l.30 `CLOUD_COST_ALLOCATION_LINE_ITEM_CAP = 100_000`; l.223-224 `period_exceeds_line_item_cap` (recusa alta); `take: 100_000` ainda presente na l.246 | MÉDIA (declarada no cabeçalho e na linha). **O índice mostra ALTA** porque `severidade()` (gerador l.75-79) casa `\bALTA\b` com re.I em qualquer ponto do corpo ("recusa **alta**") | `6f27faae` (2026-06-08) | pre-existente | 5-confiabilidade | — | bloco de cloud-costs (proposta `B-CLOUD-COSTS-CURSOR`) | NAO — acima do teto o rateio falha alto, visível e reexecutável; nunca trunca em silêncio | fixture com cap+1 linhas: a run conclui `completed` com soma exata contra referência BigInt, e o teto sai |
| P-O6R-B06-DIVERGENCIA-ESCOPO-TESTES-DB | 7386 | duas suítes -db fora do §6 foram tocadas; aguardava a ratificação da junta | **FECHADA (texto diz ABERTA)** — `15ef3fbe` | `git show 15ef3fbe:…/votos/B-O6R-06/C3-contrato-kpi-voto.json` l.162 → "P-O6R-B06-DIVERGENCIA-ESCOPO-TESTES-DB (MÉDIA) — ratifico: medi que o diff é exatamente o declarado …"; `J-B-O6R-06.md` §6.3 → "Duas divergências de escopo, declaradas pelo dev e devolvidas à junta … A C3 confirmou"; veredito APROVADO 3×0 (l.3) e merge 15ef3fbe. A linha `status:` ainda diz "ABERTA (para a junta ratificar …)" | MÉDIA | `15ef3fbe` (#385) | dentro-do-bloco | 9-documentacao/rastreabilidade | — | a junta do B-O6R-06 já decidiu → falta atualizar a linha canônica | NAO — divergência de escopo de teste, ratificada; nenhum guard afrouxado | `status:` → FECHADA citando C3 l.162 + ata §6.3, e índice regenerado |
| P-GOV-AUDITOR-FORA-DA-CI | 7562 | o auditor de elenco e o `sync-agent-skills --check` não rodam na CI | ABERTA | `git show 15ef3fbe:.github/workflows/ci.yml \| grep -n "node scripts/"` → censo completo: l.70 `sync-agent-agents.mjs --check` e l.447 `smoke-compose-persistence.mjs`, só esses dois; blob do auditor `c82c5928` idêntico em 90d30f8a e 15ef3fbe | MÉDIA | ci.yml nunca teve os passos; o instrumento nasceu no #381 | pre-existente | 6-contratos/testes | mesmo dono de P-GOV-C10-ENCERRADO, P-GOV-AUDITOR-SEM-CHECAGEM-DE-LINK e P-GOV-VEREDITO-SEM-PARSER | próximo bloco que tocar `.github/workflows/` (proposta `B-GOV-CI-AUDITOR`) | NAO — gate de governança de agentes; nenhum efeito no produto | 2 passos novos no job backend, sem `continue-on-error`; mutação (SKILL.md aninhado) → job vermelho |
| P-GOV-VEREDITO-SEM-PARSER | 7605 | o veredito de junta é prosa; nenhum gate lê a linha final da ata | ABERTA | censo dos `run:` do `ci.yml` @15ef3fbe (7 jobs): npm ci/check/test/build, prisma, db:seed, suítes -db, guard de skip (l.255), frontend, portais, flutter, `node scripts/sync-agent-agents.mjs --check` (l.70) e `smoke-compose-persistence.mjs` (l.447); nenhum passo lê `J-*.md` ou parecer. `git grep -l -E "ANULADO\|HOMOLOGADO\|LIBERADO COM RESSALVA" 15ef3fbe -- scripts tests .github` → nenhum arquivo | MÉDIA (declarada; o índice mostra ALTA porque o corpo cita "gravidade **ALTA**" do componente dentro-do-bloco) | D-SAN-AUTONOMIA (2026-07-13) | pre-existente | 6-contratos/testes | P-GOV-AUDITOR-FORA-DA-CI | próximo bloco de `.github/workflows/` ou de `tests/` de governança (`B-GOV-CI-AUDITOR`) | NAO — gate de processo; não altera o produto | parser de veredito com allowlist e `else` que nega, consumido por exit code na CI; mutação: ata com "ANULADO" ou string desconhecida → job vermelho |
| P-GOV-BASH-EM-QUEM-JULGA | 7636 | papéis que julgam têm `Bash`, logo podem reescrever registros e votos | ABERTA (a metade (a); a (b) fechou no #381) | `git show 15ef3fbe:.claude/agents/<papel>.md \| grep -m1 ^tools:` → `Bash` em porteiro-pos-merge, inspetor-de-terreno-da-junta, validador-mestre, critico-adversarial, guardiao-fail-closed, agente-ci-doutor (6 de 6 amostrados) | ALTA | 2026-08-12 (porteiro) | pre-existente | 9-documentacao/rastreabilidade | §C7.7 P1/P2 (o jurado grava a própria evidência por Bash) | decisão do dono / bloco de ferramentas de agente (proposta `B-GOV-FERRAMENTA-ESCRITA-RESTRITA`) | NAO — separação de papéis no processo de agentes; não toca o produto | papel que julga sem ferramenta capaz de escrever fora de `votos/<bloco>/`; mutação (jurado grava pendencias.md) → negado |
| P-GOV-ESPELHO-CONTRATO-SEM-GUARD | 7685 | nenhum guard compara CLAUDE.md com AGENTS.md | ABERTA | `git grep -l "AGENTS\.md" 15ef3fbe -- tests scripts .github` → só `scripts/sync-agent-agents.mjs`, e a única ocorrência (l.31) é texto de template ("§C7 do `AGENTS.md`"), sem leitura nem comparação; a CI roda só esse script (l.70) | MÉDIA | D-INTEROP-CLAUDE-CODEX (2026-07-28) | pre-existente | 6-contratos/testes | P-GOV-AUDITOR-FORA-DA-CI | próximo bloco de governança de contrato (proposta `B-GOV-GUARD-ESPELHO-CONTRATO`) | NAO — contrato de agentes; não toca o produto | guard que compara as seções espelhadas dos dois contratos (EOL-neutro) e fica vermelho se só um mudar; mutação: regra editada só no CLAUDE.md → ec≠0 |
| P-GOV-KPI-DISPLAY-SEM-GUARD | 7743 | nenhum guard compara o card (display) com o value do JSON | ABERTA (sem instância viva hoje) | `git ls-tree 15ef3fbe tests \| grep kpi` → 3 suítes (achados-paridade, dashboard-charts, dashboard-contraste); o único "display" nelas é CSS (`kpi-dashboard-charts.test.ts` l.135-141, `[hidden]{display:none}`); `kpis-latest.json` @15ef3fbe → as 10 métricas têm display coerente (ex.: `blocks_completed` 163/"163", `backend_tests` 2995/"2995/2997") | MÉDIA | ciclo 1 do B-GOV-ELENCO (o display errado); a ausência do guard é pre-existente | dentro-do-bloco (declarado) | 6-contratos/testes | — | próximo bloco que tocar `tests/kpi-*` (proposta `B-KPI-GUARD-DISPLAY`) | NAO — painel interno de KPI; nenhum efeito no produto | asserção: display só de dígitos ⇒ `value === Number(display)`; forma N/M ⇒ `N === value`; mutação: display "161" com value 163 → vermelho |
| P-GOV-C10-ENCERRADO | 7771 | a C10 mede peso e não sabe se o bloco do efêmero encerrou | ABERTA (com instância viva) | blob `c82c5928` inalterado desde o #381; l.609-614 → "C10 — PESO do elenco efêmero … A detecção mecânica de 'bloco encerrado' está desenhada e deferida"; a checagem não usa `kpis-history` nem `bloco:`. **Instância viva:** `git ls-tree --name-only 15ef3fbe .claude/agents/especialistas/` → 12 arquivos, todos jurado-06* e jurado-06d* do B-O6R-06, que mergeou no próprio 15ef3fbe | MÉDIA | `90d30f8a` (#381, 2026-09-08) | dentro-do-bloco | 6-contratos/testes | P-GOV-AUDITOR-FORA-DA-CI (mesmo dono) | o bloco que fiar o auditor na CI (`B-GOV-CI-AUDITOR`) | NAO — higiene do elenco de agentes | frontmatter `bloco:` obrigatório; efêmero de bloco com merge_commit no history = BLOQUEIA por nome; mutação: os 12 do B-O6R-06 no head → ec=1 nomeando-os |
| P-GOV-AUDITOR-SEM-CHECAGEM-DE-LINK | 7799 | a checagem de link relativo (C8) saiu do auditor sem substituto | ABERTA | `git show 15ef3fbe:scripts/audit-agents-skills.mjs` l.31 → "1) **NÃO confere link.** A checagem `C8` … foi **CORTADA**"; l.532-533 idem; blob c82c5928 inalterado desde o #381 | MÉDIA | `90d30f8a` (#381, 2026-09-08) | dentro-do-bloco | 6-contratos/testes | P-GOV-AUDITOR-FORA-DA-CI; dependência nova → junta-5 + PD | mesmo dono de P-GOV-AUDITOR-FORA-DA-CI (`B-GOV-CI-AUDITOR`) | NAO — prevalência 0; só alcança skills e agentes | verificador de link (parser real ou passo de CI) + mutação M6 (link relativo quebrado numa SKILL.md) → ec=1 |
| P-GOV-INSPETOR-33-SEM-NORMA | 7842 | o contrato do inspetor mandava bloquear por norma inexistente (§C7.1-quater) | **FECHADA (texto diz ABERTA)** — `72fcdcde` (#383) | `git show 15ef3fbe:.claude/agents/inspetor-de-terreno-da-junta.md` l.94 → o item 3.3 agora é "CORPO CARREGADO × CORPO JULGADO", e `§C7.1-quater` aparece só como histórico (l.114); `git log -S "CORPO CARREGADO" 15ef3fbe -- <inspetor>` → `72fcdcde` (2026-09-08, #383); CLAUDE.md §A7 (l.103+) → "Norma citada que não existe na ref julgada não se aplica"; `decisoes.md` l.2198 idem; `git ls-tree -r 15ef3fbe \| grep -i cadeira-permanente` → só arquivos de votos, nenhum corpo de agente que exija | BAIXA (rebaixada de ALTA em 2026-09-08) | ciclo 1 do B-GOV-ELENCO (2026-09-07), edição não commitada | pre-existente | 9-documentacao/rastreabilidade | P-GOV-CAMINHO-REPO-SESSAO | já fechada por 72fcdcde → falta atualizar a linha canônica | NAO — corpo de agente | `status:` → FECHADA citando 72fcdcde e o §A7; índice regenerado |
| P-GOV-RECUSA-CANCELA-ACUSACAO | 7902 | a recusa de um arquivo suprime acusação verdadeira e o exit volta a 0 | ABERTA (NAO MEDI a construção por execução — a mutação exigiria copiar o script; medi a identidade do código) | `git ls-tree 90d30f8a scripts/audit-agents-skills.mjs` e `git ls-tree 15ef3fbe …` → o mesmo blob `c82c5928` (o código em que o C2E-01 foi medido segue intacto); l.43 → "RECUSA a medição … e não emite nenhuma outra checagem" (a supressão está presente) | MÉDIA | `90d30f8a` (#381) | dentro-do-bloco | 6-contratos/testes | P-GOV-AUDITOR-FORA-DA-CI | próximo bloco que tocar o auditor (`B-GOV-CI-AUDITOR`) | NAO — guard de elenco, prevalência 0 | a construção do C2E-01 (1 das 16), com recusa e acusação no mesmo arquivo → ec=1, em teste permanente em tests/ |
| P-GOV-CAMINHO-REPO-SESSAO | 7961 | as sessões nascem da árvore defasada demo/investidor e carregam contrato e agentes errados | PARCIAL (o cabeçalho diz FECHADA, a linha diz ABERTA — contradição invisível ao índice) | detecção PRESENTE: CLAUDE.md @15ef3fbe §A7 (l.103+) e item 3.3 do inspetor (l.94, via 72fcdcde); prevenção só em TEXTO ("Sessão a partir de um worktree que acompanha a `main`", §A7), e `git ls-tree 15ef3fbe .claude/` → só `agents/` e `skills/`, nenhum settings ou hook que a force. Reincidência PRESENTE: a sessão deste inventário carregou um CLAUDE.md sem §A7 (vai de A6 direto à PARTE B), isto é, nasceu da árvore principal em demo/investidor; a ata J-B-O6R-06-delta (l.164/178) registra a 5ª instância como ABERTA · ALTA | ALTA | runtime (carrega pelo diretório da sessão) | pre-existente | 9-documentacao/rastreabilidade | decisão do dono (item 1 do dossiê); P-GOV-INSPETOR-33-SEM-NORMA | decisão do dono (proposta `B-GOV-SESSAO-NA-MAIN`) | NAO — contamina o processo de julgamento, não o produto | artefato que force a origem da sessão (ex.: hook que recusa sessão fora de worktree na main) + linha `status:` coerente com o cabeçalho; mutação: abrir sessão em demo/investidor → recusada |
| P-O6R-SUITES-DB-SEM-TEARDOWN | 8024 | suítes -db não limpam o que semeiam; `npm test` repetido no mesmo banco fica vermelho | ABERTA (mecanismo presente; NAO MEDI a reprodução — rodar npm test é proibido nesta fatia) | `…/cloud-cost-allocation-prisma.repository.ts` l.208-212 → `listCostLineItems(periodStart, periodEnd)` com where só `billing_period_end gte` / `billing_period_start lte` (overlap puro, sem `import_id`); a emenda da junta do delta (l.8073-8081) soma 2ª causa: timeout de 5000 ms sob CPU disputada | MÉDIA | `20260611000000` / `6f27faae` (2026-06-08); vítimas de 2026-07-26 e 2026-08-10 | pre-existente | 6-contratos/testes | P-O6R-LISTCOSTLINEITEMS-SEM-ESCOPO-IMPORT | bloco de arnês a nomear (proposta `B-ARNES-TEARDOWN-DB`) | NAO — a CI sobe Postgres novo por job; só afeta execução local repetida | 2 `npm test` seguidos no mesmo banco com o mesmo denominador e fail 0 (N≥3); mais caso com dois imports na mesma janela |
| P-O6R-B07B-DATAURI-NO-VALUE | 7039 | data-URI base64 no `value` da resposta de checklist é gravado sem teto próprio | ABERTA | `git show 15ef3fbe:src/modules/mobile/mobile-checklist-sync.ts` l.684 `value: action.payload.value ?? null` e l.699 `value: existingAnswer?.value ?? action.payload.value ?? null` (gravação crua). A recusa de data-URI que existe (`describeRejectedValue`, l.943/961, origem `287bda36` #344 de 2026-08-10) só redige o eco de ação REJEITADA, não a gravação. O único teto é `express.json({ limit: "2mb" })` (`src/app.ts:106`) | MÉDIA | CHK PR-03 (#344, 2026-08-10) | pre-existente | 3-seguranca/permissoes (binário contorna o gate de upload) | P-O6R-B07B-SCANNER-AV-REAL (o gate que esta via contorna) | trilha mobile/checklists (proposta `B-CHK-VALUE-SEM-BINARIO`) | NAO — nenhum consumidor renderiza `data:`; o risco é guardar binário não verificado, até 2 MB | sync com `payload.value = "data:image/png;base64,…"` → recusado (4xx) e nada gravado; assinatura e foto só pela via V4 |
| P-O6R-B06-USAGE-BEST-EFFORT-RESIDUAL | 7243 | métricas de anexo e de job (base do rateio de storage/jobs) seguem gravadas fora da transação | ABERTA | `git grep -n recordCloudUsageBestEffort 15ef3fbe -- src` → 8 chamadas em `cloud-usage.events.ts` (l.35-108), 2 em `src/infra/jobs/job.worker.ts` (l.66/80), 2 em `notification.service.ts` (l.135/150); as regras usam essas chaves como base (`cloud-cost-allocation.rules.ts` l.24-25, 39-40, 49); o rateio alimenta a cobrança por tenant (`cloud-charge-prisma.repository.ts` l.108 `source_allocation_run_id`, l.214-219 lê run e alocações), e as rotas de cobrança são só `requirePlatformPermission("platform:cloud-charge…")` | ALTA | `0648a8e1` (2026-06-08) | pre-existente | 4-faturamento/dinheiro | P-O6R-B08 (infra/jobs sem lease); outbox genérico da Ω6R D-002 (não deliberado) | `B-O6R-06b` ou bloco de outbox genérico | NAO — só perde quando a gravação falha (esporádico), sempre para menos, no repasse de custo de nuvem da plataforma; o cliente nunca é cobrado a maior | fault-injection na gravação da métrica de anexo/job: o fato de origem faz rollback junto ou a unidade persiste uma vez por chave (molde `cloud-usage.capture.ts`) |
| P-O6R-B06-BASE-SEM-PRODUTOR | 7270 | quatro chaves de base do rateio não têm produtor; `api_requests` cai sempre em unallocated | ABERTA | `git grep -n -E "api_request\.count\|api_requests_count\|storage_gb_month\|storage_bytes_current" 15ef3fbe -- src` → só `cloud-cost-allocation.rules.ts` l.37, 38, 57 e `cloud-usage.types.ts` l.2, 3, 15, 16 (declaração de tipo), nenhum escritor; o censo dos `metricKey:` literais em src dá 15 chaves, nenhuma das 4; a regra `api_requests` (l.55-57) só tem essas duas | ALTA | `6f27faae` / `0648a8e1` (2026-06-08) | pre-existente | 4-faturamento/dinheiro | o produto decide se as métricas existem | bloco de cloud billing / produto (proposta `B-CLOUD-USAGE-PRODUTORES`) | NAO (limítrofe) — é sistemático, mas o efeito é o custo de API e metade de storage nunca repassados a tenant (subcobrança da plataforma; rotas só de plataforma). Vira BLOQUEIA se o repasse de custo de nuvem fizer parte da oferta vendável | censo: toda `basisMetricKeys` de toda regra tem produtor (estender o C2 de `o6r06-billing-census.test.ts` a todas as categorias) + rateio com custo de API gera alocação ≠ unallocated |
| P-O6R-B06-LEITURA-PLATAFORMA-SOB-FORCE-RLS | 7293 | leituras de uso de nuvem na plataforma, sem GUC, dão zero sob papel sem BYPASSRLS | ABERTA (NAO MEDI o papel de produção: não é declarado em artefato) | migração `20260611000000_add_cloud_usage_metering` l.57-58 e l.64-65 → `ENABLE` + `FORCE ROW LEVEL SECURITY` nas duas tabelas; `RlsPrismaCloudUsageRepository.listEvents` (l.166-173) e `listDailyAggregates` (l.190-197) → sem `tenantId`, usam `new PrismaCloudUsageRepository(this.prismaClient)` cru, sem `withTenantRls`/GUC | ALTA | `20260611000000` (2026-06-08) | pre-existente | 5-confiabilidade | P-O6R-B06-AGGREGATE-DAILY-SEM-AGENDA (o /daily já seria vazio de todo modo) | bloco de plataforma (proposta `B-PLATFORM-CLOUD-USAGE-RLS`) | NAO — só a visão de plataforma (resumo de uso) zera; os fluxos de tenant e o rateio, lido por tenant sob contexto desde o #385, não dependem disso | drill sob papel NOSUPERUSER NOBYPASSRLS: `GET /platform/cloud-usage/summary` com eventos semeados em 2 tenants devolve a soma dos 2 (hoje dá 0) |
| P-O6R-B06-AGGREGATE-DAILY-SEM-AGENDA | 7318 | ninguém enfileira o job da projeção diária de uso | ABERTA | censo de `aggregate-daily\|aggregateDailyUsage` em src → registro (`job.registry.ts:50`), tipo (`job.types.ts:6`), handler (`cloud-usage.jobs.ts:4/12`), agregador (`cloud-usage.aggregator.ts:3/6`), serviço (`cloud-usage.service.ts:46/83`) e um comentário (`cloud-cost-allocation.types.ts:115`); a constante `CLOUD_USAGE_AGGREGATE_DAILY_JOB` só aparece na definição; o censo de `enqueue(` em src dá `charging.accrue-daily`, `impound.reconcile-removals`, `impound.notify-due`, `notifications.scan-due` e o publisher de eventos, nenhum é este | MÉDIA | `0648a8e1` (2026-06-08) | pre-existente | 5-confiabilidade | P-O6R-B08 (agenda singleton) | `B-O6R-08` ou produto | NAO — desde o #385 a projeção não é base de dinheiro; só o endpoint /daily fica vazio | agenda singleton que enfileira o job (padrão de `charge.jobs.ts`); depois do tick, `cloud_usage_daily_aggregates` tem linha do dia que teve eventos |
| P-GOV-DEFAULT-DENY-POR-NOME-BASE | 7917 | a allowlist de escrita do auditor é chaveada pelo nome do arquivo, não pelo papel | ABERTA | `scripts/audit-agents-skills.mjs` @15ef3fbe l.418 `const base = caminho.split("/").pop().replace(/\.md$/, "")` e l.459 `const podeEscrever = PODE_ESCREVER.has(base)` — decide pelo nome-base, não por `fm.name` (l.439); `PODE_ESCREVER` (l.150-155) tem 4 nomes; blob c82c5928 inalterado desde o #381 | MÉDIA | `90d30f8a` (#381) | dentro-do-bloco | 6-contratos/testes | P-GOV-MODELO-FIXADO-SEM-MECANISMO (mesma forma) | próximo bloco que tocar o auditor (`B-GOV-CI-AUDITOR`) | NAO — guard de elenco, prevalência 0 | mutação: `especialistas/agente-fabrica.md` com `name: jurado-x` e `Write` → BLOQUEIA (hoje herda a autorização); a chave vira identidade verificada |
| P-GOV-MODELO-FIXADO-SEM-MECANISMO | 7931 | a lista de papéis com modelo fixado é manual; gate novo nasce sem conferência | ABERTA (com instância viva) | `scripts/audit-agents-skills.mjs` l.128-132 → `MODELO_FIXADO` = { planejador-mestre, porteiro-pos-merge, inspetor-de-terreno-da-junta } (3); `decisoes.md` @15ef3fbe (D-FALLBACK-MODELO-FABLE-OPUS, l.2051-2053) → a obrigação alcança 4: "inspetor-de-terreno-da-junta, porteiro-pos-merge, o assento permanente e o planejador-mestre". O assento permanente não está na lista nem tem corpo em `.claude/agents/` (`git ls-tree -r 15ef3fbe \| grep -i cadeira-permanente` → só arquivos de votos), e votou no B-O6R-06 (`votos/B-O6R-06/99-cadeira-permanente.md`) | MÉDIA | `90d30f8a` (#381) | dentro-do-bloco | 6-contratos/testes | P-GOV-DEFAULT-DENY-POR-NOME-BASE; P-GOV-ESGOTADO-SEM-TESTE | próximo bloco que tocar o auditor (`B-GOV-CI-AUDITOR`) | NAO — governança do modelo dos gates | lista derivada da fonte (papéis com modelo fixado no contrato) com fail-closed; mutação: gate novo com a obrigação e sem entrada → BLOQUEIA; o assento passa a ser conferido |
| P-O6R-B07B-CHECKLIST-JSON-FILEURL | 7025 | o ramo JSON do anexo de checklist aceita fileUrl/mimeType arbitrários e os devolve crus | ABERTA | `checklist.validator.ts` l.225 `fileUrl: z.string().trim().min(1)` (sem esquema) e `mimeType` livre; `checklist.dto.ts` l.180-182 devolve `attachment.fileUrl` cru quando `!hasManagedStorage`; no web, `assertSafeFileUrl` (`checklist-attachments.adapter.ts` l.103-108) só recusa caminho `C:\`/UNC. O preview baixa pela rota por ID (`downloadChecklistAttachmentFromApi`, l.62-65 → `/mobile/checklist-runs/:runId/attachments/:id/download` via `apiBlobRequest`), não pelo fileUrl cru, então não há vazamento de token para host arbitrário; nenhum `href`/`src` do módulo usa o fileUrl cru (só `preview.objectUrl`, `ChecklistEvidencePreview.tsx:56-58`) | MÉDIA | Ω3/CHK | pre-existente | 3-seguranca/permissoes | P-O6R-B07B-ATTACHMENT-STORED-DO-CLIENTE (mesma classe) | trilha CHECKLIST (proposta `B-CHK-ANEXO-SO-POR-BYTES`) | NAO — o web só abre o anexo pela rota autenticada por ID; o fileUrl cru fica guardado e é devolvido, mas ninguém o segue | POST JSON com `fileUrl` arbitrário → 400 (ou o ramo JSON sai), idem em `registerDivergence`; com `fileUrl: "https://externo"` nada é gravado |
| P-GOV-ESGOTADO-SEM-TESTE | 7881 | a queda Fable→Opus exige declaração, não evidência do esgotamento | ABERTA | CLAUDE.md @15ef3fbe, §C7 item 6-bis (l.462-493): a substituição "é DECLARADA … registra … **qual papel · qual modelo rodou · por que o Fable faltou**" — o conteúdo obrigatório são 3 campos declarativos, nenhum de evidência; `git show 15ef3fbe:CLAUDE.md \| grep -niE "429\|request.id\|rate_limit"` → nada | MÉDIA | D-FALLBACK-MODELO-FABLE-OPUS (2026-09-07/08, #381) | dentro-do-bloco | 9-documentacao/rastreabilidade | P-GOV-MODELO-FIXADO-SEM-MECANISMO | próximo bloco de governança de modelo (proposta `B-GOV-FALLBACK-COM-EVIDENCIA`) | NAO — governança do modelo dos gates | o 6-bis passa a exigir o erro (HTTP 429 + `model` + `request id`) no artefato; checagem que reprova parecer "rodou em Opus" sem esse bloco |

## FLIPS

| ID | texto diz | código/artefato diz | commit | prova |
|---|---|---|---|---|
| `P-O6R-B06-DIVERGENCIA-ESCOPO-TESTES-DB` | ABERTA ("para a junta ratificar") | **FECHADA** — ratificada e mergeada | `15ef3fbe` (#385) | `votos/B-O6R-06/C3-contrato-kpi-voto.json` l.162 "ratifico"; `J-B-O6R-06.md` §6.3; APROVADO 3×0 |
| `P-GOV-INSPETOR-33-SEM-NORMA` | ABERTA · BAIXA | **FECHADA** — o 3.3 da main é outro item; "norma inexistente não se aplica" está escrita | `72fcdcde` (#383) | inspetor l.94 (novo 3.3), CLAUDE.md §A7 l.103+, decisoes.md l.2198 |
| `P-GOV-CAMINHO-REPO-SESSAO` | cabeçalho FECHADA × linha `status:` ABERTA (contradição interna; o índice não a vê porque o CABEC, l.48, exige `**FECHADA`) | **PARCIAL** — detecção entregue, prevenção só em texto, reincidência presente | `72fcdcde` (detecção) | §A7 + item 3.3 presentes; `.claude/` na main só tem `agents/` e `skills/`; esta própria sessão carregou um CLAUDE.md sem §A7 |
| `P-DERIVADO-ESQUECIDO` | ABERTA (as 3 instâncias corrigidas) | **PARCIAL** — 4 instâncias corrigidas por artefato, classe sem guard e com instância viva (a linha acima) | — | fechados 13/13 com `por`/`em`; SUITES-LIST-CI 2× FECHADA; nenhum guard do gerador em tests/.github/scripts |

**Correção de fato, sem flip:** `P-C3-DOIS-PRS-SEM-KPI` afirma que o #371 não tocou `Kpis/*`. `git show --name-only 99f1840` lista `Kpis/app.js` e `Kpis/kpis-latest.json`; faltou só `kpis-history.*`. O #370 de fato não tocou nada em `Kpis/` e segue sem entrada `pr: 370` no history.

**Nenhuma `P-O6R-B06-*` desta fatia foi fechada pelo merge do B06**, exceto a DIVERGENCIA-ESCOPO acima. As outras 7 (RECONCILE, DIVERGENCIA-MOBILE, BEST-EFFORT, BASE-SEM-PRODUTOR, FORCE-RLS, AGGREGATE-DAILY, CURSOR-100K) nasceram no próprio #385 e seguem ABERTAS no código.

## BLOQUEIAM VENDAVEL

| ID | motivo |
|---|---|
| `P-O6R-B07B-SCANNER-AV-REAL` | upload impossível em produção: a factory só tem noop ou unavailable, e com NODE_ENV=production (fly.production.toml:40, fly.staging.toml:31) todo upload responde 503 nas 5 vias |

**Limítrofe (marcada NAO, com condição):** `P-O6R-B06-BASE-SEM-PRODUTOR`. O custo de API e metade do de storage nunca são repassados a tenant, de forma sistemática. As rotas de cobrança são só de plataforma, então hoje é subcobrança do fornecedor. Vira BLOQUEIA se o repasse de custo de nuvem fizer parte da oferta vendável.

## OBSERVAÇÕES (medidas, fora das 13 colunas)

1. **A coluna "severidade" do índice não é a declarada.** `severidade()` do gerador (l.75-79) devolve a primeira palavra-chave achada em QUALQUER ponto do corpo (re.I), não a da linha `status:`. Na fatia isso diverge em 3 casos: `P-O6R-B06-RATEIO-CURSOR-100K` (declarada MÉDIA, índice ALTA — "recusa **alta**"), `P-GOV-VEREDITO-SEM-PARSER` (MÉDIA → ALTA) e `P-GOV-INSPETOR-33-SEM-NORMA` (BAIXA → ALTA).
2. **Segundo `take: 100_000` silencioso, fora do rateio:** `src/modules/cloud-charges/cloud-charge-prisma.repository.ts:223`, em `listAllocationTenantAllocations`, sem contagem prévia. É a mesma classe do DIN-007. O impacto realista é baixo (alocações por run = tenants × categorias). Não achei registro em `pendencias.md` @15ef3fbe pelos identificadores `listAllocationTenantAllocations` nem `cloud-charge-prisma` (ausência de grep: não prova que falte registro sob outro nome).
3. **Reincidência de `P-GOV-CAMINHO-REPO-SESSAO` nesta própria sessão:** o CLAUDE.md carregado no contexto do inventariante vai de §A6 direto à PARTE B (sem o §A7 que está na main). A sessão nasceu da árvore principal em demo/investidor.
