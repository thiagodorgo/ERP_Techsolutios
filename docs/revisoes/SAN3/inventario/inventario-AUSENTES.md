# Inventário de pendências AUSENTES de `pendencias.md` (P2 — gravação incremental)

- Inventariante: agente Opus 5 (sessão 3ad1b87d), 2026-09-11
- Terreno: `.claude/worktrees/gov-elenco` — SOMENTE LEITURA
- Head esperado: `15ef3fbe` (main com B-O6R-06 / #385)
- Head medido: `15ef3fbed57e217b66e6b307f9ae96502777fd09` (`main`, worktree `gov-elenco`, `git --no-optional-locks status --porcelain` vazio). Único commit desde a auditoria de governança (`a01fc014`): `15ef3fbe` (#385, B-O6R-06).
- Método: `grep -n -i -E` no `pendencias.md` do head por ID, palavra-chave e arquivo; leitura do corpo da pendência candidata; prova por presença no head (`arquivo:linha`) para cada AUSENTE. Nenhuma escrita no repo, nenhum build/teste, nenhuma base viva, árvore principal não lida (itens de disco da árvore principal = NAO MEDI).
- Classes usadas: `JA REGISTRADA (P-…)` · `PARCIALMENTE (P-…)` (existe entrada, falta componente nomeado) · `AUSENTE` · `SUPERADA` (fechada/transitória, com prova) · `NAO-PENDENCIA` (nota de método, decisão por desenho ou roadmap sem defeito).
- Status: **CONCLUÍDO** (2026-09-11) — 140 linhas-candidato · 37 já registradas · 8 parciais · **50 IDs ausentes confirmados** · **10 bloqueiam vendável** · 24 superadas · 13 não-pendência

## 1. Tabela de TODOS os candidatos

| fonte | item | classificação | P- existente |
|---|---|---|---|
| J-B-O6R-06-delta §3 #1 | marcador §C3.3 perdido em 5 trilhas do `kpis-latest.json` | SUPERADA — `Kpis/kpis-latest.json` no head tem `B-O6R-06 §C3.3 (2026-09-11): valor CARREGADO` (4+ ocorrências) | — |
| delta §3 #2 / J06 §9 ress.2 | `fail 0` não herdável; flake de teardown | JA REGISTRADA | `P-O6R-SUITES-DB-SEM-TEARDOWN` (l.8024) |
| delta §3 #3 | comentário do helper `2026-06 n=209` | JA REGISTRADA | `P-O6R-B06-DELTA-RESIDUAIS` item 1 (l.8106) |
| delta §3 #4 | guard reserva janela pela forma textual | JA REGISTRADA | `P-O6R-B06-DELTA-RESIDUAIS` item 2 |
| delta §3 #5 + §5 / J06 §9 ress.3 | molde de plano/briefing não contempla os corpos de jurado que a junta exige criar (classe recorrente) | AUSENTE | — (grep `lista fechada\|corpos de jurado\|12 corpos\|molde` → só `P-SYNC-AGENTS-NAO-RECURSIVO`, outro assunto) |
| delta §3 #6 / §4.2 R4 | PR #385 BEHIND | SUPERADA — mergeado em `15ef3fbe` | — |
| delta §3 #7 + J06 §9 ress.5/6/7 + delta #9/#11 | resíduos de ata do B06 (plano 4+4 fora dos autos; "LIBERADO" sem "COM RESSALVA"; sem `00-quedas.md`; par renomeado 2-por-2; blank line EOF) | AUSENTE (agrupado, BAIXA) | — |
| delta §3 #8 | `listCostLineItems` sem escopo por import | JA REGISTRADA | `P-O6R-LISTCOSTLINEITEMS-SEM-ESCOPO-IMPORT` (l.8085) |
| delta §3 #10 / §4.2 R7 | armadilha de método (base do delta; `grep -c` conta linhas) | NAO-PENDENCIA (nota de método em ata; classe em `P-METODO-FERRAMENTA-SINTATICA-COMO-PROVA`) | — |
| delta §3 #12 | 2 pendências sem `status:` canônico | SUPERADA — índice do head: SEM STATUS = 0, CONTRADITORIAS = 0 | `P-STATUS-NEGRITO-INVISIVEL-AO-GERADOR` segue aberta como classe |
| delta §4.2 R1/R2 · §5 | contrato/corpo carregado ≠ ref (5ª instância) | JA REGISTRADA (reincidência apensada l.8002) | `P-GOV-CAMINHO-REPO-SESSAO` (l.7961, status ABERTA/ALTA l.7999) |
| delta §4.2 R3/R5 | CI não 7/7 no instante; âncora §0 desatualizada | SUPERADA (reexecutado 7/7 pela C3; merge feito) | — |
| delta §4.2 R6 | obituário l.9 "17 identidades" × 21 | SUPERADA — `OBITUARIO-IDENTIDADES.md:9` diz "As **21** identidades" | — |
| J-B-O6R-06 §9 ress.1 | main andou, merge conflita | SUPERADA (junta do delta 3×0 + merge) | — |
| J-B-O6R-06 §9 ress.4 | série da cadeira 0/0 × 1/0 | NAO-PENDENCIA — a cadeira não existe na `main` (§A7: norma ausente não se aplica) | — |
| J-B-O6R-06 §9 ress.8 | `core.autocrlf` publicado por 1 de 3 cadeiras | NAO-PENDENCIA (nota de briefing; classe coberta por §C7.1-ter(c)) | — |
| REGULARIZACAO G-1 | §C2.8 violado; regularização retroativa não está escrita no contrato | AUSENTE | — (grep `lacuna normativa\|exceção escrita\|regulariza` → só l.7530/8009, menção narrativa) |
| REGULARIZACAO G-2 / auditoria M-06 | #384 (skill) sem registro em `decisoes.md` | AUSENTE (status-geral:4305 registrou; `decisoes.md` não tem `backend-review-ts-prisma`) | — |
| REGULARIZACAO G-3 | status "fechada" legível como ABERTA; `REBAIXADA` fora do vocabulário | SUPERADA — `WORKTREES` l.7526 `status: FECHADA`; `INSPETOR-33` l.7875 `status: ABERTA · severidade BAIXA`; índice 0 SEM-STATUS. Resíduo: cabeçalho da `CAMINHO` (l.7961) ainda diz "FECHADA" com linha ABERTA (l.7999) | `P-GOV-CAMINHO-REPO-SESSAO` |
| REGULARIZACAO M-1 / auditoria M-03 | índice defasado | SUPERADA — 310 cabeçalhos `## P-` = 310 no índice; ambos tocados por `15ef3fbe` | classe: `P-DERIVADO-ESQUECIDO` |
| REGULARIZACAO M-2 / auditoria M-05 / PORTEIRO-381 R5 | `status-geral.md` parado | SUPERADA — `#382`=4, `#383`=5, `#384`=5, `#385`=1 ocorrências | — |
| REGULARIZACAO M-3 / auditoria A-04/A-05 / PORTEIRO-381 R6 | prevenção fora do diff; sessões nascem de `demo/investidor` | JA REGISTRADA | `P-GOV-CAMINHO-REPO-SESSAO` |
| REGULARIZACAO B-1 | #382 sobrescreveu `backfill_note` do #381 | NAO-PENDENCIA — atenuante medido pelo porteiro: conteúdo sobrevive no `description` | — |
| REGULARIZACAO B-2 | medição própria que respondeu QUASE | NAO-PENDENCIA (auto-correção de método) | — |
| REGULARIZACAO "fechar" (1) índice | regenerar `pendencias-indice.md` | SUPERADA (ver M-1) | — |
| REGULARIZACAO "fechar" (2) | linhas `status:` + `REBAIXADA` | SUPERADA (ver G-3) | — |
| REGULARIZACAO "fechar" (3) | status-geral + ID/registro do #384 | PARCIALMENTE — status-geral feito; `decisoes.md` não (ver G-2) | — |
| REGULARIZACAO "fechar" (4) | reabrir `CAMINHO-REPO-SESSAO` | SUPERADA — linha de status ABERTA (l.7999) | `P-GOV-CAMINHO-REPO-SESSAO` |
| REGULARIZACAO "fechar" (5) | fechar `INSPETOR-33-SEM-NORMA` | JA REGISTRADA, não fechada — segue ABERTA/BAIXA como nota da decisão do assento (l.7868-7877) | `P-GOV-INSPETOR-33-SEM-NORMA` |
| PORTEIRO-381 R1 | backfill do #381 | SUPERADA — `kpis-history.json` pr 381: `merge_commit 90d30f8a…`, `approved_head 81b977f3` | — |
| PORTEIRO-381 R2 | auditor fora da CI | JA REGISTRADA (ainda verdadeiro: `ci.yml:69-70` só `sync-agent-agents --check`) | `P-GOV-AUDITOR-FORA-DA-CI` (l.7562) |
| PORTEIRO-381 R3 | pendência falsa (worktrees) | SUPERADA | `P-GOV-WORKTREES-NAO-IGNORADAS` FECHADA |
| PORTEIRO-381 R4 | INSPETOR-33 | JA REGISTRADA | `P-GOV-INSPETOR-33-SEM-NORMA` |
| PORTEIRO-381 R7 | branches só locais; D-DURABILIDADE fora da main | PARCIALMENTE — branches agora em `origin/chore/gov-auditoria-elenco` e `origin/chore/gov-elenco-fatia-b` (refs remotas locais); `grep -c D-DURABILIDADE decisoes.md` = 0 no head | `P-GOV-D-DURABILIDADE-FORA-DA-MAIN` (l.6883) |
| PORTEIRO-381 final (5)/(6) | dono do auditor fora da CI; dono do caminho repo→sessão | JA REGISTRADA (ambas com "dono: …") | `P-GOV-AUDITOR-FORA-DA-CI`, `P-GOV-CAMINHO-REPO-SESSAO` |
| DOSSIE-B-GOV-ELENCO §7 | `P-GOV-MODELO-CODEX-SEM-NOME` (modelo do Codex nunca nomeado; sync leva `model: fable`) | AUSENTE — o dossiê a lista como pendência, mas o ID não existe no `pendencias.md` (grep `MODELO-CODEX\|OpenAI\|Codex.*model` = 0) | — |
| DOSSIE §7 | "degrau único" (Fable e Opus esgotam) | JA REGISTRADA — escada termina em PARADA (`AGENTS.md:491-518`), lacuna de prova coberta | `P-GOV-ESGOTADO-SEM-TESTE` (l.7881) |
| DOSSIE §7 | `P-GOV-BASH-EM-QUEM-JULGA` | JA REGISTRADA | l.7636 |
| DOSSIE §7 | `A-C3-01` — `agente-devops-provisionador` com Write/Edit na allowlist E votante de junta | AUSENTE (grep `A-C3-01\|devops-provisionador` = 0) | — |
| DOSSIE §7 | `P-GOV-SKILLS-RELEVANCIA` | JA REGISTRADA | l.7540 |
| DOSSIE §7 | AUDITOR-FORA-DA-CI · WORKTREES · VEREDITO-SEM-PARSER · ESPELHO-CONTRATO-SEM-GUARD · NOTA-KPI-CONGELADA · MAQUINAS-DE-DESFAZER | JA REGISTRADAS | l.7562 · 7494 · 7605 · 7685 · 7699 · 7473 |
| DOSSIE §4/§5 | opção 3+1 do auditor (regex na fronteira da gramática) | JA REGISTRADA (decidida: C8 cortado) | `P-GOV-AUDITOR-SEM-CHECAGEM-DE-LINK` (l.7799) |
| go-live §0/§3.1 | rotação da chave Google Maps | SUPERADA — `P-GOLIVE-SECRET-ROTATE` FECHADA (dispensada pelo dono, 2026-08-29) | l.1330 |
| go-live §3.2-§3.10, §3.12, §4 ALTA/MÉDIA | R1 dados no Brasil, conta/billing Fly, apps, domínio, secrets, S3/PITR, staging, R2 drill, ata go-live, health URLs, smoke autenticado, tier do Postgres | JA REGISTRADA por referência ao checklist de 12 passos (sev. "a classificar", dono "a atribuir") | `P-GOLIVE-GATES` (l.1364) |
| go-live §3.11 | `VALIDATE CONSTRAINT` | JA REGISTRADA | `P-GOLIVE-VALIDATE-CONSTRAINT` (l.1355) |
| go-live §5 + §0 | o próprio documento afirma "nenhum código bloqueia o go-live" e manda rotacionar chave já dispensada — contradiz a J-6R 5×0 e o AV fail-closed | AUSENTE | — (grep `go-live-readiness` só nas 2 pendências GOLIVE) |
| auditoria A-01 | corpo `cadeira-permanente-backend-review` armado em disco (árvore principal) | SUPERADA pelo B1, conferido pelo porteiro da regularização §7 ("nenhum dos 4 caminhos era rastreado… nada rastreado perdido"). NAO MEDI o disco hoje (árvore principal fora do terreno) | — |
| auditoria A-02 | 16 untracked + 33 especialistas na árvore principal; `.claude/agents/especialistas/` fora do `.gitignore` | PARCIALMENTE — o peso do elenco está em `P-GOV-C10-ENCERRADO` (l.7771); a decisão "especialistas no `.gitignore`?" (plano §6.5) não tem pendência. NAO MEDI o disco | `P-GOV-C10-ENCERRADO` |
| auditoria A-03 | cópia reprovada da skill carregada no runtime | SUPERADA pelo B1 (mesma prova de A-01). NAO MEDI o disco | — |
| auditoria A-06 | item 3.3 do inspetor confere corpo de AGENTE, não SKILL | AUSENTE — head `.claude/agents/inspetor-de-terreno-da-junta.md:94-118`: só `.claude/agents/<x>.md`, zero "skill" | — |
| auditoria A-07 | `repo-erp.md:23` "teste `-db` se auto-pula" no job `backend` (falso: `ci.yml` define `DATABASE_URL`) | AUSENTE — texto intacto no head: `.claude/skills/backend-review-ts-prisma/references/repo-erp.md:23` e `SKILL.md:59` (plano B4 não executado) | — (grep `repo-erp\|auto-pula` → l.4160/4203, contexto anterior à skill) |
| auditoria A-08 | 3 merges sem porteiro | SUPERADA (parecer de regularização); a lacuna normativa → ver G-1 | — |
| auditoria A-09 | item 1.1 do inspetor manda md5 CRU (falso BLOQUEADO sob `autocrlf`) | AUSENTE — head `inspetor-de-terreno-da-junta.md:53-54` `git show <head>:<caminho> \| md5sum × md5sum <arvore>/<caminho>` | — (grep `md5` no `pendencias.md` = 0) |
| auditoria A-10 | item 3.3: caminho ausente nos dois lados dá md5 de vazio = IGUAL (falso VERDE) | AUSENTE — head `:100-101`, pipeline `git show … \| tr -d '\r' \| md5sum` sem ec por variável | — |
| auditoria A-11 | `docs/claude-code-handoff/CLAUDE.md`: 2º contrato na ref, sem §C7, KPI "pós-avaliação humana" | AUSENTE — existe no head, sem banner (l.1-5 = abertura original), `grep -c C7` = 0 | — (`P-CLAUDE-ABERTURA-PRECEDENCIA-DESATUALIZADA` é outro defeito) |
| auditoria A-12 | auditor resolve a raiz por `import.meta.url`; sem `--dir` | AUSENTE — head `scripts/audit-agents-skills.mjs:63`; `--dir`/`process.cwd` = 0 | — |
| auditoria A-13 | item 3.3 sem terceiro estado para corpo NOVO da junta | AUSENTE — head `:111-112` só "Divergência = ressalva; no corpo com VETO = BLOQUEADO"; nada sobre corpo ausente da ref | — |
| auditoria M-01 | `sync-agent-agents.mjs` aceita argv desconhecido e cai em ESCRITA | AUSENTE — head `scripts/sync-agent-agents.mjs:25` `process.argv.includes('--check')`; os `exit(2)` (l.63/82) só cobrem origem vazia | — (grep `argv` = 0) |
| auditoria M-02 | `decisoes.md:1968` sustenta decisão viva no parecer de um gate inexistente | AUSENTE — head `decisoes.md:1968` "O `cadeira-permanente-backend-review`, na homologação nº 1, conferiu e mediu" | — |
| auditoria M-03 | índice publica 208 × 224 | SUPERADA (ver M-1) | — |
| auditoria M-04 | 4 linhas de status ilegíveis | SUPERADA (índice SEM STATUS = 0) | classe: `P-STATUS-NEGRITO-INVISIVEL-AO-GERADOR` |
| auditoria M-05 | status-geral parado | SUPERADA | — |
| auditoria M-06 | skill do #384 sem linha em `agent-orchestration/` | AUSENTE parcial (ver G-2) | — |
| auditoria B-01 | `screen-refs/` só com README | JA REGISTRADA | `P-SCREEN-REFS-PATH` (l.1590) |
| auditoria B-02 | `ERP Mobile.dc.html` inexistente | JA REGISTRADA | `P-ERP-MOBILE-DC-HTML` (l.1597) |
| auditoria B-03 | higiene do contrato: "assento" em prosa (l.476), `1-ter` (l.359) antes de `1-bis` (l.391), §9 no plural, "0." aninhado | AUSENTE (BAIXA) — medido no head `CLAUDE.md:359,391,476` | — |
| auditoria B-04 | ` M` fantasma no `gov-elenco` | SUPERADA — `status --porcelain` vazio hoje | — |
| auditoria §4(b) | monocultura de modelo: 3 gates pinados em `fable` (disponibilidade correlacionada) | AUSENTE — `.agents/agents/{planejador-mestre,porteiro-pos-merge,inspetor-de-terreno-da-junta}.md:4` `model: fable` | — (grep `monocultura\|correlacionad` = 0) |
| plano B1/B2/B3/B6 | desarmar runtime, parecer, B06, registro | SUPERADAS (executadas: regularização, #385, índice regenerado) | — |
| plano B4 | frase da skill sobre CI | AUSENTE (= A-07) | — |
| plano B5a | `--dir` + 4 estados + argv estrito | AUSENTE (= A-12, A-10 metade, M-01) | — |
| plano B5b | corpo do inspetor 1.1/3.3 | AUSENTE (= A-06, A-09, A-10, A-13) | — |
| plano B7 | dossiê das 5 decisões do dono (`DOSSIE-GOV-2026-09-09.md`) | AUSENTE — `ls agent-orchestration/omega/ \| grep DOSSIE` só devolve `DOSSIE-B-GOV-ELENCO.md` | — |
| plano §5 | `.gitattributes` (`* text=auto eol=lf`) adiado para bloco próprio | AUSENTE (vai junto de A-09 como conserto estrutural) | — (grep `gitattributes` só l.4240, pendência FECHADA de outra classe) |
| plano §5 | não varrer os 33 especialistas | NAO-PENDENCIA (recomendação de não-fazer) | — |
| plano §5 | `o6r07a-anon-lockout-db` na lista `SUITES` | NAO-PENDENCIA — acusação falsa; roda no job `backend` | — |
| plano §5 | hook `SessionStart` | NAO-PENDENCIA (recusado por não resolver) | — |
| plano §6.1 | onde as sessões nascem | JA REGISTRADA | `P-GOV-CAMINHO-REPO-SESSAO` |
| plano §6.2 | destino do assento permanente | JA REGISTRADA | `P-GOV-INSPETOR-33-SEM-NORMA` (dono: decisão do dono) |
| plano §6.3 | PR de governança é exceção escrita ao §C2.8? | AUSENTE (= G-1) | — |
| plano §6.4 | destino de `docs/claude-code-handoff/CLAUDE.md` | AUSENTE (= A-11) | — |
| plano §6.5 | especialistas no `.gitignore`; `REBAIXADA` como vocabulário | PARCIALMENTE — vocabulário resolvido na prática (l.7875 usa ABERTA); `.gitignore` sem pendência (= A-02) | — |
| sintese A1/B29 | subrecursos de OS sem escopo por objeto | JA REGISTRADA (Ω6R — não refeito) | `P-O6R-SUBRECURSO-OBJECT-SCOPE` (l.6501) |
| sintese A3 | alocação de técnico é determinística, não IA | NAO-PENDENCIA (fato do produto; o defeito é o selo "IA" → `P-WEB-CLOUD-BILLING-CARTAZ`) | — |
| sintese A8 | alertas de frota sem agendador (só `POST /notifications/fleet-alerts/run`) | AUSENTE | — |
| sintese A9 | check-in pede "2 últimos dígitos da placa" e confere o código da OS | AUSENTE | — (grep `placa` → só linhas de outros assuntos) |
| sintese B1/B2/B3 + C1 selo "IA" + C2 registry + D8 | Cloud Billing: tela de literais (0 hooks), 856 linhas de dados órfãs, 22 rotas sem cliente de produto, selo "IA" sobre 6 frases, `status: "implemented"` | AUSENTE — `P-PLATFORM-MOCK-WIRING` lista só Overview/Health/TenantDetail; `P-O6R-B06-DECIMAL-NA-BORDA` cita "painel de cloud billing" sem dizer que é cartaz | — |
| sintese B4/B5/B48 | 4 jobs de nuvem sem enfileirador; projeção diária vazia | JA REGISTRADA — o único sem gatilho algum é `cloud-usage.aggregate-daily`; os outros 3 têm rota POST síncrona | `P-O6R-B06-AGGREGATE-DAILY-SEM-AGENDA` (l.7318) |
| sintese B6 / D7 | `api_requests` cai sempre em `unallocated` | JA REGISTRADA | `P-O6R-B06-BASE-SEM-PRODUTOR` (l.7270) |
| sintese B7 | leitura de plataforma sob FORCE RLS dá zero | JA REGISTRADA | `P-O6R-B06-LEITURA-PLATAFORMA-SOB-FORCE-RLS` (l.7293) |
| sintese B8 + C2 (CUR manual, tipos `s3_cur`/`athena_query`) + D5 | cobrança calculada morre na tabela; ingestão do custo AWS é CSV manual; console manual × faturamento não decidido | AUSENTE | — (grep `TenantCloudCharge\|manual_csv\|s3_cur\|console manual` = 0) |
| sintese B9 + B10 | conta bancária e baixa de título (`POST /financial-titles/:id/pay`) sem tela | AUSENTE | — (l.1017 é borda de moeda do Ω4-4; nada sobre tela de baixa) |
| sintese B11 + B12 + B13 | cheques, fechamento de período e política de comissão sem tela | AUSENTE (`P-Ω4-6-FRONT-RESOLVE-NAME` só prescreve como a futura tela deve resolver nome) | — |
| sintese B14 / A4 | motor de aplicabilidade sem rota nem permissão (PR-04c-B) | AUSENTE como pendência (trabalho previsto na trilha CHK P1; nenhuma entrada nomeia o efeito) | — |
| sintese B15 | chips do inspector sem consumidor | JA REGISTRADA | `P-CHK-CHIPS-SEM-CONSUMIDOR` (l.2000) |
| sintese B16 + C3 (PR-05) | "Ver execuções deste modelo" leva à lista de MODELOS; `listRuns` sem rota | AUSENTE (`P-RBAC-GATING-MOCKSHELLS` chama a `ChecklistRunsPage` de "REAL", sem notar que lista modelos) | — |
| sintese B17 + B18 + D9 | 8 portas do menu com dado inventado; Organizações navega para ids fabricados | PARCIALMENTE — Pedidos/Relatórios/Console constam como "cascas mock" em `P-RBAC-GATING-MOCKSHELLS`/`P-PURCHASE-ORDERS-BACKEND-GATE` (balde C, só pelo ângulo RBAC); Planos e Módulos, Auditoria Global, APIs e a lista de Organizações: AUSENTE | `P-RBAC-GATING-MOCKSHELLS` (l.1397), `P-PURCHASE-ORDERS-BACKEND-GATE` (l.1576) |
| sintese B19 | Configurações da Plataforma: "MFA obrigatório" e "Auditoria de operações críticas" ligados em `Toggle` literal | AUSENTE | — (grep `PlatformSettings\|MFA` = 0) |
| sintese B20 + B21 + B22 | 5 rotas web sem porta; provisionamento de módulos sem link; Console Dispatcher sem menu e sem ação | AUSENTE (Console tem só o ângulo "casca mock" em `P-RBAC-GATING-MOCKSHELLS`) | — |
| sintese B23 | barramento de eventos do web cicla `mockEvents` e aponta `/api/v1/events/stream` inexistente | AUSENTE (BAIXA) | — |
| sintese B24 + B27 | segundo menu nunca montado; 3 páginas mortas | JA REGISTRADA | `P-004` (l.53, balde C) |
| sintese B25 | 27 dos 51 itens do menu fora do gate de módulo contratado | AUSENTE | — (grep `navigation.registry\|esconde-fino\|módulo contratado` = 0) |
| sintese B26 | menu não consulta permissão; `mapBackendRole` sem `inventory` | JA REGISTRADA (menu é por papel: `appSidebarNav.ts:285` `NAV_BY_ROLE[roleKindFor(roles)]`) | `P-026` (l.312, balde C) |
| sintese B28 | verbos sem consumidor (`/vehicle-identities` merge/unmerge, `identity-links`, `/roles`, `/tenants`) | AUSENTE (BAIXA) | — |
| sintese B30 | fluxo Prestador sem porta de entrada | AUSENTE | — |
| sintese B31 | tela de Conclusão (comissão) sem porta | AUSENTE | — |
| sintese B32 + B43 | módulo Estoque sem porta; catálogo de módulos nunca renderizado e apontando `/checklists` e `/notifications` inexistentes | AUSENTE | — |
| sintese B33 | consentimento LGPD de GPS (`/location`) sem porta | AUSENTE | — (grep `consent\|LGPD` = 0) |
| sintese B34 | perna de ENTREGA do guincho inalcançável | AUSENTE | — |
| sintese B35 | seeds OS-1042/43/44 | PARCIALMENTE — a exibição está registrada; falta o componente "limpar o que já foi gravado" (`_upsertRemoteOrders` nunca apaga) | `P-MOBILE-OS-SEEDS` (l.1912) |
| sintese B36 | banner "Integração remota ainda não ativa" | JA REGISTRADA | `P-MOBILE-BANNER-INTEGRACAO` (l.1898) |
| sintese B37 + B42 | 7 stubs `Pending*` mortos (`dev-token-local`); `MockExpenseSyncApi` em código de produção | AUSENTE (BAIXA) | — |
| sintese B38 | sync de RDV sem Authorization | JA REGISTRADA (Ω6R — mesmo defeito, não duplicado) | `Ω6R-QUA-001` em `P-O6R-B03` (l.2728) |
| sintese B39 | 5 tipos de ação nunca drenados | PARCIALMENTE — `inventory_entry/exit.create` e `work_order_material.add` no `Ω6R-QUA-002` (`P-O6R-B04`); `work_order.approval_request` e `work_order.unable_to_start`: AUSENTE | `P-O6R-B04` (l.2772) |
| sintese B40 | estoque do técnico = 8 SKUs fixos; `/mobile/technician/stock` não existe | AUSENTE (l.3224 é o `forEach` do QUA-005, outro defeito) | — |
| sintese B41 | OCR: andaime de dados sem consumidor | AUSENTE (BAIXA) | — |
| sintese B44 | "Minhas OS" = OS da organização inteira, sem filtro nem paginação | AUSENTE | — (grep `Minhas OS\|workOrdersForUser` = 0) |
| sintese B45 + §6(a6) | `/approvals` placeholder com "Permissao workflow:request nao retornou"; cabeçalho com papel cru; app "flutter_app"; README template | AUSENTE (o nome "Tenant Demo" já está em `P-CHK-SEED-DEMO-SUJO`) | parcial: `P-CHK-SEED-DEMO-SUJO` (l.2059) |
| sintese B46 / D2 | antivírus: todo upload 503 em produção | JA REGISTRADA (Ω6R) | `P-O6R-B07B-SCANNER-AV-REAL` (l.6920) |
| sintese B47 / D13 | worker de jobs OFF por default | SUPERADA — `src/config/env.ts:512-517` recusa boot em produção sem `JOBS_WORKER_ENABLED=true` (Ω6R-DIN-006 FECHADO, l.2866); ligar em ambiente = `P-GOLIVE-GATES` | `P-O6R-B05` |
| sintese B49 | 2 handlers "Placeholder" efetivamente enfileirados | AUSENTE (BAIXA) | — |
| sintese B50 | notificação sem canal de saída | AUSENTE | — |
| sintese B51 / A2 | outbox Sivec: escrito por 5 repositórios, lido só em teste | AUSENTE | — (grep `Sivec` = 0) |
| sintese B52 / D3 | pipeline de deploy `skipped` (variáveis vazias) | JA REGISTRADA | `P-GOLIVE-GATES` |
| sintese C1 (IA) / D4 | IA de diagnóstico/alocação inexistente; o que comunicar | NAO-PENDENCIA (fora do MVP: RF-IA-002 "Enterprise"); o selo falso vai em `P-WEB-CLOUD-BILLING-CARTAZ` | — |
| sintese C2 (visão do cliente) | cliente não vê a própria conta de nuvem | NAO-PENDENCIA (excluído por desenho, `status-geral.md:2379`) | — |
| sintese C3 push | despacho não chega ao app (sem push/polling/deep-link) | AUSENTE | — (grep `firebase_messaging\|push de despacho` = 0; `P-002` é git push) |
| sintese C3 PR-06 | impressão do conteúdo da vistoria | NAO-PENDENCIA (fatia planejada da trilha CHK P1, `J-CHK-04C-EMENDA`) | — |
| sintese C3 telas + C3 chat/perfil/basemap + D15 | Rotas Logísticas, Técnicos·Disponibilidade, Chat, Notificações e Perfil no app, basemap; tela "Organizações·Clientes" a aposentar | AUSENTE (BAIXA, agrupado) | — |
| sintese C3 Cognito + ocr/scan | `CLAUDE.md` §2.4 promete Cognito (código é JWT próprio); `API_CONTRACTS.md` do handoff promete `POST /mobile/ocr/scan` | AUSENTE (BAIXA) — `P-O6R-B01-IDP-SUBJECT` (l.2470) presume Cognito futuro, não registra a divergência | — |
| sintese C3 script de reparação / D6 | subfaturamento histórico sem reparação | JA REGISTRADA | `P-O6R-B06-RECONCILE-BLOQUEADO` (l.7164) |
| sintese C3 RF-IA-001 / RF-IA-003 | motor de inconsistência / assistente | NAO-PENDENCIA (roadmap Enterprise) | — |
| sintese D1 | deliberar os rascunhos D-001..D-004 | PARCIALMENTE — a deliberação da J-6R está citada verbatim em `P-O6R-BACKLOG` (l.2318-2321), sem dono nem prazo | `P-O6R-BACKLOG` |
| sintese D10 | abrir o gate da CHECKLIST P1 por escrito (por bloco × por achado) | JA REGISTRADA (append) | `P-O6R-SUBRECURSO-OBJECT-SCOPE` via EMENDAS l.7457-7464 |
| sintese D11 | cláusula "autor OU update" do D-Ω3F-5-COMMENT | JA REGISTRADA | `P-O6R-SUBRECURSO-OBJECT-SCOPE` |
| sintese D12 | ler as 76 do balde C | JA REGISTRADA | `P-SAN2-LEITURA-DAS-79` (l.4805) |
| sintese D14 | caminho canônico do `screen-refs/` | JA REGISTRADA | `P-SCREEN-REFS-PATH` |
| sintese §6(b) | teste de alcançabilidade de rotas do app ausente | AUSENTE | — |
| sintese §7 | `roadmap` do painel congelado em 2026-08-19 (B-O6R-02/07 `a_fazer`, mergeados) | AUSENTE | — (l.2800 usa o roadmap como prova; ninguém o registra como defeito) |
| sintese §7 | `mvp_demo 99`/`mvp_vendavel 88` medem código escrito e convivem com `REPROVADO PARA PRODUÇÃO` | AUSENTE (`P-KPI-CARIMBO-MVP-DEFASADO-SAN2-5` trata só do carimbo) | — |

## 2. AUSENTES confirmadas (13 campos)

Toda prova abaixo foi lida no head `15ef3fbe` (caminhos relativos ao repositório; no app, relativos a `mobile/flutter_app/lib/` quando começam por `features/`, `core/`, `shared/` ou `app/`). Onde a frase vem da auditoria e eu não a re-medi, está escrito.

### 2b. Governança (atas, pareceres, auditoria, plano, dossiê)

| ID proposto | fonte | descrição (≤15 palavras) | status_comprovado | prova | gravidade | origem | escopo | risco | dependências | bloco_dono | impacto_vendavel | teste_de_encerramento |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| P-GOV-MOLDE-SEM-CORPOS-DE-JURADO | J-B-O6R-06 §9 ress.3; delta §3 #5 e §5 | Molde de plano/briefing não prevê os corpos de jurado que a junta exige criar | ATIVO | `agent-orchestration/codex/comando-template.md` e `docs/claude-code-handoff/comando-template.md`: `grep -c especialistas` = 0, `grep -ci jurado` = 0; reincidiu nas duas juntas do B06 ("12 corpos … fora da lista literal do §5") | BAIXA | 2026-09-07 / 09 | pre-existente | 9-documentacao/rastreabilidade | — | próximo bloco de governança | NAO — processo | molde lista `.claude/agents/especialistas/<id>*.md` + espelho no escopo; checagem falha se o diff tocar `especialistas/` fora do declarado |
| P-O6R-B06-ATA-RESIDUOS-DE-REGISTRO | J06 §9 ress.5/6/7; delta §3 #7, #9, #11 | Ata e votos do B06 com resíduos: plano fora dos autos, ressalva omitida, sem quedas | ATIVO (ress.5 NAO MEDI — não identifiquei os "dois hashes") | `J-B-O6R-06.md:59` "passada 2 **LIBERADO**" (o inspetor deu COM RESSALVA); `ls votos/B-O6R-06/` sem `00-quedas.md`; `BRIEFING-B-O6R-06-delta.md:67` plano 4+4 "em `scratchpad`"; `votos/B-O6R-06/C1-banco-rls-evidencia.md` termina em linha vazia; par 2-por-2 só descrito no delta §3 #9 | BAIXA | 2026-09-07/09 | dentro-do-bloco (B-O6R-06, mergeado) | 9 | — | próximo PR de registro em `omega/juntas/` (candidato: backfill do #385) | NAO | "LIBERADO COM RESSALVA" na linha do inspetor; `00-quedas.md` ou "quedas: 0"; plano commitado; `git diff --check` limpo |
| P-GOV-C28-REGULARIZACAO-NAO-ESCRITA | REGULARIZACAO G-1; auditoria A-08; plano §6.3 | §C2.8 trava todo bloco sem parecer e não prevê regularização nem exceção | ATIVO | `CLAUDE.md:263` "Sem parecer dele, nenhum bloco novo começa"; `grep -n regulariza CLAUDE.md` = 0 (662 l.); o parecer de 2026-09-09 destravou "por este parecer" sem norma (`votos/REGULARIZACAO-382-383-384/01-parecer-porteiro.md:165-166`) | MÉDIA | 2026-09-08 (#382-#384) | pre-existente | 9 | decisão do dono (plano §6.3) | governança de contrato (espelho `CLAUDE.md`/`AGENTS.md`) | NAO | §C2.8 nomeia a regularização (quem emite, o que mede) ou a proíbe; espelho idêntico |
| P-GOV-SKILL-384-SEM-DECISAO | REGULARIZACAO G-2 e "fechar (3)"; auditoria M-06 | Skill `backend-review-ts-prisma` (#384) entrou sem decisão registrada | ATIVO (parcial: status-geral registrou) | `agent-orchestration/docs/status-geral.md:4305` cita o #384; `grep backend-review-ts-prisma agent-orchestration/controle/decisoes.md` = 0 | BAIXA | #384 (2026-09-08) | dentro-do-bloco (#384) | 9 | — | próximo PR de governança | NAO | entrada em `decisoes.md` com ID, data, motivo e escopo |
| P-GOV-MODELO-CODEX-SEM-NOME | DOSSIE-B-GOV-ELENCO §7 | Modelo do Codex nunca nomeado; espelho leva `model: fable` sem tradução | ATIVO | `.agents/agents/{planejador-mestre,porteiro-pos-merge,inspetor-de-terreno-da-junta}.md:4` `model: fable`; `AGENTS.md` sem ID de modelo OpenAI; o dossiê cita o ID como pendência e ele não existe no `pendencias.md` | MÉDIA | 2026-09-08 | pre-existente | 5-confiabilidade | decisão do dono (os dois IDs) | governança de elenco (`sync-agent-agents`) | NAO | `AGENTS.md` nomeia os modelos; sync traduz `model:` por tabela; `--check` verde |
| P-GOV-DEVOPS-ESCREVE-E-VOTA | DOSSIE §7 (`A-C3-01`) | `agente-devops-provisionador` pode escrever e se declara votante de junta | ATIVO | `.claude/agents/agente-devops-provisionador.md:4` `tools: Read, Write, Edit, Grep, Glob, Bash`; descrição "Vota nas juntas de infra" (`grep -c` = 1 no head); `.gitattributes` inexistente no head (`git cat-file -e HEAD:.gitattributes` ec=128, relevante à ficha do inspetor); `ci.yml:15` = `DATABASE_URL: postgresql://…` no head (relevante à ficha da skill); `scripts/audit-agents-skills.mjs:150-151` `PODE_ESCREVER` o contém | MÉDIA | 2026-09-07 | pre-existente | 3-seguranca/permissoes (§C7.4-bis) | `P-GOV-BASH-EM-QUEM-JULGA` | governança de elenco | NAO | papel escritor não vota (ou votante sem Write/Edit), com checagem no auditor |
| P-GOV-INSPETOR-RECEITAS-1.1-E-3.3 | auditoria A-06, A-09, A-10, A-13; plano B5b e §5 (`.gitattributes`) | Receitas do inspetor: md5 cru, falso verde, cegas a skills e a corpo novo | ATIVO | `.claude/agents/inspetor-de-terreno-da-junta.md:53-54` md5 cru; `:100-101` pipeline sem ec (ausente nos 2 lados = md5 de vazio = IGUAL); `:94-118` só `.claude/agents/<x>.md` (sem skills, sem `especialistas/`); `:111-112` sem estado para corpo ausente da ref | MÉDIA | #383 (2026-09-08) e anteriores | pre-existente | 6-contratos/testes | P-GOV-AUDITOR-RAIZ-POR-IMPORT-META (4 estados); decisão `.gitattributes` | governança de gate (B5b), agente distinto do que fizer o auditor | NAO | fixtures: árvore limpa com CRLF → LIBERA; ausente nos 2 lados → AUSENTE (nunca IGUAL); corpo novo com hash no briefing e sem poder normativo → RESSALVA; skill divergente → acusa |
| P-GOV-SKILL-BACKEND-REVIEW-MENTE-SOBRE-CI | auditoria A-07; plano B4 | Skill afirma que teste `-db` se auto-pula no job `backend`; o `ci.yml` define `DATABASE_URL` | ATIVO | `.claude/skills/backend-review-ts-prisma/references/repo-erp.md:23` "teste `-db` **se auto-pula**"; `SKILL.md:59` "se auto-pula sem `DATABASE_URL`" (idem espelho `.agents/skills/`); contra-fato: `.github/workflows/ci.yml:15` e `scripts/run-backend-tests.mjs:82` `SKIP_BUDGET_DB` (auditoria) | MÉDIA | #384 (2026-09-08) | pre-existente | 6-contratos/testes | — | governança de skills (B4), agente de redação técnica | NAO | `grep "se auto-pula" repo-erp.md` = 0; linha do `ci.yml` citada ainda vale na ref; espelho idêntico EOL-neutro |
| P-GOV-AUDITOR-RAIZ-POR-IMPORT-META | auditoria A-12; plano B5a | Auditor de elenco só audita a árvore onde o script mora, nunca a da sessão | ATIVO | `scripts/audit-agents-skills.mjs:63` `const ROOT = join(dirname(fileURLToPath(import.meta.url)), "..")`; `--dir`/`process.cwd` = 0 | MÉDIA | #381 | pre-existente | 6-contratos/testes | `P-GOV-AUDITOR-FORA-DA-CI` | governança de ferramentas (B5a) | NAO | `--dir <cópia>` acusa corpo intruso como AUSENTE-NA-REF; `--ref a01fc014` mantém a saída de controle |
| P-GOV-SYNC-AGENTS-ARGV-FROUXO | auditoria M-01; plano B5a | `sync-agent-agents.mjs` com argumento desconhecido cai em modo escrita | ATIVO | `scripts/sync-agent-agents.mjs:25` `const CHECK = process.argv.includes('--check')`, sem validar argv; os `exit(2)` (`:62-63`, `:81-82`) só cobrem origem vazia | MÉDIA | anterior ao #381 | pre-existente | 6-contratos/testes | — | governança de ferramentas (B5a) | NAO | `--naoexiste` → ec≠0 e `git status --porcelain -- .agents` vazio |
| P-GOV-SEGUNDO-CONTRATO-NO-HANDOFF | auditoria A-11; plano §6.4 | Segundo "contrato de execução" na ref, sem §C7 e com KPI pós-avaliação humana | ATIVO | `docs/claude-code-handoff/CLAUDE.md` (334 l.): abre como "contrato de execução" (l.1-5), `grep -c C7` = 0, `:207` "## C3. Política de KPI pós-avaliação humana (permanente)" | MÉDIA | #175 | pre-existente | 9 | decisão do dono (some × banner × §A1 nomeia o canônico) | governança de contrato | NAO | banner no topo apontando o `CLAUDE.md` da raiz (ou remoção) + §A1/§A7 nomeando o caminho canônico |
| P-GOV-DECISOES-1968-GATE-INEXISTENTE | auditoria M-02; plano B6 | Decisão viva sustentada pelo parecer de um gate que não existe na ref | ATIVO | `agent-orchestration/controle/decisoes.md:1968` "O `cadeira-permanente-backend-review`, na homologação nº 1, conferiu e mediu" (em `D-QUORUM-B-GOV-ELENCO`) | BAIXA | 2026-09-08 | pre-existente | 9 | decisão do assento (`P-GOV-INSPETOR-33-SEM-NORMA`) | governança de registro | NAO | conflito registrado §A2, sem apagar, com remissão à decisão do assento |
| P-GOV-CONTRATO-HIGIENE-TEXTUAL | auditoria B-03 | Contrato com "assento" solto, `1-ter` antes de `1-bis` e numeração aninhada | ATIVO | `CLAUDE.md:359` (1-ter) antes de `:391` (1-bis); `:476` "…porteiro, inspetor ou assento…" | BAIXA | #381-#383 | pre-existente | 9 | não fazer junto de P-GOV-INSPETOR-RECEITAS (mesmos arquivos) | governança de contrato | NAO | ordem 1-bis < 1-ter; zero "assento" normativo; espelho idêntico |
| P-GOV-MONOCULTURA-FABLE-NOS-GATES | auditoria §4(b) | Os três gates fixados no mesmo modelo caem juntos na mesma cota | ATIVO (fato); efeito de diversificar = HIPÓTESE não medida | `.agents/agents/{planejador-mestre,porteiro-pos-merge,inspetor-de-terreno-da-junta}.md:4` `model: fable` (espelho de `.claude/agents/`); 429 do Fable registrado nos pareceres de 2026-09-08/09 | BAIXA | D-PLANEJADOR-MODELO-FABLE; D-FALLBACK-MODELO-FABLE-OPUS | pre-existente | 5-confiabilidade | `P-GOV-ESGOTADO-SEM-TESTE` | governança de modelo | NAO | medir custo e qualidade fora de Fable antes de trocar; decisão registrada |
| P-GOV-ESPECIALISTAS-SEM-POLITICA-DE-VERSIONAMENTO | auditoria A-02; plano §6.5 | Não há regra se corpos de especialista são versionados ou ignorados | ATIVO no lado da ref; disco da árvore principal NAO MEDI | `.gitignore:48-52` só `.claude/worktrees/`; o #385 versionou 12 corpos `jurado-06d-*` (delta §3 #5) enquanto a árvore da sessão tem dezenas de `?? .claude/agents/especialistas/*` (git status de abertura desta sessão) | BAIXA | 2026-09-08 | pre-existente | 9 | decisão do dono (§6.5); `P-GOV-C10-ENCERRADO` | governança de elenco | NAO | regra escrita (versionar tudo ou ignorar com exceção nomeada) + checagem no auditor |

### 2c. PARCIALMENTE — a pendência existe, falta o componente nomeado (proposta: APPEND, não ID novo)

| P- existente | componente que falta | prova no head |
|---|---|---|
| `P-MOBILE-OS-SEEDS` (l.1912) | limpar seeds já gravados no aparelho | `features/work_orders/data/work_order_repository.dart:181+` `_upsertRemoteOrders` sem nenhum `delete/remove/clear`; seed só quando `_remoteApi == null` (`:118-121`) — afeta aparelhos que rodaram o build local |
| `P-O6R-BACKLOG` (l.2318-2321) | deliberação D-001..D-004 sem dono nem prazo | a J-6R diz "O humano delibera os rascunhos arquiteturais D-001..D-004"; nenhuma linha `dono:` nessa entrada |
| `P-RBAC-GATING-MOCKSHELLS` (l.1397) | chama `ChecklistRunsPage` de "REAL" — ela lista modelos; Console sem porta | `ChecklistRunsPage.tsx:9` só `listAvailableChecklists`; `/dispatch/console` sem referência fora de `App.tsx:648` |
| `P-O6R-B04` / Ω6R-QUA-002 (l.2772) | `work_order_material.add` também não é drenado | `InventorySyncActionTypes.materialAdd` só em `prestador_repository.dart:125` e `api_contracts.dart:135`; nenhum replay de inventário |
| `P-GOV-D-DURABILIDADE-FORA-DA-MAIN` (l.6883) | estado mudou: as branches já estão no remoto; a decisão segue fora da `main` | `git branch -r`: `origin/chore/gov-auditoria-elenco`, `origin/chore/gov-elenco-fatia-b`; `grep -c D-DURABILIDADE decisoes.md` = 0 |
| `P-PLATFORM-MOCK-WIRING` (l.1505) / `P-PLATFORM-TENANTDETAIL-REAL` (l.1565) | defasadas: Overview e Detalhe já têm dado real; as telas fictícias hoje são outras | `PlatformOverviewPage.tsx` e `PlatformTenantDetailPage.tsx` com 3 hooks cada; ficção atual em P-WEB-PLATAFORMA-TELAS-FICCAO |
| `P-GOV-CAMINHO-REPO-SESSAO` (l.7961) | cabeçalho ainda diz "FECHADA" com linha de status ABERTA | l.7961 × l.7999 (classe G-3 do porteiro; invisível ao detector `CABEC`, que exige negrito) |
| `P-GOLIVE-GATES` (l.1364) | severidade "a classificar" e dono "a atribuir" com o deploy bloqueado pela J-6R e o AV fail-closed | l.1369 |

### 2a. Produto (sintese.md) — parte 1

| ID proposto | fonte | descrição (≤15 palavras) | status_comprovado | prova | gravidade | origem | escopo | risco | dependências | bloco_dono | impacto_vendavel | teste_de_encerramento |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| P-WEB-CLOUD-BILLING-CARTAZ | sintese B1-B3, C1 (selo), C2 (registry), D8 | Tela Cloud Billing é cartaz de literais com selo "IA"; dados e rotas órfãos | ATIVO | `frontend/src/modules/platform/cloud-billing/pages/PlatformCloudBillingPage.tsx`: 245 l., 0 `useEffect/useState/await/fetch/.service`, 0 `onClick`, literal "48,2k", selo `IA` l.181, insights literais l.62; `cloud-billing.{adapter,service,mock}.ts` sem página importadora; `src/modules/navigation/navigation.registry.ts:41` `status: "implemented"` | MÉDIA | `d5a4ed43` (#111) trocou a versão ligada `4d6e1219` por literais | pre-existente | 8-acabamento-web/mobile | decisão do dono D5/D9 (religar × parada honesta) | bloco web de plataforma | NAO — só `platform_admin` alcança; BLOQUEIA demo honesta | smoke: página chama o adapter real (API mockada), 0 literal monetário no JSX, sem selo "IA" sem fonte; `status` do registry coerente |
| P-DONO-CLOUD-BILLING-ESCOPO | sintese B8, C2, D5 | Cobrança de nuvem calculada nunca vira fatura; custo AWS entra só por CSV manual | ATIVO (por desenho, sem decisão) | `src/modules/cloud-charges/cloud-charge.types.ts:24` status `draft/ready/locked/voided`; `src/modules/cloud-costs/aws-cur.importer.ts:7,24` só `manual_csv/mock_fixture`; `aws-cur.types.ts:4` declara `s3_cur`/`athena_query` sem importador | MÉDIA | escopo dos blocos cloud ("fora de escopo mantido: fatura…", status-geral:2367) | pre-existente | 4-faturamento/dinheiro | decisão do dono; P-WEB-CLOUD-BILLING-CARTAZ | dono → bloco de produto cloud billing | NAO — receita da plataforma, não fluxo do cliente | decisão em `decisoes.md`; enum de fonte = importadores existentes (teste); se faturar, transição `invoiced` testada |
| P-WEB-FIN-BAIXA-E-CONTA-SEM-TELA | sintese B9, B10, A6 | Web emite título mas não liquida: sem tela de baixa nem de conta | ATIVO | `grep -rl financial-account frontend/src` = 0; `grep -rn "/pay" frontend/src` = 0; front só lista/cria/muda status (`ChargesPage`, `PaymentsPage`, `FinanceiroPage` via `financial-titles.adapter`); backend `financial-entry.routes.ts:95` `POST /financial-titles/:id/pay`, único caminho a `paid` (`financial-title.service.ts:284-286`, sintese) | ALTA | Ω4-2/Ω4-4 (2026-07): backend sem fatia de front | pre-existente | 7-fluxo-necessario-para-venda | B-O6R-02 (atomicidade do pay) já mergeado; conta antes da baixa | bloco web financeiro ("ciclo do dinheiro") | **BLOQUEIA** — pela web um título nunca vira pago nem caixa | smoke: criar conta → emitir título → baixar pela tela → saldo em `GET /financial-accounts/:id/balance`; título `paid`; 403 e estados §7 |
| P-WEB-FIN-CHEQUE-FECHAMENTO-COMISSAO-SEM-TELA | sintese B11-B13 | Cheques, fechamento de período e política de comissão sem tela; backend pronto | ATIVO | `grep -rln /cheques frontend/src` = 0; `grep -rln financial-period frontend/src` = 0; `commissions.service.ts:44,112` consome só `statements/summary`, `my-summary`, `settlements` — `policies`/`basis-events` sem consumidor | MÉDIA | Ω4-6/Ω4-7, Ω3F-6 (2026-07) | pre-existente | 7-fluxo | conta (linha acima) para cheque | bloco web financeiro | NAO — não é pré-requisito de venda; condição: comissão configurável vendida → BLOQUEIA | smoke por tela (cheque depositar/compensar; fechar/reabrir; CRUD de política) com 403 por permissão |
| P-CHK-APLICABILIDADE-SEM-ROTA | sintese B14, A4 | Motor de aplicabilidade de checklist existe sem rota, permissão nem tela | ATIVO | `src/modules/work-orders/work-order.service.ts:1991` "entre o 04c-A e o 04c-B não existe rota nem permissão", `:2025` "nenhuma linha pode nascer `resolved`"; `grep -c applicab` em `src/app.ts` e `checklist.routes.ts` = 0 | MÉDIA | CHK P1 PR-04c-A (2026-08-14) | pre-existente | 7-fluxo | gate da CHK P1 (D10, registrado) | trilha CHECKLIST P1, PR-04c-B | NAO — execução de checklist funciona; condição: "checklist certo por tipo de serviço" vendido → BLOQUEIA | CRUD de regra com teste RBAC; OS com regra ativa gera `WorkOrderChecklist` `resolved` com `rule_id` |
| P-WEB-CHK-EXECUCOES-INEXISTENTES | sintese B16, C3 (PR-05) | "Ver execuções" leva à lista de modelos; execuções não têm tela nem rota | ATIVO | `ChecklistEditorPage.tsx:92` `CHECKLIST_RUNS_PATH = "/operations/checklists"`; `App.tsx:701-704` renderiza `ChecklistRunsPage`, que importa só `listAvailableChecklists` (`:9`); `listRuns` só no repositório (`checklist-prisma.repository.ts:585,1019`) | MÉDIA | CHK P1 PR-02a; PR-05 planejado | pre-existente | 8-acabamento-web/mobile | — | trilha CHECKLIST P1, PR-05 | NAO — dossiê de pátio mostra runs por processo | rota de runs com escopo por papel + tela §7; botão abre execuções do modelo |
| P-WEB-PLATAFORMA-TELAS-FICCAO | sintese B17 (residual), B18, D9 | Planos e Módulos, Auditoria Global, APIs e lista de Organizações exibem dado inventado | ATIVO | `modules/platform/pages/PlatformPlansModulesPage.tsx` (73 l.), `PlatformAuditPage.tsx` (90), `PlatformApisPage.tsx` (78): 0 hooks; `PlatformTenantsPage.tsx:18-19` ids `ten-sp`/`ten-agromax`, `:81` navega para id fabricado | MÉDIA | WS-SCALE (2026-07-20) | pre-existente | 8 | D9 (padrão `invoices-nfe-honest-stop`) | bloco web de plataforma | NAO — só operador; BLOQUEIA demo. `P-PLATFORM-MOCK-WIRING`/`P-PLATFORM-TENANTDETAIL-REAL` estão defasadas (Overview e Detalhe já têm 3 hooks cada) | guard "sem número fabricado" por tela; lista de Organizações de `GET /platform/overview` |
| P-WEB-PLATAFORMA-SEGURANCA-FABRICADA | sintese B19 | Configurações da Plataforma mostram MFA e auditoria "ligados" por literal | ATIVO | `PlatformSettingsPage.tsx:8` `function Toggle({ on }: { on: boolean })`; `:43` "MFA obrigatório para admins … `<Toggle on />`"; `:44` "Auditoria de operações críticas … `<Toggle on />`" | ALTA | WS-SCALE (2026-07) | pre-existente | 3-seguranca/permissoes | — | bloco web de plataforma | **BLOQUEIA** — UI entregue declara controle de segurança inexistente | teste de render: nenhum toggle de controle sem fonte real (ou ligado a config persistida e testada) |
| P-WEB-ROTAS-SEM-PORTA | sintese B20-B22 | Rotas vivas sem menu/link; provisionamento de módulos só por URL digitada | ATIVO | `App.tsx:648` `/dispatch/console`, `:640` `/field-operators`, `:674` `/finance/invoices`, `:570` `/controle/usuarios/logs`: nenhuma referência fora do próprio módulo; `/logistics` só no menu não montado; `App.tsx:247` `/platform/tenants/:tenantId/modules` sem `navigate`/`Link`; `DispatchConsolePage.tsx`: 11 botões, 5 `onClick` | MÉDIA | 2026-07 | pre-existente | 8 | P-WEB-GATE-MODULO-INCOMPLETO | bloco web de navegação | NAO — há contorno por URL | teste de alcançabilidade web: toda rota de `App.tsx` tem porta ou está em allowlist nomeada |
| P-WEB-EVENTBUS-MOCK | sintese B23 | Provider de eventos cicla mocks a cada 9 s contra rota inexistente | ATIVO | `providers/EventProvider.tsx:33` `createPollingClient()`; `services/realtime/pollingClient.ts:2` importa `mockEvents`, `:8` 9000 ms, `:23` `/api/v1/events/stream`; consumidores: `AppProviders.tsx` e `pages/WorkOrderFormPage.tsx` (morta) | BAIXA | pré-C3 (2026-07) | pre-existente | 5-confiabilidade | `P-004` | bloco web de faxina | NAO — nenhum consumidor vivo exibe | provider na SSE real ou removido; teste: nenhum import de `mocks/` em `providers/` |
| P-WEB-GATE-MODULO-INCOMPLETO | sintese B25, A5 | 27 itens do menu fora do gate de módulo; backend não recusa módulo não contratado | ATIVO (lado backend medido por padrões, não exaustivo) | `src/modules/navigation/navigation.registry.ts`: 36 entradas `path:`; `frontend/src/layouts/appSidebarNav.ts` `MVP_NAV_PATHS` = 51; grep `requireModule\|requireTenantModule\|assertModuleEnabled\|requireFeature\|module_disabled` em `src/` só acha flags do bootstrap mobile (`mobile.routes.ts:221-257`) | ALTA | esconde-fino (2026-07) | pre-existente | 4-faturamento/dinheiro | P-WEB-ROTAS-SEM-PORTA (link de provisionamento); decisão de planos | bloco "gate comercial" | **BLOQUEIA** se a venda for por plano/módulo — Starter vê (e, sem recusa por rota, usa) o Enterprise | tenant sem módulo X: item some E rota do módulo X = 403; guard: todo path de `MVP_NAV_PATHS` está no registry |
| P-API-VERBOS-SEM-CONSUMIDOR | sintese B28 | Verbos de administração sem nenhuma tela (merge de identidade de veículo) | ATIVO | `grep -rln vehicle-identities frontend/src mobile/flutter_app/lib` = 0; `identity-links` = 0 | BAIXA | Ω-VID / B-O6R-01 | pre-existente | 8 | — | backlog de telas de administração | NAO | tela ou remoção por verbo, com teste de consumidor |
| P-MOBILE-PRESTADOR-SEM-PORTA | sintese B30 | Fluxo Prestador (diagnóstico, execução, materiais) sem porta de entrada | ATIVO | `mobile/flutter_app/lib/app/router.dart:159,339` únicos registros de `/work-orders/:id/service`; nenhum `push/go` para ele em `lib/` (o único `go` de `prestador_service_screen.dart:192` sai para `/execute`) | ALTA | era local-first | pre-existente | 7-fluxo | P-MOBILE-ESTOQUE-TECNICO-FABRICADO; Ω6R-QUA-005 | bloco mobile "portas" | **BLOQUEIA** — prestador não registra diagnóstico nem material faturável | widget test: OS `serviceType != tow` tem ação que navega a `/service` |
| P-MOBILE-CONCLUSAO-SEM-PORTA | sintese B31 | Tela de Conclusão, onde a comissão aparece, é inalcançável | ATIVO | `router.dart:165,345` únicos registros de `/work-orders/:id/conclusion`; zero `push/go` para ela | MÉDIA | era local-first | pre-existente | 7-fluxo | P-MOBILE-PRESTADOR-SEM-PORTA | bloco mobile "portas" | NAO — comissão acumula no backend (`basis-events`); condição: "comissão no app" vendida → BLOQUEIA | widget test: fim do atendimento navega a `/conclusion` |
| P-MOBILE-CATALOGO-MODULOS-INERTE | sintese B32, B43 | Catálogo de módulos nunca renderiza; Estoque sem porta; 2 rotas apontadas inexistem | ATIVO | `core/modules/module_resolver.dart:9` `visibleModules` sem chamador; `/inventory` só em `router.dart:236,414` e `bootstrap_repository.dart:160`; `bootstrap_repository.dart:153-155` `tenant_checklist`→`/checklists`, `:170` `/notifications` — nenhum é `path:` do router | MÉDIA | B-090 | pre-existente | 8 | Ω6R-QUA-002 | bloco mobile "portas" | NAO — estoque mobile já não sincroniza (Ω6R) | todo `route` do catálogo existe no router; home renderiza `visibleModules` |
| P-MOBILE-LGPD-GPS-SEM-PORTA | sintese B33 | Consentimento LGPD de GPS nunca pode ser dado: `/location` sem porta | ATIVO (efeito na captura: HIPÓTESE forte, ramo não lido) | `router.dart:232,410` `/location` → `LocationConsentScreen`; zero `push/go` para `/location`; `core/location/geolocator_device_location_provider.dart:56-59` exige `LocationConsentStore` | ALTA | era telemetria | pre-existente | 7-fluxo | — | bloco mobile "portas" | **BLOQUEIA** — sem consentimento a posição do técnico não entra; Mapa/alocação por distância sem dado de campo | widget test: primeiro uso oferece consentimento; unidade: captura só com consentimento, e o aceite é alcançável |

### 2a. Produto (sintese.md + go-live) — parte 2

| ID proposto | fonte | descrição (≤15 palavras) | status_comprovado | prova | gravidade | origem | escopo | risco | dependências | bloco_dono | impacto_vendavel | teste_de_encerramento |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| P-MOBILE-GUINCHO-ENTREGA-INALCANCAVEL | sintese B34 | Perna de entrega do guincho nunca acende: sem `kind=delivery` nem passos 3/4 | ATIVO | `features/work_orders/ui/work_order_execute_screen.dart:94` push `/checklists/…/run?workOrderId=` sem `kind`; `router.dart:194,373` "ausência de `?kind=` … = coleta"; `features/work_orders/domain/work_order_steps.dart` (guincho): índice vai de 2 a 5 (`pendingApproval/completed/approved/cancelled => 5`) | ALTA | era local-first; `kind` na CHK P1 PR-04b | pre-existente | 7-fluxo-necessario-para-venda | P-MOBILE-TESTE-ALCANCABILIDADE | bloco mobile "portas" | **BLOQUEIA** — o app cobre só a coleta, metade do serviço de guincho | widget test: OS em rota de entrega abre run com `kind=delivery`; passos "Rota entrega"/"Entrega" acendem; comparação coleta×entrega alcançável |
| P-MOBILE-STUBS-MOCKS-MORTOS | sintese B37, B42 | Stubs `Pending*` mortos (token local nunca nulo) e mock de sync em produção | ATIVO | `core/auth/auth_repository.dart:97` `accessToken: 'dev-token-local'`; `core/sync/sync_providers.dart:80,124` escolhem stub por `accessToken == null`; `sync_providers.dart:41` `MockExpenseSyncApi(SyncApiResult.success)` | BAIXA | era local-first | pre-existente | 6-contratos/testes | Ω6R-QUA-001 (mesmo arquivo) | faxina mobile junto do B-O6R-03 | NAO | guard: nenhum `Mock*`/`Pending*` alcançável fora de teste |
| P-MOBILE-FILA-OS-NAO-DRENADA | sintese B39 (residual não-Ω6R) | "Pedir aprovação" e "não consigo iniciar" entram na fila e nunca saem | ATIVO | enfileirados em `features/work_orders/data/work_order_repository.dart:474` (`approvalRequest`) e `:637` (`unableToStart`), sem chamada REST paralela; `core/sync/sync_replay_service.dart:625+` drena só `create`, `statusUpdate`, `assign`; backend `src/modules/mobile/mobile-work-order-sync.ts:213-260` aceita `create/assign/mileage/status_change` | ALTA | era local-first | pre-existente | 1-perda/corrupcao-de-dados | vocabulário acordado com o backend | bloco mobile de sync (vizinho do B-O6R-11) | **BLOQUEIA** — ação do técnico aceita pela UI e mostrada na timeline local some em silêncio | enfileirar os 2 tipos → replay envia → backend persiste ou recusa com código visível; fila vazia ao fim |
| P-MOBILE-ESTOQUE-TECNICO-FABRICADO | sintese B40 | Estoque do técnico é catálogo semente de 8 SKUs; o endpoint não existe | ATIVO | `features/prestador/data/prestador_repository.dart:38-87` 8 SKUs literais (`ELE-0102`…`FIX-0012`); `grep "technician/stock" src` = 0 | ALTA | era local-first | pre-existente | 7-fluxo | P-MOBILE-PRESTADOR-SEM-PORTA; Ω6R-QUA-002 | bloco mobile + endpoint backend | **BLOQUEIA** (junto do Prestador) — material faturável escolhido de lista inventada | `GET /mobile/technician/stock` escopado por custódia + teste; app mostra vazio honesto |
| P-MOBILE-OCR-ANDAIME-INERTE | sintese B41 | OCR de recibo: estados, colunas e permissão sem consumidor | ATIVO | `features/expenses/data/expense_repository.dart:341` `markReceiptOcrReviewed` sem chamador em `lib/` | BAIXA | trilha despesas | pre-existente | 8-acabamento-web/mobile | decisão de OCR (serviço externo) | backlog mobile | NAO | andaime removido ou ligado; todo estado de `ReceiptOcrStatus` alcançável |
| P-MOBILE-MINHAS-OS-SEM-FILTRO | sintese B44 | "Minhas OS" lista a organização inteira, sem filtro de atribuição nem paginação | ATIVO | `shared/ui/home_screen.dart:272` "Minhas OS", `:122` filtra só `tenantId`; `work_order_repository.dart:88` `workOrdersForUser` sem chamador; `work_order_remote_api.dart:74-91` `GET` sem `assignedUserId` nem página | ALTA | era local-first | pre-existente | 7-fluxo | `P-MOBILE-OS-SEEDS` (mesma lista) | bloco mobile | **BLOQUEIA** — acima de 20 OS abertas (default do backend) a OS do técnico pode sumir, e ele vê as alheias | 25 OS, 1 atribuída → lista mostra a dele, pagina, não mostra as outras |
| P-MOBILE-FAXINA-TERMOS-TECNICOS | sintese B45, §6(a6) | App mostra papel cru, placeholder técnico, nome "flutter_app" e README de template | ATIVO | `shared/ui/home_screen.dart:153-154,407` `'${session.user.tenantRole} · …'`; `shared/ui/module_placeholder_screen.dart:42` "Permissao $requiredPermission nao retornou no bootstrap" (`/approvals`, `router.dart:253-255`); `android/app/src/main/AndroidManifest.xml:6` `android:label="flutter_app"`; `README.md` "A new Flutter project." | MÉDIA | era local-first | pre-existente | 8-acabamento-web/mobile | `P-CHK-SEED-DEMO-SUJO` ("Tenant Demo") | faxina mobile | **BLOQUEIA** (custo de horas) — viola §3; app não se distribui com nome "flutter_app" | cabeçalho com rótulo de negócio; guard de strings sem permissão/papel técnico; label = nome do produto |
| P-MOBILE-TESTE-ALCANCABILIDADE | sintese §6(b) | Nenhum teste prova que toda rota do app tem porta de navegação | ATIVO | medido aqui: `/service`, `/conclusion`, `/location` sem `push/go` em `lib/`, com a suíte Flutter verde (864 no painel) | MÉDIA | — | pre-existente | 6-contratos/testes | — | bloco mobile "portas" (1º commit) | NAO — é a prevenção da classe | teste extrai os `path:` do router e exige alvo de navegação ou allowlist nomeada; vermelho-controle com rota órfã plantada |
| P-MOBILE-DESPACHO-SEM-PUSH | sintese C3 | Despacho só chega ao técnico se ele abrir e atualizar o app | ATIVO | `mobile/flutter_app/pubspec.yaml`: 0 ocorrências de `firebase_messaging\|flutter_local_notifications\|workmanager\|web_socket\|socket_io` | MÉDIA | fora do MVP mobile | pre-existente | 7-fluxo | serviço externo (FCM/APNs) → junta-5 + PD | bloco próprio pós-decisão | NAO — contorno declarável (despachante avisa; técnico atualiza); objeção comercial forte | despacho criado → notificação entregue (provedor mockado) e abre a OS |
| P-NOTIF-SEM-CANAL-EXTERNO | sintese B50 | Notificação só grava linha; não há canal de e-mail, SMS ou push | ATIVO | `src/modules/notifications/notification.jobs.ts:13-14` só `createFromDomainEvent`; grep `nodemailer\|sendgrid\|twilio\|smtp\|firebase\|fcm\|webpush\|client-ses\|client-sns` em `src`+`package.json` = 0 | MÉDIA | Ω4C Central de Notificações | pre-existente | 7-fluxo | serviço externo → junta-5 + PD | bloco próprio | NAO — central in-app funciona; quem não abre o ERP não é avisado | adapter de canal com teste de entrega (mock) e reenvio |
| P-JOBS-HANDLERS-PLACEHOLDER | sintese B49 | Dois handlers vazios ("Placeholder") consomem fila a cada evento | ATIVO | `src/infra/jobs/job.registry.ts:47-49,53-55` corpo vazio "Placeholder"; `src/infra/events/domain-event.publisher.ts` `eventJobMap`: `checklist_run.attachment_uploaded`→`checklist-attachment-postprocess`, `audit_log.created`→`audit-log-fanout` | BAIXA | trilha jobs | pre-existente | 5-confiabilidade | B-O6R-08 | B-O6R-08 ou faxina | NAO | handler com corpo testado ou mapeamento removido |
| P-IMPOUND-OUTBOX-SIVEC-SEM-CONSUMIDOR | sintese B51, A2 | Outbox Sivec é escrito por 5 repositórios e nunca lido fora de teste | ATIVO (por desenho: "captura, nunca envio real", `prisma/schema.prisma:3122`) | `src/modules/impound/impound.outbox.repository.ts`: `listOutboxEventsTx` sem chamador; `listOutboxEventsForTenant` só em `tests/impound-outbox.test.ts`; `appendOutboxEventTx` em 5 repositórios | BAIXA | Ω5P PR-20 | pre-existente | 5-confiabilidade | decisão de interop Sivec | trilha Pátios | NAO — interop fora do MVP; a fila cresce sem poda | decisão registrada; consumidor/poda testado, ou parar de escrever |
| P-FLEET-ALERTAS-SEM-AGENDADOR | sintese A8 | Alertas de frota só disparam por botão; nenhum job os agenda | ATIVO | `src/modules/notifications/fleet-alerts.runner.ts:91`; único chamador `notification.controller.ts:82` (rota `notification.routes.ts:121`); `job.registry.ts` (12 registros) sem job de frota | MÉDIA | Ω4C Fase 1 | pre-existente | 5-confiabilidade | B-O6R-08 (agenda singleton) | B-O6R-08 ou notificações | NAO — disparo manual funciona | job agendado idempotente: vencimento gera alerta sem clique |
| P-MOBILE-CHECKIN-CONFERE-CODIGO-DA-OS | sintese A9 | Check-in pede dígitos da placa e compara com o código da OS | ATIVO | `features/work_orders/ui/work_order_detail_screen.dart:918-919` `_expected = wo.code.substring(code.length - 2)`; `:931` "Informe os 2 ultimos digitos da placa"; `:1083` "Digitos nao conferem com a placa do veiculo" | MÉDIA | era local-first | pre-existente | 3-seguranca/permissoes | placa no DTO da OS | bloco mobile | NAO — GPS coexiste; mas o controle é ineficaz e o rótulo mente (D-007) | placa `ABC1D23`, código `OS-1042`: "23" aceita, "42" recusa |
| P-CATALOGO-TELAS-PLANEJADAS-AUSENTES | sintese C3 (telas), D15 | Telas do catálogo nunca feitas: rotas logísticas, disponibilidade, chat, perfil, basemap | ATIVO (NAO MEDI por tela; herdado da sintese com fonte) | web: Rotas Logísticas e Técnicos·Disponibilidade sem rota; app: sem chat/perfil-editar/notificações; mapa do app em `CustomPaint` (`work_order_operational_map_screen.dart:471`); "Organizações·Clientes" sem decisão de aposentadoria | BAIXA | catálogo de 74 telas | pre-existente | 8-acabamento-web/mobile | decisão de escopo do dono | roadmap | NAO | rota + estados §7 por tela, ou saída do catálogo por decisão |
| P-DOC-CONTRATOS-PROMETEM-INEXISTENTE | sintese C3 (Cognito, ocr/scan) | Contrato promete Cognito e `POST /mobile/ocr/scan`; código tem JWT próprio e nenhuma rota | ATIVO | `CLAUDE.md` §2.4 "Cognito em prod"; `grep -rni cognito src` → só comentário (`authority-credential.types.ts:2`); `docs/claude-code-handoff/API_CONTRACTS.md:106` `POST /mobile/ocr/scan` | BAIXA | contrato 2026-07 | pre-existente | 9-documentacao/rastreabilidade | decisão do dono (integrar × escrever "JWT próprio") | governança de contrato (espelho `CLAUDE.md`/`AGENTS.md`) | NAO | contrato = código; teste: todo endpoint do `API_CONTRACTS` existe no router |
| P-KPI-ROADMAP-CONGELADO | sintese §7 | Roadmap do painel parado em 2026-08-19 marca blocos mergeados como "a_fazer" | ATIVO | `Kpis/kpis-latest.json` `roadmap.as_of` = 2026-08-19; `B-O6R-02` e `B-O6R-07` `a_fazer` (mergeados em #371 e #369/#380) | MÉDIA | 2026-08-19 | pre-existente | 9-documentacao/rastreabilidade | — | próximo PR que tocar `Kpis/` | NAO | guard: bloco com `merge_commit` no history não fica `a_fazer`; `as_of` ≥ último merge |
| P-KPI-MVP-REGUA-CODIGO-ESCRITO | sintese §7 | `mvp_demo 99`/`mvp_vendavel 88` medem código escrito, não caminho de usuário | ATIVO | `Kpis/kpis-latest.json`: `mvp_demo` 99, `mvp_vendavel` 88 e, no mesmo arquivo, `production_readiness.veredito` "REPROVADO PARA PRODUÇÃO", `deploy_bloqueado: true` | MÉDIA | régua do painel | pre-existente | 9-documentacao/rastreabilidade | decisão do dono sobre a régua | bloco de KPI | NAO — não trava venda; induz a crer que está vendável | régua com fonte medida (porta + dado real) ou rótulo "estimativa"; guard de coerência com `production_readiness` |
| P-DOC-GO-LIVE-READINESS-VENCIDO | `docs/go-live-readiness.md` §0, §5 | Documento diz "nenhum código bloqueia o go-live" e manda rotacionar chave dispensada | ATIVO | `docs/go-live-readiness.md:76` "**Nenhum código bloqueia o go-live.**"; `:11-14`, `:44` rotação obrigatória (dispensada: `P-GOLIVE-SECRET-ROTATE` FECHADA, `pendencias.md:1330`); contradiz J-6R 5×0 e o AV fail-closed | MÉDIA | 2026-07-19 | pre-existente | 9-documentacao/rastreabilidade | — | dono de `P-GOLIVE-GATES` (hoje "a atribuir") | NAO — documento; mas quem o ler conclui que o go-live está pronto | revisão datada no topo apontando J-6R e AV; `rg "Nenhum código bloqueia"` = 0 ou qualificado |

## BLOQUEIAM VENDAVEL

**Critério (meu, declarado):** BLOQUEIA = sem isso um fluxo que o cliente compra não se completa, ou o produto perde dado/dinheiro, ou a UI entregue afirma um controle que não existe. Tela que só o `platform_admin` alcança e mostra ficção = NAO para venda (bloqueia a demo honesta, não o cliente).

### As 10 ausentes que bloqueiam

| # | ID | por que bloqueia | natureza do conserto |
|--:|---|---|---|
| 1 | `P-WEB-FIN-BAIXA-E-CONTA-SEM-TELA` | pela web um título nunca vira pago nem caixa: o ERP emite e não recebe | fiação sobre backend pronto e testado (B-O6R-02 mergeado) |
| 2 | `P-MOBILE-MINHAS-OS-SEM-FILTRO` | "Minhas OS" = OS da organização inteira, 1ª página de 20, sem filtro de atribuição | filtro `assignedUserId` + paginação |
| 3 | `P-MOBILE-GUINCHO-ENTREGA-INALCANCAVEL` | o app cobre só a coleta: a perna de entrega nunca acende | navegação com `kind=delivery` + status 3/4 |
| 4 | `P-MOBILE-PRESTADOR-SEM-PORTA` | fluxo Prestador inteiro sem porta de entrada | um botão no detalhe da OS |
| 5 | `P-MOBILE-ESTOQUE-TECNICO-FABRICADO` | material faturável escolhido de 8 SKUs inventados; endpoint inexistente | endpoint + consumo (acompanha o nº 4) |
| 6 | `P-MOBILE-FILA-OS-NAO-DRENADA` | "pedir aprovação" e "não consigo iniciar" somem em silêncio | replay + vocabulário no backend |
| 7 | `P-MOBILE-LGPD-GPS-SEM-PORTA` | consentimento de GPS nunca pode ser dado → sem posição do técnico no Mapa | entrada na Home/Perfil (efeito na captura: HIPÓTESE forte, ramo não lido) |
| 8 | `P-WEB-GATE-MODULO-INCOMPLETO` | **se a venda for por plano/módulo**: 27 itens fora do gate e nenhuma recusa por rota achada no backend | registrar os 27 + recusa server-side |
| 9 | `P-WEB-PLATAFORMA-SEGURANCA-FABRICADA` | UI declara "MFA obrigatório para admins" ligado; não existe | remover ou ligar a config real |
| 10 | `P-MOBILE-FAXINA-TERMOS-TECNICOS` | app se chama "flutter_app"; cabeçalho mostra `field_technician`; placeholder técnico | horas; viola §3 |

### Condicionais (NAO hoje; BLOQUEIA se o item estiver no que se vende)

- `P-CHK-APLICABILIDADE-SEM-ROTA` — se "checklist certo por tipo de serviço" for vendido.
- `P-WEB-FIN-CHEQUE-FECHAMENTO-COMISSAO-SEM-TELA` — se política de comissão configurável for vendida.
- `P-MOBILE-CONCLUSAO-SEM-PORTA` — se "comissão visível no app" for vendida.
- `P-MOBILE-DESPACHO-SEM-PUSH` — objeção comercial forte; contorno manual declarável.
- `P-WEB-CLOUD-BILLING-CARTAZ` e `P-WEB-PLATAFORMA-TELAS-FICCAO` — bloqueiam a **demo honesta**, não a venda.

### Já registradas que também bloqueiam (não duplicadas aqui)

- `P-MOBILE-OS-SEEDS` (ALTA) — seeds de OS no build padrão; o componente de **purga** falta (ver 2c).
- `Ω6R-QUA-001` em `P-O6R-B03` — sync de RDV sem `Authorization` (é o B38 da sintese; não duplicado).
- `P-O6R-B07B-SCANNER-AV-REAL` — todo upload responde 503 em produção.
- P0 Ω6R ainda abertos (ver `inventario-O6R.md`): `DIN-009`, `DAT-002`, `DAT-003`, residual do `SEC-002`.

### Placar desta medição

| | qtde |
|---|---:|
| linhas-candidato na tabela 1 (itens das fontes, alguns agrupados) | **140** |
| JA REGISTRADA | 37 |
| PARCIALMENTE (pendência existe, falta componente — propostas em 2c) | 8 |
| AUSENTE (linhas) | 58 |
| → **IDs ausentes confirmados por presença, com 13 campos** | **50** (35 produto/documentação + 15 governança) |
| → dos quais **BLOQUEIA vendável** | **10** |
| SUPERADA (fechada ou transitória, com prova) | 24 |
| NAO-PENDENCIA (método, desenho, roadmap) | 13 |

### Limites da medição (o que NÃO medi)

- Disco da árvore principal (`demo/investidor`): fora do terreno — A-01/A-02/A-03 dependem do parecer da regularização, não de medição minha.
- Recusa de módulo no backend: grep por padrões nomeados (`requireModule`, `module_disabled`…), não leitura de todas as rotas.
- LGPD: li que o provider exige `LocationConsentStore`; não li o ramo que pula a captura sem consentimento.
- `P-CATALOGO-TELAS-PLANEJADAS-AUSENTES`: herdado da sintese por tela, não re-medido tela a tela.
- J06 §9 ress.5 ("dois hashes" do §5 do plano): não identifiquei quais hashes.
- Nenhum teste, build ou base viva; `git status` só com `--no-optional-locks`.
