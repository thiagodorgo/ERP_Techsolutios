# Inventário — balde C (DIFERIDO-LEVE), linhas 1–38

- worktree (SOMENTE LEITURA): `C:/Users/AMP/Documents/GitHub/ERP_Techsolutios/.claude/worktrees/gov-elenco` · branch `main`
- head medido: `15ef3fbed57e217b66e6b307f9ae96502777fd09` (`15ef3fbe`, 2026-09-11 05:10:50 -0300, "fix(cloud-usage): unidade faturável na transação e resumo somado no banco (B-O6R-06) (#385)"); `git status --porcelain` vazio (sem mutação viva)
- fonte: `agent-orchestration/controle/pendencias-indice.md` §"ABERTAS · balde C — DIFERIDO-LEVE" (l.230), linhas 234–271 da tabela (P-004 … P-Ω4-2A-COBERTURA); texto de cada entrada em `pendencias.md` pela coluna `linha`
- data da medição: 2026-09-11
- método: status pelo CÓDIGO no head (leitura + `grep` + `git log -S` + `git ls-tree`), nunca pelo texto da entrada. Nada foi executado: nem teste, nem build, nem banco. Onde a prova exigiria execução: `NAO MEDI` + motivo
- esta versão sobrescreve a rodada anterior (morta por limite de sessão, head `ca5fd19a` em `b06`), sem herdar nada dela
- linhas gravadas incrementalmente durante a medição (P2); esta é a reescrita final ordenada, com numeração corrigida

## Placar desta fatia

| status_comprovado | qtde |
|---|---:|
| ABERTA | 27 (3 delas mais amplas/graves do que o texto diz: P-008, P-Ω3a, P-Ω3F4C) |
| PARCIAL | 9 |
| FECHADA (texto diz ABERTA) | 2 |

| gravidade medida | qtde |
|---|---:|
| ALTA | 1 |
| MÉDIA | 6 |
| BAIXA | 29 |
| — (fechadas) | 2 |

**Contaminação desta fatia:** 7 materiais escondidas (MÉDIA+) em 38 = 18%, e 5 bloqueiam o vendável. A estimativa da casa, de ~40%, veio de 11 amostradas.

## Tabela (13 colunas)

| # | ID | linha | descrição | status_comprovado | prova | gravidade | origem | escopo | risco | dependências | bloco_dono | impacto_vendavel | teste_de_encerramento |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | P-004 | 53 | Páginas mortas em `frontend/src/pages/` e duas fontes de navegação que divergem | ABERTA | `git ls-tree HEAD frontend/src/pages/` ainda lista `WorkOrdersListPage.tsx`, `WorkOrderFormPage.tsx`, `WorkOrderDetailPage.tsx`, com zero importadores em `frontend/src`, `frontend/tests` e `tests` (App.tsx:15-16 importa só Dashboard/Logistics de `pages/`; App.tsx:38 importa o Detail de `modules/work-orders/pages/`). Também mortos: `modules/work-orders/components/WorkOrdersTable.tsx` e `WorkOrderAssignForm.tsx` (0 importadores; o AssignForm tem `label="User ID"` e "A selecao visual sera implementada", andaime inerte por estar morto). "Sidebar dupla": `NAV_BY_ROLE`/`MVP_NAV_PATHS` só mudaram de arquivo, para `frontend/src/layouts/appSidebarNav.ts:173,214` (usado por `AppShell.tsx:12,44`), e seguem separados de `frontend/src/navigation/tenantNavigation.ts` (o próprio arquivo diz isso, l.69/171). Agravante: `.claude/skills/ui-ux-pro-max/SKILL.md:34` manda espelhar `WorkOrdersListPage.tsx`, a página morta | BAIXA | A5 (2026-07-07); páginas mortas desde `fb0ea65b` (2026-05-26) | pre-existente | 6-contratos/testes | P-026, P-032 (mesma navegação) | proposta `B-web-nav-fonte-unica` | NAO — o cliente não vê; o risco é teste de RBAC exercitar a navegação que não é a renderizada | (a) `git ls-tree HEAD frontend/src/pages/` sem as 3 páginas; (b) teste que monta a sidebar REAL (`buildSidebarNav`) para os 9 papéis e compara com `tenantNavigation` |
| 2 | P-005 | 64 | Skill ui-ux-pro-max prescreve `search.py`, que não existe | ABERTA | `git ls-tree HEAD .claude/skills/ui-ux-pro-max/`: `data` e `scripts` são blobs 100644 cujo conteúdo é `../../../src/ui-ux-pro-max/data` e `.../scripts` (symlink materializado; o alvo não existe); nenhum `search.py` no disco; SKILL.md l.54-60 ("Domínios liberados do search.py") ainda manda rodar `--domain ux/chart/react/web` | BAIXA | 2026-07-07 | pre-existente | 9-documentacao/rastreabilidade | — | proposta `B-gov-skill-uiux-limpeza` (espelhar em `.agents/skills/`) | NAO — ferramenta de agente, nada chega ao produto | SKILL.md sem instrução de comando inexistente, OU `search.py` presente; `git ls-tree` sem os 2 blobs-link |
| 3 | P-008 | 87 | O fallback do módulo de OS fabrica dados em três casos: lista vazia, create com erro e detalhe com erro | ABERTA — e mais ampla do que o texto diz | `frontend/src/modules/work-orders/work-orders.service.ts`: (i) l.28: a API responde 200 com 0 itens e a função devolve as 6 OS de `work-orders.mock.ts` (OS-000101 "Atlas Refrigeracao", "Hospital Santa Clara"…) com `source:"fallback"`; `WorkOrdersPage.tsx:302-305` mostra então "Sem conexão com a API — exibindo dados locais", o que é falso, porque a API respondeu. A página usa `STABLE_FILTERS` (l.133), então basta a organização não ter nenhuma OS. (ii) l.46-50: `createWorkOrder` engole QUALQUER erro (o `apiRequest` lança `ApiError` em todo non-2xx, `services/api/client.ts:80,98,109`) e devolve uma OS fabricada, `id:"fallback-created-work-order"`, `code:"OS-FALLBACK"`. `WorkOrderCreatePage.tsx:25-38` navega para ela, e o `setError("Nao foi possivel salvar a OS.")` fica inalcançável para erro de API: 409 de viatura indisponível, 422 de tarifa, 400 de destino e 403 somem, e o usuário cai na OS-000101 mock com a faixa "sem conexão". (iii) l.60-72: detalhe com erro (404/403) mostra OS mock. (iv) `registry/service-quotes/useServiceQuoteReferences.ts:54` e `operations/dispatches/dispatches.service.ts:107` herdam a lista mock, de modo que o select de OS do orçamento lista OS inventadas quando a organização está vazia ou quando o papel não tem `work_orders:read`. Nenhum teste cobre `fallback-created-work-order`/`OS-FALLBACK` (grep em `frontend/tests` e `tests` = 0). Código de origem: `9f12ea99` (2026-06-09) | ALTA | work-orders UI `9f12ea99` (2026-06-09); registrada em C2 (2026-07-07) | pre-existente | 1-perda/corrupcao-de-dados | P-Ω3a (select de OS do orçamento); D-007 | proposta `B-web-wo-sem-fallback-fabricado` | BLOQUEIA — organização nova vê 6 OS inventadas com aviso falso de "sem conexão"; e um create recusado pelo backend aparece como sucesso e leva a uma OS falsa, de modo que o que o operador digitou se perde sem aviso | testes de service: (a) 200 com lista vazia → `items: []` e `source:"api"`; (b) `createWorkOrder` com 422 → rejeita (propaga `ApiError`) e a página mostra o erro; (c) detalhe 404 → estado de erro, nunca OS mock; grep `fallback-created-work-order` em `frontend/src` = 0 |
| 4 | P-009 | 100 | Texto de conteúdo em #94A3B8 (2,56:1) e token muted #718196 (3,98:1), ambos abaixo do AA | ABERTA | 188 linhas com `color: "#94A3B8"` em `frontend/src` (ex.: `components/TablePage.tsx:44,58,100`, `layouts/AppShell.tsx:227`, `components/kpi/KpiDetailModal.tsx:150`); `styles/tokens/tokens.css:53 --text-muted: #718196`, inalterado desde `fb0ea65b` (`git log -S`). Contraste que calculei pela luminância relativa WCAG: #94A3B8 sobre branco = 2,56:1; #718196 sobre branco = 3,98:1 (piso 4,5:1) | BAIXA | C2 (2026-07-07) | pre-existente | 8-acabamento-web/mobile | DS congelado (REGRA ZERO da skill): precisa de decisão de DS | proposta `B-web-ds-contraste-muted` | NAO — reduz legibilidade mas não impede a demo; passa a BLOQUEIA se a venda exigir WCAG (ex.: setor público) | zero `color: "#94A3B8"` em texto de conteúdo e `--text-muted` com ≥4,5:1; teste de contraste sobre os tokens |
| 5 | P-010 | 111 | Engine B-124 de derivação do dashboard segue exportada, sem uso fora dos testes | ABERTA | `frontend/src/modules/dashboard/dashboard.adapter.ts`: `:18 deriveDashboardKpis`, `:243 deriveEnrichedDashboardKpis`, `:295 buildCriticalQueue`, `:430 deriveDashboardAlerts`, `:497 deriveDashboardEvents`; `pages/DashboardPage.tsx:4-11` importa só `deriveActiveDispatchRows`/`deriveFieldStatusRows`/`relativeTimeFrom` + tipos; o único consumidor dos 5 é `frontend/tests/dashboard.adapter.test.ts` (20 refs) | BAIXA | C3 (2026-07-07) | pre-existente | 6-contratos/testes | — | proposta `B-web-chore-dashboard-adapter` | NAO — tree-shaken, sem efeito em runtime; só infla a contagem de testes com código morto | grep dos 5 nomes em `frontend/` = 0 (definição e teste saem juntos); suíte frontend verde |
| 6 | P-012 | 135 | Tile "km/L médio da frota" não era clicável | FECHADA (texto diz ABERTA) | `frontend/src/modules/fleet/fuel/pages/AbastecimentoPage.tsx:331-343`: o tile está dentro de `<ClickableKpiCard detail={kpiDetails.kmL}>`. `git log -S"kpiDetails.kmL"` aponta `b1559d32` (2026-07-20, #248, "fan-out de cards clicáveis…") | — (era BAIXA) | F1 (2026-07-08) | pre-existente | 8-acabamento-web/mobile | — | nenhum; só encerrar o registro citando `b1559d32` | NAO | já satisfeito: `ClickableKpiCard detail={kpiDetails.kmL}` presente |
| 7 | P-013 | 150 | `assign` de OS aceita viatura em manutenção; o guard existe só no create | ABERTA | `src/modules/work-orders/work-order.service.ts:442` chama `assertVehicleAvailable` só no create; `assign()` (l.1669) lê `vehicleId` (l.1676) e faz apenas `assertReferenceExists` (l.1678). Em `src/`, `assertVehicleAvailable` aparece em 2 linhas: a definição (l.644) e o create. A web não manda vehicleId no assign (`frontend/.../work-orders.types.ts:259-263`), mas `POST /work-orders/:id/assign` (`work-order.routes.ts:196`) aceita | BAIXA | F2 (2026-07-08) | pre-existente | 5-confiabilidade | — | proposta `B-api-wo-assign-guard-viatura` | NAO — regra operacional, sem dinheiro nem isolamento envolvido | teste de rota: viatura com OM `em_execucao` + `POST /work-orders/:id/assign {vehicle_id}` → 409 (hoje passa) |
| 8 | P-014 | 163 | Cancelar multa é gateado por papel; não existe a permissão `fines:cancel` | ABERTA | `src/modules/fines/fine.service.ts:337-349 assertCancelPermission` confere `actor.roles` contra `FINE_CANCEL_ROLES = ["tenant_admin","super_admin"]` (`fine.types.ts:17`); `src/modules/core-saas/permissions/catalog.ts:165-167` só tem `fines:read/create/update` (grep `fines:cancel` = 0 em `src` e em `frontend/src`); a UI também gateia por papel (`MultasPage.tsx:49`) | BAIXA | F3 (2026-07-08) | pre-existente | 3-seguranca/permissoes | P-027 (reconciliação de catálogo) | proposta `B-rbac-fines-cancel` | NAO — o backend é a autoridade e nega corretamente; o que falta é granularidade | `fines:cancel` no catálogo + teste: papel com a permissão cancela; papel sem ela recebe 403 |
| 9 | P-015 | 175 | `driver_id` de multa aceita qualquer string; a coluna é UUID | ABERTA | `src/modules/fines/fine.validators.ts:217-221 parseOptionalUserId` aceita string de até 160 caracteres; `prisma/schema.prisma`: `Fine.driver_id String? @db.Uuid`, `User.id @db.Uuid` (l.145). No create/update o risco é contido: `resolveDriver` → `coreService.getUserForTenant` (`fine.service.ts:429-431`), e a falha fechada vira 400 `invalid_driver_reference` (testes `tests/fines-routes.test.ts:222`, `tests/fines.test.ts:136`). No filtro de listagem, NÃO: `fine.service.ts:95` passa o valor direto para `fine-prisma.repository.ts:195 { driver_id: input.driverId }`, sem tradução de erro. Pela leitura, `GET /fines?driver_id=abc` faz o Postgres recusar o cast para uuid e responde 500; NAO MEDI por execução (fora do terreno) | BAIXA | F3 (2026-07-08) | pre-existente | 5-confiabilidade | — | proposta `B-api-fines-driver-uuid` | NAO — 500 só com entrada malformada de quem já tem `fines:read`; sem vazamento nem corrupção | teste de rota prisma: `GET /fines?driver_id=abc` → 400 (hoje, pela leitura, 500) |
| 10 | P-016 | 188 | Indicador "viatura sem apólice vigente" em Viaturas e no Mapa não existe | ABERTA | `src/modules/insurance-policies/insurance-policy.service.ts:225 hasActivePolicy` tem ZERO chamadores (grep em `src` e `frontend/src` devolve só a definição); nenhuma string "sem apólice" em `frontend/src` | BAIXA | F4 (2026-07-08) | pre-existente | 8-acabamento-web/mobile | o Mapa real já existe (#338) | proposta `B-fleet-indicador-apolice` | NAO — alerta de conformidade da frota, fora do fluxo de venda | selo em Viaturas e no marker do Mapa para viatura sem apólice vigente; `hasActivePolicy` com ≥1 chamador e teste |
| 11 | P-017 | 201 | Barra de vigência de apólice cancelada aparece verde | ABERTA | `frontend/src/modules/fleet/insurance/insurance.adapter.ts:261` devolve `tone: "default"` para `cancelada`; `pages/SegurosPage.tsx:71` mapeia `default: "var(--color-status-success)"` (verde) | BAIXA | F4 (2026-07-08) | pre-existente | 8-acabamento-web/mobile | — | proposta `B-fleet-seguros-tom-cancelada` | NAO — o Chip "Cancelada" na mesma linha continua sendo a informação correta | teste de adapter: tom de `cancelada` diferente do tom de sucesso, e barra renderizada sem verde |
| 12 | P-019 | 224 | Telas de plataforma com dados estáticos (a persona "Marina Costa" era o sintoma) | PARCIAL | "Marina Costa" sumiu das telas: grep em `frontend/src` e `src` só acha `frontend/src/mocks/auth/context.ts:18` (persona do login em modo mock, intencional). `PlatformTenantDetailPage` virou dado real em `179b52c2` (2026-07-21, #256). MAS `frontend/src/modules/platform/pages/PlatformAuditPage.tsx:17-38` continua 100% estática: `ROWS` com 6 eventos inventados ("Field Operations LATAM — Organização suspensa", "AgroMax Cooperativa — Upgrade de plano"…) e `KPIS` fixos ("1.284" eventos em 24h, "48" organizações ativas, "0" incidentes), sem nenhum rótulo de demonstração (grep `demonstr/ilustrativ/mock` = 0), roteada em `/platform/audit` (App.tsx:199-202) e no menu da plataforma como "Auditoria Global" (`navigation/platformNavigation.ts:54-55`) | MÉDIA | F6 (2026-07-08); tela desde `d5a4ed43` (2026-07-02, #111) | pre-existente | 3-seguranca/permissoes (tela de auditoria de segurança com trilha e "0 incidentes" inventados) | endpoint de auditoria de plataforma (`platform:audit:read` já existe no catálogo) | proposta `B-platform-auditoria-real` | BLOQUEIA (demonstração) — o console da plataforma mostra trilha de auditoria e contagem de organizações inventadas, sem aviso. O cliente-organização não vê essa tela, mas qualquer demo do console vê | teste SSR: sem API, a tela mostra estado vazio/erro e nunca `ROWS`; grep `const ROWS` em `PlatformAuditPage.tsx` = 0 |
| 13 | P-023 | 269 | Lista de usuários sem "último acesso" | ABERTA | frontend: grep `lastLogin/lastAccess/Último acesso/Ultimo acesso` em `frontend/src` = 0; `model User` (`prisma/schema.prisma:144`) sem campo de acesso. Já existe fonte PARCIAL no backend: `last_login_at` gravado no login local (`src/modules/auth/repositories/local-auth-credential.repository.ts:146`) e `AuthSessionRepository.lastAccessByTenant` (`auth-session.repository.ts:79`, agregado por organização, não por usuário) | BAIXA | F9 (2026-07-09) | pre-existente | 8-acabamento-web/mobile | login Cognito de prod (hipótese: não grava `last_login_at` local) | proposta `B-users-ultimo-acesso` | NAO — a omissão é honesta (não há "—" perpétuo na tela) | coluna "Último acesso" com fonte que cubra login local e Cognito; teste de DTO de `/users` |
| 14 | P-028 | 301 | Strings de UI sem acento seguem espalhadas | PARCIAL | "Situacao" = 0 ocorrências (a dívida mais citada foi paga). Restam ~50 linhas com literais sem acento em `frontend/src` (grep bruto por `Nao foi possivel/Servico/usuario/Descricao/Configuracoes/indisponivel/Operacao/Acoes/Logistico`, fora de comentários), inclusive em telas VIVAS: `guards/PermissionGuard.tsx:30` "Seu usuario nao possui permissao para visualizar esta area." (texto de TODA tela de acesso negado; o guard aparece 151 vezes em App.tsx), `modules/checklists/pages/ChecklistRuntimePage.tsx:85,133,166,287` (roteada, App.tsx:9), `ChecklistRuntimeField.tsx:300,322,392,424,483`, `work-orders/pages/WorkOrderCreatePage.tsx:37,46` ("Ordens de Servico"), `platform/pages/PlatformTenantModulesPage.tsx:50`, e o rótulo de papel `"Operador Logistico"` (`modules/auth/types.ts:5`) | BAIXA | F12 (2026-07-09) | pre-existente | 8-acabamento-web/mobile | P-Ω3F2B-ACENTOS (subconjunto) | proposta `B-web-copy-acentuacao` | NAO — cosmético; mas aparece no texto de acesso negado e no runtime de checklist, que surgem em qualquer demo | varredura por lista de palavras sem acento em literais de `frontend/src` = 0, com teste de lint de copy |
| 15 | P-026 | 312 | Usuário com papel `inventory` fica sem rótulo na UI e recebe o menu inteiro do gestor | PARCIAL | Resolvido desde o registro: `support`→"Supervisor" e `field_dispatcher`→"Operação de Campo" são distintos (`frontend/src/modules/auth/auth.adapter.ts:234-235`). Aberto: `mapBackendRole` não tem ramo para `inventory` (l.225-238 → `null`), e `auth.adapter.ts:220` filtra os nulos; um usuário só `inventory` fica com `roles=[]`, e `roleKindFor([])` cai em `"gestor"` (`layouts/appSidebarNav.ts:271-277`) → `NAV_BY_ROLE.gestor`, 7 grupos com OS, Despachos, Mapa, Frota, Pátios, Telemetria e Administração (l.177). `buildSidebarNav` filtra só por allowlist MVP e provisionamento, nunca por permissão (l.281-291; `AppShell.tsx:40-44`), então o estoquista vê dezenas de itens que o guard nega. `operator` e `field_technician` também se fundem em "Operador Logistico" (l.231) | MÉDIA | F11 (2026-07-09) | pre-existente | 8-acabamento-web/mobile | P-004 (fonte única de nav); P-027 (grants de inventory) | proposta `B-web-rbac-papel-inventory` | NAO — o backend nega corretamente e a demo pode evitar a persona Estoque; vira BLOQUEIA se o cliente-alvo tiver estoquista | teste: sessão com `roles:["inventory"]` → `buildSidebarNav` só com Estoque/Pedidos/Relatórios/Notificações; `mapBackendRole("inventory")` ≠ null |
| 16 | P-027 | 324 | Catálogo diverge da matriz: finance e inventory sem `dashboard:read` abrem num painel quebrado | PARCIAL | FECHADO: `purchase_orders:read` e `reports:read` existem no catálogo (`src/modules/core-saas/permissions/catalog.ts:181,198`) desde `0450ae9f` (2026-07-21, #257, PR-SCALE-1). ABERTO: os blocos `finance` (l.809-872) e `inventory` (l.873-894) não têm `dashboard:read` nem `work_orders:read`, enquanto `RBAC_MATRIX.md:34` (arquivo-base, fonte §A1.2) dá "scoped" aos dois em "Dashboard and operational overview". Consequência, pela leitura: `ContextSelectionPage.tsx:45` manda TODO papel para `/dashboard`; o guard da rota passa porque o front injeta `dashboard:view` para todos (`auth.adapter.ts:243,250`; `App.tsx:275`); `GET /dashboard/summary` exige `dashboard:read` (`dashboard.routes.ts:17`) → 403 → `modules/dashboard/repository.ts:47-48`, e a home de finance e de inventory abre com "Não foi possível carregar os indicadores do painel agora." (`DashboardPage.tsx:373-375`), com fila e eventos indisponíveis (l.405, 455, 553) | MÉDIA | F11 (2026-07-09) | pre-existente | 3-seguranca/permissoes | P-026; P-Ω3a (mesma lacuna de grants de finance) | proposta `B-rbac-reconciliar-matriz-catalogo` | BLOQUEIA (demonstração) — a persona Financeiro entra no produto por uma home em estado de erro, e o módulo Financeiro é vitrine da venda | teste de catálogo × `RBAC_MATRIX.md` para os 9 papéis; teste de rota: `finance` em `GET /dashboard/summary` → 200 com recorte (ou home própria sem chamada negada) |
| 17 | P-030 | 353 | Residuais do gate de Tarifas: comentário 422, mapeamento P2003 específico, busca A6 | PARCIAL | (a) FECHADA: `frontend/src/modules/registry/tariffs/tariffs.types.ts:74-76` agora diz que `status` é texto livre, sem máquina de estado, com selects desabilitados na edição; o comentário "→ 422" não existe mais. (b) o mapeamento segue igual ao original: `src/modules/tariffs/tariff-prisma.repository.ts:213-224` depende de `foreignKeyTarget(error)` (join de `meta`), que vem da origem `c26467e3` (2026-07-12, #165). Se ele dispara no Prisma 7: NAO MEDI — exige provocar P2003 contra Postgres, fora do terreno permitido. (c) A6 (busca server-side nos selects acima de 100 registros): NAO MEDI | BAIXA | Ω2-a.2 (2026-07-12) | pre-existente | 9-documentacao/rastreabilidade | — | proposta `B-api-tariffs-chore-p2003` | NAO — o genérico `invalid_reference` responde 400 com mensagem clara | teste de integração prisma: FK inválida de `price_table_id` → `invalid_price_table_reference` (ou os ramos específicos removidos) |
| 18 | P-032 | 377 | Item "Configurações" do tenantNavigation ainda exige `tenant.manage`; P-033 (seed do auditor) | ABERTA (P-032); P-033: a consequência declarada não se sustenta pelo código | P-032: `frontend/src/navigation/tenantNavigation.ts:505-511` (item `tenant-settings`) segue com `requiredPermissions: ["tenant.manage","tenant:manage"]`, enquanto a rota usa `tenant_settings:read`. Não é só inerte: `components/command-palette/logic.ts:2,11` deriva o mapa rota→permissão do `tenantNavigation`, então o manager (que tem `tenant_settings:read`) não recebe o atalho no Ctrl+K. P-033: `prisma/seed.ts:252` ainda só semeia `STANDARD_ROLES` (sem auditor, `catalog.ts:281-287`), MAS a autorização em runtime vem do CATÁLOGO, não do banco (`src/modules/auth/routes/auth.routes.ts:26-33 resolvePermissionsForRoles`), e `prisma/seed-users.ts:59-67` grava `ROLE_PERMISSIONS` para os papéis demo, legados incluídos. Pela leitura, o "GET 403 ao vivo p/ auditor" não se reproduz. Resta uma divergência de banco: `notification-prisma.repository.ts:160-196` lê `role_permissions` do BANCO para escolher destinatários; efeito em produção NAO MEDI | BAIXA | Ω2-e | pre-existente | 3-seguranca/permissoes | P-004 (fonte única de nav) | proposta `B-web-nav-fonte-unica` (junto de P-004) | NAO — o que falta é o atalho no Ctrl+K; o backend decide o acesso | teste: manager com `tenant_settings:read` vê "Configurações" no command palette; teste de destinatários de notificação para papéis legados em modo prisma |
| 19 | P-Ω3a | 389 | Finance tem `service_quotes:create`, mas não consegue montar orçamento pela UI; `quotes[]` fora do detalhe da OS | ABERTA — e mais grave do que o texto diz | (i) o bloco `finance` do catálogo (`catalog.ts:809-872`) tem `service_quotes:read/create/update/approve`, mas NÃO `service_catalog:read`, `customers:read` nem `work_orders:read`; o create exige serviço (`service-quote.service.ts:70 parseRequiredUuid(service_catalog_id)`). `useServiceQuoteReferences.ts:52-54` recebe 403, e `service-catalog.service.ts:27-33` e `customers.service.ts:24-31` devolvem `items: []` (honesto); o select de serviço do "Novo orçamento" (botão visível para finance, `OrcamentosPage.tsx:72,276`) fica vazio e a criação é inviável, enquanto o select de OS recebe as 6 OS FABRICADAS de P-008 (`listWorkOrdersFromApi` → 403 → mock). `RBAC_MATRIX.md:115` diz que finance é "quem constrói o orçamento". (ii) `quotes[]` no detalhe da OS: `work-order.controller.ts:57 toWorkOrderDto(workOrder, links, checklists)`, sem quotes (adiado; o filtro `/service-quotes?workOrderId=` supre) | MÉDIA | Ω3-a | pre-existente | 7-fluxo-necessario-para-venda | P-008 (OS fabricadas no select); P-027 (grants de finance) | proposta `B-rbac-finance-orcamento` | NAO — manager e operator montam o orçamento normalmente; bloqueia só a persona finance, contra o que diz a matriz (fonte §A1.2) | teste: sessão finance → "Novo orçamento" com serviço e cliente listados e POST 201; select de OS sem item fabricado |
| 20 | P-037 | 441 | Assimetria memory×prisma no `updated_by` do congelamento de checklist e do geocode | ABERTA | prisma grava `updated_by: input.actorUserId ?? null` (`src/modules/work-orders/work-order-prisma.repository.ts:446,466`); memory grava `updatedBy: input.actorUserId ?? current.updatedBy` (`work-order.repository.ts:329,349`) | BAIXA | Ω3-c (validador) | pre-existente | 6-contratos/testes | — | proposta `B-api-wo-paridade-updated-by` | NAO — inócuo no fluxo real (o despacho sempre tem ator) | teste de paridade: freeze/geocode sem ator → mesmo `updatedBy` nos dois repositórios |
| 21 | P-Ω3d | 451 | Anexos de OS: limpeza de órfão e auditoria em prisma sem teste | ABERTA | o código existe: `src/modules/work-orders/work-order-attachment.service.ts:101-104`, com catch pós-store → `deleteStoredWorkOrderAttachmentFile(...)`. Nenhum teste o exercita: grep `deleteStoredWorkOrderAttachmentFile/orphan/órfão` em `tests` e `frontend/tests` = 0; os 3 arquivos `tests/work-order-attachment*.test.ts` não têm caso de auditoria (a única linha com "audit" é o papel `auditor` no RBAC, `work-order-attachments-routes.test.ts:141,146`). 413 e `file_required` estão cobertos segundo a entrada (não re-medido) | BAIXA | Ω3-d | pre-existente | 6-contratos/testes | repo-stub injetável no service | proposta `B-tests-wo-anexos-orfao-auditoria` | NAO — o cleanup existe no código; falta a rede de teste | teste que força falha de insert pós-store e prova a remoção do blob; teste prisma-mode da auditoria de upload sem `storage_key`/`path` no metadata (§2.8) |
| 22 | P-SAN-PROD-BOOTSTRAP | 537 | Não existe bootstrap versionado do 1º platform_admin real em produção | ABERTA | `git ls-tree -r HEAD scripts prisma` sem arquivo de bootstrap (grep `bootstrap/platform-admin/first-admin` só acha `prisma/seed-guard.ts`); `docs/go-live-readiness.md` fala de "sem seed em prod" (l.19, 23, 33), mas não traz o procedimento do 1º admin; `User.tenant_id` segue NOT NULL (`prisma/schema.prisma:146`) e o seed de prod é bloqueado pelo `seed-guard` | MÉDIA | Ω-INFRA-3 (2026-07-14) | pre-existente | 7-fluxo-necessario-para-venda | ativação de produção (fronteira humana); Cognito | proposta `B-infra-bootstrap-platform-admin` | BLOQUEIA (go-live) — sem ele, a primeira organização real em produção só nasce por SQL manual fora do repositório; não afeta a demo | script idempotente `scripts/bootstrap-platform-admin.ts` + teste: 2 execuções = 1 tenant de sistema + 1 admin; recusa sem `ALLOW_PROD_SEED` |
| 23 | P-SAN-PROD-WEBIMG | 548 | Frontend sem imagem no GHCR, sem rollback por imagem | ABERTA | `.github/workflows/ci.yml:402-403` publica só `ghcr.io/.../erp-backend:{sha,latest}`; os outros dois `docker/build-push-action` (l.415, 431) são `push: false` (imagens locais do smoke: `erp-techsolutions-api:ci-smoke` e `erp-o6r-smoke-migrate`) | BAIXA | Ω-INFRA-3 (2026-07-14) | pre-existente | 5-confiabilidade | ativação de produção | proposta `B-infra-ghcr-web` | NAO — mitigado por `fly releases`; é simetria de rollback | `ci.yml` com push de `erp-web:<sha>` + rollback do web documentado por imagem |
| 24 | P-SAN-INFRA1-NITS | 558 | Cinco nits do Ω-INFRA-1: imagem grande, compose em memory, depends_on do web, custo e health | PARCIAL | (2) FECHADA: `docker-compose.prod.yml:56 CORE_SAAS_PERSISTENCE: prisma` desde `a8901ffe` (2026-08-15, #353, B-O6R-05). (3) ABERTA: o serviço web ainda tem `depends_on: - api` sem `condition` (`docker-compose.prod.yml:97-98`), enquanto os demais usam `service_healthy` (l.36-38, 81-87). (1) `Dockerfile:5,23` segue `node:20-bookworm-slim`; tamanho da imagem NAO MEDI (exige build). (4)(5) são nits de documento: NAO MEDI | BAIXA | J-SAN-4 (2026-07-13) | pre-existente | 5-confiabilidade | — | proposta `B-infra-compose-web-healthy` | NAO — 502 transitório só na subida | `docker-compose.prod.yml` com `web.depends_on.api.condition: service_healthy`; tamanho da imagem medido e registrado |
| 25 | P-Ω3F1-ENTITYTYPE | 570 | Enum técnico cru na linha "Entidade" da aprovação | FECHADA (texto diz ABERTA) | `frontend/src/modules/work-orders/approval.types.ts:13-23`: `ENTITY_TYPE_LABELS` (`work_order`→"Ordem de serviço", `checklist_run`→"Checklist", `evidence`→"Evidência"), com comentário citando esta pendência e fallback "Registro"; `components/tabs/GeneralInfoTab.tsx:285` e `pages/ApprovalDetailPage.tsx:125` usam `entityTypeLabel(...)`; grep da interpolação crua `${approval.entityType}`/`{approval.entityType}` em `frontend/src` = 0. `git log -S"entityTypeLabel(approval.entityType)"` → `5a1b433d` (2026-07-15, #188, Ω3F-3b) | — | J-OMEGA3F-1 (2026-07-14) | pre-existente | 8-acabamento-web/mobile | — | nenhum; só encerrar o registro citando `5a1b433d` | NAO | já satisfeito (grep da interpolação crua = 0) |
| 26 | P-Ω3F2B-ACENTOS | 580 | Rótulos e mensagens do formulário de Nova OS sem acento | ABERTA | `frontend/src/modules/work-orders/components/WorkOrderForm.tsx:171` "Revise os campos obrigatorios", `:176` "Identificacao", `:178` "Titulo", `:180` "Descricao"; `work-orders.adapter.ts:243` "Titulo obrigatorio.", `:247` "Agendamento invalido.". A tela é VIVA: `/work-orders/new` (App.tsx:776), aberta pelo botão primário "Nova OS" (`WorkOrdersPage.tsx:233`, `DashboardPage.tsx:365`). O último toque no form, `166358e4` (2026-07-14, #186), não mexeu nesses rótulos | BAIXA | J-OMEGA3F-2B (2026-07-14) | pre-existente | 8-acabamento-web/mobile | P-028 | proposta `B-web-copy-acentuacao` (junto de P-028) | NAO — cosmético, mas fica no formulário central de qualquer demo (§11.3) | teste SSR do `WorkOrderForm`: rótulos "Identificação/Título/Descrição" e mensagens acentuadas |
| 27 | P-Ω3F4B-SHARE-TOKEN-UNIQUE | 626 | `share_token` do orçamento sem índice único; o endpoint público ainda não existe | ABERTA | `prisma/schema.prisma:1707 share_token String?` sem `@unique`/`@@index`; a migration `20260804000000_add_service_quote_items/migration.sql:27` só faz `ADD COLUMN "share_token" TEXT`, e nenhuma migration cria índice sobre ele (grep `share_token` + `index/unique` = 0); nenhuma rota de leitura por token em `src` | BAIXA | J-OMEGA3F-4B (2026-07-15) | pre-existente | 3-seguranca/permissoes | fatia do endpoint público; junta com secops | a fatia que abrir `GET /orcamentos/compartilhado/:token` | NAO — inerte enquanto não houver leitura pública | quando o endpoint nascer: migration com índice único em `share_token` + teste de colisão e de não-vazamento de tenant (§2.8) |
| 28 | P-Ω3F4B-APPROVE-CRASH | 638 | Crash duro entre a reserva e o carimbo deixa orçamento aprovado sem OS | ABERTA | `src/modules/service-quotes/service-quote.service.ts:259 claimForApproval` (CAS draft→approved) → `workOrders.create` (l.278) → carimbo de `createdWorkOrderId` (l.317). Só há compensação para erro do create (catch l.302-308, que volta a `draft`); nenhum `$transaction`, outbox ou job de reconciliação (grep `createdWorkOrderId/created_work_order_id` em `src/infra` e `scripts` = 0) | BAIXA | J-OMEGA3F-4B ciclo 1 (2026-07-15) | pre-existente | 5-confiabilidade | outbox genérico (Ω6R D-002) | proposta `B-api-quote-approve-reconciliacao` | NAO — falha segura (nunca cria 2ª OS nem fatura em dobro); janela estreita | job de reconciliação "approved sem OS há N min" com teste, OU reserva+create+carimbo numa transação única |
| 29 | P-Ω3F4C-ACTIVATION-PROMPT | 651 | Aprovar orçamento não coleta destino; se o serviço exige destino, a aprovação sempre falha | ABERTA — e mais grave do que o texto diz | a UI aprova com corpo vazio: `frontend/src/modules/work-orders/components/tabs/QuoteTab.tsx:106` e `registry/service-quotes/pages/OrcamentosPage.tsx:108` chamam `approveServiceQuote(context, quote.id, {})`, sem `activation_mode` em nenhum `.tsx` (grep = 0). O approve só repassa o destino que vier NO CORPO (`src/modules/service-quotes/service-quote.service.ts:291-296`), e `ServiceQuote` não guarda destino (grep `destination/origin` no model = 0). O create da OS recusa sem destino quando o serviço exige (`work-order.service.ts:455 assertDestinationForType` → 422 `destination_required`, l.719-725), e o tenant liga essa exigência pela própria UI (`registry/service-catalog/components/ServiceFormModal.tsx:136` "Exige endereço de destino"; persistido em `service-catalog-prisma.repository.ts:28,91`). Pela leitura, todo orçamento de serviço marcado "Exige destino" (o caso de reboque, para o qual a flag existe) volta a `draft` com erro ao ser aprovado pela UI. Nenhum teste cobre approve + destino (grep `approve` × `destination_required/requires_destination` em `tests` = 0). O seed demo não liga a flag (`requires_destination` ausente em `prisma/seed*.ts`; default `false`) | MÉDIA | J-OMEGA3F-4C (2026-07-15); regra de destino do Ω3F-2a | pre-existente | 7-fluxo-necessario-para-venda | P-Ω3F4B-APPROVE-CRASH (mesmo approve) | proposta `B-web-quote-approve-dialogo-destino` | BLOQUEIA (condicional) — quebra o fluxo orçamento→OS de reboque assim que o cliente de guincho marca "Exige endereço de destino", que é a configuração natural dele; não aparece na demo com o seed atual | teste de rota: serviço com `requires_destination=true` + approve com destino → 201 e OS com destino; sem destino → 422 legível na UI; teste SSR do diálogo de aprovação coletando destino e modo de acionamento |
| 30 | P-Ω3F5-DOC-TYPE | 663 | Anexo de OS sem categoria de documento selecionável | ABERTA | grep `documentType/document_type` em `src/modules/work-orders/work-order-attachment*.ts` e em `frontend/src/modules/work-orders` = 0 | BAIXA | Ω3F-5 (2026-07-15) | pre-existente | 8-acabamento-web/mobile | — | proposta `B-wo-anexo-categoria` | NAO — a aba usa `description` como rótulo (decisão D-Ω3F-5-UPLOAD-TYPE) | `metadata.documentType` no DTO + select na UI + teste de contrato |
| 31 | P-Ω3F5A-TAG-TOCTOU | 674 | Comentário e tags gravados em operações separadas | ABERTA | `src/modules/work-order-comments/work-order-comment.service.ts:52` pré-valida (`assertTagsActive`), `:54` faz `repository.create` do comentário e `:61-62` percorre `tagAssignments.attach(...)` um a um, sem transação que envolva os dois | BAIXA | J-OMEGA3F-5A (2026-07-15) | pre-existente | 5-confiabilidade | — | proposta `B-api-wo-comment-atomico` | NAO — sob corrida rara, o comentário fica com uma tag a menos e o cliente recebe 422; o estado resultante é válido | teste de corrida: tag hard-deletada entre a pré-validação e o attach → nenhum comentário gravado (rollback), OU o comentário gravado com todas as tags |
| 32 | P-Ω3F6B-MENUITEM-INLINE | 772 | Menus de Danos, Multas e Manutenção seguem sem hover (background inline) | ABERTA | o DS foi consertado (`frontend/src/styles/app.css:524-526 .ui-menu-item { background: transparent }`, `:528 :hover`), mas os três consumidores legados ainda passam `style={menuItemStyle}` com `background: "transparent"` inline: `fleet/damages/pages/DanosPage.tsx:84-89` (usado em l.665-666), `fleet/fines/pages/MultasPage.tsx:84-89` (l.681-682), `fleet/maintenance/pages/ManutencaoPage.tsx:76-81` (l.543-544). `git log -S'background: "transparent"'` em DanosPage só acha a criação `72729b48` (2026-07-08): a string nunca saiu | BAIXA | J-OMEGA3F-6B (2026-07-17) | pre-existente | 8-acabamento-web/mobile | — | proposta `B-web-chore-menuitem-hover` | NAO — só o hover; o menu funciona | grep `background: "transparent"` nos 3 `menuItemStyle` = 0; teste de hover |
| 33 | P-Ω3F6B-DS-NITS | 786 | Cinco nits de DS/a11y do Ω3F-6b | ABERTA (os 5) | (1) `.ui-button--primary` usa `var(--color-core-primary)` (`frontend/src/styles/app.css:410`), enquanto `WorkOrderActionBar.tsx:117` pinta "Abrir checklist" com `#2563EB` inline; (2) o menu ⋮ não fecha com Esc nem com clique fora (grep `Escape/mousedown/pointerdown/contains(` em `WorkOrderActionBar.tsx` e `WorkOrderActionsMenu.tsx` = 0), e o menu tem item destrutivo (Cancelar); (3) `Modal` (`components/ui/index.tsx`, `role="dialog"` na l.140, `aria-modal` na l.141) não tem Escape nem gestão de foco; (4) ícones sem `aria-hidden` em `WorkOrderActionBar.tsx:117,122,131`; (5) o tipo `WorkOrderStatusPayload` segue em `work-orders.types.ts:238`, só com um import de tipo em `work-orders.service.ts:18` | BAIXA | J-OMEGA3F-6B (2026-07-17) | pre-existente | 8-acabamento-web/mobile | DS congelado (o item 1 exige junta de DS) | proposta `B-web-ds-a11y-menus-modal` | NAO — a11y e consistência visual; nada impede a demo | testes: Esc e clique fora fecham o ⋮; `Modal` com foco inicial, trap e Esc; zero ícone decorativo sem `aria-hidden` na ActionBar |
| 34 | P-Ω3F-9-SLA-FIELD | 849 | Badge de atraso da lista de OS segue binário, embora o prazo/SLA real já exista no schema | PARCIAL | a fonte de dado passou a existir: `prisma/schema.prisma:2354 sla_due_at DateTime?` (migration aditiva `046939f9`, 2026-07-21, #258, M-7 PR-A J-MAPAS-8), lida pelo adapter (`frontend/src/modules/work-orders/work-orders.adapter.ts:313 slaDueAt`). A lista NÃO a usa: `components/WorkOrderDelayBadge.tsx:8` segue "Nunca mostra 'Xh restantes'", com selo derivado de `scheduled_for` | BAIXA | Ω3F-9 (D-Ω3F-9-BADGE) | pre-existente | 8-acabamento-web/mobile | — | proposta `B-web-wo-lista-sla-restante` | NAO — o selo binário é honesto | teste do badge: OS com `sla_due_at` futuro → "Xh restantes"; vencido → "Atrasada"; sem SLA → cai no derivado |
| 35 | P-Ω3F-9-DISPATCH-DTO | 858 | Lista de OS não expõe "envio ativo"; o botão Revogar é heurístico | ABERTA | grep `hasActiveDispatch/activeDispatchId/has_active_dispatch` em `src` = 0; o único `hasActiveDispatch` está em `frontend/src/modules/operations/map/allocation.ts:40,148`, que é do mapa (por localização do operador), não do DTO da lista de OS | BAIXA | Ω3F-9 | pre-existente | 8-acabamento-web/mobile | contrato da lista de OS | proposta `B-api-wo-list-dispatch-flag` | NAO — o clique já trata ausência com mensagem benigna e 409 | DTO de `GET /work-orders` com `has_active_dispatch` + teste de contrato; botão visível só quando verdadeiro |
| 36 | P-Ω4-2A-NITS | 868 | Observações do Ω4-2a: `closing` sem trava, ordem de erro divergente, PATCH sem limpeza | PARCIAL | (1) FECHADA: `src/modules/financial-titles/financial-title.repository.ts:56 PERIOD_WRITE_BLOCKING_STATUSES = ["closing","closed"]` desde `99f18403` (2026-09-04, #371, B-O6R-02 ciclo 5); `financial-period-close.types.ts:7` registra que o v1 nunca escreve `closing`. (2) ordem de erro no request duplamente inválido: NAO MEDI (exige execução nos dois repositórios). (3) PATCH não limpa opcional: declarado intencional no v1, não re-medido | BAIXA | junta Ω4-2a (2026-07-17) | pre-existente | 6-contratos/testes | — | proposta `B-api-titles-paridade-erro` | NAO | teste de paridade memory×prisma: request com conta inválida + amount inválido → mesmo código |
| 37 | P-Ω4-ACCOUNT-ACTIVE | 897 | Título aceita conta financeira inativa; a liquidação já recusa | PARCIAL | a liquidação/lançamento recusa conta inativa: `src/modules/financial-entries/financial-entry.repository.ts:67` → 422 `account_inactive` (`financial-entry.service.ts:513`), desde `e11c44a8` (2026-07-18, #214, Ω4-4); o cheque também (`cheques/cheque.repository.ts:61`). O título continua aceitando: `financial-title.service.ts:353` diz, no próprio comentário, "FK satisfeita (independe de is_active)" | BAIXA | Ω4-2a (2026-07-17) | pre-existente | 4-faturamento/dinheiro | — | proposta `B-api-titles-conta-ativa` | NAO — o dinheiro não se move para conta inativa (a liquidação dá 422); sobra um título agendado que falhará na baixa | teste: POST/PATCH de título com `account_id` inativo → 422 `account_inactive` (ou decisão registrada de aceitar) |
| 38 | P-Ω4-2A-COBERTURA | 905 | Título excluído ainda aparece no GET por id; falta índice por `created_at`; helper morto | ABERTA (os 3) | (1) `src/modules/financial-titles/financial-title.service.ts:212-218 get()` devolve o que `findById` achar, e `financial-title-prisma.repository.ts:85` faz `findFirst({ where: { tenant_id, id } })` sem `deleted_at: null` → título soft-deletado responde 200, enquanto list e mutações o escondem (l.91, 141, 186); (2) a listagem ordena por `created_at desc` (l.77), mas os índices de `FinancialTitle` são `(tenant_id,direction,status)`, `(tenant_id,due_date)` e `(tenant_id,deleted_at)` (`schema.prisma:1946-1948`), sem `(tenant_id, created_at)`; (3) `function nullable` (l.493) tem 0 chamadas | BAIXA | Ω4-2a | pre-existente | 6-contratos/testes | — | proposta `B-api-titles-chore-cobertura` | NAO — o detalhe de excluído é só leitura; perf só pesa com volume | teste: GET de título soft-deletado → 404 (ou contrato explícito "200 com `deleted`"); migration de índice `(tenant_id, created_at)` |

## FLIPS

Status comprovado pelo código que diverge do registro. As entradas seguem ABERTAS em `pendencias.md`.

**Fechadas no código, abertas no texto (2):**
- `P-012`: tile km/L clicável desde `b1559d32` (2026-07-20, #248).
- `P-Ω3F1-ENTITYTYPE`: enum humanizado desde `5a1b433d` (2026-07-15, #188); o próprio código cita a pendência.

**Parciais: o texto diz aberta, mas parte já fechou (9):**
- `P-019`: "Marina Costa" saiu das telas e o detalhe da organização virou dado real (#256), mas a Auditoria Global continua 100% inventada.
- `P-026`: `support` e `field_dispatcher` foram mapeados; `inventory` continua sem mapeamento.
- `P-027`: as permissões ausentes foram criadas (#257); os grants de dashboard de finance e inventory não.
- `P-028`: "Situacao" zerada; restam ~50 literais sem acento.
- `P-030`: comentário 422 corrigido.
- `P-SAN-INFRA1-NITS`: compose de produção em prisma (#353).
- `P-Ω3F-9-SLA-FIELD`: `sla_due_at` existe (#258).
- `P-Ω4-2A-NITS`: `closing` agora trava escrita (#371).
- `P-Ω4-ACCOUNT-ACTIVE`: a liquidação recusa conta inativa (#214).

**Premissa do texto que o código não sustenta (1):**
- `P-032`/`P-033`: o "403 ao vivo para auditor" pressupõe autorização lida do banco, mas o runtime usa o catálogo (`auth.routes.ts:26-33`). P-032 segue aberta.

**Mais amplas ou graves do que o texto diz (3):**
- `P-008`: não é só o detalhe offline. Lista vazia vira 6 OS inventadas com aviso falso de "sem conexão", e o create engole erro do backend e navega para uma OS falsa.
- `P-Ω3a`: não é "degradação graciosa". O papel finance não consegue criar orçamento (serviço obrigatório com select vazio), e o select de OS recebe OS fabricadas.
- `P-Ω3F4C-ACTIVATION-PROMPT`: não é só fidelidade ao vídeo. Sem o diálogo, orçamento de serviço com "Exige endereço de destino" nunca é aprovado pela UI (422 `destination_required`).

## BLOQUEIAM VENDAVEL

| ID | gravidade | por quê |
|---|---|---|
| `P-008` | ALTA | Dado fabricado na tela principal (organização nova vê 6 OS inventadas) e perda silenciosa do que o operador digitou quando o backend recusa o create |
| `P-027` | MÉDIA | Persona Financeiro (e Estoque) entra no produto por uma home em estado de erro (403 no `/dashboard/summary`), contra a `RBAC_MATRIX.md:34` |
| `P-019` | MÉDIA | Console da plataforma exibe trilha de auditoria e contagens inventadas, sem rótulo (bloqueia a demonstração do console, não a do cliente-organização) |
| `P-Ω3F4C-ACTIVATION-PROMPT` | MÉDIA | Condicional: fluxo orçamento→OS de reboque quebra quando o cliente marca "Exige endereço de destino" |
| `P-SAN-PROD-BOOTSTRAP` | MÉDIA | Go-live: sem bootstrap versionado do 1º platform_admin, a primeira organização real em produção só nasce por SQL manual |

## MATERIAIS ESCONDIDAS

Rotuladas pela triagem como leves (severidade "—" ou BAIXA), medidas aqui MÉDIA+ ou ALTA.

| ID | triagem | medida | resumo |
|---|---|---|---|
| `P-008` | — | **ALTA** | fallback fabrica OS na lista vazia, no create com erro e no detalhe; contamina o select de OS do orçamento |
| `P-027` | — | MÉDIA | finance/inventory sem `dashboard:read` → home em erro; divergência entre catálogo e arquivo-base |
| `P-Ω3a` | — | MÉDIA | finance não consegue criar orçamento pela UI, apesar de a matriz dizer que ele "constrói o orçamento" |
| `P-Ω3F4C-ACTIVATION-PROMPT` | — | MÉDIA | aprovação de orçamento de serviço com destino obrigatório falha sempre pela UI |
| `P-019` | — | MÉDIA | Auditoria Global da plataforma 100% inventada ("0 incidentes"), roteada e sem rótulo |
| `P-026` | — | MÉDIA | usuário `inventory` recebe o menu inteiro do gestor (roles=[] → "gestor") |
| `P-SAN-PROD-BOOTSTRAP` | — | MÉDIA | sem bootstrap versionado do 1º admin de produção |

## NAO MEDI (com motivo)

- `P-015`: o 500 de `GET /fines?driver_id=abc` foi deduzido da leitura; confirmar exige executar contra Postgres.
- `P-030` (b)(c): disparo do P2003 específico no Prisma 7 e busca A6; ambos exigem execução contra banco.
- `P-SAN-INFRA1-NITS` (1)(4)(5): tamanho de imagem exige build; (4)(5) são nits de documento.
- `P-Ω4-2A-NITS` (2): ordem de erro memory×prisma exige execução.
- `P-032`: efeito, em produção, da divergência de `role_permissions` no banco sobre os destinatários de notificação.
- `P-023`: que o login Cognito não grava `last_login_at` é hipótese, não medida.
