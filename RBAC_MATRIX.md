# ERP Techsolutions — RBAC Matrix

## Scope
Baseline authorization matrix for the ERP Techsolutions multi-tenant SaaS ERP. This matrix should support the agent mission of preserving operational clarity, tenant isolation, traceability, and continuity across product, architecture, UX, execution, and audit.

## Roles
- platform_admin
- tenant_admin
- manager
- operator
- finance
- inventory
- field_technician
- auditor
- support

## Permission principles
- Backend is the final authorization authority
- UI may hide or shape access, but never replace backend checks
- tenant_id is mandatory for tenant-scoped actions
- tenant_role and tenant_roles should shape access resolution
- permissions should support fine-grained overrides where needed
- platform-level actions must be isolated from tenant-level actions
- no role should silently cross tenant boundaries
- support access must remain constrained, auditable, and policy-bounded
- the matrix must preserve segregation of responsibility across platform governance, tenant governance, operation, finance, inventory, field execution, audit, and support

## Baseline matrix
| Module / Capability | platform_admin | tenant_admin | manager | operator | finance | inventory | field_technician | auditor | support |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Platform configuration | full | none | none | none | none | none | none | read | limited-support |
| Tenant administration | full | full | read | none | none | none | none | read | limited-support |
| User and role assignment | full | full | limited | none | none | none | none | read | limited-support |
| Dashboard and operational overview | full | full | full | full | scoped | scoped | scoped | read | scoped |
| Backend navigation menu | full | tenant-scoped | scoped | scoped | scoped | scoped | scoped | read | scoped |
| Field operator location | support-audited | full-tenant | read-history | send-own/read-tenant | none | none | send-own | full-read | support-view |
| Master data | full | full | approve/edit | edit-scoped | read | read/edit-scoped | read | read | read-support |
| Customer registry (Cadastros - Clientes) | full | full | create/edit | read | none | none | read | read | none |
| Vehicle registry (Cadastros - Viaturas) | full | full | create/edit | read | none | none | read | read | none |
| Team registry (Cadastros - Equipes) | full | full | create/edit | read | none | none | read | read | none |
| Service catalog (Cadastros - Serviços) | full | full | create/edit | read | none | none | read | read | none |
| Price tables (Configurações - Tabela de Valores) | full | full | create/edit | read | none | none | read | read | none |
| Configurable checklist templates | support-audited | create/read/update/delete/publish | read | none | read | read | read | read | support-view |
| Checklist executions and answers | support-audited | full-tenant | read/complete-by-scope | create/answer/complete-by-scope | read | read/answer-by-scope | answer-assigned | full-read | support-view |
| Work orders / service orders | full | full | full | create/edit | read | material-view | execute/update-assigned | read | support-view |
| Workflow / approvals | full | full | full | request | approval-by-policy | approval-by-policy | request/ack | read | support-view |
| Inventory movements | full | full | read | request/use | read | full-operational | consume/confirm | read | support-view |
| Purchasing | full | full | request/approve-policy | request | budget-check | stock-driven-request | none | read | support-view |
| Finance | full | tenant-finance-admin | manager-view | none | full | stock-cost-view | none | read | support-view |
| Billing | full | full | read | none | full | none | none | read | support-view |
| Cloud usage metering | full | none | none | none | none | none | none | read | support-view |
| Cloud cost import | full | none | none | none | none | none | none | read | support-view |
| Cloud cost allocation | full | none | none | none | none | none | none | read | support-view |
| Cloud charge markup rules | full | none | none | none | none | none | none | read | support-view |
| Reports and analytics | full | full | full | scoped | finance-scoped | inventory-scoped | field-scoped | audit-full-read | support-scoped |
| Audit logs | full | read | scoped | none | scoped | scoped | none | full-read | support-scoped |
| Cross-tenant support operations | full | none | none | none | none | none | none | read | scoped-support-only |

## Role semantics
- **platform_admin** governs platform-wide configuration, integrity, and cross-tenant controls, but is not the default approver of tenant business flows.
- **tenant_admin** governs the tenant's local configuration, users, policies, and administrative exceptions.
- **manager** coordinates operations, approves within policy, and manages execution tradeoffs.
- **operator** performs operational work and requests approval where required.
- **finance** owns finance-sensitive views, controls, and approvals.
- **inventory** owns stock control, movement integrity, and inventory-sensitive approvals.
- **field_technician** executes field work, records evidence, and updates assigned operational flows.
- **auditor** has strong read and traceability visibility, not execution authority by default.
- **support** operates inside bounded and auditable support policies without breaking segregation of function.

## Auth and claim alignment
This matrix assumes the authenticated context includes:
- sub
- tenant_id
- tenant_role
- tenant_roles
- permissions
- email
- scope

Operational rules:
- tenant_id and tenant_role are mandatory for authenticated tenant access
- React and Flutter may shape the interface from claims, but claims do not replace backend enforcement
- permissions may narrow or extend role behavior only within backend-authorized limits
- active tenant resolution must be explicit when a user belongs to more than one tenant
- configurable checklist permissions are tenant-scoped and must include `tenant_checklists:*` and `checklist_runs:*` checks in backend routes/services
- internal notifications are tenant-scoped and must keep `notifications:read`/`notifications:update` limited to the authenticated user's own inbox; the "future admin endpoint explicitly designed" is now the Ω4C PR-04 scheduled-notifications engine, gated by the NEW `notifications:create` (management/operation only — see the notifications module line below)
- cloud usage metering is platform-scoped and must require `platform:cloud-usage:read`; tenant roles do not receive cross-tenant cloud usage access
- cloud cost import is platform-scoped and must require `platform:cloud-costs:read` or `platform:cloud-costs:import`; tenant roles do not receive raw cloud cost access
- cloud cost allocation is platform-scoped at API level and must require `platform:cloud-cost-allocation:read` or `platform:cloud-cost-allocation:run`; tenant roles do not receive allocated cloud cost access in this branch
- `tenant_cloud_cost_allocations` remains tenant-scoped at database level with RLS, even though HTTP access is restricted to the Platform boundary in this branch
- cloud charge markup rules are platform-scoped at API level and must require `platform:cloud-charge-rules:read`, `platform:cloud-charge-rules:write`, `platform:cloud-charges:read`, or `platform:cloud-charges:calculate`; tenant roles do not receive cloud price or margin access in this branch
- `tenant_cloud_charges` remains tenant-scoped at database level with RLS, even though HTTP access is restricted to the Platform boundary in this branch
- backend navigation menu is exposed by `GET /api/v1/navigation/menu`, filters by resolved roles, permissions, tenant modules and scope, and never replaces backend authorization on domain endpoints
- navigation platform items use `platform:dashboard:read`, `platform:tenants:read`, `platform:cloud-charges:read` and `platform:audit:read`
- navigation tenant/operations/logistics/finance items use planned permissions including `dashboard:read`, `tenant_settings:read`, `users:read`, `audit:read`, `work_orders:*`, `field_location:*`, `field_operator:*`, `field_dispatch:*`, `logistics:*`, `finance:read`, `billing:read`, `invoices:read` and `payments:read`
- work orders foundation uses `work_orders:read`, `work_orders:create`, `work_orders:update`, `work_orders:assign`, `work_orders:status`, `work_orders:cancel` and reserved `work_orders:delete`; all routes derive `tenant_id` from authenticated context and protect `work_orders`, `work_order_events` and `work_order_assignments` with RLS
- OS integration (B1) adds optional cadastro references on work orders (`customer_id`/`vehicle_id`/`team_id`/`service_catalog_id`, composite FKs `(tenant_id, <col>)` to the cadastro tables, `ON DELETE RESTRICT`); when `customer_id` is provided on OS create the backend derives the customer snapshot (`customer_name`/`customer_document`/`customer_phone`) server-side from the tenant's customer and overrides client-sent values; cross-tenant references are rejected (400 `invalid_<entity>_reference`). No new permission — uses `work_orders:create`/`work_orders:read`
- mobile viatura/equipe selection (D1) extends `work_order.assign` (mobile sync and `POST /work-orders/:id/assign`) with optional `vehicle_id`/`team_id`; refs are validated tenant-scoped (400 `invalid_vehicle_reference`/`invalid_team_reference`, cross-tenant rejected) and written to the B1 FKs. Permission stays `work_orders:assign` (manager/field_dispatcher — decision D-009: dispatcher selects, not the field technician). Contract `mobile_work_order_actions_sync` bumped to `2026-07-07.d1` (additive)
- field operator location uses `field_location:send` for mobile self-location, `field_location:read` for latest tenant positions, and `field_location:history` for historical queries; the backend must derive `tenant_id` and `operator_user_id` from the authenticated context and protect `field_operator_locations` with RLS
- field dispatch foundation uses `field_dispatch:read`, `field_dispatch:create`, `field_dispatch:update`, `field_dispatch:cancel` and `field_dispatch:reassign`; dispatches are tenant-scoped, must link only to Work Orders and operators from the same tenant, and protect `field_dispatches` and `field_dispatch_events` with RLS
- customer registry (Cadastros - Clientes) uses `customers:read`, `customers:create`, `customers:update`; all routes derive `tenant_id` from authenticated context, enforce composite uniqueness `@@unique([tenant_id, document])`, use logical deactivation via `is_active` (no physical delete) and protect `customers` with RLS. Write access (create/update/deactivate) is limited to tenant_admin, manager (and super_admin); operator, field_technician and auditor read; support has no access
- vehicle registry (Cadastros - Viaturas) uses `vehicles:read`, `vehicles:create`, `vehicles:update`; same tenant-from-claim, composite uniqueness `@@unique([tenant_id, plate])`, logical deactivation via `is_active` and RLS on `vehicles`. Write limited to tenant_admin, manager (and super_admin); operator, field_technician and auditor read; support has no access
- service catalog (Cadastros - Serviços) uses `service_catalog:read`, `service_catalog:create`, `service_catalog:update`; tenant_id from claim, composite uniqueness `@@unique([tenant_id, name])`, `base_price DECIMAL(12,2)` (emitted as JSON number), logical deactivation via `is_active` and RLS on `service_catalog`. Write limited to tenant_admin, manager (and super_admin); operator, field_technician and auditor read; support has no access
- price tables / Tabela de Valores (Ω2-a.1, Configurações) uses `price_tables:read`, `price_tables:create`, `price_tables:update` (mirror of `service_catalog:*` — same role distribution); tenant_id from claim, composite uniqueness `@@unique([tenant_id, name])`, publish state machine `draft→published→archived` (invalid transition → 422), logical deactivation via `is_active` and RLS on `price_tables`. Published table stays editable in this slice (deferral D-OMEGA2A); the monetary value lives in `tariffs` (Ω2-a.2)
- tariffs / Tarifas (Ω2-a.2, Configurações) uses `tariffs:read`, `tariffs:create`, `tariffs:update` (mirror of `service_catalog:*` — same role distribution); belongs to a price table (`(tenant_id, price_table_id)` composite FK, ON DELETE CASCADE), optional service/customer references (Restrict), `unit_price DECIMAL(12,2)` emitted as JSON number, mandatory `origin`, natural key `@@unique([tenant_id, price_table_id, service_catalog_id, customer_id])` — includes `customer_id` so a default tariff (customer NULL) and per-customer tariffs coexist (RN-CAD-009 / decisão A1 em D-OMEGA2A); RLS on `tariffs`; km/horário/região fields deferred to Scale/W15 (RF-CAD-008, freetext `origin`/`rule` meanwhile)
- work_orders:comment / Comentários da OS (Ω3-b → **atualizado Ω3F-5a**) — comentário do usuário é um AGREGADO PRÓPRIO mutável `WorkOrderComment` (não mais evento de timeline): criar/editar (carimba edited_at)/excluir (delete lógico) via `POST/PATCH/DELETE /work-orders/:id/comments` + tags coloridas via `TagAssignment` polimórfico (`POST|DELETE /comments/:id/tags/:tagId`). Rota exige `work_orders:comment`; editar/excluir/moderar exige **autor OU `work_orders:update`** (403 comment_forbidden). Tags: 422 tag_not_found (inexistente/inativa/cross-tenant), 409 duplicate_tag_assignment, detach = hard-delete. §2.8: DTO sem tenant_id, auditoria sem o corpo. `WorkOrderEvent` fica audit-only (eventos `work_order_comment` legados inertes; filtro P-034 do dashboard permanece)
  `work_order_comment` na timeline existente da OS (Opção A da decisão checklist-unificado/D4; SEM migration,
  `event_type` é String livre). Papéis que atuam na OS: manager/operator/technician/field_technician/field_dispatcher
  (+ tenant_admin/super_admin). `POST /work-orders/:id/comments` (400 comment_required, 422 comment_too_long >4000,
  404 cross-tenant, 403); auditoria com ALLOWLIST `{workOrderId,eventType,messageLength}` — corpo NUNCA em auditoria (§2.8).
- Despacho endurecido (Ω3-b) — o ALVO de `FieldDispatch` (`operatorUserId`) deve ser técnico DE CAMPO
  (`field_technician`|`technician`); operador-web (`operator`) direciona mas NÃO é alvo (ver `controle/D-OMEGA3B`).
  Guard único cobre `create` e `reassign` → 422 `target_not_field_technician` (existência 404 antes do papel).
  Nova rota `GET /operations/dispatches/:id/timeline` (`field_dispatch:read`).
- service_quotes / Orçamentos (Ω3-a, Operação — `/operations/quotes`) uses `service_quotes:read` (broad: super_admin/tenant_admin/manager/operator/finance/field_technician/auditor), `service_quotes:create`, `service_quotes:update` (super_admin/tenant_admin/manager/operator/finance — quem constrói o orçamento); **congela** o preço (`frozen_unit_price`/`frozen_currency`/`quantity`/`frozen_total DECIMAL(12,2)` emitidos como number) a partir da Tarifa vigente de uma Tabela de Valores PUBLICADA no momento da criação — a Tarifa NUNCA é relida depois (anti-refaturamento; `source_tariff_id`/`source_price_table_id` só rastreabilidade). Máquina de situação `draft→approved/rejected/void` (transição inválida → 422); `approved` imutável (PATCH preço/qtd → 422 `quote_not_editable`); `void` = delete lógico. Chave natural PARCIAL `(tenant_id, work_order_id, service_catalog_id) WHERE is_active AND status<>'void'` (índice SQL bruto, não `@@unique` cheio — permite re-orçar após void; avulsos com work_order NULL coexistem); FKs compostas ON DELETE RESTRICT (registro financeiro não cascateia); RLS on `service_quotes`. Nav path GOVERNADO (`operations.quotes`, `requiredPermissions: service_quotes:read`) — inventory/support não veem o item. **Ω3F-4b — `service_quotes:approve`** (super_admin/tenant_admin/manager/operator/finance, espelhando `service_quotes:update`): `POST /service-quotes/:id/approve` **cria a OS a partir do orçamento** de forma **idempotente** (âncora `created_work_order_id` — um orçamento gera no máx. 1 OS: replay → 409 `quote_already_approved`; não-draft → 409 `quote_not_approvable`; `valid_until` vencido → 422 `quote_expired`; `frozen_total`≤0 → 422 `quote_empty`; cross-tenant → 404). O approve chama `WorkOrderService.create` com opção INTERNA `skipApplicableTariffCheck` (a OS derivada já é precificada pelo preço CONGELADO — não re-exige tarifa viva; nunca aceita no corpo REST) e grava o `activation_mode` em `service_details` da OS. `POST /service-quotes/:id/share` (permissão `service_quotes:update`) gera/reusa `share_token` (idempotente) e devolve o link ao dono — o token NUNCA entra em auditoria nem no DTO normal (§2.8); leitura pública por token ADIADA
- work_orders:mileage_correct / Correção de quilometragem (Ω3F-7a, base/escritório) — a permissão DEDICADA da rota `PATCH /work-orders/:id/mileage` (a BASE corrige o km que o app enviou). É SEPARADA de `work_orders:update` DE PROPÓSITO: field_technician/technician/operator têm :update, então gatear a correção por :update deixaria o técnico de campo forjar `mileage_source=base` e anular a proveniência app×base (a razão da feature). Papéis: super_admin, platform_admin, tenant_admin, manager, operator (despacho web = base). NÃO: field_technician, field_dispatcher, technician, finance, inventory, support, auditor, viewer. O app PREENCHE o km pela fila offline (ação de sync `work_order.mileage`, perm `work_orders:status` que o campo tem) — `source` é decidido pelo SERVIDOR, nunca pelo corpo. Corpo sem km → 400 mileage_required; final<inicial → 422 invalid_mileage_range; km>DECIMAL(10,1) → 400 invalid_mileage; cross-tenant → 404.
- work_order audit-logs (leitura por OS) / aba Logs (Ω3F-8a) — `GET /work-orders/:id/audit-logs` gated por `work_orders:read`. DECISÃO (junta J-Ω3F-8A): a trilha POR-OS é um recorte POR-ENTIDADE (entity=work_order + entity_id), **DISTINTA** da capability tenant-wide "Audit logs" da matriz-base (que dá `none` a operator/field_technician e `read` a auditor). Princípio: quem vê a OS vê o histórico DELA — não o console de auditoria da organização inteira. Logo operator/field_technician/technician/manager/tenant_admin/super/platform (todos com work_orders:read) leem os logs da OS que já enxergam; finance/inventory/support (sem work_orders:read) → aba mostra "acesso não permitido" + backend 403. Valida a OS via WorkOrderService.get → 404 cross-tenant (não vaza existência). §2.8: DTO sem tenant_id; actorName resolvido (UI mostra o nome, nunca o UUID); metadata RE-sanitizado na leitura (dropa tenant/token/storage_key/path/…). Read-only.
- work_order map (Ω3F-8b, aba **Mapa** do Detalhe de OS) — 2 rotas, SEM permissão nova (reusa as existentes): `GET /work-orders/:id/map-start-points` (perm `work_orders:read`) devolve os pontos de partida do mapa — SÓ a posição do técnico ATRIBUÍDO (não a frota — LGPD/minimização; distinto de `/field-locations/latest` que dá a frota sob `field_location:read`) + origem/destino + bases (POI categoria "base"); §2.8 (DTO sem tenant_id/place_id/operatorUserId; técnico só {lat,lng,capturedAt}); NENHUMA coordenada em log (o read não audita). `POST /work-orders/:id/geocode-destination` (perm `work_orders:update`, mesma do geocode da origem — a correção de coordenada é do escritório) espelha o contrato da origem (404/409/422 no_destination_address/200 sem match/502; gate GEOCODING_ENABLED Noop default; auditoria só código+fonte, nunca coordenada). Rota/km = haversine linha reta US$0 (plano J-MAPAS-5), sem SKU pago. Ambas 404 cross-tenant via WorkOrderService.get.
- work_orders lista — ações de linha (Ω3F-9, tela **Ordens de Serviço** `/work-orders`) — SEM permissão nova, reusa 3 já existentes: **Dar andamento** avança o status forward-only pela linha via `PATCH /work-orders/:id/status` (perm `work_orders:status`) — a UI (`canAdvanceRow`) NUNCA oferece `cancelled`/terminais/`open`/`in_progress` (não reabre P-Ω3F6-STATUS-BYPASS; backend é a autoridade → 409 em transição inválida). **Revogar envio** cancela o despacho ativo da OS via `PATCH /operations/dispatches/:id/status {status:cancelled,reason}` (perm `field_dispatch:cancel`; motivo obrigatório → 400), com descoberta LAZY do despacho por `GET /operations/dispatches?workOrderId=X` (perm `field_dispatch:read`). COERÊNCIA verificada: todo portador de `field_dispatch:cancel` (super_admin/platform_admin/tenant_admin/manager/field_dispatcher) também tem `field_dispatch:read` → a descoberta nunca dá 403 silencioso para quem vê o botão. Ambas as ações SOMEM (não só disabled) sem a permissão (gate ligado ao JSX). Consistente com a matriz de tela (`docs/navigation-matrix.md`: operator "E" avança; finance/inventory/auditor "R" só leem; support "—"). **Badge de atraso** é DERIVADO (`scheduled_for`×status), leitura pura sob `work_orders:read` — sem rota nem dado sensível. Cross-tenant coberto pelo tenant-scope+RLS das rotas reusadas.
- work_order_financials / Financeiro da OS (Ω3F-3, Operação — aba **Financeiro** do Detalhe de OS) uses `work_order_financials:read` (broad: super_admin/tenant_admin/manager/operator/finance/field_technician/auditor), `work_order_financials:create`, `work_order_financials:update` (super_admin/tenant_admin/manager/operator/finance — quem lança), espelhando exatamente a distribuição de `service_quotes:*`. Lançamentos financeiros (`work_order_financial_items`) por OS: **congelam** o preço (`unit_amount`/`currency`/`quantity`/`total_amount DECIMAL(12,2)` emitidos como number) a partir da Tarifa vigente de uma Tabela de Valores PUBLICADA no momento do lançamento — a Tarifa NUNCA é relida depois (anti-refaturamento; `tariff_id`/`price_table_id` só rastreabilidade). `source=manual` aceita `unit_amount` do corpo (ex.: pedágio) e exige `description`. PATCH recomputa `total_amount` a partir do preço congelado (nunca reconsulta a tarifa); **homogeneidade de moeda por OS** (o 1º item fixa a moeda; divergência → 422 `currency_mismatch`) para o total agregado ser sempre single-currency. DELETE = delete lógico (`deleted_at`). Idempotência tenant-scoped por `(tenant_id, work_order_id, client_action_id)` (índice PARCIAL `WHERE client_action_id IS NOT NULL AND deleted_at IS NULL`) → replay 409; FKs compostas ON DELETE RESTRICT; RLS ENABLE+FORCE+policy `tenant_isolation` on `work_order_financial_items`. DTO omite `tenant_id`/`client_action_id` (§2.8). Aba GOVERNADA por revelação progressiva (C2) — só visível a partir do Ω3F-3 sob `work_order_financials:read`. **Ω4-3 IMUTABILIDADE:** item FATURADO (`invoiced_at != null`, carimbado ao faturar a OS) vira IMUTÁVEL — PATCH/DELETE → **422 `item_invoiced`** MESMO para quem tem `work_order_financials:update` (a trava é de ESTADO, não de permissão); colunas `invoiced_at`/`title_id` + FK composta `(tenant_id,title_id)`→financial_titles RESTRICT (item faturado não órfão). DTO expõe `invoiced` bool.
- financial_accounts / Contas financeiras (Ω4-1, Financeiro — caixa/banco/carteira do tenant, base do módulo financeiro) uses `financial_accounts:read`, `financial_accounts:create`, `financial_accounts:update`. **read** → super_admin, platform_admin, tenant_admin, finance, manager, auditor, viewer; **create+update** → super_admin, platform_admin, tenant_admin, finance (só a tesouraria e admins escrevem; manager/auditor/viewer só leem). NÃO: operator, inventory, field_technician, field_dispatcher, technician, support (contas bancárias da empresa ≠ `work_order_financials:*`, que é o financeiro da OS e o operator tem). CRUD `/api/v1/financial-accounts`; DELETE é lógico (is_active=false + status=inactive) sob `:update`. Chave natural PARCIAL `(tenant_id, name) WHERE is_active=true` (recria após soft-delete). `opening_balance` DECIMAL(12,2) ≥ 0; currency allowlist {BRL} no v1; kind {cash,bank,wallet}. RLS ENABLE+FORCE+policy `tenant_isolation`; FK (tenant_id)→tenants RESTRICT; `@@unique([tenant_id,id])` (para as FKs compostas de Título/Caixa nas fatias seguintes). DTO §2.8 omite tenant_id; auditoria (created/updated/deleted) só `{name,kind,status}` — nunca account_number/document. Cross-tenant → 404.
- financial_titles / Títulos a pagar/receber (Ω4-2a, Financeiro — núcleo das telas Cobranças/Pagamentos) uses `financial_titles:read`, `financial_titles:create`, `financial_titles:update`. **read** → super_admin, platform_admin, tenant_admin, finance, manager, auditor, viewer; **create+update** → super_admin, platform_admin, tenant_admin, finance (mesma distribuição de financial_accounts). NÃO: operator, inventory, field_technician, field_dispatcher, technician, support (título é tesouraria do tenant ≠ `work_order_financials:*`). Rotas `/api/v1/financial-titles` (GET list + filtros direction/status/partyType/overdue/due-range · POST · GET/:id · PATCH/:id · **PATCH/:id/status** máquina open↔scheduled/→in_dispute→open/→cancelled; partially_paid/paid dirigidos por pagamentos Ω4-4 · DELETE lógico via deleted_at) todas sob `:update` para escrita. **CHOKEPOINT `assertPeriodOpen`** (D-Ω4-A3): toda escrita (create/update/status/delete) atravessa antes de gravar → 422 `period_closed` se a competência (YYYY-MM derivada de issue_date server-side) estiver fechada. Party polimórfico (party_type customer|supplier|other + party_name snapshot; party_id sem FK nativa). FK composta `(tenant_id, account_id)`→financial_accounts RESTRICT + `(tenant_id, work_order_id)`→work_orders RESTRICT (proveniência, setada no Ω4-3). amount Decimal(12,2)>0; currency {BRL}. **Ω4-3 FATURAMENTO:** `POST /work-orders/:id/invoice` (gated por **`financial_titles:create`** — consumo CRUZADO a partir de rota sob `/work-orders`; faturar = mintar 1 título) minta um título a RECEBER do agregado CONGELADO do Financeiro da OS (Σ frozen total_amount, NUNCA relê tarifa); idempotente por OS (índice PARCIAL `(tenant_id,work_order_id,direction) WHERE deleted_at IS NULL` → replay 409 already_invoiced); agregado ≤0 → 422 nothing_to_invoice; passa pelo chokepoint (competência fechada → 422 period_closed); cross-tenant OS → 404; due_date default hoje+30d. RLS ENABLE+FORCE+policy; DTO §2.8 omite tenant_id/deleted_at (expõe `active`+`overdue` derivado); auditoria só `{direction,status,party_type}`. Cross-tenant → 404.
- professional_statements / Extrato do profissional (Ω4C PR-03, Frota — razão financeiro POR profissional; a "folha do profissional", razão distinta da tesouraria do tenant) uses `professional_statements:read`, `professional_statements:create`, `professional_statements:update`. **read** → super_admin, platform_admin, tenant_admin, finance, manager, auditor, viewer; **create+update** → super_admin, platform_admin, tenant_admin, finance (mesma distribuição de financial_titles). NÃO (nem read): operator, inventory, field_technician, field_dispatcher, technician, support (folha sensível; field_technician veria extratos de terceiros — self-service fica p/ Ω5). Rotas `/api/v1/professional-statements` (GET extrato de UM profissional [`operatorProfileId` OBRIGATÓRIO → 400 `operator_profile_required`; profissional de outro tenant → 404; nunca lista global] devolvendo items[parcelas]+summary{currentBalance,totalDebits,totalCredits,count}+runningBalance por linha+pagination · POST cria **SÓ AJUSTE** [entry_type forçado a `adjustment`; body `{operator_profile_id,direction,description,amount,installment_total,first_due_date,currency?}`; gera group_id + N parcelas: split igual com resto na 1ª, vencimentos mensais] · GET/:groupId [lançamento + parcelas] · PATCH/:groupId [SÓ description; campo financeiro → **409 `statement_entry_locked`**] · DELETE/:groupId [soft "retirar do extrato" sob `:update`, trava RN-EXT-01]). **Modelo (D-Ω4C-EXTRATO-MODEL):** tabela ÚNICA `professional_statement_entries`, 1 linha por PARCELA agrupada por `group_id`; enums em INGLÊS (entry_type damage|fine|remuneration|adjustment · direction debit|credit · status pending|settled|cancelled · source_type damage|fine|remuneration|manual) com labels PT-BR na UI; amount Decimal(12,2)>0 = valor DA PARCELA. **Saldo (D-Ω4C-EXTRATO-DIRECTION):** Σcredit − Σdebit DERIVADO server-side (debit=desconto, credit=provento). **Trava RN-EXT-01 (D-Ω4C-EXTRATO-TRAVA):** lançamento com parcelas é FATO travado — PATCH de campo financeiro sempre 409; DELETE só se NENHUMA parcela `settled` (≥1 settled → 409); reversão só por AJUSTE compensatório. FK composta `(tenant_id, operator_profile_id)`→operator_profiles RESTRICT; source_id sem FK nativa (app-level; índice parcial de idempotência de origem foundation-ready). RLS ENABLE+FORCE+policy; DTO §2.8 omite tenant_id/source_id/client_action_id (nome do profissional só como label, jamais CNH); auditoria só `{operatorProfileId,entryType,direction,installmentTotal,amount}`. Backend é a autoridade → finance escreve; manager POST → 403 real. Cross-tenant → 404.
- notifications (scheduled engine) / Motor de notificações (Ω4C PR-04, transversal — o "cadastro" de notificação avulsa do AutEM + scheduler idempotente) uses the NEW `notifications:create` for the scheduled layer, while the inbox stays on `notifications:read`/`notifications:update` (own inbox, INTOCADOS). **create** (criar/cancelar uma notificação AGENDADA, que pode fazer broadcast PÚBLICA/PERSONALIZADA) → super_admin, platform_admin, tenant_admin, manager, operator, field_dispatcher. NÃO: finance, inventory, field_technician, technician, auditor, viewer, support (um técnico não dispara broadcast pra org inteira; self-reminder privado p/ campo → Ω5). **Model (D-Ω4C-NOTIF-MODEL):** DUAS CAMADAS — nova tabela `scheduled_notifications` (DEFINIÇÃO agendada) + a tabela `notifications` (ENTREGA/inbox) INTOCADA (o fan-out do disparo cai nela → sino/central já leem). Enums em INGLÊS SEM CHECK (visibility private|public|custom · status pending|fired|cancelled · source_type maintenance_item|fine|insurance_policy|financial_title|manual) com labels PT-BR na UI. **Scheduler (D-Ω4C-NOTIF-SCHEDULER):** job recorrente `notifications.scan-due` REUSA o `job.worker.ts` (setInterval → dequeue → registry; re-enfileira a si com delay 60s) — SEM node-cron; atrás da flag `JOBS_WORKER_ENABLED` (default OFF; CI/testes nunca sobem o loop). notify_at ancorado no fuso de negócio (parseBusinessDate, America/Sao_Paulo). **Idempotência (D-Ω4C-NOTIF-IDEMPOTENCY):** cada definição tem no máx. 2 ocorrências (reminder_at, notify_at); guardas reminder_fired_at/fired_at + backstop DURO `idempotencyKey = sched:<id>:<occurrence>` na unique de entrega `(tenant_id, recipient_user_id, idempotency_key)` → 2 disparos com o mesmo `now` = mesmas entregas. **Visibilidade (D-Ω4C-NOTIF-VISIBILITY):** private → só o criador; public → todos ATIVOS do tenant; custom → recipients ∩ ativos (descarta stale/cross-tenant); fora do alvo NUNCA recebe. Rotas `/api/v1/notifications/scheduled` (POST cria [dispara INLINE se notify_at≤now — funciona com worker OFF] · GET lista do próprio criador · GET/:id · DELETE soft-cancel para ocorrências FUTURAS; entregues permanecem no inbox), todas sob `notifications:create` (creator-scoped → cross-tenant/cross-criador 404). FK composta `(tenant_id, created_by)`→users CASCADE; source_id sem FK nativa (foundation-ready). RLS ENABLE+FORCE+policy; DTO §2.8 omite tenant_id/client_action_id; auditoria só `{visibility, sourceType, hasReminder, customRecipientCount, status}` — nunca título/mensagem/lista de destinatários. Backend é a autoridade → papel sem `create` no POST → 403 real.
- sessions / Sessões de usuário + Acessos + Logs globais (Ω4C PR-11, Controle > Usuários — sobre a infra REAL de `auth_sessions` + `audit_logs`, ZERO migração; D-Ω4C-SESS-NOMIG) uses the NEW `sessions:read` + `sessions:revoke` (Logs globais e Acessos REUSAM `audit.read`). **`sessions:read`** (listar sessões ativas + histórico de acessos) → super_admin, platform_admin, tenant_admin, auditor. **`sessions:revoke`** (encerrar/revogar a sessão de OUTRO usuário) = ADMINISTRATIVA forte → SÓ super_admin, platform_admin, tenant_admin — **auditor LÊ mas NÃO revoga** (segregação de funções, não escala). NÃO (nem read): manager, operator, finance, inventory, field_technician, field_dispatcher, technician, viewer, support. Rotas `/api/v1/sessions` (GET ativas, `sessions:read`, WHERE `revoked_at IS NULL AND expires_at>now`, tenant-scoped, filtrável por `?userId=`) · `POST /sessions/:id/revoke` (`sessions:revoke`, idempotente 200 `{revoked:true}`; id inexistente/já-revogado/**de outro tenant** → 404 via RLS + `updateMany` count=0, nunca vaza existência cross-tenant; sem 409) · `GET /sessions/access-history` (`audit.read`, último login por usuário derivado de `auth_sessions.created_at`, D-Ω4C-ACESSO-SOURCE) · `GET /api/v1/audit-events` estendido (`audit.read`) com filtros server-side `?action=&actorId=&from=&to=` + paginação `?limit=&offset=` (422 malformado). **Revogação REAL (D-Ω4C-SESS-REVOKE-REAL):** marca `revoked_at=now` sem exigir o refresh token da vítima; `refreshSession()` já barra `revoked_at` → o próximo refresh FALHA de verdade. **Caveat honesto (NÃO parada):** o access token é JWT stateless TTL 15m → segue válido até expirar; o refresh é bloqueado imediato → re-login forçado em ≤15 min (a UI comunica a janela, não promete logout instantâneo). A própria revogação é AUDITADA (`auth.session.revoked`, ator=admin). **§2.8 (D-Ω4C-AUD-ALLOWLIST-2.8):** SessionView `{id,userLabel,loginAt,lastActivityAt,deviceLabel(rótulo grosseiro do user_agent, ex. "Chrome · Windows"),status}` e AuditEventView SEM `refresh_token_hash`/`ip_address` cru/`tenant_id` externo/token/user_agent cru/CNH/corpo; o IP é OMITIDO (não mascarado). Backend é a autoridade → auditor no POST revoke → 403 real.
- financial_entries / Lançamentos de caixa + Extrato (Ω4-4, Financeiro — Caixa e liquidação de títulos) uses `financial_entries:read`, `financial_entries:create`, `financial_entries:update`. **read** → super_admin, platform_admin, tenant_admin, finance, manager, auditor, viewer; **create+update** → super_admin, platform_admin, tenant_admin, finance (mesma distribuição de financial_accounts/titles). NÃO: operator, inventory, field_technician, field_dispatcher, technician, support. Rotas: `/api/v1/financial-entries` (GET list/extrato + filtros account_id/direction/from/to/category · POST lançamento avulso · GET/:id · PATCH/:id [edita só category/description; amount/direction/account/occurred_at IMUTÁVEIS; reconciliado/período-fechado → 422] · DELETE lógico); `POST /financial-entries/:id/reverse` (`:update`) = ESTORNO por contra-lançamento (direction invertida, reversal_of, idempotente → 409 already_reversed, sem UPDATE destrutivo); **`POST /financial-titles/:id/pay` (`financial_entries:create`) = LIQUIDAÇÃO** (cria lançamento + dirige o paid_amount/status do título via applyPayment — ÚNICO caminho a partially_paid/paid; guard overpayment/cancelled/já-pago → 422; idempotência client_action_id → 409 duplicate_payment); `GET /financial-accounts/:id/balance` (`:read`) = saldo = opening + Σin − Σout SOMADO no backend. **Ω4-5 CONCILIAÇÃO:** `PATCH /financial-entries/:id/reconcile` (`:update`, reusa a perm — sem permissão nova) marca/desmarca o lançamento como conciliado com o extrato: body `{reconciled, divergence_type?, reconciliation_ref?}`; `divergence_type` ∈ **{value, date}** (400 `invalid_divergence_type` fora — `missing`/`duplicate` são razões de NÃO-conciliação, inalcançáveis; D-Ω4-5-DIVERGENCE-NARROW); `reconciled=false` DESCONCILIA (limpa divergence/ref/at/by); `reconciled_at`/`reconciled_by` carimbados server-side. Filtros de lista `?reconciled=` e `?divergence_type=`. Colunas aditivas divergence_type/reconciliation_ref/reconciled_at/reconciled_by (migration 20260813000000). **ACOPLAMENTO documentado:** `financial_entries:create` dirige o estado do TÍTULO (paid_amount/status) via a liquidação — quem pode lançar caixa pode liquidar; o conjunto de papéis com `financial_entries:create` = o com `financial_titles:update` (finance+admins), sem furo. **CHOKEPOINT `assertPeriodOpen`** em toda escrita de MOVIMENTO (create/update/delete/reverse/pay) → 422 period_closed (competência derivada de occurred_at server-side). **EXCEÇÃO Ω4-5 (D-Ω4-5-RECONCILE-META):** o `reconcile` é META-DADO (não altera amount/direction/saldo da competência) e **ATRAVESSA período fechado** — o extrato bancário chega após o fechamento; conciliar não passa pelo chokepoint. Coerente com D-Ω4-POS-FECHAMENTO (ato é evento da competência corrente). O fix P-Ω4-4-REVERSE-MUTABLE fechou: `reverse` agora chama assertMutable → estornar lançamento CONCILIADO → 422 (desconcilie antes). Conta OBRIGATÓRIA e ATIVA (inativa → 422 account_inactive); currency = moeda da conta (422 currency_mismatch). FK composta `(tenant_id,account_id)`→financial_accounts RESTRICT + `(tenant_id,title_id)`→financial_titles RESTRICT. RLS ENABLE+FORCE+policy; DTO §2.8 omite tenant_id/deleted_at/client_action_id (auditoria só `{direction,payment_method}`). Cross-tenant → 404.
- financial_period / Fechamento de período (Ω4-6, Financeiro — trava retroativa da competência) uses `financial_period:read`, `financial_period:close`, `financial_period:reopen`. **read** → super_admin, platform_admin, tenant_admin, finance, manager, auditor, viewer; **close** → super_admin, platform_admin, tenant_admin, finance (a tesouraria fecha); **reopen** → SÓ super_admin, platform_admin, tenant_admin (override administrativo — FORA de finance, separação de funções RN-FIN-009: `finance` fecha 200 mas reopen → 403). NÃO (nem read): operator, inventory, field_technician, field_dispatcher, technician, support. Rotas `/api/v1/financial-periods` (GET list · GET/:period status+snapshot+checklist AO VIVO · **POST/:period/close** · **POST/:period/reopen**); `:period` = YYYY-MM (malformado → 400 `invalid_period`). CLOSE atômico (uma withTenantRls tx): já fechado → 409 `period_already_closed`; closing → 409 `period_closing_in_progress`; pendência BLOQUEANTE (título `in_dispute` na competência) sem force → 422 `pending_items_block_close`; `force:true` ignora só o gate bloqueante mas EXIGE `reason` (400 `reason_required`) e carimba `forced:true` no snapshot + os ids das disputas sobrepostas na AUDITORIA (server-side). Não-liquidados e não-conciliados são INFORMATIVOS (nunca bloqueiam — carry-over normal; extrato chega após o fechamento). REOPEN: `reason` OBRIGATÓRIO (400 `reason_required`); linha inexistente/≠closed → 422 `period_not_closed`; closed→reopened (isPeriodClosed volta a false). **GUARD M2:** `isPeriodClosed` bloqueia status ∈ {closing,closed} (título E lançamento via o mesmo chokepoint); `reconcile` NÃO atravessa o chokepoint → exento (extrato pós-fechamento). **SNAPSHOT (D1):** a base MATERIAL re-derivável = {título: sumAmount+count por direção, competencia; lançamento: in/out/net} — EXCLUI paid_amount/status/balance/byStatus (mudam via pagamento cross-mês) e reconciled/updated_* (informativos point-in-time); congelado no fechamento como {latest, history} append-only (imutável entre reclose/reopen — RN-AUD-005). RLS já habilitada na 20260810000000. DTO §2.8 omite tenant_id; snapshot só agregados de dinheiro (sem party_name/document/ids de título). Cross-tenant → 404/invisível (tenant do ator + RLS + filtro tenant_id explícito nas leituras de competência).
- cheques / Cheques (Ω4-7, Financeiro — instrumento de pagamento com ciclo próprio) uses `cheques:read`, `cheques:create`, `cheques:update`. **read** → super_admin, platform_admin, tenant_admin, finance, manager, auditor, viewer; **create+update** → super_admin, platform_admin, tenant_admin, finance (mesma distribuição de financial_entries). NÃO: operator, inventory, field_technician, field_dispatcher, technician, support. Rotas `/api/v1/cheques` (GET list + filtros direction/status/account_id · POST registro · GET/:id · PATCH/:id [edita cheque_number/bank/due_date/notes SÓ enquanto 'registered' → senão 422 `cheque_not_editable`; amount/direction/account/currency IMUTÁVEIS] · DELETE lógico SÓ 'registered'); verbos dedicados de transição: `POST /cheques/:id/deposit` (`cheques:update`) registered→deposited; `POST /cheques/:id/cancel` (`cheques:update`) registered→cancelled; **`POST /cheques/:id/clear`** (SEMPRE move caixa) exige `cheques:update` **E** `financial_entries:create` (cadeia de dois requirePermission na rota + assertCanMoveMoney no serviço). **`POST /cheques/:id/bounce`** é POLIMÓRFICO: a rota exige só `cheques:update` e o SERVIÇO aplica `assertCanMoveMoney` (403 `financial_write_forbidden`) SÓ no caminho `cleared→bounced` que posta contra-lançamento — `deposited→bounced` (sem caixa) fica acessível a `cheques:update`. O gate de dinheiro fecha a escalada de privilégio (a chamada service→service a entryService.create não reatravessa a rota de lançamentos). Máquina de estados: registered→{deposited,cancelled}; deposited→{cleared,bounced}; cleared→{bounced}; bounced/cancelled terminais (transição ilegal → 422 `invalid_transition`; a transição é FLIP CONDICIONAL atômico → corrida perdida → 409 `transition_conflict`). **COMPENSAR** (deposited→cleared): posta 1 lançamento via entryService.create (received→'in', issued→'out', payment_method='check', category='cheque_clearing') na competência CORRENTE server-now (due_date "bom para" é MEMO, nunca entra na competência) → CHOKEPOINT (mês corrente fechado → 422 `period_closed`, rollback p/ 'deposited'); vincula cleared_entry_id. **DEVOLVER** deposited→bounced (sem caixa) OU cleared→bounced (posta CONTRA-lançamento NOVO invertido, category='cheque_bounce' — NÃO reverse(), logo NÃO travado por lançamento conciliado Ω4-5; D-Ω4-7-BOUNCE-NEW-ENTRY). amount Decimal(12,2) validado no REGISTRO (>0 + range → sempre compensável). title_id FORA de escopo (liquidar título com cheque = payTitle; ver P-Ω4-7-DUPLA-CONTAGEM). FK composta `(tenant_id,account_id)`→financial_accounts RESTRICT; conta OBRIGATÓRIA+ATIVA no registro (422 `account_inactive`), currency = moeda da conta (422 `currency_mismatch`). RLS ENABLE+FORCE+policy (migration 20260815000000); DTO §2.8 omite tenant_id/deleted_at (auditoria só `{direction,status}`). Cross-tenant → 404.
- branches / Filiais (Ω2-b, Configurações) uses `branches:read`, `branches:create`, `branches:update` (mirror of `service_catalog:*`); CRUD over the pre-existing `branches` table (no new migration), natural key `@@unique([tenant_id, code])` → 409 duplicate_code, soft-delete via `status: "inactive"` (the model has no `is_active`), RLS pre-enabled on `branches`
- suppliers / Fornecedores (Ω2-b, Configurações) uses `suppliers:read`, `suppliers:create`, `suppliers:update` (mirror of `service_catalog:*`); new `suppliers` table (additive migration with RLS ENABLE+FORCE), natural key `@@unique([tenant_id, name])` → 409 duplicate_name, structural email validation (normalized lowercase), logical deactivation via `is_active`
- operator profiles / Profissionais (Ω2-c, Configurações) uses `operator_profiles:read`, `operator_profiles:create`, `operator_profiles:update` (mirror of `service_catalog:*`); 1-1 with a User via composite FK `(tenant_id, user_id)` UNIQUE (ON DELETE CASCADE), CNH + tracking-consent (LGPD sensitive). Audit metadata is allowlisted to `{userId, hasConsent, hasCnh}` — CNH number and full name NEVER reach the audit log; `user_id` immutable after create; 409 duplicate_profile; RLS on `operator_profiles`
- tags / Tags (Ω2-d, Configurações) uses `tags:read`, `tags:create`, `tags:update` (mirror of `service_catalog:*`); etiquetas (name natural key → 409 duplicate_name, optional hex color `#rrggbb` → 400 invalid_color), logical deactivation via `is_active`, RLS on `tags`. TagAssignment (polymorphic join) deferred to a future block
- pois / Pontos de Interesse (Ω2-d, Configurações) uses `pois:read`, `pois:create`, `pois:update` (mirror of `service_catalog:*`); geo points with `latitude`/`longitude` NUMERIC(10,7) NOT NULL (emitted as JSON number), coordinate validated with the Ω1 map predicate (finite, in-range, rejects 0/0 sentinel → 400 invalid_coordinate), name natural key → 409, RLS on `pois`
- tenant settings / Configurações da Organização (Ω2-e) uses `tenant_settings:read` (page) and `tenant_settings:update` (inline edit); data-backed key-value parameters from `GET /api/v1/tenant-settings` + `PUT /tenant-settings/:key` (upsert), tenant_id from claim, RLS on `tenant_settings`. Seed brings `organization.theme`/`organization.currency`/`organization.timezone`/`organization.business_name`; the frontend groups by `category` and never fabricates values (D-007: mock/error → empty). tenant_admin edits, manager reads only
- dashboard summary (C3) exposes `GET /api/v1/dashboard/summary` guarded by the existing `dashboard:read` permission; it returns tenant-scoped REAL aggregates (COUNT/GROUP BY/recent-SELECT over `work_orders`, `work_order_events` and the cadastros `customers`/`vehicles`/`teams`/`service_catalog`) — never a local constant or mock; `tenant_id` from the claim, wrapped in RLS, and the payload never exposes `tenant_id`/PII
- team registry (Cadastros - Equipes) uses `teams:read`, `teams:create`, `teams:update`; `teams` + `team_members` junction where leader and members reference same-tenant users via composite FK; tenant_id from claim; composite uniqueness `@@unique([tenant_id, name])` (team) and `@@unique([tenant_id, team_id, user_id])` (membership); logical deactivation of the team via `is_active` (member removal is a hard delete of the junction row); RLS on `teams` and `team_members`. Write limited to tenant_admin, manager (and super_admin); operator, field_technician and auditor read; support has no access. Member add/remove requires `teams:update`
- checklist reads and writes must validate `tenant_id` together with template, field, run and answer identifiers
- M10/M11/M12 must render checklist schemas from API data rather than hardcoded mobile field definitions

## Notes
- "limited-support" means support actions must be constrained, logged, and never silently exceed tenant-scoped support permissions.
- Approval-related permissions should be refined together with APPROVAL_LIMITS.md.
- This matrix must stay consistent with the operational mission in PRODUCT_CONTEXT.md and the rules consolidated in agent-orchestration/docs/regras-de-negocio.md.
- This matrix is the baseline and should be decomposed later into project documentation under the standard structure.
