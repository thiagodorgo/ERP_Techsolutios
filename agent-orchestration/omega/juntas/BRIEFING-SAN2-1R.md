# BRIEFING — junta do `SAN2-1R` (resgate do SAN2-1, opção C do dono)

**Head:** `8860fc3` · base `origin/main` = `a0a1075` (#361) · branch `chore/san2-1-resgate`.
**Quórum:** maioria de 3 (diff de código de produto VAZIO — item de bateria, confiram).
**Ciclo 1 de 2** (`D-TETO-DOIS-CICLOS`). Junta sob o `PROTOCOLO-JUNTA-RESILIENTE` (P1/P2/P4/P5).

## Natureza do bloco — julguem com isto em mente

O `SAN2-1` foi **reprovado duas vezes e PARADO** pelo teto de dois ciclos. O dossiê foi ao dono; ele
escolheu a **opção C**: *salvar o que as juntas verificaram, trocar a etiqueta falsa por uma verdadeira,
adiar a leitura das 79 com registro*. **Este bloco é a execução daquela decisão** — não é o ciclo 3 do
SAN2-1 (não existe ciclo 3). O que se julga aqui: **a execução da opção C está fiel e completa?**

## O que conferir

1. **A etiqueta**: as 79 dizem agora *"adiada por triagem automática; NÃO verificada item a item"*? Sobrou
   alguma com a frase antiga ("sem consequência de produto, dado, segurança ou número") **afirmando**?
2. **P-036**: fechada **como duplicata** da `P-CHK-TEMPLATE-PRISMA-V7` (resolvida 02/08)? A evidência do
   gêmeo procede (mesma chamada, mesma causa)?
3. **Tripwire de tarifa** (`P-Ω3F3B-UPDATE-VALIDA4`): fora do balde C, com o motivo escrito?
4. **`P-SAN2-LEITURA-DAS-79`**: existe, com dono, prazo relativo (pós-ciclo 5) e critério de fechamento?
5. **Dívidas do porteiro do #361**: backfill (`a0a1075`/`48dc863`) na entrada #361; §C7 renumerado
   (4, 4-bis, 5, 6, 7) **idêntico nos dois contratos**; artefatos pós-merge do SAN2-R presentes.
6. **Portagem íntegra**: o trabalho verificado veio da branch parada **sem reescrita** (blobs iguais onde
   não havia correção a fazer); `decisoes.md` da main é prefixo do novo (só o append da `D-GOLIVE`).
7. **Diff de código VAZIO** e bateria (16/16 · 6/6 · freeze em dia · índice idempotente).

**Inelegível:** o orquestrador (autor do diff e do resgate). Cadeiras: (1) **fidelidade à opção C** (veto) ·
(2) **diff/portagem/append-only** (veto) · (3) **KPI/registro**.
