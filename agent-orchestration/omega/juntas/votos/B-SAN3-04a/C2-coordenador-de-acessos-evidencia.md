# Evidência — C2 coordenador-de-acessos — junta B-SAN3-04a (objeto fbda96b0) — 2a INSTÂNCIA

- Modelo: claude-opus-5[1m] (Opus 5). Início 2a instância: 2026-09-18 19:36. A 1a instância caiu por HTTP 429 (~16:43); o parcial dela está em `C2-coordenador-de-acessos-evidencia.parcial-instancia1.md` e `-voto.parcial-instancia1.json` (copiados antes de escrever). É ROTEIRO (P3): tudo citado aqui foi REEXECUTADO nesta instância.
- Participação prévia (R2): `coordenador-de-acessos` foi C2 do ciclo 1 da junta do plano SAN3 (#386); o achado C2-04 moldou o critério "o teste do item 15 cobre as quatro divergências". Aqui ele é medido pela ref (T1 + mutações), não pelo que foi pedido.
- Carga da máquina: juntas simultâneas vivas (contêineres `j-b04a-c1/c2-*`, worktrees `j-b04a-*`, `j-b11-*`; dev `bsan301`); tempos medidos aqui não são comparáveis.

## Medições (apensadas em ordem)

### E0 — terreno (19:36)
- `git -C .../j-bsan304a-c2 rev-parse HEAD` = `fbda96b016ac65f88fe99d695295329e83938bea`, `status --porcelain` vazio; `.../j-bsan304a-c2-base` = `02bd7dab2ffa29999920da8b7da345b6a5958b67`, vazio.
- `docker ps -a`: `j-bsan304a-c2-pg` Up (127.0.0.1:56443), `j-bsan304a-c2-redis` Up (127.0.0.1:56444). Resíduo alheio inerte (não tocado): `erp-postgres-alt` (exited 55432), `erp-postgres`/`erp-redis` exited, `pastrack-*`, `bsan301-*`, `j-b04a-*`.

### E1 — as 9 concessões e 2 revogações × matriz × o que cada chave destrava (19:40, reexecutado)
- `git diff 02bd7dab fbda96b0 -- catalog.ts snapshot.json RBAC_MATRIX.md prisma/seed.ts`: finance +5 (`work_orders:read`, `customers:read`, `service_catalog:read`, `tenant_checklists:read`, `checklist_runs:read`); inventory +2 (`tenant_checklists:read`, `checklist_runs:read`); field_technician +1 (`tenant_checklists:read`); operator +1 (`work_orders:create`); manager −2 (`checklist_runs:update`, `checklist_runs:acknowledge`) + `DELIBERATE_REVOCATIONS` com as mesmas 2 (decisão nomeada). Snapshot: exatamente as mesmas 11 linhas. Matriz: só l.38 e l.41 coluna finance `none→read`. Seed: `SEEDED_SYSTEM_ROLES = [...STANDARD_ROLES, "auditor"]`.
- Células na ref (`git show fbda96b0:RBAC_MATRIX.md`): finance l.45 `read` · l.38 `read` · l.41 `read` · l.43 `read` · l.44 `read`; inventory l.43 `read` · l.44 `read/answer-by-scope` (só `read` concedido; padrão da própria matriz "read/edit-scoped" l.37 = leitura incondicional + escrita qualificada); field_technician l.43 `read`; operator l.45 `create/edit` (incondicional). manager l.44 `read/complete-by-scope` → `update`/`acknowledge` não nomeados (excedentes) → revogados.
- `grep -rn '"<chave>"' src` (fora do catálogo) + gate por rota (`grep -nE 'router\.(get|post|patch|delete)|requirePermission|requireAny'`):
  - `work_orders:read`: só GET (`work-order.routes.ts:72,80,105,121,205,218,232,267`; comments GET `:39`; audit-logs GET `:35`; timeseries GET `:33`) + flag `work_orders.enabled` do bootstrap mobile (`mobile.routes.ts:224`). Nenhum POST/PATCH/DELETE.
  - `customers:read` / `service_catalog:read`: só GET (`customer.routes.ts:31,47`; `service-catalog.routes.ts:33,49`).
  - `tenant_checklists:read`: só GET (`checklist.routes.ts:34,42,58,66`).
  - `checklist_runs:read`: só GET (`checklist.routes.ts:98,106` requireAny read|create; `:117,149,186`); `impound.routes.ts:204` (GET, dual com `impound:read`, que finance/inventory não têm); flag `checklists.enabled` do bootstrap mobile (`mobile.routes.ts:229`); **`notification.recipient-resolver.ts:30`: `checklist_run.completed` notifica quem tem `checklist_runs:read` → finance e inventory viram destinatários** (efeito não listado no §7 do plano; teto de 20 destinatários por evento, l.21).
  - `work_orders:create` (operator): `POST /work-orders` (`:113`), `POST /work-orders/:id/duplicate` (`:189`), `POST /:id/attachments` (`:225` requireAny create|update — operator já tinha update), sync mobile `work_order.create` (`mobile-work-order-sync.ts:214`).
- Veredito parcial: nenhuma das 9 concessões é "por escopo" nem destrava mutação fora da célula; CE-6 respeitado nas concessões. As concessões qualificadas sem escopo que existem (manager `complete`, operator `update/complete`, field_technician `update/complete/acknowledge`) já estavam em `02bd7dab` (conferir em E1b).

### E2 — login real dos 9 papéis no objeto (19:45-20:15)
- Terreno: API da 1ª instância AINDA VIVA (PIDs 22856/24488 em :56445 sobre `c2_prov`; 10812/896 em :56446, worktree `-base`) — não reusada; `taskkill` delas NEGADO pelo classificador de permissão ("Interfere With Workloads") → ficam vivas; reportadas na limpeza. Portas: faixas excluídas do Windows 56500-56899 e 56907-57006; 56443/56444/56447/56448 fora delas.
- Bases NOVAS (2ª instância), `postgresql://postgres:***@127.0.0.1:56443/<base>`, `REDIS_URL=redis://127.0.0.1:56444`:
  - `i2_prov` (objeto): `prisma migrate deploy` ec=0 → `db:provision-rbac` = "12 criado(s) · 908 criada(s) · 0 já existiam · revogações deliberadas: 0 removida(s) · CONVERGIDO" → `db:seed` → `db:seed:users` (9 contas, 1 por papel). Papéis globais: auditor 56 · field_dispatcher 42 · field_technician 43 · finance 61 · inventory 17 · manager 139 · operator 68 · super_admin 198 · support 10 · technician 44 · tenant_admin 185 · viewer 45. manager ∩ `checklist_runs:*` = {complete, read, reopen}.
  - `i2_seed` (objeto, forma da CI: migrate + `db:seed` só): auditor 56 · field_dispatcher 42 · manager 139 · super_admin 198 · technician 44 · tenant_admin 185 · viewer 45 → auditor PRESENTE só com seed (item 14); finance/inventory/operator/field_technician/support ausentes (`P-SAN3-04A-SEED-PAPEIS-LEGADOS`, dono B-SAN3-07).
  - `i2_base` (head-base 02bd7dab, worktree `-base`): migrate + provision ("901 criada(s)") + seed + seed:users: finance 56 · inventory 15 · manager 141 · operator 67 · field_technician 42 · auditor 56 · support 10.
- API do objeto: `CORE_SAAS_PERSISTENCE=prisma PORT=56447 JOBS_WORKER_ENABLED=false npx tsx src/server.ts` (DATABASE_URL `…/i2_prov`, REDIS_URL :56444); `GET /api/v1/health` → `{"status":"ok"}`. Carga: CPU 100%, 25 processos node, 2,7 GB livres (juntas simultâneas) — tempos de resposta 0,2-7 s.
- Harness `scratchpad/c2h/harness.mts` (roteiro da 1ª instância, revisado e REEXECUTADO): `POST /auth/login` por conta → permissões do BANCO; `GET /navigation/menu`; código REAL do front (`resolveFrontendRoles`, `resolveFrontendPermissionsFromBackend`, `computeHiddenNavPaths`, `roleKindFor`, `buildSidebarNav`, `isPlatformAdmin`); guard de cada rota do parse do `App.tsx`; GET representativo por item visível e por rota fora do sidebar. Saída `c2h/i2-matriz-efetiva-obj.json`, ec=0.
- Login = 200 nos 9. Claims do JWT: `sub` presente, `tenant_id` = organização demo, `roles` = [papel]; SEM `tenant_role`, `permissions`, `scope` no token (permissões vêm do banco na resposta do login). `src/modules/auth/**` fora do diff do bloco.
- Menu efetivo (status do GET que a tela consome; 400/422 = validação DEPOIS do gate):
  - finance (kind finance): OPERAÇÃO [OS 200, Orçamentos 200, Aprovações 200, Checklists 200] · FROTA [Abastecimento, Manutenção, Multas, Seguros, Danos 200; Extrato 400] · GESTÃO [Clientes 200, Serviços 200, Estoque 200, Pedidos (sem back), Remunerações 200, Relatórios (sem back), Financeiro 200, Cobranças 200, Pagamentos 200] · ADMINISTRAÇÃO [Notificações 200, Modelos de Checklist 200]; sem Auditoria; Dashboard escondido. = §4.5 do plano.
  - inventory (kind inventory, rótulo "Estoque"): OPERAÇÃO [Checklists 200] · GESTÃO [Estoque 200, Pedidos, Relatórios] · ADMINISTRAÇÃO [Notificações 200, Modelos de Checklist 200]; Dashboard escondido. = §4.5. Visível-negado: 0.
  - visível com guard do front negando e backend 403: manager "Sessões" (`sessions:read`); field_technician "Seguros" e "Estoque" — os 3 da `P-SAN3-04A-MENU-RESIDUAL` (pré-existente, dono B-SAN3-06a). Demais papéis: 0.
  - rota FORA do sidebar com guard do front ABERTO e backend 403: finance e inventory `/dashboard` (`dashboard:view` que `auth.adapter.ts:244,251` injeta em todo usuário; `GET /dashboard/summary` 403); inventory e support `/work-orders` e `/approvals` (apelido `os.read → work_orders:read`, `auth.adapter.ts:375`, desde `9f12ea99` 2026-06-09; `os.read` no inventory/support já em `02bd7dab`). `/finance/commissions` p/ operator/field_technician: artefato do harness (a tela usa `/commissions/statements/my-summary` p/ `read_own` → 200 medido).
- Veredito parcial: cadeia login→claims→banco→menu→guard→backend coerente para os itens que o bloco mexeu; incoerências achadas são de classes anteriores ao bloco (ver achados).

### E3 — tabela §7 (CE-G2), login real, objeto × head-base (20:15-20:25)
- Script `c2h/passos.mts` (roteiro da 1ª instância, reexecutado): login real por papel, chamada de cada passo; 403 = gate negou; 400/404 = gate PASSOU (validação/objeto inexistente depois do gate). Objeto: API :56447/`i2_prov`; head-base: API :56448/`i2_base` (worktree `-base`, 02bd7dab, provisionada). Saídas `c2h/i2-passos-obj.txt`, `c2h/i2-passos-base.txt`, ec=0 nos dois.
- Mudaram base→objeto (e SÓ estes): finance `GET /work-orders` 403→200 (P1/CE-3); finance `GET /customers` e `/service-catalog` 403→200 (P2/CE-5); finance e inventory `GET /tenant/checklists` e `/mobile/checklists/available` 403→200; field_technician `GET /tenant/checklists` 403→200; manager `POST /mobile/checklist-runs/:id/acknowledgement|divergence|markers` 400→403 (item 15: o gate agora nega); operator `POST /work-orders` 403→400 e `/duplicate` 403→404 (A6: gate passa); finance `GET /approvals/pending` 403→200.
- Inalterados (controles): finance `POST /work-orders` 403; inventory `GET /customers|/service-catalog` 403 (a concessão é do papel); tenant_admin passa o gate de acknowledgement (400); manager mantém `complete` (404 = passou); inventory 403 em markers/complete/acknowledgement (A5); finance 403 em `POST /mobile/checklist-runs`; K1: finance 403 em `/branches|/suppliers|/tags|/pois|/operator-profiles|/tariffs|/price-tables`; K2: inventory 403 em `GET|POST /branches`, operator 403 em `POST /branches|/suppliers|/tags|/pois`; X2: inventory e finance 403 em `POST /approvals/:id/approve`; finance 403 em `PATCH /work-orders/:id`, `/status`, `GET /audit-events`, `POST /customers`, `POST /service-catalog`; auditor 200 em `/tags|/pois|/tenant-settings|/audit-events` (base provisionada); finance/manager 200 em `/financial-titles`, finance 200 em `/financial-summary`.
- `grep` das linhas citadas no §7 (na ref): `navigation.routes.ts:31` = `"/menu"` sem `requirePermission` (só 401 sem ator) ✓ · `work-order.routes.ts:30` `read: "work_orders:read"` ✓ · `customer.routes.ts:17,31` ✓ · `service-catalog.routes.ts:17,33` ✓ · `checklist.routes.ts:98` requireAny(readRuns, createRuns) em `/mobile/checklists/available` ✓ · `:202` acknowledgeRuns ✓ · `:194` updateRuns em divergence ✓ · `:157` updateRuns em markers ✓ · `tag.routes.ts:33` ✓ · `poi.routes.ts:33` ✓ · `tenant-setting.routes.ts:17,32` ✓ · `audit.routes.ts:45` `requirePermission("audit.read")` ✓ · `financial-title.routes.ts:33` ✓ · `rbac.middleware.ts` `permissions.some(p => tenantContext.permissions.includes(p))` — comparação exata, sem apelido ✓.
- **Duas citações imprecisas no §7:** `work-order.routes.ts:72` é o gate de `GET /approvals/pending` (path l.71); o de `GET /work-orders` é l.105 (path l.104). `checklist.routes.ts:34` é o gate de `GET /tenant/checklist-components` (l.33); o de `GET /tenant/checklists` é l.42. Em ambas a PERMISSÃO citada é a certa (mesma constante) e o passo, executado, dá o código esperado.
- Veredito parcial: CE-G2 cumprido na permissão e no código de resposta; CE-3/CE-5 provados com permissões do banco e vermelho-controle no head-base; K1/K2/A5/X2 não concedidos, provados por 403 real nos dois heads.

### E4 — T1/T3/T4 verdes + MUTAÇÕES REAIS nos arquivos-fonte (20:25-20:45; R2: critério do item 15 medido pela ref)
- `DATABASE_URL=postgresql://ficticio:***@localhost:1/ficticio node --test --import tsx tests/san3-04a-matriz-x-catalogo-guard.test.ts tests/san3-04a-menu-front-x-catalogo.test.ts tests/san3-04a-seed-semeia-auditor.test.ts` → `# tests 30 · pass 30 · fail 0 · skipped 0`, ec=0 (`c2h/i2-t134-verde.tap`).
- `c2h/i2-mutacoes.mjs` (+`-b`): âncora por substring EXATA com 1 ocorrência, aplica, roda T1+T3+T4, restaura os bytes, confere sha256. 27 mutações = as 7 do mandato (M1-M5, T3a, T3b) + 20 minhas. `git status --porcelain` depois: vazio. Resultado (`c2h/i2-mutacoes.log`, `i2-mutacoes-b.log`):
  - As 7 do mandato: M1 finance sem `customers:read` → 3 vermelhos ([A], [menu×guard] finance, [mutação]); M2 manager + `acknowledge` → [item 15·l.44] + [§4.4]; M3 célula l.38 finance=`foo` → o arquivo T1 lança (17 testes, 1 not ok = arquivo); M4 linha nova na tabela → idem; M5 inventory + `checklist_runs:update` → [B] + [l.44]; T3a Auditoria de volta ao finance → 3 vermelhos; T3b sem ramo Estoque → 3 vermelhos. **7/7 vermelhas, restauradas.**
  - As 4 divergências do item 15 (C2-04), uma a uma: M15a field_technician sem `tenant_checklists:read` → 3 vermelhos; M15b finance sem `checklist_runs:read` → 3; M15c inventory sem `tenant_checklists:read` → 4; M15d manager + `update` → 2 (e M2 = manager + `acknowledge`). **Critério "o teste do item 15 cobre as quatro divergências" CUMPRIDO pela ref.**
  - As 4 disposições fail-closed, regredidas uma a uma: K1 finance + `branches:read` → [§A2] vermelho; K2 operator + `branches:create` → [B] vermelho; A5 inventory + `checklist_runs:complete` → [l.44] vermelho (e M5 = + `update`); X2 inventory + `work_orders:approve` → [B]; X2 finance + `work_orders:approve` → [B]; l.56 finance + `audit:read` → [B] + [mutação]. **Toda concessão "por escopo" sem escopo que eu tentei reintroduzir ficou vermelha.**
  - Outras: M6 operator sem `work_orders:create` → [A]; M7 finance sem `work_orders:read` → 4; M13 `/finance` volta a `finance:read` → [P1/P2/item 13]; M13b `/finance/payments` volta a `payments:read` → idem; M14 seed volta a `STANDARD_ROLES` → [item 14]; F2 finance sem Clientes → [P1/P2]; F1b/F3b Clientes (caminho NÃO governado) no menu do support/inventory → [menu×guard] (+ conjunto exato no inventory).
  - **2 mutantes equivalentes (verdes, não são furo):** F1/F3 = OS (`/work-orders`) no menu do support/inventory → 30/30. `/work-orders` é caminho governado; o menu do backend não o traz para quem não tem `work_orders:read`, e o esconde-fino (`computeHiddenNavPaths`) o oculta — o sidebar visível não muda. Confirmado pela F1b/F3b (item não governado → vermelho).
- Veredito parcial: T1/T3/T4 verdes; 25 de 27 mutações vermelhas, 2 equivalentes explicadas; nenhuma mutação deixou resíduo.

### E5 — MATRIZ EFETIVA (objeto, login real, permissões do banco `i2_prov`) e diff contra `docs/navigation-matrix.md` (20:45-21:05)
Tabela (`c2h/i2-matriz-efetiva-tabela.md`; "visível-negado" = item no sidebar cujo guard do front nega e o backend dá 403; "fora do menu" = rota do `App.tsx` sem item no sidebar, sem `:id`, `/new` e `/platform/*`):
| papel | rótulo→kind | visíveis (N) | visível-negado | fora do menu: URL acessa (front abre + back 200) | fora do menu: front abre + back 403 |
|---|---|---|---|---|---|
| platform_admin(super_admin) | ["Super Admin"]→gestor | 50 | 0 | 4 (/controle/usuarios/logs /field-operators /dispatch/console /administrator/settings) | 0 |
| tenant_admin | ["Administrador"]→admin | 50 | 0 | 4 (/controle/usuarios/logs /field-operators /dispatch/console /administrator/settings) | 0 |
| manager | ["Gestor Operacional"]→gestor | 50 | /controle/usuarios/sessoes | 4 (/controle/usuarios/logs /field-operators /dispatch/console /administrator/settings) | 0 |
| operator | ["Operador Logistico"]→dispatcher | 28 | 0 | 7 (/cadastros/filiais /cadastros/fornecedores /cadastros/tags /cadastros/pontos-interesse /cadastros/profissionais /field-operators /dispatch/console) | /finance/commissions |
| finance | ["Financeiro"]→finance | 21 | 0 | 0 | /dashboard |
| inventory | ["Estoque"]→inventory | 6 | 0 | 0 | /dashboard /approvals /work-orders |
| field_technician | ["Operador Logistico"]→dispatcher | 26 | /fleet/insurance /inventory | 8 (/cadastros/filiais /cadastros/fornecedores /cadastros/tags /cadastros/pontos-interesse /cadastros/profissionais /field-operators /dispatch/console /administrator/checklists) | /finance/commissions |
| auditor | ["Auditor"]→gestor | 49 | 0 | 4 (/controle/usuarios/logs /field-operators /dispatch/console /administrator/settings) | 0 |
| support | ["Supervisor"]→support | 3 | 0 | 5 (/dashboard /controle/usuarios/logs /controle/usuarios/acessos /operations/checklists /administrator/checklists) | /approvals /work-orders |
(`/finance/commissions` de operator/field_technician = artefato: a tela usa `/commissions/statements/my-summary` para `read_own`, medido 200.)
- Base (head 02bd7dab, API :56448/`i2_base`, mesmo harness, `c2h/i2-matriz-efetiva-base.json`): inventory `[]` → gestor, 28 visíveis, **24 visível-negado**; finance 14 visíveis, visível-negado Aprovações e Auditoria, sem OS/Clientes/Serviços/Checklists/Financeiro/Cobranças/Pagamentos; manager/auditor sem `/finance*` no menu do backend.
- Diff doc × efetivo (`c2h/i2-diff-navmatrix.mjs`; doc lido da ref por `git show fbda96b0:docs/navigation-matrix.md`; 42 linhas de tela × 9 papéis, células "M" puladas; acesso sim/não, sem a nuance E/R). Saída `c2h/i2-diff-navmatrix.out`:
  - **NOVAS (7, criadas pelo bloco):** finance × `/operations/checklists` (doc "—", objeto acessa) · inventory × `/operations/checklists` · finance × `/cadastros/clientes` · finance × `/cadastros/servicos` · finance × `/administrator/checklists` · inventory × `/administrator/checklists` · field_technician × `/administrator/checklists` (sem item; por URL o guard abre e `GET /tenant/checklists` = 200). Todas seguem a `RBAC_MATRIX.md` (l.38/41/43/44 e P1/P2 do dono): é o doc que ficou velho.
  - **RESOLVIDAS (29):** finance × `/work-orders`, `/approvals` (leitura), `/finance`, `/finance/charges`, `/finance/payments`; manager e auditor × `/finance*` (6); inventory × 20 itens que o doc diz "—" e a base mostrava negados (Cadastros 11, Frota 5, Remunerações, Usuários e outros).
  - **PERSISTENTES (33, já na base):** finance/inventory × `/dashboard` (front abre por `dashboard:view`, back 403, `P-027`); inventory × `/work-orders` (doc R, back 403) e `/approvals`; support × `/dashboard`, `/work-orders`, `/approvals`, `/operations/checklists`, `/administrator/checklists`; operator/field_technician × 5 cadastros mestres (doc R, sem item, URL 200); field_technician × `/approvals`, `/fleet/fines` (doc "—", acessa), `/fleet/insurance`, `/inventory` (visível-negado); operator × `/purchase-orders`, `/reports` (doc R, oculto); 4 papéis × `/administrator/settings` (sem item); finance/inventory × `/audit` (doc R; base visível-negado, objeto oculto).
  - `grep -c navigation-matrix` na ref: `pendencias.md` 2 (P-027 e uma de 2026-07), `decisoes.md` 2 (antigas), plano 2 (só a evidência B10), `00-dev.md` 0, comando 0; `git diff 02bd7dab fbda96b0 | grep navigation-matrix` = só a evidência B10 e um comentário de código. **Nenhum registro das 7 células novas.** `git log -1 -- docs/navigation-matrix.md` = `aff48fbb` 2026-08-08. O comando do bloco não põe `docs/**` no escopo permitido.
- Provisionamento (item 3 do corpo): `tenants.modules` da organização demo = `{dashboard,work_orders,field_operations,logistics,finance,checklists,tenant_checklist,notifications,users,audit}`; o catálogo de módulos (`platform-modules.service.ts:3-24`) tem `customers`, `inventory`, `purchasing`, `reports`, `approvals`, NÃO provisionados. Itens visíveis cujo módulo (mapeamento por nome, inferência minha) não está provisionado: finance × Clientes (`customers`), Aprovações (`approvals`), Estoque (`inventory`), Pedidos (`purchasing`), Relatórios (`reports`); inventory × Estoque, Pedidos, Relatórios. São caminhos NÃO governados (`MVP_NAV_PATHS` 51 − registro 36 = 27 fora do registro, medido; `/cadastros/clientes`, `/cadastros/servicos`, `/inventory`, `/purchase-orders`, `/reports`, `/approvals` entre eles) e o backend não recusa por módulo em rota nenhuma (`grep requireModule|hasModule src` = só `navigation.service.ts` e `mobile.routes.ts`). Classe = item 16 do `PLANO_SAN3.md` (l.130; `P-WEB-GATE-MODULO-INCOMPLETO` + `P-NAV-MODULOS-NAO-RESOLVIDOS-LIBERA-MENU`), dono `B-SAN3-18` (l.259; os 27 caminhos entram na lista dele). Comando deste bloco: "Fica de fora: o gate de módulo (`B-SAN3-18`)".
- "Feature provisionada sem item" (relatar): finance/manager/auditor passam a ter `/finance/invoices` (status `future`) no menu do backend sem item no sidebar; field_technician passa a ter `/administrator/checklists` no menu do backend sem item (kind `dispatcher` sem Modelos de Checklist); support segue com `/dashboard`, `/administrator/checklists`, `/administrator/audit`, `/operations/checklists` no backend e sidebar só com Usuários/Notificações/Auditoria (já na base).
- Pedidos (`/purchase-orders`) e Relatórios (`/reports`), 2 dos 6 itens do menu novo do Estoque (e já no do Financeiro): telas com dado FIXO no código (`PedidosPage.tsx` `ORDER_ROWS`; `ReportsPage.tsx` `ROWS` e KPIs 24/6/148/1), sem endpoint no backend; "Gerar relatório" sem gate. Já na base (inventory via gestor, finance direto). Pendências `P-PURCHASE-ORDERS-BACKEND-GATE` e `P-RBAC-GATING-MOCKSHELLS` (MÉDIA, "bloqueia o vendável"), dono `B-SAN3-06a`.

### E6 — revogação nomeada (voto nominal, emenda (b)), smoke, efeitos colaterais (21:05-21:20)
- Drill na base antiga `i2_base` (provisionada pelo head-base: manager = {acknowledge, complete, read, reopen, update}) com o `scripts/provision-rbac.ts` do OBJETO: `--dry-run` → "concessões: 9 a criar · revogações deliberadas: 2 a remover — manager → checklist_runs:update (…); manager → checklist_runs:acknowledge (…)" e o banco CONTINUA com as duas; aplicar → "9 criada(s) · 2 removida(s) · CONVERGIDO"; 2ª execução → "0 criada(s) · 908 já existiam · 0 removida(s) · CONVERGIDO". Contagens antes→depois: finance 56→61, inventory 15→17, field_technician 42→43, operator 67→68, manager 141→139; os outros 7 papéis inalterados; manager ∩ `checklist_runs:*` = {complete, read, reopen}.
- Consumo web das duas revogadas: `ChecklistRuntimePage` (único chamador de `acknowledgeRun`) está sob `PermissionGuard ["checklist_runs:create"]` (`App.tsx:711`), que o manager não tem (`D-CHK-DISPATCH-CREATE`): nenhum fluxo web do gestor dependia de `update`/`acknowledge`.
- `npm --prefix frontend run test:smoke`: objeto `# tests 1135 · pass 1135 · fail 0 · skipped 0`, ec=0 (`c2h/i2-smoke.tap`); head-base `1126 · 1126 · 0 · 0`, ec=0 (`c2h/i2-smoke-base.tap`) → Δ +9 = o arquivo T5 (`san3-04a-sidebar-estoque-financeiro.test.tsx`), único acrescido à lista `test:smoke`.
- Termo técnico: T3 [§3] verde; strings de UI do diff = "Estoque" (subtítulo e rótulo), descrições do registro "Visão financeira da organização." / "Cobranças da organização." / "Faturas da organização." / "Pagamentos da organização." (saíram "tenant" e "planejada"). Nenhum termo técnico novo.
- Botões nas telas que o bloco passou a mostrar: Clientes/Serviços escondem Novo/Editar/Desativar sem permissão de escrita (`ClientesPage.tsx:44-45,111,145`; `ServicosPage.tsx:52-53,142,176`); Checklists/Modelos gateiam por `can()` (`ChecklistRunsPage.tsx:22`, `TenantChecklistsPage.tsx:147-152`); Aprovações só mostra decidir com `work_orders:cancel|approve` (`ApprovalsPage.tsx:294`); detalhe da OS gateia cancelar/duplicar e abas (`WorkOrderActionBar.tsx:50-56`, `canAccessTab`). **Exceção: "Nova OS" na lista de OS (`WorkOrdersPage.tsx:233-236`) sem gate**: o Financeiro passa a vê-lo; o clique leva a `/work-orders/new`, cujo guard (`work_orders:create`, `App.tsx:776-780`) NEGA no front, e `POST /work-orders` = 403 (E3). `git blame` na base: `d43314bd` 2026-08-04; na base o auditor (kind gestor, `work_orders:read` sem `create`) já via o botão. `grep -i "Nova OS|/work-orders/new"` em `pendencias.md` da ref: nenhuma pendência sobre o gate do botão.
- Notificação: `notification.recipient-resolver.ts:30` (`0461ee39`, 2026-06-08) faz destinatário de `checklist_run.completed` quem tem `checklist_runs:read` → finance e inventory passam a receber; candidatos em ordem `created_at asc` (`notification-prisma.repository.ts:180-182`) com teto de 20 (`recipient-resolver.ts:21`, `491bf751`, 2026-06-14). Efeito não declarado no §7 do plano nem no `00-dev.md`. Deslocamento de gestores pelo teto numa organização com mais de 20 elegíveis = HIPÓTESE, não medida.
- Claims (item 2 do corpo): JWT = `tenant_id, email, roles, type, identity_id, sub, iat, exp, iss, aud` (sem `tenant_role`, `tenant_roles`, `permissions`, `scope`); o corpo do login traz `roles` e `permissions` (finance 61 = banco). `RBAC_MATRIX.md:70-81` e `CLAUDE.md` §2.4 dizem `tenant_role` obrigatório. `src/modules/auth/**` fora do diff (último commit na base `dc8168b9` 2026-09-04). `grep tenant_role` em `pendencias.md`/`decisoes.md` da ref: 0.

## Achados (detalhe no JSON do voto)
| id | gravidade | escopo | resumo |
|---|---|---|---|
| C2-01 | bloqueia | pre-existente (docs/** fora do escopo; 62 células divergentes já na base; doc sem commit desde aff48fbb 2026-08-08) | `docs/navigation-matrix.md` × matriz efetiva: 40 células no objeto (7 novas do bloco, todas a favor da `RBAC_MATRIX.md`; 33 já na base). Pendência proposta `P-SAN3-04A-NAVIGATION-MATRIX-DEFASADA`, N=40, dono B-SAN3-06a |
| C2-02 | ajuste | dentro-do-bloco | as 7 divergências novas com o doc não foram registradas em `controle/` (§A2) |
| C2-03 | bloqueia | pre-existente (item 16, B-SAN3-18; comando exclui o gate de módulo) | itens visíveis com módulo não provisionado (finance × Clientes, Aprovações, Estoque, Pedidos, Relatórios; inventory × Estoque, Pedidos, Relatórios) |
| C2-04 | ajuste | pre-existente (apelido `os.read` 9f12ea99 2026-06-09; P-027) | rota fora da matriz com front aberto e back 403: inventory/support × `/work-orders`, `/approvals`; finance/inventory × `/dashboard` |
| C2-05 | ajuste | pre-existente (d43314bd 2026-08-04; arquivo fora do escopo) | "Nova OS" sem gate, agora à vista do Financeiro; front e back negam o destino |
| C2-06 | ajuste | pre-existente (auth fora do diff; dc8168b9) | JWT sem `tenant_role`/`tenant_roles`/`permissions`/`scope`; não registrado |
| C2-07 | nota | dentro-do-bloco | finance/inventory viram destinatários de `checklist_run.completed` (não declarado; teto de 20 = hipótese) |
| C2-08 | nota | dentro-do-bloco | 2 citações de linha imprecisas no §7 (permissão certa) |
| C2-09 | nota | pre-existente | Pedidos/Relatórios = telas com dado fixo, sem backend (P-PURCHASE-ORDERS-BACKEND-GATE) |
| C2-10 | nota | pre-existente | mapa estático do front nega os itens novos após troca de organização (pendência do próprio bloco, medida) |
| C2-11 | nota | dentro-do-bloco | feature provisionada sem item: field_technician × `/administrator/checklists`; `/finance/invoices` (future) |

**Veredito: APROVADO** — 0 achado `bloqueia` + `dentro-do-bloco`. Veto de CE-6 não acionado: nenhuma concessão "por escopo" sem escopo aplicado no backend. Voto nominal na revogação do manager (emenda (b)): APROVO.

## Limpeza (21:30)
- `docker rm -f j-bsan304a-c2-pg j-bsan304a-c2-redis` → removidos (0 com esse nome depois).
- As 2 APIs desta instância (:56447, :56448/:56449) encerradas (`taskkill /T` das árvores que criei).
- `git worktree remove --force` de `j-bsan304a-c2` e `j-bsan304a-c2-base`: DESREGISTRADOS (0 em `git worktree list`), mas os DIRETÓRIOS FICARAM ("failed to delete … Invalid argument"): arquivos presos pelas 2 APIs da 1ª instância (PIDs 22856→24488 em `j-bsan304a-c2`, :56445/:3100; 10812→896 em `j-bsan304a-c2-base`, :56446), cujo `taskkill` foi NEGADO pelo classificador de permissão. Não contornei. Pendente para o orquestrador: encerrar essas duas árvores e apagar os dois diretórios (sem junction: `dir /AL` vazio na raiz e em `frontend/`). Sem `git worktree prune`.
- `bsan304a` e árvore principal: só leitura. Scratchpad `c2h/i2-*` mantido como evidência.
