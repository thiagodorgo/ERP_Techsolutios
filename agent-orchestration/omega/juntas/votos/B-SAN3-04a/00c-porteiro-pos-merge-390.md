# PORTEIRO PÓS-MERGE — PR #390 (B-SAN3-04a — catálogo, banco e menu convergem à `RBAC_MATRIX.md`)

- **Papel:** `porteiro-pos-merge` · **Modelo que rodou:** Fable 5.1 (`claude-fable-5-1`), sem fallback · **Corpo aplicado:** `aadaa6d51be950e152ca6a6f15327bc9989039de:.claude/agents/porteiro-pos-merge.md` (lido por `git show`; difere do corpo da sessão só pela nota `D-FALLBACK-MODELO-FABLE-OPUS`).
- **Merge auditado:** `aadaa6d51be950e152ca6a6f15327bc9989039de` (squash) · head aprovado e mergeado `a62d04e2bbe42533e58639643a19104bdccc0ab6` · objeto julgado pela junta `fbda96b0` (pré-rebase).
- **Data:** 2026-09-20 (13:22–14:10, fuso local). Parecer gravado incrementalmente; o parcial anterior está em `PORTEIRO-390.parcial-anterior.md`.

## 0. Isolamento
- Worktree detached PRÓPRIO `C:/Users/AMP/Documents/GitHub/ERP_Techsolutios/.claude/worktrees/porteiro-390` em `aadaa6d5` (`git rev-parse HEAD` confere), `npm ci` (326 pacotes, ec=0) e `npm --prefix frontend ci` (103, ec=0) próprios, sem junction.
- Incidente meu, desfeito: com `MSYS_NO_PATHCONV=1` exportado, `git worktree add /c/Users/...` criou o worktree em `C:/c/Users/...` (a armadilha R5 do inspetor desta junta). Removido pelo nome (`git worktree remove --force`) + `rmdir` da cadeia vazia (`C:/c` ausente ao fim); recriado com `C:/Users/...`.
- Postgres `porteiro-390-pg` (postgres:16, 127.0.0.1:56890) e Redis `porteiro-390-redis` (redis:7, 127.0.0.1:56891) descartáveis MEUS; bancos novos `erp_porteiro_390` e `erp_porteiro_390_seed` (`prisma migrate deploy` → 107 migrations cada). `DATABASE_URL`/`REDIS_URL` exportadas só para eles; `CORE_SAAS_PERSISTENCE` não exportada. A base viva não recebeu comando (`erp-postgres` Exited há 9 dias; `erp-redis` e `erp-postgres-alt` Exited).
- Resíduo alheio visto e NÃO tocado: worktrees `b04a` (738ff531), `b11` (43557a17), `gov-descuido`, `gov-elenco`, `j2-b11-c2`, `j2-b11-c3`; diretório `.claude/worktrees/san2-r` fora do `git worktree list`; contêineres `pastrack-teste-banco-teste-1` (Up), `erp-postgres`/`erp-redis`/`erp-postgres-alt` (Exited).

## 1. O merge existe e está íntegro — CONFERE
- `git fetch origin main` + `git log origin/main -3`: `aadaa6d5 fix(rbac): catalogo, banco e menu convergem a matriz de papeis (B-SAN3-04a)` ← `83a3c68c` (#387) ← `02bd7dab` (#386).
- `gh pr view 390`: `state MERGED` · `mergeCommit.oid aadaa6d51be950e152ca6a6f15327bc9989039de` · `headRefOid a62d04e2bbe42533e58639643a19104bdccc0ab6` · `mergedAt 2026-09-20T16:08:21Z` · `mergedBy thiagodorgo` · `fix/rbac-catalogo-banco-matriz` → `main`.
- CI do head `a62d04e2`: `gh run list --commit …` → `ci: completed/success`.

## 2. A promessa × o entregue — CONFERE (1 nota)
- `git show --stat HEAD`: **57 arquivos, +8756/−495**. Código: `src/modules/core-saas/permissions/catalog.ts`, `src/modules/navigation/navigation.registry.ts`, `scripts/provision-rbac.ts`, `prisma/seed.ts`, `RBAC_MATRIX.md` (2 hunks: l.38 e l.41, `finance none→read`), `frontend/src/layouts/appSidebarNav.ts`, `auth.adapter.ts` (+1), `types.ts` (+3), 4 testes backend novos + 1 do front, 6 existentes ajustados, snapshot regenerado, `frontend/package.json` (`test:smoke` +1). Registro: comando, plano + apoio, ata, 12 arquivos de votos, obituário, aposentadoria, quedas do ciclo 2 do `B-SAN3-01`, `J-B-SAN3-01.md` (+4), 4 corpos `jurado-san3-01c2-*` nos dois espelhos, KPI (4), `docs/deployment.md`, `.github/workflows/deploy-production.yml`. Todo arquivo do diff está nomeado no corpo do PR ou na emenda 3 (cc) do comando — **nenhum escopo cresceu em silêncio**. `git diff --check 83a3c68c aadaa6d5` ec=0.
- Promessas do corpo, conferidas no fonte do merge: manager sem `checklist_runs:update`/`:acknowledge` (`catalog.ts:567-577`) e `DELIBERATE_REVOCATIONS` com exatamente as 2 do `manager` (`satisfies` tipado); finance +5, inventory +2, field_technician +1, operator +`work_orders:create` (diff `83a3c68c..aadaa6d5`); `prisma/seed.ts:256` `SEEDED_SYSTEM_ROLES = [...STANDARD_ROLES, "auditor"]`; `provision-rbac.ts:63` `APPLY_MODE = !process.argv.includes("--dry-run")` e passo 3-bis; `navigation.registry.ts` governa `/finance*` por `financial_entries:read`/`financial_titles:read`; "Estoque" com `RoleKind` e menu próprios (`appSidebarNav.ts:57,91,203,224,290`).
- `.github/workflows/deploy-production.yml` `83a3c68c..a62d04e2`: só o comentário (l.146-156) e o `name:` do passo; o `run:` (`npx prisma generate` / `npm run db:provision-rbac`) **inalterado** — nenhuma mudança de comportamento do workflow. `docs/deployment.md`: só texto (l.49 e a seção do provisionamento).
- **Nota N1 (não grave):** o corpo diz "5 existentes ajustados no sujeito do controle negativo"; são **6** (5 backend + `frontend/tests/sidebar-nav.test.tsx`) e em 2 (`tests/navigation-menu.test.ts`, `frontend/tests/sidebar-nav.test.tsx`) muda o valor esperado, não o sujeito — é o C3-N1 já na ata. `assert.` antes→depois: 67→67, 16→17, 31→31, 51→51, 56→56, 29→30 — **nenhuma asserção removida**.

## 3b. A prova que sustenta o merge (rebase byte-idêntico) — CONFERE
- `git diff 02bd7dab fbda96b0 -- src frontend/src prisma scripts tests RBAC_MATRIX.md` × `git diff 83a3c68c a62d04e2 -- <mesmos>`: **106 621 bytes cada, sha256 `0a051562b5f7582e` nos dois, `diff` exit 0.**
- Incluindo `frontend/tests` (que não está sob `frontend/src`): **2022 linhas cada, `cmp` idêntico.** Direção inversa (`fbda96b0..a62d04e2` × `02bd7dab..83a3c68c`): **3653 linhas cada, `cmp` idêntico** — o que entrou a mais é exatamente o #387. (A ata cita 2002/3633: mesma prova; meu conjunto de caminhos inclui `frontend/tests`.)
- Fora dos caminhos de código, o pós-rebase acrescenta só o texto do CD e do `docs/deployment.md` (C1-A1), os 4 corpos `jurado-san3-01c2-*` (A1) e registro; `frontend/package.json` resolvido por união (142 arquivos no `test:smoke`).

## 3. Os números são reais — REPRODUZEM
- **Backend:** `npm test` (= `node scripts/run-backend-tests.mjs`), `DATABASE_URL=…127.0.0.1:56890/erp_porteiro_390`, `REDIS_URL=redis://127.0.0.1:56891`, `CORE_SAAS_PERSISTENCE` não exportada (o runner assume memory), node v20.19.5 → **287 arquivos · # tests 3054 · pass 3052 · fail 0 · cancelled 0 · skipped 2** (os 2 de `permission-catalog-db-parity`, gated por `RBAC_DB_PARITY`), 460,8 s, `BACKEND_EXIT=0`; `not ok` = 0; resíduo `pg_roles like 'o6r_b01_%'` = 0. **= 3052/3054 declarado.**
- **Smoke do frontend:** `npm --prefix frontend run test:smoke` → **# tests 1202 · pass 1202 · fail 0 · skipped 0**, 102,3 s, `SMOKE_EXIT=0`. **= 1202/1202 declarado.**
- **Blocos:** 165 = 164 (`main` após #387) + 1 — `B-SAN3-01` e `B-SAN3-04a` são blocos distintos; nota coerente em `kpis-latest.json` e `kpis-history.md`.
- **Guards:** 3 guards de KPI (`kpi-achados-paridade`, `kpi-dashboard-charts`, `kpi-dashboard-contraste`) → **29/29, fail 0** · `node scripts/kpi-freeze.mjs --check` → "em dia (snapshot 2026-09-20)" · `node --check Kpis/app.js` OK · `node scripts/sync-agent-agents.mjs --check` → "OK — 25 agentes, espelho consistente".
- **Índice de pendências:** gerador `agent-orchestration/controle/gerar-indice-pendencias.py` (Python 3.13) sobre o blob `aadaa6d5:pendencias.md` numa cópia → `cmp` com o blob `aadaa6d5:pendencias-indice.md` = **BYTE-IDÊNTICO** (md5 `4767f89cbc03`; 410 cabeçalhos / 399 IDs; 110 FECHADA / 300 ABERTA). O checkout está em CRLF (autocrlf) e `git diff` sai vazio.
- **Drills do produto, na minha base:**
  - D4 (base nova, `migrate deploy` + `npm run db:seed` só, forma da CI): papéis globais `auditor=56 · field_dispatcher=42 · manager=139 · super_admin=198 · technician=44 · tenant_admin=185 · viewer=45` — o `auditor` nasce do seed com as 56 do catálogo; o `manager` já sem as 2.
  - D3 (base só migrada → `db:provision-rbac` = 12 papéis, 908 concessões, 0 revogações; depois **injetei** por SQL as 2 concessões do `manager` global, simulando a base provisionada pelo head-base → 910, manager = {acknowledge, complete, read, reopen, update}): `--dry-run` → "0 a criar · 910 já existiam · **revogações deliberadas: 2 a remover** — manager → checklist_runs:update (…); manager → checklist_runs:acknowledge (…) · simulação encerrada — nada foi escrito" e o banco **continua 910** com as 2; aplicar → "**2 removida(s)** · CONVERGIDO" → 908, manager = {complete, read, reopen}, os outros 11 papéis intactos (`auditor=56 field_dispatcher=42 field_technician=43 finance=61 inventory=17 manager=139 operator=68 super_admin=198 support=10 technician=44 tenant_admin=185 viewer=45`); 2ª aplicação → "0 criada(s) · 908 já existiam · 0 removida(s)". **Revoga só as nomeadas, idempotente, `--dry-run` só relata.**
  - Paridade `RBAC_DB_PARITY=1 tests/permission-catalog-db-parity.test.ts` na base convergida → **2/2, 0 skip**. T2 `tests/san3-04a-menu-com-permissoes-do-banco-db.test.ts` no **braço 1** (base provisionada) → **26/26**; resíduo `o6r_b01_%` = 0.
- **Mutações dos votos reproduzidas (4)** — scripts `porteiro-390-mutacoes*.mjs` no scratchpad (âncora com 1 ocorrência, aplica, roda T1+T3+T4 sem banco, restaura bytes, confere sha256; `git status --porcelain` vazio ao fim). Baseline 30/30:
  - **M2** manager volta a ter `checklist_runs:acknowledge` (`catalog.ts`) → **30 · 28 · fail 2**: `[item 15 · l.44] execuções: manager = {read, complete, reopen}…` e `[§4.4] DELIBERATE_REVOCATIONS: lista nomeada, cada item ausente do papel…` — o mesmo par do voto da C2.
  - **M1** finance sem `customers:read` → **30 · 27 · fail 3**: `[A] nenhuma célula INCONDICIONAL da matriz fica sem a permissão…`, `[menu × guard] finance…`, `[mutação] devolver AUDITORIA ao finance…` — os 3 da C2.
  - **M14** `prisma/seed.ts` volta a `[...STANDARD_ROLES]` → fail 1: `[item 14] prisma/seed.ts declara SEEDED_SYSTEM_ROLES = STANDARD_ROLES + auditor`.
  - **T3b** sem o ramo `Estoque` em `roleKindFor` (`appSidebarNav.ts:290`) → T1+T3+T4 fail 3 (`[menu × guard] inventory…`, `[item 38] inventory tem rótulo Estoque…`, `[item 38] roleKindFor…`) + T5 do front fail 4.
  - Restauração: sha256 igual nas 4.

## 4. KPI §C3.5
- `Kpis/kpis-latest.json` `release`: `pr 390` · `merge_commit null` · `approved_head null` · `published_per_pr` → **dívida do PR seguinte**, com os valores: `merge_commit aadaa6d51be950e152ca6a6f15327bc9989039de` · `approved_head a62d04e2bbe42533e58639643a19104bdccc0ab6`.
- `kpis-history.json` (161 entradas): #160 `B-SAN3-04a` `pr 390` null/null (autoria, por contrato); **#158 e #159 (`pr 387`) = `merge_commit 83a3c68ce50129d96d0357b3e7ab6ff725b9659d` · `approved_head 8adaaa31f3709e2a01ad81b8154aba0243fa7a66`** — **A4 do #387 PAGO**.
- `Kpis/kpis-history.md:2708` — entrada `2026-09-20 — B-SAN3-04a (PR #390)` apensada no pré-merge com a lacuna da autoria declarada dentro dela (emenda 4 (gg)).

## 5. Registro da junta (§C7.1) — CONFERE
- Ata `agent-orchestration/omega/juntas/J-B-SAN3-04a.md`: ciclo 1, **APROVADO 3 × 0**, quórum de unanimidade de 3 (bloco de permissão, §C7.1-ter(b)), inspetor `LIBERADO COM RESSALVA` (R1–R6; parecer versionado em `votos/B-SAN3-04a/00b-inspetor-terreno.md`, Fable 5.1, corpo da `origin/main`), seção pré-merge de 2026-09-20.
- Votos (JSON lidos por node): C1 `agente-dba-guardiao` APROVADO (3 achados, 0 bloqueia) · C2 `coordenador-de-acessos` APROVADO (11 achados; **2 `bloqueia`, ambos `pre-existente` com evidência de data/origem; 0 `dentro-do-bloco`**) · C3 `agente-ci-doutor` APROVADO (3 notas). Evidências com comandos e números nos 3 `*-evidencia.md`; parciais da 1ª instância da C2 preservados. Quedas (P6) em `votos/B-SAN3-04a/00-quedas.md`. Papéis §C7.4-bis na ata (planejador `planejador-mestre`; dev `general-purpose` ×3; orquestrador nas emendas; nenhum votou).
- 4 emendas no comando `agent-orchestration/codex/comandos/B-SAN3-04a-rbac-catalogo-banco-matriz.md` (l.121, 142, 172, 200). **Emenda 4 × o que o PR fez:** (dd) `OBITUARIO-IDENTIDADES.md:261` é `### 3.7`, contígua à 3.6, com a divergência §A2 escrita e o ciclo 5 do `B-O6R-02` remarcado para §3.8 — coerente; (ee) `pendencias.md:9492` dono `B-SAN3-01b`, com a correção do dono e a divergência (06c) declarada — coerente com `D-SAN3-01-MERGE-COM-BLOCO-DE-GUARDA` (`decisoes.md:2365`); (ff) C2-05 é **emenda** em `P-SAN3-01-NOVA-OS-SEM-GATE-NO-BOTAO` (`pendencias.md:9506`), com o nome/dono que a C2 propôs escritos para reversão — coerente; (gg) entrada no `kpis-history.md` — feita; (kk) 25 = 23 + 2 corpos — o `--check` confirma.
- C2-02 (§A2): `decisoes.md` ganhou `## §A2 — RBAC_MATRIX.md × docs/navigation-matrix.md: as 7 divergências que o B-SAN3-04a criou`, com tabela célula a célula.

## 5b. Dívidas do #387 pagas por este PR — A4 ✓ · A1 ✓ · A7 ✓ · A2 ✓ (para as que o #387 nomeou)
- **A1:** `git ls-tree HEAD` mostra `jurado-san3-01c2-fail-closed-web.md` e a suplente em `.claude/agents/especialistas/` **e** `.agents/agents/especialistas/`; `sync-agent-agents --check` verde (25); sepultadas no obituário §3.7 (l.277-278). A **aposentadoria** (remoção do diretório vivo) está nomeada como dívida do próximo PR em `controle/aposentadoria-especialistas.md:118-133` ("Rodada 4 — ANUNCIADA aqui, EXECUTADA no PR seguinte"; "PR que deve aposentar: o primeiro a mergear depois deste") e no obituário. Os dois arquivos ainda existem no diretório vivo da árvore principal, como esperado até a rodada 4.
- **A7:** `votos/B-SAN3-01-c2/00-quedas.md` novo (evidência por arquivo; divergência de horário declarada em vez de harmonizada) + 3 linhas em `J-B-SAN3-01.md`.
- **A2:** `P-SAN3-01-DESPACHO-FORMS-SEM-CATCH` → `B-SAN3-06c` (com ampliação nominal declarada) e `P-SAN3-01-NOVA-OS-SEM-GATE-NO-BOTAO` → `B-SAN3-01b`, ambas com "Correção do dono (2026-09-20…)". Ver §6 para o que a mesma régua acha nas pendências que ESTE bloco abriu.

## 6. Pendências
- **Fechadas (4 + bullet):** `P-Ω4-FINANCE-READ-ORFA` (l.957/963), `P-RBAC-CATALOG-MATRIZ` (l.1476/1484, duplicata declarada — conferida pela C3 como a emenda (m) manda), `P-RBAC-CHECKLIST-DRIFT` (l.1720/1730), `P-RBAC-MATRIZ-X-CATALOGO-QUATRO-CELULAS` (l.9301/9303); bullet `P-033` dentro da `P-032` (l.411), que segue aberta.
- **Amostra no código — `P-RBAC-CHECKLIST-DRIFT`:** `catalog.ts` manager sem `update`/`acknowledge` (l.567-577); finance/inventory com `tenant_checklists:read` + `checklist_runs:read`; field_technician com `tenant_checklists:read`; `DELIBERATE_REVOCATIONS` com as 2; o guard T1 `[item 15 · l.44]` fica vermelho quando devolvo o `acknowledge` (M2) e o banco perde as 2 no provisionamento (D3). **"RESOLVIDA" é verdade.**
- **Abertas por este bloco (13 `P-SAN3-04A-*` + `P-AUTH-CLAIMS-SEM-TENANT-ROLE`), dono × §5 do `docs/revisoes/SAN3/PLANO_SAN3.md`:** `-DASHBOARD-SCOPED` e `-PERMISSOES-ORFAS` → `B-SAN3-04b` (l.255: `src/modules/dashboard/**` + `catalog.ts`) ✓ · `-CHECKLIST-POR-ESCOPO-ESCRITORIO` → `B-SAN3-22` (l.257: `src/modules/checklists/**`) ✓ · `-CHECKLIST-ESCOPO-ESTOQUE` → `B-O6R-07c` (l.254: `checklist.service.ts`) ✓ · `-MENU-RESIDUAL` → `B-SAN3-06a` (l.270: `appSidebarNav.ts`) ✓ · `-NAVIGATION-MATRIX-DEFASADA` → `B-SAN3-06a` **com ampliação nominal declarada** para `docs/navigation-matrix.md` ✓ · `-MATRIZ-L37-X-BULLETS` e `-TARIFAS-X-L41-FINANCE` → decisão do dono · `-MASTER-DATA-EDIT-SCOPED`, `-SUPPORT-SEM-POLITICA`, `-AUDIT-SCOPED`, `P-AUTH-CLAIMS-SEM-TENANT-ROLE` → fila pós-gate com regra de nomeação escrita · `-MATRIZ-BULLETS-101-103` → "o próximo bloco que tocar a `RBAC_MATRIX.md`".
- **Achado R1 (registro, classe A2 — não grave):** `P-SAN3-04A-FRONT-PERMISSOES-POR-PAPEL-DEFASADAS` (`pendencias.md:9707`) → `B-SAN3-06a`, mas o arquivo é `frontend/src/modules/auth/auth.adapter.ts:260-356`, que **não está no §5 l.270 do `06a`** (`modules/{purchase-orders,reports}/**`, `DispatchConsolePage.tsx`, `appSidebarNav.ts`, `tenantNavigation.ts`, `App.tsx`), e a entrada **não declara ampliação nominal** (a `-NAVIGATION-MATRIX-DEFASADA`, ao lado, declara). Mesma classe: `P-SAN3-04A-SEED-PAPEIS-LEGADOS` (l.9657) → `B-SAN3-07`, cujo `prisma/seed.ts` é "**autorizado só para o nome**" (l.271) — semear 5 papéis excede isso e a entrada diz só "mesmo arquivo".
- **Achado R2 (registro — não grave):** `B-SAN3-01b` (dono de `P-SAN3-01-NOVA-OS-SEM-GATE-NO-BOTAO` e das `P-SAN3-01B-*`) existe por `D-SAN3-01-MERGE-COM-BLOCO-DE-GUARDA` (`decisoes.md:2365`, decisão do dono, §A1.1) mas **não tem linha no §5 do `PLANO_SAN3.md`** (`grep SAN3-01b` no plano = 0). A decisão prevalece sobre o plano; a linha com os arquivos ainda precisa entrar.
- `agent-orchestration/docs/status-geral.md` (entradas de 2026-09-18 e 2026-09-20) e `agent-orchestration/codex/log-execucao.md` (2 entradas) coerentes com ata, KPI e pendências.

## 7. Limpeza §C5 — CONFERE
- Branch remota `fix/rbac-catalogo-banco-matriz`: ausente em `git branch -r`; branch local: ausente; `git branch --merged main` (fora `main`): vazio. O `gh` já a lia como `MERGED` (`mergedAt 16:08:21Z`) quando li; a branch foi apagada depois disso.
- Worktrees do bloco (`bsan304a`, `j-bsan304a-*`, `insp-bsan304a`, `dev-bsan304a-*`): 0 no `git worktree list` e 0 diretórios em `.claude/worktrees/`. Contêineres do bloco (`dev-bsan304a-pm-*`, `j-bsan304a-*`, `bsan304a-*`): 0 em `docker ps -a`.
- Arquivo rastreado apagado: `git status --porcelain | grep '^ D'` = 0 na árvore principal (`demo/investidor`) e no meu worktree. Base viva: o bloco só usou clusters descartáveis; `erp-postgres` está Exited há 9 dias e não podia receber comando.
- Disco: `df -h /c` → **14 GB livres (95% usado)** — acima do piso de ~10 GB, mas perto; `DEEP_CLEAN=1` fica como aviso.
- Resíduo alheio (reporto, não varro): `.claude/worktrees/san2-r` não registrado; `gov-descuido`/`gov-elenco` de outras sessões; `erp-postgres-alt` Exited em 55432.
- **Minha limpeza, pelo nome:** `docker rm -f porteiro-390-pg porteiro-390-redis` → 0 restantes; `git worktree remove --force …/porteiro-390` ec=0 → diretório ausente, `.git/worktrees/` sem a entrada; `git worktree list` final = os 7 alheios; `docker ps -a` final = só o resíduo alheio; no scratchpad ficaram só os logs/scripts de evidência (`porteiro-390-*.log`, `porteiro-390-mutacoes*.mjs`, `pr-390-body.md`, `porteiro-390-pecas/`).

## 8. O próximo bloco pode começar?
- `#389` `B-O6R-04a` `fix/inventory-consistency@738ff531` OPEN · `#388` `B-O6R-11` `fix/mobile-work-order-contracts@43557a17` OPEN — merge-base dos dois com a `main` = `02bd7dab`.
- Interseção dos arquivos que `83a3c68c..aadaa6d5` tocou com os de cada PR: **só** `Kpis/app.js`, `Kpis/kpis-history.json`, `Kpis/kpis-history.md`, `Kpis/kpis-latest.json`, `agent-orchestration/codex/log-execucao.md`, `agent-orchestration/controle/pendencias-indice.md`, `agent-orchestration/controle/pendencias.md`, `agent-orchestration/docs/status-geral.md` — nos dois. O rebase deles tende a ter delta **só de KPI e registro**; nenhum arquivo de `src/`, `tests/`, `prisma/`, `frontend/`, `mobile/` colide. Atenção: `pendencias-indice.md` se regenera pelo gerador (nunca à mão) e o `FROZEN` do `app.js` pelo `kpi-freeze.mjs` — o guard pegou exatamente isso no pré-merge do #390.
- Pendências que BLOQUEIAM #388/#389: em `pendencias.md`, as únicas que nomeiam `B-O6R-04a`/`B-O6R-11` são as que **esses blocos fecham** (`P-O6R-B11`, Ω6R-DAT-002/003 — l.256/2901/2902/3347); nenhuma pendência aberta os nomeia como PR-alvo bloqueado, e nenhuma das 14 abertas por este PR tem dono `B-O6R-04a`/`B-O6R-11`. **Nada bloqueia o merge de nenhum dos dois** pelo lado das pendências; quem decide é a junta do ciclo 2 (o último, `D-TETO-DOIS-CICLOS`).

## Executado (comandos e resultados)
1. `git fetch origin main`; `git log origin/main -3`; `gh pr view 390 --json state,mergeCommit,headRefOid,mergedAt,…`; `gh run list --commit a62d04e2…` → MERGED / `aadaa6d5` / `a62d04e2` / CI success.
2. `git worktree add --detach C:/…/porteiro-390 aadaa6d5` (após desfazer a criação errada em `C:/c/…`); `npm ci` ec=0; `npm --prefix frontend ci` ec=0.
3. `docker run` postgres:16 `porteiro-390-pg` :56890 e redis:7 `porteiro-390-redis` :56891; `CREATE DATABASE erp_porteiro_390` (+ `_seed`); `npx prisma generate`; `npx prisma migrate deploy` → 107.
4. `git show --stat aadaa6d5` (57 arquivos); `gh pr view 390 --json body`; diffs por arquivo de `deploy-production.yml`, `docs/deployment.md`, `catalog.ts`, 6 testes existentes; contagem de `assert.` antes/depois; `git diff --check 83a3c68c aadaa6d5` ec=0.
5. Prova do rebase: `git diff 02bd7dab fbda96b0 -- <caminhos>` × `git diff 83a3c68c a62d04e2 -- <caminhos>` → sha256 igual / `cmp` idêntico (2022 linhas); inversa 3653 linhas idêntica.
6. `npm test` (Postgres/Redis meus) → 3054 / pass 3052 / fail 0 / skipped 2, ec=0. `npm --prefix frontend run test:smoke` → 1202/1202, ec=0.
7. `node --test --import tsx tests/kpi-*.test.ts` (3 guards) → 29/29; `node scripts/kpi-freeze.mjs --check` em dia; `node --check Kpis/app.js` OK; `node scripts/sync-agent-agents.mjs --check` OK 25.
8. Índice: blobs `aadaa6d5:pendencias.md` + `gerar-indice-pendencias.py` numa cópia → `python gen.py` → `cmp` com `aadaa6d5:pendencias-indice.md` idêntico.
9. KPI: node sobre `kpis-history.json` (entradas 158/159/160) e `kpis-latest.json` (`release`, `recent`); `grep` em `kpis-history.md`.
10. Rito: leitura de `J-B-SAN3-04a.md`, `00-quedas.md`, `00b-inspetor-terreno.md`, 3 evidências, 3 JSON de voto (node), comando com 4 emendas, `OBITUARIO-IDENTIDADES.md` §3.7, `aposentadoria-especialistas.md` rodada 4, `J-B-SAN3-01.md` (+4), `votos/B-SAN3-01-c2/00-quedas.md`, `decisoes.md` (§A2 e `D-SAN3-01-MERGE-COM-BLOCO-DE-GUARDA`), `status-geral.md`, `log-execucao.md`.
11. Pendências: `grep`/`awk` em `pendencias.md` (13 abertas, 4 fechadas + P-033, donos), `PLANO_SAN3.md` §5 (l.254, 255, 257, 259, 270, 271; `SAN3-01b` = 0 ocorrências).
12. Drills: `npm run db:provision-rbac` (base só migrada); `INSERT` das 2 concessões do `manager`; `npm run db:provision-rbac -- --dry-run`; aplicar ×2; `psql` das contagens; `RBAC_DB_PARITY=1` paridade 2/2; T2 braço 1 26/26; `npm run db:seed` em base nova → auditor 56.
13. Mutações M2, M1, M14, T3b via script (aplica/roda/restaura/sha256) → todas vermelhas e restauradas; `git status --porcelain` vazio.
14. Limpeza: `git branch -r|--list|--merged`, `git worktree list`, `ls .claude/worktrees`, `docker ps -a`, `git status --porcelain | grep '^ D'` (árvore principal e worktree), `df -h /c`; remoção dos meus recursos pelo nome.
15. Próximo alvo: `gh pr view 388/389`; `git merge-base`; `comm` das listas de arquivos; `grep` de bloqueios em `pendencias.md`.
- **Não executado:** suíte Flutter (o PR não toca `mobile/` — `flutter_tests` 864/864 CARREGADO com nota, conferido no JSON); `bash scripts/rbac-provision-drill.sh` (o C1 o rodou; meu D3 cobre o mesmo contrato por outro caminho).

## Achados
- **N1 (nota):** corpo do PR diz "5 existentes ajustados no sujeito do controle negativo" — são 6 e em 2 muda o valor esperado (C3-N1 na ata cobre). Sem efeito no veredito.
- **R1 (ressalva, registro — classe A2):** `agent-orchestration/controle/pendencias.md:9707` `P-SAN3-04A-FRONT-PERMISSOES-POR-PAPEL-DEFASADAS` → dono `B-SAN3-06a` sem `frontend/src/modules/auth/auth.adapter.ts` no §5 (`docs/revisoes/SAN3/PLANO_SAN3.md:270`) e sem ampliação nominal; `pendencias.md:9657` `P-SAN3-04A-SEED-PAPEIS-LEGADOS` → `B-SAN3-07`, cujo `prisma/seed.ts` é "autorizado só para o nome" (`PLANO_SAN3.md:271`).
- **R2 (ressalva, registro):** `B-SAN3-01b` sem linha no §5 do `PLANO_SAN3.md` (existe só por `decisoes.md:2365`).
- **R3 (dívida contratual, §C3.5):** `Kpis/kpis-latest.json:10-11` e `kpis-history.json` #160 — `merge_commit`/`approved_head` `null` do #390; valores: `aadaa6d51be950e152ca6a6f15327bc9989039de` / `a62d04e2bbe42533e58639643a19104bdccc0ab6`.
- **R4 (dívida anunciada):** aposentadoria rodada 4 — `git rm` de `.claude/agents/especialistas/jurado-san3-01c2-{fail-closed-web,suplente-fail-closed-web}.md` e do espelho `.agents/agents/especialistas/`, `--check` verde, rodada 4 executada em `controle/aposentadoria-especialistas.md` (remoção por identificador de BLOCO, `jurado-san3-01c2-`).
- **R5 (aviso):** disco 14 GB livres; rodar `DEEP_CLEAN=1 bash scripts/post-merge-cleanup.sh` se cair de ~10 GB.
- Nada GRAVE: números reproduzem, código julgado é o mergeado, ata/votos/quedas/papéis/emendas coerentes, dívidas do #387 pagas, limpeza feita, nada bloqueia #388/#389.

## Veredito
LIBERADO COM RESSALVA: juntas de ciclo 2 (último) de B-O6R-04a (#389, objeto 738ff531) e B-O6R-11 (#388, objeto 43557a17) — rebase sobre aadaa6d5 com delta só de KPI e registro | o primeiro PR a mergear paga: (1) backfill §C3.5 do #390 em latest+history (merge_commit aadaa6d51be950e152ca6a6f15327bc9989039de · approved_head a62d04e2bbe42533e58639643a19104bdccc0ab6); (2) aposentadoria rodada 4 (git rm de jurado-san3-01c2-* nos DOIS espelhos, sync-agent-agents --check verde, rodada 4 executada em aposentadoria-especialistas.md); (3) dono real ou ampliação nominal declarada em P-SAN3-04A-FRONT-PERMISSOES-POR-PAPEL-DEFASADAS (auth.adapter.ts ∉ §5 do B-SAN3-06a) e P-SAN3-04A-SEED-PAPEIS-LEGADOS (seed.ts do B-SAN3-07 é "só para o nome"); (4) linha do B-SAN3-01b no §5 do PLANO_SAN3.md; (5) versionar este parecer
