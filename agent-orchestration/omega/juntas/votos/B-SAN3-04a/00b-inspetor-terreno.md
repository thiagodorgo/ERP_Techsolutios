# Parecer do inspetor de terreno — junta do B-SAN3-04a

- **Papel:** `inspetor-de-terreno-da-junta` · **Modelo que rodou:** Fable 5.1 (`claude-fable-5-1`, sem fallback) · **Corpo aplicado:** `origin/main:.claude/agents/inspetor-de-terreno-da-junta.md` (lido por `git show`, como o briefing manda; contém 3.1-bis e 3.3, que o corpo carregado da sessão não tem — hash EOL-neutro sessão `934a8b08` diferente de ref/main `33dc3256`).
- **Objeto:** `fbda96b0` (`fix/rbac-catalogo-banco-matriz`, PR #390) · base `origin/main@02bd7dab` · ciclo **1** do bloco (não existe `J-B-SAN3-04a*` nem `R-B-SAN3-04a*` na ref nem no scratchpad).
- **Data:** 2026-09-18 15:40-15:58 · **Veredito: `LIBERADO COM RESSALVA`** (R1-R6 abaixo; R1 é correção obrigatória do texto do briefing antes de disparar as cadeiras).

## 1. Isolamento

**1.1 Head e árvore — VERDE.** `git rev-parse --short HEAD` em `.claude/worktrees/bsan304a` = `fbda96b0`; `git status --porcelain` vazio (ec=0). `gh pr view 390 --json headRefOid,state,baseRefName,isDraft` = `fbda96b016ac...` (= objeto), `OPEN`, base `main`, `isDraft: true` (`mergeStateStatus: BLOCKED` por ser draft — não é terreno). `git ls-remote origin`: `refs/heads/fix/rbac-catalogo-banco-matriz` = `fbda96b0`, `refs/heads/main` = `02bd7dab`; `git merge-base HEAD origin/main` = `02bd7dab` (9 commits em `02bd7dab..fbda96b0`). Worktree do desenvolvedor = o próprio `bsan304a` (`00-dev.md`, cabeçalho): md5 EOL-neutro de `catalog.ts`, `provision-rbac.ts`, `prisma/seed.ts`, `navigation.registry.ts`, `appSidebarNav.ts`, `RBAC_MATRIX.md`, `role-catalog-contract.snapshot.json`, `kpis-latest.json` — **8/8 IGUAL** ao blob do head. `git diff --diff-filter=D --name-only 02bd7dab fbda96b0` vazio (nenhum `git rm`, como a emenda 2 (g) exige). 34 arquivos no diff; nenhum em `prisma/migrations`, `infra/`, lockfiles. `.env` ausente no `bsan304a`. `cmd /c dir /AL` em `bsan304a`, `bsan304a/frontend` e no meu worktree: nenhuma junction/symlink (§C7.1-ter(c)).

**1.2 Plano de isolamento — VERDE.** Briefing §0 declara por escrito: worktree detached próprio por cadeira (`j-bsan304a-<cadeira>`, `npm ci` próprio, `npm --prefix frontend ci` se rodar o front), Postgres `j-bsan304a-<cadeira>-pg` e Redis `j-bsan304a-<cadeira>-redis` descartáveis com `REDIS_URL` exportada, base viva (`erp-postgres`/`erp-redis`) fora de alvo, sem junction, remoção pelo nome ao fim.

**1.3 Resíduo — VERDE com notas (R3, R4).** `docker ps -a` (nomes/status/portas): **0** `j-bsan304a-*`, `jur-*`, `crit-*`, `insp-*`. Vivos: `bsan301-pg` (127.0.0.1:32768), `bsan301-redis` (56385) — outro bloco, declarados; `pastrack-*` (5432/5433, outro projeto, declarado). Exited: `erp-postgres` (7 dias), `erp-redis`, `pastrack-teste-banco-teste-1` e **`erp-postgres-alt` (127.0.0.1:55432) — não declarado no briefing**, inerte (o `00-dev.md` já o listava como "não tocado"). `git worktree list` ao fim: exatamente os 7 declarados (`gov-elenco` em `main`; `bsan301` avançou `ec8492fd -> a826e927` durante a inspeção — dev do #387 em curso, declarado). Durante a inspeção existiu `insp-b11` (detached `50dc83c9`, 15:44-15:55) — inspetor de OUTRA junta desta rodada, previsto pelo briefing; não é resíduo desta e saiu sozinho. `.claude/worktrees/san2-r`: diretório **vazio** (0 entradas, sem `.git`, não registrado) — resíduo inerte; reporto, não varro. `jur-probe*` / `*-probe.ts` / `*-probe.mts`: 0 em `bsan304a` e na árvore principal (`find` + `git ls-files -io`). Árvore principal (`demo/investidor`, `d1fab3bc`): 6 modificados + 27 untracked (especialistas `jurado-06/07b/c5-*`, atas O6R-02-c5/07a/SAN2-6, `audit-agents-skills.mjs`) — resíduo de outras sessões, declarado; nenhum desses nomes é cadeira desta junta.

## 2. Insumos

**2.1 Ata anterior / "a re-verificar" — VERDE com R1.** Ciclo 1: não há ata anterior deste bloco. As atas que alimentam o bloco (`J-SAN3-plano-ciclo1/2`, `R-SAN3-plano-ciclo1/2`, `PLANO_SAN3.md` §4.1) estão na ref. O briefing §3 marca o relatório do dev e o plano como "afirmações a medir, não prova" e manda cada cadeira conferir as 22 células por conta própria — correto. **Mas o §0 afirma como fato: "O ramo foi rebaseado depois do merge do `B-SAN3-01` e os números de KPI foram reexecutados".** Medido: `gh pr view 387` = `state: OPEN`, `mergedAt: null`; `origin/main` = `02bd7dab` (= #386); merge-base do objeto = `02bd7dab`; o único PR mergeado que cita SAN3 é o #386. **Não houve rebase pós-B-SAN3-01** — o cabeçalho do mesmo briefing e a emenda 2 (g) do comando dizem que ele é FUTURO. E o mandato C3 (3) manda conferir "que o rebase não duplicou nem perdeu entrada de KPI ou de registro" e "as dívidas do #386 (pagas pelo #387)": hoje isso é imensurável (não há rebase; o #387 não pagou nada na `main`), e o próprio briefing diz que "Não consigo medir" = REPROVADO — um voto pode reprovar o objeto por um não-evento. Ver R1.

**2.2 Ciclo >= 3 — N/A** (ciclo 1; sem crítico por decisão do §C7.1-ter(b), bloco de permissão).

**2.3 Plano do ciclo — VERDE.** `agent-orchestration/omega/planos/B-SAN3-04a-plano.md` na ref: §0 nomeia base `origin/main@02bd7dab` + comando `4ae19272`; §9 lista os arquivos permitidos (caminhos exatos) e os proibidos; §12 dá a bateria comando a comando **com forma e N esperado** (TAP `# tests >= 3049, fail 0, skipped 2`; `node --test --import tsx <suítes>` >= 25 / >= 18; `RBAC_DB_PARITY=1` 2/2; `test:smoke` >= 1134; drills D1-D5 "medir, não descrever"). Comando na ref com "Bateria de validação" e "Emenda 2" (D-1 a D-8, ordem dos merges). Normas citadas existem no `CLAUDE.md` da ref: `§A7` (l.103), `1-bis`, `1-ter`, `4-bis`, `D-INSPETOR-TERRENO-JUNTA`, `D-FALLBACK-MODELO-FABLE-OPUS`; `P-REDIS-DEV-LIXO-DE-FILA`, `P-GOV-CAMINHO-REPO-SESSAO` e `D-SAN3-04A-FAIL-CLOSED-POR-ESCOPO` existem em `controle/` da ref; `CE-G2` vive no comando (não é cláusula do contrato — nada a bloquear por ela).

## 3. Papéis

**3.1-bis Obituário — VERDE.** `OBITUARIO-IDENTIDADES.md` da ref (31 sepultadas, 0 reservadas): as 6 identidades (`agente-dba-guardiao`, `coordenador-de-acessos`, `agente-ci-doutor`, `inspetor-de-arnes-concorrente`, `master-teste-telas-rotas`, `validador-mestre`) aparecem só na nota do §4 (l.249-250: permanentes/suplentes "não entram aqui"). 0 colisões.

**3.1 Inelegibilidade por nome — VERDE com R2.** Planejador = `planejador-mestre`; desenvolvedor = `general-purpose` 3a instância (`00-dev.md`: "não achou, não planejou"); orquestrador, porteiro e este inspetor não sentam. Achadores dos 5 itens do bloco (`PLANO_SAN3.md` §4.1): 13 (inventariante C2), 14 (C1), 15 (B1 + crítico r2), 38 (C1), **56 ("junta do ciclo 2, lacuna da C1; conferido pelo orquestrador" = `jurado-san3c2-cobertura-de-fluxo`, sepultada)** — nenhum é cadeira. `grep` nas atas: `coordenador-de-acessos` (C2) e `validador-mestre` (suplente C3) foram **votantes e achadores do ciclo 1 do PLANO SAN3** (`J-SAN3-plano-ciclo1` l.14-15; `J-SAN3-plano-ciclo2` l.27); `agente-ci-doutor` (C3) foi C3 do ciclo 2 do plano (APROVADO) e fez a conferência de aplicação; `agente-dba-guardiao` foi suplente nunca instanciado. Objeto diferente (o plano, não este bloco): não é colisão com a lista do §C7.4-bis. Mas o achado **C2-04** de `coordenador-de-acessos` ("teste do item 15 cobre 1 de 4 divergências" — plano corrigido para "o teste do SAN3-04a cobre as quatro", `PLANO_SAN3.md` l.592) moldou um critério que a C2 agora julga. Ver R2.

**3.2 Competência — VERDE.** Achados: convergência matriz x catálogo x banco (C1 dba: drills D1-D4, T2, snapshot), permissão/fail-closed/menu/rota (C2 coordenador: veto; suplente `master-teste-telas-rotas`), diff x escopo/KPI/registro (C3 ci-doutor; suplente `validador-mestre`). Front smoke coberto pelo mandato C2 (3). Sem lacuna nomeada.

**3.3 Corpo carregado x julgado — VERDE.** md5 EOL-neutro (CR removido antes do `md5sum`) sessão `C:/Users/AMP/Documents/GitHub/ERP_Techsolutios/.claude/agents/<x>.md` x `fbda96b0:.claude/agents/<x>.md` x `origin/main`: `agente-dba-guardiao de789c12` · `coordenador-de-acessos a4141c31` · `agente-ci-doutor 55979e2c` · `inspetor-de-arnes-concorrente 8caaef86` · `master-teste-telas-rotas 2c01e7c2` · `validador-mestre 804d89f0` — **6/6 IGUAL nos três lados.** Divergem só `inspetor-de-terreno-da-junta` e `planejador-mestre` (não votam; por isso o briefing manda ler da `origin/main`). Cláusulas citadas pelos corpos (`§C7.4`, `D-JUNTA-SEPARACAO-DE-PAPEIS`) existem na ref.

## 4. Orquestração

**4.1 Fatia S0 — VERDE.** No meu worktree detached em `fbda96b0`: `node scripts/sync-agent-agents.mjs --check` = `[agents-sync] OK — 25 agentes, espelho consistente.` ec=0 (25 em `.claude/agents/**`, 26 em `.agents/agents/**` = 25 + `README.md` do protocolo).

**4.2 Baseline — VERDE.** Worktree próprio `insp-bsan304a` (detached `fbda96b0`, status vazio), `npm ci` ec=0 (326 pacotes), `DATABASE_URL` fictícia + `npx prisma generate` ec=0, **`npm run check` (`tsc -p tsconfig.json --noEmit`) ec=0**, `node --check Kpis/app.js` ec=0; status vazio depois (exit por variável, sem pipe). Não subi banco: o corpo exige `check`; drills e suíte são mandato das cadeiras.

## 5. Quórum

**5.1 Perda de jurado — VERDE.** Briefing §4: suplente nomeado re-executa o mandato inteiro; voto perdido não conta; menos de 3 votos de mérito não fecha; toda queda em `00-quedas.md` (P6); P5 máx. 2 cadeiras em paralelo. `00-quedas.md` ainda não existe no scratchpad (o orquestrador o cria na 1a queda — nota, não ressalva).

## Veredito: `LIBERADO COM RESSALVA`

Ressalvas para o prompt das cadeiras (R1 é correção obrigatória do briefing antes do disparo; não exige nova inspeção — o objeto e o terreno estão provados e o briefing vive no scratchpad):

- **R1 (todas; sobretudo C3) — premissa falsa no briefing §0.** Substituir "O ramo foi rebaseado depois do merge do `B-SAN3-01` e os números de KPI foram reexecutados" por: "O ramo está sobre `origin/main@02bd7dab` **sem rebase**; o #387 está OPEN. O rebase acontece DEPOIS do merge do #387 e a C3 confere o delta pré-merge (cabeçalho; emenda 2 (g))". No mandato C3 (3), trocar "que o rebase não duplicou nem perdeu entrada" por "que o PR, como está, **não** carrega as dívidas do #386 (nenhum `git rm`, nenhuma entrada de backfill do #386 no history — medível no diff `02bd7dab..fbda96b0`)" e deixar a conferência do rebase para o pré-merge, fora do voto. Sem isso, a regra "Não consigo medir = REPROVADO" pode reprovar o objeto por um não-evento.
- **R2 (C2 `coordenador-de-acessos`; suplente C3 `validador-mestre`; C3 `agente-ci-doutor`) — participação prévia a declarar no voto** (precedente `J-SAN3-plano-ciclo2`, "R3 do inspetor"): votaram/acharam no plano SAN3 (ciclo 1: C2-xx/C3-xx; ciclo 2: C3 APROVADO + conferência de aplicação). O C2-04 de `coordenador-de-acessos` moldou o critério "o teste do item 15 cobre as quatro divergências" que a própria C2 agora julga: a cadeira mede o critério pela ref (T1 + mutações), não pelo que pediu.
- **R3 (C1 e quem subir banco) — resíduo inerte não declarado:** container `erp-postgres-alt` (exited, 127.0.0.1:55432) e diretório vazio `.claude/worktrees/san2-r`. Não usar o nome nem a porta 55432; ninguém varre.
- **R4 (todas) — outras juntas no mesmo terreno:** `insp-b11` existiu entre 15:44 e 15:55; `bsan301-*` vivos (32768/56385); `bsan301` muda de head (dev do #387). Nomes `j-bsan304a-*` estão livres (0 containers, 0 worktrees). Remoção só pelo PRÓPRIO nome completo — nunca por prefixo `j-*`/`insp-*`.
- **R5 (todas) — armadilha de caminho medida por mim:** com `MSYS_NO_PATHCONV=1` exportado, `git worktree add /c/Users/...` cria o worktree em **`C:/c/Users/...`** (o git nativo não converte o argumento). Passar caminhos `C:/Users/...` a todo argumento de `git`/`docker`; `/c/...` só em builtins do bash. (Aconteceu comigo; desfeito — ver limpeza.)
- **R6 (nota) — `frontend/node_modules` PRESENTE no `bsan304a`** (o plano M0 o dizia ausente); irrelevante para as cadeiras, que usam worktree próprio, mas explica como o dev mediu o front ali.

## Limpeza
Criei e derrubei: worktree `insp-bsan304a` (1a tentativa em `C:/c/Users/...` por R5: `git worktree remove --force` ec=0 + `rmdir` da cadeia vazia até `C:/c`, ausente ao fim; 2a em `C:/Users/...`: `git worktree remove --force` ec=0, diretório e `.git/worktrees/insp-bsan304a` ausentes); arquivos `_*` de medição no scratchpad apagados (restou só este parecer); nenhum container criado (0 `j-bsan304a-*`/`insp-*` em `docker ps -a`); `bsan304a` intacto (`fbda96b0`, status vazio); `git worktree list` final = os 7 declarados.
