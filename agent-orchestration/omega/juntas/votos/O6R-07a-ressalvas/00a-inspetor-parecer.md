# 00a — inspetor-de-terreno-da-junta · PR #373 (B-O6R-07a, ressalvas R1+R2) · PARECER

> Head medido por mim: **`039c2dc`** (`chore/o6r07a-ressalvas`, local == remoto). Base real hoje: **`origin/main = 1a7ad4d`**.
> Evidência executada, item a item: `00a-inspetor-evidencia.md` (mesma pasta). 2026-09-05.

## Veredito: **LIBERADO COM RESSALVA**

O tabuleiro está limpo: árvore sem mutação viva (blob×worktree idênticos; a diferença em `pendencias.md` é `autocrlf`, medida), head = remoto, insumos 5/5 como blob no head, 3 cadeiras sem colisão de nome (0 ocorrências em atas, reprovações, obituário e agentes), numstat do registro **bate** (5/5 · 30/0 · 32/32), S0 ec=0, guards 4/4 verdes no head **e** na árvore mesclada com a main atual, CI **7/7** sobre o merge `daff652` (= head + main atual), zero junction, zero container de jurado, base viva intocada. Nada é `BLOQUEADO`. As ressalvas abaixo mudam **contra o que** os jurados medem — entram no briefing em destaque.

## Ressalvas (numeradas, para o briefing)

**R1 — DESTAQUE · a premissa de terreno do briefing está defasada: #374 e #375 já MERGEARAM e a `main` moveu.** `origin/main` é `1a7ad4d`, não `cae6086`. Os dois PRs da outra sessão tocaram `pendencias.md` (**104/3**), `pendencias-indice.md` (103/92), `kpis-latest.json` (39/15) e `app.js` (1/1). Medido: `git merge-tree --write-tree origin/main HEAD` → **ec=0, tree `0b711e4c`, sem conflito**, e o merge reproduz **exatamente** o diff da branch de um lado e o da main do outro; o CI 7/7 rodou sobre esse mesmo merge; os 4 guards passam na árvore mesclada. Implicações a escrever no briefing: (a) **C2.3** mede a trilha de `blocks_completed` em `origin/main` = `1a7ad4d` (o `kpis-latest.json` mudou no #374), não em `cae6086`; (b) **C3.3(b)** — os números `252/243/55 → 261/250/62` são de `cae6086`; o #375 **regenerou o índice** ("a linha de status que o gerador lê"), logo a "defasagem pré-existente" tem de ser **re-medida contra `origin/main`** — pode já não existir; (c) **C3.3(a)** — a neutralidade do apenso de 30 linhas para o índice mede-se na **árvore mesclada** (`git merge-tree --write-tree origin/main HEAD` → `git commit-tree <tree> -p origin/main -p HEAD` → worktree detached **dentro de `.claude/worktrees/`**, nome com `r07a`), com o gerador `agent-orchestration/controle/gerar-indice-pendencias.py` (não está em `scripts/`); (d) a combinação "`pendencias-indice.md` é da outra sessão" foi feita quando o #374 estava **aberto** — ele e o #375 já mergearam; o orquestrador decide se a combinação ainda vale; (e) **para quem mergear depois:** squash do #373 sobre a `main` atual produz a tree `0b711e4c` sem conflito; o `pendencias-indice.md` da `main` **não** inclui as 30 linhas deste PR — correto **se e só se** o apenso for neutro (C3.3a); se não for, o índice pós-merge nasce defasado e é achado `dentro-do-bloco`.

**R2 — §2 do briefing diz "3 arquivos"; o PR tem 5.** Além dos 3 de registro (numstat **confere**), o diff carrega `BRIEFING-O6R-07a-ressalvas.md` (59/0) e `votos/O6R-07a/00c-porteiro-pos-merge-369.md` (184/0) — docs de governança, inertes. A C3.1 ("só 3 arquivos?") deve receber a resposta correta: **5, sendo 2 docs**, nenhum em `src/`/`tests/`/`prisma/`/`.github/`/`frontend/`/`mobile/`/`scripts/`/`CLAUDE.md`/`AGENTS.md`.

**R3 — FORTE · plano de perda de jurado AUSENTE no briefing.** Quórum é maioria de 3. Regras P3 que proponho por escrito: **C1 (`decima-via`)** — sucessor lê `<cadeira>-evidencia.md`, retoma no 1º item sem `veredito parcial`; o drill S4 re-executa **do zero** num worktree **novo** com identificador do bloco (o do caído **não** se reaproveita nem se apaga — reporta-se); se a evidência já tiver a linha "km null → 111111" gravada com comando e saída, aproveita-se. **C2 (`backfill`)** — só leitura de blobs (`git show`/`git log`); sucessor re-mede os 3 hashes e a trilha de `blocks_completed` em `origin/main`; nada a montar. **C3 (`escopo-guards`)** — sucessor re-roda os 5 guards (ec por variável, gravado um a um) e a neutralidade do índice na árvore mesclada (R1c). **Quórum:** voto perdido conta como **queda registrada** (classe: streaming/cota/rede), nunca como voto; com 2 votos restantes só há decisão se os 2 coincidirem; 1×1 + queda = re-disparo **obrigatório** da cadeira caída antes de fechar a ata.

**R4 — Terreno da C1 (execução).** Precisa de worktree **próprio** (nome com `r07a`, `git -c core.longpaths=true worktree add --detach`), com **`npm ci` + `npx prisma generate`**, **sem `.env`**, `CORE_SAAS_PERSISTENCE` não exportada (arnês em memória — foi assim que C1-v2 e o porteiro mediram: zero porta, zero container); o arquivo de drill (`tests/zz-*.test.ts`) **não** se commita. **Lição medida por mim hoje:** um worktree **fora** de `.claude/worktrees/` e sem `npm ci` próprio falha com `ERR_MODULE_NOT_FOUND dotenv` mesmo em teste que passa no `r07a` — porque o `r07a` resolve pacotes da árvore principal por subida de diretório, não por link. Ou o worktree fica sob `.claude/worktrees/`, ou roda `npm ci` próprio; nunca junction.

**R5 — EOL: a premissa "`pendencias.md` tem EOL misto" não vale para o blob.** Blob no head, em `cae6086` e em `origin/main`: **0 CR** (`i/lf`); o worktree tem **6 527 CR** (todas as linhas, por `autocrlf=true`). Jurado que medir CR no arquivo do worktree vai "achar" injeção de CR que não existe. **Medir em blob** (`git cat-file -p`/`git show`), nunca no arquivo; `sed -i` continua proibido (reescreveria em CRLF e sujaria o índice).

**R6 — Sem comando/plano formal do ciclo** (`codex/comandos` não tem `*07a-ressalv*`); o briefing faz as vezes e a bateria (C3.3) **não declara N/forma**. Para um PR só de registro aceito, mas a ata deve nomear o briefing como plano e a forma dos guards: `node --test --import tsx tests/<guard>.test.ts`, `node scripts/kpi-freeze.mjs --check`, `node --check Kpis/app.js`, `git diff --check <base> <head>`.

**R7 — Resíduos alheios (inertes; reportados, NÃO tocados) e concorrência viva.** `o6r-b02-cond5` é da outra sessão e **está ativo** (mudou de branch entre duas chamadas minhas) — intocável. `gov-descuido` (26/08, sem `node_modules`) e `san2-r` (diretório **vazio**, não registrado) são órfãos alheios em `.claude/worktrees/`. Árvore principal (`demo/investidor`) com 11 arquivos de agentes do B-O6R-02 c5 modificados/untracked — alheio. O **scratchpad é compartilhado** entre sessões (centenas de arquivos; inclui `o6r07a-c3-idx/` e `o6r07a-c3-pend-added.txt` da C3 do ciclo 2 do #369): **todo arquivo temporário desta junta leva prefixo `r07a-`**, e ninguém varre nome alheio.

**R8 — "CI: 7/7 verde" no briefing era antecipação.** O run do head `039c2dc` nasceu com o push do próprio briefing (19:31:49Z) e só fechou às 19:37Z; o 7/7 anterior era do head `dfc0507`. **Agora está medido:** 7/7 pass em `039c2dc` (sobre o merge com `1a7ad4d`).

## O que NÃO medi (dito às claras)
- Mérito de C1/C2/C3 (décima via, hashes, neutralidade do índice) — não é meu papel.
- `npm run check`/suíte plena localmente — substituídos pelo CI 7/7 sobre o merge atual + os 2 testes que leem `pendencias.md` (32/32 no `r07a`); o PR tem zero `src/`/`tests/`/`prisma/` (numstat).
- A tree exata do merge do CI (`daff652`): a API devolveu `tree=null`; provei **pais idênticos** (`1a7ad4d` + `039c2dc`) e merge sem conflito — merge determinístico dos mesmos pais.

## Limpeza (1 linha)
Criei 1 worktree de prova (`wt-r07a-inspetor-merged`, commit sem ref `e7d37fb`) e 9 arquivos de saída no scratchpad; worktree **desregistrado e diretório apagado** (`rm -rf` após `worktree remove` falhar em "Filename too long"), arquivos removidos, **zero container**, base viva não lida; ficam no `r07a` só os meus 2 untracked (`00a-inspetor-evidencia.md`, `00a-inspetor-parecer.md`) e, dangling, o objeto `e7d37fb` (sem `gc`).
