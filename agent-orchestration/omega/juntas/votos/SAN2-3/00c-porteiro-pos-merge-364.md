# Parecer do porteiro-pos-merge — PR #364 (SAN2-3, squash c9fd3a1)

- **Papel:** porteiro-pos-merge (Fable por contrato, D-PORTEIRO-POS-MERGE)
- **Data:** 2026-08-31
- **Método:** parecer criado ANTES das medições; cada linha preenchida ao medir. Nada composto de memória.

## A. Promessa × diff real

- A1. Merge existe e íntegro: **OK** — `git log origin/main -3` topo = `c9fd3a1` (SAN2-3, #364); `gh pr view 364` → `state MERGED`, `mergeCommit c9fd3a1e9ecb…`, `mergedAt 2026-08-31T12:06:58Z`, head `chore/san2-3-obituario-especialistas`.
- A2. Obituário: **OK** — `agent-orchestration/omega/juntas/OBITUARIO-IDENTIDADES.md` presente (145 l.). Medido por grep nas linhas de tabela: **15 × SEPULTADA** (l.52-57 ARNES, l.71-79 c4) + **2 × RESERVADA** (l.92 `jurado-c5-arnes-catalogo-postgres`, l.93 `critico-c5-adversarial`), status `RESERVADA — ciclo 5`, classe `reservada (nunca votou)` — preservadas, não sepultadas. Placar §2 bate (17 = 15+2, 0 arquivos apagados). §5(b) registra a correção 16+1→15+2 como divergência §A2 em `controle/decisoes.md`.
- A3. Reserva na ata: **OK** — `J-B-O6R-ARNES.md` l.50-56 lida direto: o inspetor BLOQUEOU o reuso da cadeira e *"`jurado-c5-arnes-catalogo-postgres` ficou **intocado e reservado** para a junta do ciclo 5"* (verbatim, bate com a citação do obituário l.92). O `critico-c5-adversarial` é reservado via `planos/B-O6R-02-ciclo5-plano.md` (citado no obituário l.93) — coerente com a ata l.46 (crítico reservado a blocos de invariante).
- A4. Edição no inspetor: **OK** — `git diff --numstat d283903 c9fd3a1` → `.claude/agents/inspetor-de-terreno-da-junta.md` **+4/-0** e espelho `.agents/agents/…` **+4/-0**. Dentro do teto de 4 linhas.
- A5. Espelho: **OK** — `node scripts/sync-agent-agents.mjs --check` reexecutado nesta máquina (checkout Windows, autocrlf) → `[agents-sync] OK — 23 agentes, espelho consistente.` exit 0. Contagem: `.claude/agents/*.md` = **23**, `.agents/agents/*.md` = **24** (23 + README). Árvore limpa (único untracked = este parecer), logo o check mediu o conteúdo do merge.
- A6. Zero descarte físico: **OK** — `git diff --name-status d283903 c9fd3a1` = 8 M + 15 A, **nenhum D**. `origin/demo/investidor` intocada: tip `d1fab3b` (2026-08-29 08:20), anterior ao merge, e o diff não toca a branch.
- A7. Diff em código: **OK** — `git diff --stat d283903 c9fd3a1 -- src/ tests/ scripts/ prisma/ .github/ frontend/ mobile/ docs/contracts/ contracts/` = **vazio**. Só docs de orquestração, os 2 espelhos do inspetor e `Kpis/*`.

## B. Contagens reexecutadas e KPI

- B1. `agents-mirror-guard`: **reexecutado** — `node --test --import tsx tests/agents-mirror-guard.test.ts` → **12 pass / 0 fail / 0 skip** (2,3 s). Bate com o "12/12" do corpo do PR.
- B2. `kpi-dashboard-charts`: **reexecutado** — **16 pass / 0 fail / 0 skip** (5,2 s). Bate com o "16/16" do corpo.
- B3. `sync-agent-agents --check`: **reexecutado**, exit 0 (detalhe em A5).
- B4. `kpi-freeze --check`: **reexecutado** — `kpi-freeze: em dia (snapshot 2026-08-30).` exit 0.
- B5. `node --check Kpis/app.js`: **OK**.
- B6. Backfill do #363: **OK** — entrada SAN2-2 do `Kpis/kpis-history.json` lida por parser JSON: `pr: 363` · `merge_commit: "d283903"` · `approved_head: "c8dc716"`, e a description do SAN2-3 explica o porquê do head da ata (não o `e4926bd` do GitHub). A dívida do porteiro do #363 foi paga.
- B7. Entrada SAN2-3: **OK** — última entrada do history (147 no total): `blocks_completed: 153` (152→153, justificado na description), `pr/merge_commit/approved_head: null` (§C3.5, autoria), `backend_tests 2607/2609` · `frontend_smoke 1126/1126` · `flutter 864/864` **CARREGADOS com marcador §C3.3** (o PR não tocou essas trilhas — A7 prova diff vazio), e o que o PR exerceu foi reexecutado com N declarado (12/12 + 16/16, que eu re-reproduzi em B1/B2). `mvp_demo 99%` / `mvp_vendavel 88%` intocados no `kpis-latest.json` (o PR não move escopo de produto).
- B8. Dívida nova (§C3.5, viaja para o próximo PR): a entrada SAN2-3 recebe backfill com `pr: 364` · `merge_commit: c9fd3a1` · `approved_head: 23d9227` — o **head julgado consignado na ata** `J-SAN2-3.md` l.4 (*"Head julgado: `23d9227` · CI: 7/7 (run 33346995433)"*), lido da ata, não presumido. `git cat-file -t 23d9227` → `commit` (objeto existe). Ata: **APROVADO 3×0**, quórum maioria de 3 (bloco documental).
- B9. NÃO reexecutei: a suíte backend completa (2607/2609), o smoke do frontend (1126) e a suíte Flutter (864) — o merge tem diff **vazio** nessas trilhas (A7) e os números entraram como carregados com nota §C3.3, não como execução do PR; reexecutá-los aqui mediria a máquina, não a entrega. O CI do PR foi conferido via `gh pr checks 364` → **7/7 pass** (run 33353242032: backend, backend-postgres, frontend, flutter, docker, owner-portal, authority-portal), sem reauditar os logs job a job.

## C. Próximo start e limpeza

- C1. Start alcançado por bloqueio? **NÃO** — pela régua estruturada. `grep '\*\*Bloqueia:\*\*' pendencias.md` = **14 campos**: 2 negados (l.2656, l.3280 "nada") e 12 em pendências `Ω6R-*`. Desses 12, um está em pendência **FECHADA** (`Ω6R-DIN-006`, fechada 2026-08-15 PR #353 — o Bloqueia é registro histórico) e os 11 abertos travam **feature nova de domínio** (RBAC/auth, despesas/RDV, estoque, cloud billing, OS/aprovações, evidências/upload, jobs/SSE, despacho/Mapa, portal do proprietário, app de campo) e **deploy produtivo**. Nenhum alcança **SAN2-4a** (medição de arnês), **SAN2-4b** (correção de arnês) nem o **ciclo 5 do B-O6R-02** — que aparece nas pendências como **dono** (`P-O6R-B02-SUITES-LIST-CI` l.3695), não como alvo bloqueado. `grep -i 'ciclo 5|B-O6R-02|arnês|harness'` no entorno dos campos Bloqueia = vazio; `grep 'SAN2-4'` em pendencias.md = vazio.
- C2. Limpeza §C5: **OK** — `git ls-remote --heads origin chore/san2-3-obituario-especialistas` = 0 (remota apagada); `git branch --list 'chore/san2-3*'` = 0 (local apagada); `git remote prune origin --dry-run` = vazio (em dia); `git branch --merged origin/main` sem sobras locais mergeadas; `git worktree list` = **4** (raiz em `demo/investidor` `d1fab3b`, `agent-af6ea…` em `feat/o6r-b02-financial-uow` `12c3825`, `gov-descuido`, `san2-r` na `main` `c9fd3a1`); `docker ps` → **erp-postgres Up 2 days (healthy)** · **erp-redis Up 2 days (healthy)** — intocados; disco `df -h /c` → **18 GB livres** (acima do gatilho de ~10 GB do DEEP_CLEAN; nota: 93% usado, vale vigiar). Árvore sem `^ D` (0 rastreados apagados).
- C3. Achados vivos (confirmar ou derrubar):
  - P-OBITUARIO-DERIVADO-DO-DIRETORIO: **CONFIRMADA** — `pendencias.md` l.4506-4594: ABERTA · MÉDIA · dono **SAN2-5** (parte 3, l.4594). N=15 com forma (só votos JSON com campo de autor, juntas concluídas) e causa (derivou do diretório, não das atas); "**o número real é MAIOR que 15**" registrado nas l.4529-4531 (jurados caídos + inspetores fora da conta). Escopo `pre-existente` com evidência de data (atas de 08-29/08-30 anteriores ao head julgado `23d9227`). É o achado C1/A-1 da junta — publicado como pendência em vez de reprovar (§C7.1-ter(a)), com C1 não consertando (§C7.4-bis).
  - P-KPI-PAINEL-NAO-RENDERIZA-SUMMARY: **CONFIRMADA** — l.4405-4501: ABERTA · MÉDIA · dono **SAN2-5** (parte 1, l.4501 — dono REAL preenchido por este bloco, era "a atribuir").
  - P-SAN2-2-INDICE-DONO-SEMPRE-SIM: **CONFIRMADA e re-reproduzida por mim** — l.4345-4400: ABERTA · MÉDIA · dono **SAN2-5** (parte 2, l.4400). O `pendencias-indice.md` continua dizendo coluna dono="sim" para TODAS as 5 pendências conferidas (l.87/93/94/95/177), inclusive `P-SAN2-2-PORTA-55432-RESERVADA` cujo dono real é "a atribuir" (l.4340) — o falso-sim segue vivo, como o corpo do PR admite.
  - P-SAN2-2-PORTA-55432-RESERVADA: **CONFIRMADA** — l.4289-4340: ABERTA · BAIXA · dono "a atribuir" (armadilha de terreno, não defeito de produto; sem campo Bloqueia).
  - P-O6R-B02-SUITES-LIST-CI: **CONFIRMADA** — l.3690-3695: ABERTA · MÉDIA · dono = **o PR que mergear o B-O6R-02 (ciclo 5)**, re-atribuído pelo SAN2-2. Não bloqueia o start; fecha junto com o ciclo 5.

## Fecho

- O próximo bloco pode começar? **SIM.** O merge é íntegro, a promessa bate com o diff (registro puro, zero descarte, +4 no inspetor, espelho verde), os números reproduzem (12/12, 16/16, exit 0, freeze em dia, CI 7/7), o §C7.1 está registrado (ata APROVADO 3×0 + briefing + 5 votos/pareceres no diff), o backfill do #363 foi pago, as pendências estão registradas com dono e nenhum `**Bloqueia:**` aberto alcança SAN2-4a/4b/ciclo 5, e a limpeza §C5 está feita. A única dívida é a de regra: o backfill §C3.5 do próprio #364, que só pode existir pós-merge e viaja para o próximo PR.

**LIBERADO COM RESSALVA: SAN2-4a (medição do arnês — intermitência do `authority-portal` com N≥10, as duas listas contraditórias da "bateria barata", censo dos 68 roles `rls_test_`) | dentro dele, backfill §C3.5 da entrada SAN2-3 do `Kpis/kpis-history.json`: `pr: 364` · `merge_commit: c9fd3a1` · `approved_head: 23d9227` (o head julgado consignado na ata `J-SAN2-3.md` l.4).**
