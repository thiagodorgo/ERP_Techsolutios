# C2 — coordenador-de-acessos — evidência (PR #386, SAN3)
Formato: comando → saída resumida → veredito parcial.

## T0 — terreno
- `git worktree add --detach .../j-san3-c2 a143d2c3` + `git rev-parse HEAD` → a143d2c33674cc2c3562de40cc9d3a95350cabe5 → head medido = conteúdo julgado.
- `git diff --stat 15ef3fbe a143d2c3 -- src tests prisma frontend mobile .github scripts` → vazio; `git diff --stat a143d2c3 0436d51c -- . ':!agent-orchestration/omega/juntas'` → vazio → o código do produto é o de 15ef3fbe; o head do PR só acrescenta juntas.
- `merge-base a143d2c3 15ef3fbe` → 15ef3fbe → base declarada confere.

## T1 — leitura dos insumos (plano §2/§4/§5/§8.7/§12/§13; crítico r1/r2; inspetor; 8 fatias)
- `Read PLANO_SAN3.md` (520 l.) → §2 regra: item só fica fora com condição medida (§4.3); itens 9–18 no §4.1; blocos C2 no §5.2/5.3/5.4; §8.7 "composição de leitura: primeiro passo do bloco é o teste que reproduz, com vermelho-controle; verde no head-base só sai por ata" → roteiro das medições.
- `Read inventario-{A1,A2,B1,B2,C1,C2,AUSENTES}` + grep O6R → candidatos de isolamento/permissão/segurança fora do gate a medir: P-006, P-Ω4-3-INVOICE-LEASTPRIV, P-IMPOUND-CHK-VISIBILITY, P-CHK-RUN-ASSIGNEE-SCOPE, P-O6R-B07B-ATTACHMENT-STORED-DO-CLIENTE, P-O6R-B07B-CHECKLIST-JSON-FILEURL, P-O6R-B07B-DATAURI-NO-VALUE, P-O6R-B07-RATE-LIMIT-DISTRIBUIDO, P-O6R-B01-ROUTE-ERROR-LEAK, P-O6R-B01-ROLE-LITERAIS, P-014, P-AUDIT-FOLLOWUPS(2), P-O6R-B07-APPROVAL-BY-POLICY, P-Ω4-6-REOPEN-FOUR-EYES, P-MOBILE-CHECKIN-CONFERE-CODIGO-DA-OS, P-AUTHORITY-N-NAO-CANONICO, P-AUTH-KDF-ROTACAO-V2, P-O6R-B01-RELIGACAO-SEM-REMEDIO, P-RBAC-CATALOG-MATRIZ(finance/inventory sem tenant_checklists:read), field_technician sem tenant_checklists:read (B1 achado extra).
- Nenhum veredito ainda; só roteiro.

## M1 — item 9 (P-INFRA-RLS)
- `grep -n DATABASE_URL docker-compose.prod.yml` → l.35,57 `postgresql://postgres:postgres@…` (superusuário); `grep -rniE "rolsuper|rolbypassrls|NOBYPASSRLS" src scripts docs fly.*.toml .github` → só comentários, a sonda `login-readiness.ts:202` (dono da função SECURITY DEFINER, não o runtime) e texto em `docs/deployment.md:447` ("Em PRODUCAO o app conecta com role NAO-superuser … Confirmar na ativação") → nada IMPÕE o papel; `fly.production.toml:9` DATABASE_URL é secret.
- Veredito parcial: item 9 real pelo crit. 2 (a 2ª barreira RLS fica inerte sem trava; o texto do plano está no que foi medido).
## M2 — item 10 (LEITURA-PLATAFORMA-SOB-FORCE-RLS)
- `sed cloud-usage.service.ts:108-114` → `getPlatformUsageSummary` → `summarizeEvents` → `repository.listEvents(filters)` sem tenantId; `cloud-usage-prisma.repository.ts:167-173,191-197` → sem tenantId usa `new PrismaCloudUsageRepository(this.prismaClient)` cru, sem `withTenantRls`; migração `20260611000000` l.57-58/64-65 `FORCE ROW LEVEL SECURITY`.
- Veredito parcial: pré-requisito real do item 9; o teste do SAN3-05 ("/platform/cloud-usage/summary soma 2 tenants sob papel efêmero") mira o caminho certo; fronteira do SAN3-05 inclui o arquivo. OK.
## M3 — item 11 (SEC-002 residual)
- `grep -rn assertMutationObjectScope src` → definição :808, chamadas só :852 (update) e :1319 (changeStatus); `work-order.routes.ts:224-225,238-239,247-248,256-257` attachments POST/DELETE e geocode* só `requirePermission`; `work-order-comment.service.ts:139-142` autor OU `work_orders:update`; `mobile-work-order-sync.ts:245-247` mileage exige só `work_orders:status`; catálogo field_technician tem `work_orders:update`/`status`; matriz l.45 field_technician = "execute/update-assigned".
- Veredito parcial: item 11 real pelo crit. 3.
- Censo de sync (mesmo comando, laço sobre src/modules/mobile/*.ts): `mobile-evidence-sync.ts wo=10 assign=0`, `mobile-evidence-upload.ts wo=3 assign=0`, `mobile-inventory-sync.ts wo=2 assign=0` → referenciam OS sem nenhuma checagem de atribuição; FORA da fronteira do B-O6R-07c (só `mobile-work-order-sync.ts`). A medir se são vias de escrita em OS alheia.
## M4 — itens 12/13/15 (catálogo × matriz)
- `grep -n '"dashboard:read"' catalog.ts` → 415,580,629,677,731,896,945,1014; blocos finance (809-872) e inventory (873-894) SEM `dashboard:read`; matriz l.34 finance/inventory = "scoped"; `dashboard.routes.ts:17,31` exige `dashboard:read` → item 12 real (crit. 3/7).
- `grep '"finance:read"|billing|payments|invoices'` → só PERMISSION_CATALOG (l.195-197, 228); finance tem `finance.read` (l.811, ponto); `navigation.registry.ts:344-393` /finance exige `finance:read`, charges `billing:read`, payments `payments:read` → item 13 plausível por composição (confere com a fatia C2).
- manager (l.414-578): `checklist_runs:update` (572), `:complete` (573), `:acknowledge` (574), `:reopen` (577); comentário `catalog.ts:569-571` "update/acknowledge do manager são drift residual"; matriz l.44 manager = "read/complete-by-scope". finance e inventory: ZERO permissão de checklist; matriz l.43-44 finance = read/read, inventory = read/read+answer-by-scope. field_technician: sem `tenant_checklists:read` (matriz l.43 = read).
- Veredito parcial: item 15 real (crit. 3), mas o item e o teste do SAN3-04 nomeiam SÓ `acknowledge` do manager — a medir contra a entrada `P-RBAC-CHECKLIST-DRIFT` (3 papéis) no registro da ref.
## M5 — item 14 (P-033)
- `catalog.ts:281-302` STANDARD_ROLES = {super_admin, tenant_admin, manager, field_dispatcher, technician, viewer}; `auditor` ∈ LEGACY_ROLES; `prisma/seed.ts:252` `for (const role of STANDARD_ROLES)`; `prisma/seed-users.ts:37,60` cria usuário demo auditor e grava `ROLE_PERMISSIONS[demo.role]`; `scripts/provision-rbac.ts:58,145` provisiona DEFAULT_ROLES (inclui auditor); `deploy-production.yml:155` `db:provision-rbac`; `ci.yml:169` só `db:seed`; runtime prisma: `persistent-rbac-context.middleware.ts:50-60,95-108` → `RoleRepository` (`role.repository.ts:118` `rolePermission.findMany`).
- Veredito parcial: premissa da C2 (permissões do banco) confirmada; o efeito "auditor 403 ao vivo" depende de qual base (db:seed só × provision) — o plano o trata como condicional com vermelho-controle obrigatório e saída só por ata (SAN3-04). A julgar no item 3.

## M6 — item 16 (P-WEB-GATE-MODULO-INCOMPLETO): contagem, método e guard
- `grep -cE '^\s+path:' navigation.registry.ts` → 36; MVP_NAV_PATHS (appSidebarNav.ts) → 51; `comm -23 mvp reg` → 27 (confere com o plano).
- MAS: `node` por bloco do registro → `/telemetria/{quilometragem,acessos,recusas,dispositivos,rastreamento}` (L405-462) e `/patios/{patios,processos,liberacoes,leiloes,perfis,tarifas}` (L481-610) com `requiredModules=AUSENTE`; os 11 estão em MVP_NAV_PATHS (`grep ^/(patios|telemetria) c2_mvp.txt` → 12, incl. /patios/painel que já está nos 27). `navigation.service.ts:114-115` `hasModule` = true quando `!item.requiredModules?.length` → esses 11 itens aparecem para QUALQUER organização.
- Catálogo de módulos `src/modules/platform/platform-modules.service.ts:4-22`: 19 chaves, NENHUMA para Pátios, Telemetria ou Frota.
- Backend: `grep requireModule|module_disabled|moduleKey` → só `mobile.routes.ts` (bootstrap) e `navigation.service.ts:115` → nenhuma recusa por módulo nas rotas de domínio (confere com a fatia).
- Veredito parcial: o item 16 é real, mas o plano mede 27 pelo método "MVP − registro", que exclui por construção 10 itens de menu no registro sem `requiredModules` (Pátios, Telemetria) — tenant menu fora do gate = 27 + 10 = 37. O guard do SAN3-18 ("todo caminho do menu está no registro") passa verde com os 10 sem gate; e a fronteira do SAN3-18 não inclui o catálogo de módulos (sem chave para Pátios/Telemetria não há o que provisionar). Item visível sem feature provisionável = VETO (papel).
## M7 — item 15 × registro
- `sed pendencias.md:1690-1701` (P-RBAC-CHECKLIST-DRIFT, corpo) → divergências de `manager` (`update`+`acknowledge`) E de `finance`/`inventory` (matriz read / read+answer-by-scope); emenda SAN3 da própria entrada (l.1701) reduz a "manager tem checklist_runs:acknowledge". `pendencias.md:1447-1456` (P-RBAC-CATALOG-MATRIZ, reescrita neste PR) → "duplicata de P-RBAC-CHECKLIST-DRIFT (… finance/inventory × checklists) — tratar junto; bloco dono = o de P-RBAC-CHECKLIST-DRIFT" (= SAN3-04).
- PLANO §5.2 teste do SAN3-04: só "`manager` sem `checklist_runs:acknowledge`".
- Veredito parcial: o teste de encerramento cobre 1 das divergências que o próprio PR roteou para o item 15 (ficam `manager:update`, finance/inventory sem leitura de checklist, field_technician sem `tenant_checklists:read`). Ajuste.
## M8 — check-in (candidato fora do gate, fatia AUSENTES)
- `sed work_order_detail_screen.dart:918-919,931,1083` → `_expected` = 2 últimos caracteres de `wo.code`; texto "Informe os 2 ultimos digitos da placa"; erro "Digitos nao conferem com a placa do veiculo"; backend sem checagem de check-in/placa (`grep check.?in|placa src/modules/work-orders src/modules/mobile` → só título de OS de remoção).
- `sed pendencias.md:8822-8836` (registrada NESTE PR): risco "3-seguranca/permissoes"; "plano SAN3: não nomeada no gate (§4.1)"; impacto "NAO — GPS coexiste; mas o controle é ineficaz e o rótulo mente (D-007)". `grep CHECKIN PLANO_SAN3.md` → 0 (fora do §4.3 nominal; só na linha genérica "demais ABERTAS").
- Veredito parcial: item de segurança fora do gate SEM condição de inaplicabilidade — "GPS coexiste" é mitigação, exceção que o §2 proíbe ("sem exceção … por 'mitigado no cliente'"); mesma classe do item 17 (UI declara controle de segurança inexistente) e do aviso falso do item 48 (mesmo app). BLOQUEIA (dentro-do-bloco: a classificação é deste PR).
## M9 — item 18 (GPS) e itens 17
- `grep '/location' router.dart` → só registros l.232,410; `grep -rn "LocationConsentScreen("` → só a própria classe; `geolocator_device_location_provider.dart:88-94,110-114` recusa captura sem `hasAcceptedManualCapture`; nenhum `Geolocator.getCurrentPosition` fora do provider; backend `telemetry.service.ts:108-111` rejeita GPS sem `trackingConsent` do PERFIL → captura é fail-closed; o defeito é de fluxo (crit. 7), não de RBAC. Rótulo "3" do item 18 impreciso; pertença ao gate se sustenta pelo 7. Nota.
- `PlatformSettingsPage.tsx:43-44` `<Toggle on />` "MFA obrigatório para admins"; `grep -rniE "mfa|totp|2fa" src` → 0 → real; é UI que mente (crit. 13), não RBAC (crit. 3). Pertença mantida. Nota.

## M10 — SAN3-05: a "lista medida" de leituras de plataforma sob FORCE RLS
- `grep -c withTenantRls` → cloud-charge-prisma.repository.ts 0, cloud-cost-allocation 0 (mas usa `set_config` por tenant, l.104-199/256/331, #385), aws-cur 0 (tabelas sem RLS); `cloud-charge-prisma.repository.ts:24-25,166-212,238-240` `replaceTenantCharges` (deleteMany+create) e `listTenantCharges` no `prisma` cru; `migration 20260614000000…:158-160` política de `tenant_cloud_charges` com USING **e** WITH CHECK em `app.current_tenant_id`; `tests/rls-tenant-isolation.test.ts:2573-2587` afirma que sem contexto as cobranças NÃO são visíveis.
- Veredito parcial: há um 3º caminho de plataforma que quebra sob o papel sem BYPASSRLS (cálculo e leitura de cobrança de nuvem: INSERT recusado pela WITH CHECK, leitura = []). O §5.2 diz "hoje a lista medida tem estas duas" — falso; fallback "vira pendência nomeada" mantém o gate protegido pela regra do §2. Ajuste.
## M11 — censo de sync × fronteira do B-O6R-07c; escopo "answer-assigned"
- `mobile-evidence-sync.ts:280-295` evidência de OS exige só `work_orders:update` + `work_order_id`, sem atribuição (persistência não medida); `mobile-checklist-sync.ts:70-77` mapeia ações só a permissões; `grep assigned|assignee|not_assigned src/modules/checklists src/modules/mobile/mobile-checklist-sync.ts` → 0; `awk model ChecklistRun schema.prisma` → sem campo de usuário (só `related_entity_type/id`); `checklist.routes.ts:133-202` responder/concluir/ciência só por permissão; catálogo field_technician `checklist_runs:update/complete/acknowledge` (l.939-942, #320 2026-08-01); matriz l.44 field_technician = "answer-assigned". Anexos genéricos: allow-list v1 sem work_order (`attachment-entity-resolver.ts:20-21`) → não é via.
- Veredito parcial: o censo vinculante do 07c vai achar vias fora da fronteira (checklist sync, evidence sync) e o plano não dá saída (o 05 tem "vira pendência", o 03a "o bloco para") → ajuste. E há um defeito de permissão fora de toda fatia: técnico responde/conclui run de OS alheia (crit. 3) — pré-existente (#320, 2026-08-01; #344 2026-08-10).
## M12 — item 13 composição, item 14 red-control, item 12 recorte, item 38 fronteira, item 32, field-location
- `appSidebarNav.ts:299-308` `computeHiddenNavPaths` esconde path governado não devolvido; `useNavigationMenu.ts` adota `metadata.governedPaths` quando o backend responde → composição do item 13 fecha por leitura (registro exige `finance:read`/`billing:read`/`payments:read`; papel finance só `finance.read`). Fail-closed; vermelho-controle possível; saída só por ata (linha do SAN3-04). OK.
- Item 14: receita do teste do SAN3-04 = `db:provision-rbac` + `db:seed`; `provision-rbac.ts:58,145` cria `auditor` (DEFAULT_ROLES) → neutraliza a premissa de `seed.ts:252` (STANDARD_ROLES); vermelho-controle impossível na receita; o que avermelha é a base só-`db:seed` (`ci.yml:169`, `package.json:22` test:e2e). Saída por ata preserva o fail-closed. Ajuste.
- Item 12: `src/modules/dashboard/*` sem recorte por papel (grep role/scope só em tipos); matriz l.34 finance/inventory = "scoped"; teste do plano "200" (a fatia C1 pedia "200 com recorte"); fronteira sem `src/modules/dashboard/**`. Ajuste.
- Item 38: `frontend/src/modules/auth/types.ts:1-9` união `UserRole` sem rótulo de estoque; `roleKindFor` (`appSidebarNav.ts:271-277`) decide por rótulo → dar menu próprio ao `inventory` exige editar `types.ts`, fora da fronteira do SAN3-04. Ajuste.
- Item 32 (§8.7 tirou do crit. 3): `work-order.service.ts:402-418` `list()` sem escopo por papel (`assignedUserId` opcional, 51238552 2026-06-09); matriz l.45 field_technician "execute/update-assigned", princípio l.19 (UI não substitui backend). "O backend oferece o filtro" não é argumento de autorização. Nota (pré-existente).
- `field-location.service.ts:21-38` grava posição sem checar `tracking_consent` do perfil (15739c1b 2026-06-09), ao contrário de `telemetry.service.ts:108-111` (fc7f6be0 2026-07-24) → o teste do item 18 prova o consentimento só no cliente. Ajuste (pré-existente).
## Limites declarados
- Login real por papel (corpo do papel) NÃO executado: o mandato desta junta proíbe banco/container (Docker parado, R2) e os 3 itens são mensuráveis por leitura; nenhum item do mandato ficou sem medição.

## Correção (append-only) e limpeza
- CORREÇÃO de M6: os itens de menu no registro sem `requiredModules` são 11, não 10 (`/telemetria/rastreamento` L462 também não tem; conferido bloco a bloco por `node` sobre o registro). Total de itens de menu da organização fora do gate de módulo = 27 + 11 = 38. O voto usa 38.
- Limpeza: `git worktree remove --force .../j-san3-c2` → removido (`git worktree list` → principal, gov-descuido, gov-elenco, san3; nenhum j-san3-c2); arquivos auxiliares meus `votos-SAN3/c2_reg.txt`, `c2_mvp.txt` e `/tmp/c2_reg.txt` apagados. Nenhum banco nem container; worktree `san3` e árvore principal não tocados.
