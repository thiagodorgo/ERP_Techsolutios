# C1 estrategista — evidência (PR #386, conteúdo a143d2c3)

## M0 — terreno
- `git worktree add --detach .../j-san3-c1 a143d2c3` + `git rev-parse HEAD` → `a143d2c33674cc2c3562de40cc9d3a95350cabe5` → head medido = conteúdo julgado.
- `git diff --stat a143d2c3 0436d51c -- . ':!agent-orchestration/omega/juntas'` → vazio; `git diff --stat 15ef3fbe a143d2c3 -- src tests prisma frontend mobile .github scripts` → vazio; merge-base = 15ef3fbe → terreno conforme o briefing §0.
- `wc -l docs/revisoes/SAN3/PLANO_SAN3.md` → 520; inventário 8 fatias; 00b-inspetor não está em a143d2c3 (entra em 0436d51c) → leio-o por `git show 0436d51c:`.

## M2a — os 8 tempos e as medianas (item 2)
- `gh pr view <n> --json number,commits,mergedAt,createdAt,additions,deletions,changedFiles` para 360 353 359 380 357 369 385 371 (+386) → `node c1-times.mjs` (1º commit→mergedAt, authoredDate e committedDate, e também min(commits)) → #360 1,27 · #353 1,43 · #359 4,94 · #380 12,83 · #357 44,06 · #369 57,04 · #385 102,73 · #371 376,70 h (idênticos nas 4 formas de medir); mediana 8 = 28,45; mediana dos 5 "grandes" = 57,04; mais rápido dos 5 = 12,83 → os 8 números e as duas medianas do §9 BATEM.
- Tamanho (add/files): #360 6364/46 · #353 4705/36 · #359 1457/14 · #380 10825/69 · #357 12872/90 · #369 12373/65 · #385 23465/93 · #371 19010/88 → os 5 "grandes" são também os 5 maiores em linhas (≥10,8 k) — a classe não é circular (não foi escolhida pelo tempo). Nota: #360 (6,4 k linhas) foi em 1,27 h.
- #386 (este PR): 1º commit 2026-09-11T14:54:26Z, último 2026-09-12T18:40:14Z, não mergeado → ≈27,8 h de relógio até o briefing; o §9 soma "este PR, ≈ 3 h antes de tudo" → parcial: número do PR subestimado (a apurar se é "restante" ou "total").

## M3 — cobertura §4.1 × §5 e destrave (item 3)
- `node c1-cover.mjs PLANO_SAN3.md` (parse das linhas da tabela §4.1 `| n | … | \`Bloco\` | ✓ |` e das linhas `| \`B-…\` · \`branch\` | Fecha |` do §5) → 49 itens (1–49), 34 blocos, 34 branches distintas, **0 divergências** (todo item tem bloco; todo bloco fecha exatamente os itens que o §4.1 lhe aponta; nenhum bloco sem item) → cobertura OK. Reclass ✓ = 3,6,7,9,10,14,15,20,21,22,23,25,26,29,30,31,36,38,41,46,48,49 (22) = lista do §8.7.
- `git show a143d2c3:agent-orchestration/controle/decisoes.md | grep -n REGISTRO-SAN3-CONFLITOS|D-Ω4-C1|D-Ω4-7-NO-TITLE` → l.2314 `## REGISTRO-SAN3-CONFLITOS (2026-09-11)`, item 1 `P-Ω4-3-REFATURAR-DELTA × D-Ω4-C1` (índice `financial_titles_wo_direction_active_key`, migration l.25-27), item 2 `P-Ω4-7-DUPLA-CONTAGEM × D-Ω4-7-NO-TITLE`; "Quem resolve: a junta de cada bloco (B-SAN3-02, B-SAN3-20; unanimidade de 3 + crítico) revê a decisão … pergunta ao dono, default sim … Até a revisão, as duas decisões seguem valendo" → registrado.
- Autoridade das decisões revistas: l.600-608 "Plano do planejador-mestre, 10 decisões ratificadas pelo orquestrador" (D-Ω4-C1) e l.669 `## D-Ω4-7 — Cheque … (2026-07-18, desenho pós-ataque adversarial 3-lentes)` → nenhuma é decisão do dono (§A1.1); a revisão por junta de bloco é competência de junta (§C7.1). PLANO §10.5 (l.462-465): (a) faturar o delta, default sim → junta do SAN3-02 revê D-Ω4-C1; (b) vincular cheque ao título, default sim → junta do SAN3-20 revê D-Ω4-7-NO-TITLE → pergunta ao dono com default presente. Parcial item 3: CONFORME.

## M1a — sondas de dependência de dado (item 1)
- MVP_NAV_PATHS (`appSidebarNav.ts:214`) → 51 caminhos, nenhum `/platform` → SAN3-06b (platformNavigation.ts) não muda a base do "27 = 51 − 36" do SAN3-18 → sonda descartada (a favor do plano).
- ARQ-004 (`inventario-O6R.md:204-214`): despacho e evento = `repository.create` e `repository.createEvent`, cada um com `withTenantRls` próprio em `field-dispatch-prisma.repository.ts` → conserto dentro de `src/modules/field-dispatch/**`; não precisa de `domain-event.publisher.ts` (que o SAN3-03 edita 44–57) → sonda descartada.
- Web × faturar OS: `grep -rn -E "invoice|financial-titles" frontend/src --include=*.ts` + `grep -rn -i -E "invoice\(|/invoice|Faturar" frontend/src --include=*.tsx` → 0 chamadas a `POST /work-orders/:workOrderId/invoice` (rota em `work-order-financial.routes.ts:78`); o front só tem `POST /financial-titles` (título manual) e `/financial-titles/:id/status` (`finance/titles/financial-titles.service.ts:50,69`); `grep -rn -i "invoice|fatur" tests/e2e` → 0; registro e inventário: nenhuma pendência "faturar OS pela web" → o fluxo (b) do SAN3-10 "OS criar → despachar → **faturar** → baixar" por persona na web não tem ação de faturamento na web nem bloco que a construa (SAN3-12 = baixa+contas; SAN3-02 = backend) → parcial: DEPENDÊNCIA OMITIDA no bloco que fecha o gate.
- Porteiro (item 2): `git log --all -i --grep=porteiro` × `gh pr view <n> --json mergedAt` → #360 merge 01:15:15 → parecer ca52e922 01:36:20 (-03) = 21 min; #359 merge 14:05:11 → 819ff833 14:33:48 = 29 min; #366 19:58:08 → 44a30e48 20:13:02 = 15 min; #367 01:52:35 → b324258d 02:11:14 = 19 min → "21–29 min medidos" reproduzido (amostra maior: 15–29 min). Nenhum parecer de porteiro na árvore registra a duração (grep `min|duração|início`) — a fonte é o log.

## M1b — travas de mesmo arquivo × agenda §6 (item 1)
- `git ls-tree -r --name-only a143d2c3 > c1-tree.txt` (3343) + `node c1-overlap2.mjs` (fronteira de cada bloco lida do §5, expandida contra a árvore; `prisma/migrations/**` tratado como histórico único; janelas = §6) → 28 pares em frentes diferentes com arquivo comum; 26 serializados; **2 SOBREPOSTOS**: `SAN3-02 (F1 26–39) × B-O6R-12 (F2 37,5–42,5)` e `SAN3-20 (F1 39–44) × B-O6R-12 (F2 37,5–42,5)`, ambos em `prisma/schema.prisma` + histórico de `prisma/migrations/**` (os três blocos têm a autorização nominal no §5: índice do faturamento; vínculo cheque→título; snapshot do regime). A lista "Travas de mesmo arquivo respeitadas" do §6 (l.313-321) não nomeia `prisma/schema.prisma`; o §6 l.302-303 afirma "dois blocos não tocam o mesmo arquivo ao mesmo tempo" → parcial: FALSO para 2 pares.
- Mesmo script → Dep.×agenda: 0 violações (SAN3-21 depende de 10 blocos com `frontend/`, todos terminam ≤ 55,5); duração×esforço: 0 divergências; 34 blocos = 11 G/18 M/5 P; fins de frente 57/55,5/57/59,5; fecho 72,5 → batem com §5/§9.
- Pares serializados por acaso (não declarados, sem efeito): `impound-prisma.repository.ts` (SAN3-11 28–29,5 → O6R-12), `.github/workflows/ci.yml` (AV-REAL → SAN3-10).
- Sonda de direção: `navigation.registry.ts:344-393` → Financeiro/Cobranças/Faturas/Pagamentos `status: "future"` com `finance:read`/`billing:read`/`invoices:read`/`payments:read` (órfãs) — o menu Financeiro em modo real só volta no SAN3-04 (26–31), mas SAN3-12 (5–10) e SAN3-24 (10–23) vêm antes pela trava do arquivo de menu → a prova por persona Financeiro em modo real desses dois não é possível antes de t=31; o plano não diz → nota.

## M2b — simulação da agenda sob o gate literal do porteiro (item 2)
- `node c1-sim.mjs` (ordem por frente do §6 + Dep. do §5; gate §C2.8: nenhum início antes do parecer do último merge; porteiros em série) → sem porteiro reproduz a agenda **exatamente** (34/34 inícios e fins) = 72,5 h; G=12,8 → 72,1; porteiro 0,25/0,35/0,48 h → 77,0/77,6/81,1; + reexecução de KPI 0,25/0,5/1 h por merge → 87,4/94,0/105,1; realista G57 M10 P2 → 295 h (F1 = 238 = caminho crítico; F4 191) e 308 h com porteiro 0,48 + KPI 1 → 12,3–12,8 dias.
- CI como proxy da reexecução: `gh run list --json workflowName,createdAt,updatedAt` → runs `ci` de 7,8 a 10,0 min (ex.: #385 `fix/billing-durability` 08:00:03→08:09:16) → q ≈ 0,15–0,5 h → soma porteiro+KPI ≈ 10–21 h → "≈ 10–20 h" e "≈ 85–95 h" SE SUSTENTAM; "12 a 14 dias" SE SUSTENTA (sem os atos do dono, como declarado).
- "Não cabe em 48 h": cadeia móvel em série (11→13→14→15→16→17→04b→03b→19) = 49,5 h + SAN3-10 13 h = 62,5 h > 48 mesmo com paralelismo infinito → SUSTENTADO com folga.
- Com a trava de `schema.prisma` imposta (O6R-12 depois do SAN3-20, mesma ordem de frente) → 76,5 h (+4 h); +porteiro 0,48 → 84,1 h → a faixa publicada passaria a ≈ 89–99 h; conclusão inalterada.
- Caminho crítico: melhor caso = F4 (59,5) → SAN3-10; realista = F1 (4 G) → SAN3-10 — coincide com o §9 (as frentes 57/55,5/57/59,5 e "a frente 1 tem 4 G em série").

## M1c — o registro já nomeia a lacuna de faturar pela web? (item 1)
- `git show a143d2c3:agent-orchestration/controle/pendencias.md | grep -n -i -E 'faturar|/invoice' | grep -i -E 'front|web|tela|bot|UI|menu|React'` → só l.971 (delta do faturamento, backend) e l.8469 (`P-WEB-ROTAS-SEM-PORTA`, rota /finance/invoices sem porta) → nenhuma pendência "a web não fatura OS".
- `frontend/src/modules/finance/pages/InvoicesPage.tsx` → "Os títulos a receber e o faturamento continuam disponíveis no Financeiro." (a cópia do produto afirma a capacidade); `Kpis/kpis-latest.json` `mvp_vendavel.note` → "faturamento idempotente … núcleo vendável"; `financial-title.validators.ts`/`routes.ts` sem `work_order_id` → o título manual da web não é faturamento de OS → parcial: C1-02 confirmado (bloqueia, dentro-do-bloco).

## Fecho
- Scripts e saídas de apoio movidos para `votos-SAN3/C1-apoio/` (rodar de dentro dessa pasta: `node c1-cover.mjs <PLANO>`, `node c1-overlap2.mjs`, `node c1-sim.mjs`, `node c1-times.mjs`).
- Voto: REPROVADO — item 1 NÃO CONFORME (C1-02 bloqueia; C1-01 ajuste), item 2 CONFORME COM RESSALVA, item 3 CONFORME.
