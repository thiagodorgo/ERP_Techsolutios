# C3 — validador-mestre — evidência (PR #386, conteúdo a143d2c3)
Formato: comando → saída resumida → veredito parcial.

## Terreno
- `git worktree add --detach .../j-san3-c3 a143d2c3` → `rev-parse HEAD = a143d2c33674cc2c3562de40cc9d3a95350cabe5`; `git log 15ef3fbe..0436d51c` = 10 commits, head do PR (gh 386) = 0436d51c, OPEN, base main → terreno conforme.
- `git diff --stat a143d2c3 0436d51c -- . ':!agent-orchestration/omega/juntas'` → vazio (os 3 arquivos do head estão só em omega/juntas) → conforme.
- `git diff --quiet 15ef3fbe a143d2c3 -- CLAUDE.md AGENTS.md` → intocados; norma = CLAUDE.md @15ef3fbe (§A7, §C3.5 lidos na ref).

## Item 1 — diff × regras
- `git diff --stat 15ef3fbe a143d2c3 -- src tests prisma frontend mobile .github scripts` → saída vazia → PASSA.
- `git diff --name-status 15ef3fbe a143d2c3` → 48 arquivos: 24 D (12 `jurado-06-*`/`jurado-06d-*` × 2 espelhos), Kpis/{app.js,kpis-history.json,kpis-latest.json}, controle/{aposentadoria,decisoes,pendencias,pendencias-indice}, status-geral, log-execucao, OBITUARIO, 2 pareceres do crítico, omega-pd, O6R/{REGISTRO_ACHADOS,achados.jsonl}, SAN3/{PLANO + 8 inventários} → nenhum arquivo de código; parcial OK.
- `git ls-tree 15ef3fbe .claude/agents/especialistas/` e `.agents/...` → 12 + 12 (exatamente os 6 `jurado-06-*` + 6 `jurado-06d-*`); `@a143d2c3` → 0 + 0 → remoção só por prefixo de bloco (`jurado-06-`/`jurado-06d-`), nenhum outro arquivo no diretório → PASSA.
- `gh pr view 385` → MERGED 2026-09-11T08:10:50Z, mergeCommit 15ef3fbe, headRefOid ca5fd19a; e26eb9e5 = commit de branch 2026-09-09 (head julgado do delta); 764a3b04 existe (2026-09-09 08:33 -0300) → datas do obituário §3.5 conferem.
- OBITUARIO §1.5 na ref: "Quem sepulta uma identidade nova acrescenta a linha no mesmo PR em que a junta fecha." → as 6 `jurado-06d-*` foram acrescentadas no §3.5 (#386, não #385) com a omissão declarada → dívida pré-existente paga e declarada (origem #385, ressalva 2 do porteiro).
- `kpis-latest.json` release @ref: pr=385, merge_commit=15ef3fbe…, approved_head=e26eb9e5 (base: null/null/null) → backfill feito; conferência de equivalência e26eb9e5×ca5fd19a abaixo.

- `git diff --numstat e26eb9e5 ca5fd19a -- src tests prisma frontend mobile .github scripts | wc -l` → **0** (e26eb9e5 é ancestral de ca5fd19a; o delta são só 25 arquivos de junta/KPI) → pré-condição do approved_head=e26eb9e5 PASSA.
- `node --test --import tsx tests/kpi-achados-paridade.test.ts tests/kpi-dashboard-charts.test.ts tests/kpi-dashboard-contraste.test.ts` (npm ci próprio no j-san3-c3) → **28/28 pass, 0 fail** → PASSA.
- `node scripts/kpi-freeze.mjs --check` → "kpi-freeze: em dia (snapshot 2026-09-11)." exit 0 · `node --check Kpis/app.js` → OK → PASSA.
- `node scripts/sync-agent-agents.mjs --check` → "[agents-sync] OK — 23 agentes, espelho consistente." exit 0 → PASSA.

## Item 2 — registro (medições brutas)
- Gerador rodado em cópia fora do repo (`scratchpad/c3gen/`, pendencias.md + .py extraídos com `git -c core.autocrlf=false show a143d2c3:`) → "indice: 363 cabecalhos / 352 IDs | FECHADA 105, ABERTA 258 | baldes -105 C71 B90 A97 | diferidas-materiais 14"; `cmp` gerado × commitado → **BYTE-IDENTICO** (sha256 604b7fe9…, 57604 bytes os dois) → PASSA.
- `git diff --numstat 15ef3fbe a143d2c3 -- controle/pendencias.md` → 1106 add / 58 del; as 58 linhas removidas listadas uma a uma: todas são linhas `status:`/`Estado:` ou a continuação física delas → nada apagado além de status → PASSA (preservação do valor antigo: conferência por hunk abaixo).
- Classificação base×ref com as regex do próprio gerador (`cls.py`): BASE 310 cab/299 IDs (F72/A238) → REF 363 cab/352 IDs (F105/A258); 53 cabeçalhos novos (53 IDs novos), 0 sumidos; 42 cabeçalhos existentes com linha de status mudada: **32 ABERTA→FECHADA + 10 ABERTA→ABERTA** → a reconciliar com "31 FECHADA / 52 flips / 49 ausentes" declarados.
- `cls2.py` (1ª linha de status COMPLETA, base×ref, pareada por id+ocorrência) → 52 reescritas: 32 ABERTA→FECHADA + 20 ABERTA→ABERTA(PARCIAL); + `P-O6R-ARNES-ISOLAMENTO` (`**Estado:** ABERTO` → `ABERTA (PARCIAL — …)`, que o extrator de linha perde porque o `\s*` da LINHA atravessa `\n`) = **53 linhas canônicas reescritas = 52 flips (31 F + 21 P) + `P-KPI-RECENT-CONGELADO` (fechada pelo próprio PR)**; 53 cabeçalhos novos = 49 ausentes + 4 `P-SAN3-*`; FECHADA 72→105 = 31 flips + RECENT + ROADMAP (nova, FECHADA) → números do §0 do plano CONFEREM.
- `status.diff` (status-geral.md, texto novo do PR) → "índice: 363 cabeçalhos, **103 FECHADAS, 260 ABERTAS**, 0 contraditórias" na MESMA frase que diz "72 → 105"; gerador na ref → **105 FECHADA / 258 ABERTA** → DIVERGÊNCIA dentro-do-bloco (item 3).
- `oldval.py` sobre o diff -U0 do pendencias.md → 54 hunks com remoção; **0** sem o texto antigo integral ou o marcador "Valor anterior, preservado:" → valor antigo preservado em todas as linhas reescritas → PASSA.
- `P-KPI-ROADMAP-CONGELADO` (nova, FECHADA pelo próprio PR, pend-ref L8873-8886): "teste de encerramento: guard: bloco com merge_commit no history não fica a_fazer; **as_of ≥ último merge**" × `kpis-latest.json@a143d2c3 roadmap.as_of = "2026-08-19"` (último merge 2026-09-11) e nenhum guard novo (diff sem tests/) → fechada sem cumprir o próprio critério; título da pendência é "Roadmap do painel parado em 2026-08-19" → dentro-do-bloco.
- `P-KPI-RECENT-CONGELADO` (L5524-5530): "Critério de fechamento: … a atualização não depende de alguém lembrar — ou a seção passa a derivar do history, ou um guard permanente fica vermelho … Prova por mutação" × linha nova "FECHADA — os dados … foram atualizados … A causa … fica com o B-GOV-GUARD-DERIVADOS" → metade feita marcada FECHADA (a regra 2 do próprio índice: "parcialidade nunca fecha"); tratamento desigual com os 21 PARCIAL → dentro-do-bloco.
- `gh pr view 347..386` → #360..#384 todos MERGED (25); recent lista 369/371/380/381 à parte → 21 no agregado ✓; datas de recent em UTC coerentes com mergedAt ✓; #386 OPEN (item de autoria).
- roadmap @ref: B-O6R-02 a_fazer→concluido (8 achados = os 8 de production_readiness.fechados por #371 ✓); B-O6R-07 a_fazer→parcial ✓ (SEC-003 fechado; SEC-002/004 residuais ✓). Resíduos NÃO tocados: B-O6R-06 nota "Entregue em 2026-09-07 (PR na autoria)" sem `pr` (mergeado #385 em 2026-09-11); `trilha_bloqueada.bloqueada_por` ainda lista B-O6R-06 (P-O6R-B06 "BLOQUEAVA a trilha" agora FECHADA) → texto herdado do #385 (idêntico em 15ef3fbe) = pre-existente, mas convive com o fechamento do ROADMAP-CONGELADO.
- Gerador na BASE (cópia) → 310 cab/299 IDs, FECHADA 72 / ABERTA 238; ABERTAS por balde A63 / B99 (92 IDs) / C76; 231 IDs distintos → §1 do plano ("238 linhas / 231 IDs", "63 · 99/92 · 76") CONFERE.
- `desc.cjs` sobre os 12 corpos de `15ef3fbe:.claude/agents/especialistas/` → soma `description.length` = **24897** (~24,3 KB) → aposentadoria rodada 2 CONFERE.
- Obituário §3.5: `votos/B-O6R-06-delta/C{1,2,3}-*-voto.json@a143d2c3` → `jurado` = jurado-06d-{banco…,invariante…,contrato…}, `voto: APROVADO`, head_medido e26eb9e5; `J-B-O6R-02-ciclo5.md:17` = C1 jurado-c5-arnes-catalogo-postgres APROVADO; `:78` = inspetor bloqueou por critico-c5-adversarial; os dois arquivos de voto/parecer existem → emenda do §3.3 e §3.5 CONFEREM.
- numstat docs: omega-pd 348/0, aposentadoria 35/0, status-geral 44/0, log 27/0, decisoes 130/1 (a linha "-para quem for dono daquele trabalho." re-adicionada idêntica, fim de arquivo) → aditivo.
- O6R/REGISTRO_ACHADOS: DIN-005/007 status reescritos com "Texto anterior, preservado" ✓; guard `kpi-achados-paridade` verde (executado acima).
- R3 (código @a143d2c3), 1ª passada: P-018 content-sniff.ts existe, serve-verified-file.ts:90 `attachment; filename=`, :149 nosniff, upload-gate.ts:150 sniffMimeType, fe2748c8 ancestral ✓ · P-029 TariffFormModal.tsx:157/181/194 `disabled={isEdit}`, c26467e3 ancestral ✓ · STATUS-BYPASS work-order.service.ts:1333-1339 422 cancel_via_status_forbidden, 3aad5277 ancestral ✓ · TERMINAL-GUARD assertWorkOrderNotCancelled definido :292-298 (chamadas a confirmar) · ZERO-ATOMICIDADE prisma-repo :104 updateMany + :154-155 withTenantRls ✓ (consumidor a confirmar) · REVERSE-IDEM migration :38-40 índice condicional + service :312-324 uow.run+findByIdForUpdate(original)+findActiveReversalOf dentro da tx (serializa mesmo sem o índice) + teste -db existe, 99f18403 ancestral ✓ · CLEAR-ATOMIC cheque.service :173/:218 uow.run→moveMoneyInUnit, :288 `if (!linked) throw`, teste -db existe ✓ · ENTRY-OWNERSHIP: `cheque_entry_immutable` só em repository.ts:153 (definição) + 2 comentários no service (lançador a confirmar) · CLEAR-RETRO decisoes.md:678 D-Ω4-7-COMPETENCIA-CLEAR + cheque.service :170/:215 `new Date()` ✓ · WOTS useWorkOrderTimeseries.ts:24/:55 forbidden, 308c9efb ancestral ✓ (texto na UI a confirmar) · SCALE-RBAC decisoes.md:758 D-SCALE-RBAC-PURCHASING ("o dono autorizou…"), catalog.ts:181/182/198 + grants :540-542, 0450ae9f ancestral ✓.
- R3 2ª passada (chamadores @a143d2c3): P-018 checklist.service.ts:3 e damage.service.ts:7 importam `verifyUploadContent` do upload-gate (+ attachments, mobile-evidence) → os caminhos de checklist E danos da pendência passam pelo sniff ✓ · TERMINAL-GUARD `assertWorkOrderNotCancelled(` em :60 create, :171 update, :227 delete, :347 invoice ✓ · ZERO-ATOMICIDADE work-order-financial.service.ts:250 `softDeleteAllByWorkOrder`; work-order.service.ts:1416/:1488 `zeroFinancialItems` (corpo a ler) · ENTRY-OWNERSHIP: testes `tests/cheques.test.ts:498-524` e `tests/cheque-clear-bounce-atomic-db.test.ts:134-157,374-388` afirmam 422 cheque_entry_immutable em reverse e delete (lançador no src a localizar).
- `decisoes.md@a143d2c3` l.607 **D-Ω4-C2**: "idempotência do faturamento = unique parcial `(tenant_id, work_order_id, direction) WHERE deleted_at IS NULL` — SEM competencia na chave (senão duplo-faturamento entre meses)"; l.608 **D-Ω4-C1**: "ao faturar, carimbar invoiced_at/title_id nos itens incluídos; … REJEITAR mutação/delete de item faturado (422 item_invoiced)"; migration add_invoicing:25-27 = o índice da D-Ω4-C2. REGISTRO-SAN3-CONFLITOS item 1 diz "P-Ω4-3-REFATURAR-DELTA × **D-Ω4-C1**" e descreve o índice sem nomear a D-Ω4-C2; plano §8.7 escreve "D-Ω4-C1 (um título ativo por OS e direção)" → atribuição errada: "um título ativo por OS e direção" é a D-Ω4-C2 → REGISTRO NÃO fiel (item 1 dele).
- l.690-692 **D-Ω4-7-NO-TITLE**: "o registro de cheque é INDEPENDENTE de título (title_id FORA de escopo)… risco de PROCESSO — fora do escopo do backend deste bloco" → REGISTRO item 2 fiel (paráfrase de pontuação dentro das aspas: "—"→",") ✓.
- Origem do 103/260: gerador em `544ab67f` → 363 cab, **FECHADA 103 / ABERTA 260** (a frase era verdadeira ali; `git log -S` a data em 544ab67f); em `a143d2c3` → 105/258, e a frase segue 103/260 → número defasado por correção posterior do próprio PR (a v4 fechou RECENT e ROADMAP e editou a mesma frase para "72 → 105" sem o parêntese) → dentro-do-bloco.
- inventário AUSENTES §1: 140 linhas = 37 JA REGISTRADA + 8 PARCIALMENTE + **58 AUSENTE (linhas) → 50 IDs** + 24 SUPERADA + 13 NAO-PENDENCIA ✓; o §1 do plano soma "50" (IDs) com as outras classes (linhas) → 132 ≠ 140 impresso → nota de unidade.
- achados.jsonl: DIN-005/007 `fechado_por` reescrito com "Texto anterior, preservado" ✓ · 9 linhas "emenda (inventário SAN3, fatia AUSENTES §2c)" em entradas existentes (plano: 8 parciais) → nota.
- R3 fecho: STATUS-BYPASS mobile `work_order_models.dart:71-92` `allowedTransitions` sem `cancelled` em estado nenhum ✓ · ZERO-ATOMICIDADE `work-order.service.ts:1488` importa o serviço financeiro e usa o `zero` → `work-order-financial.service.ts:250 softDeleteAllByWorkOrder` ✓ · ENTRY-OWNERSHIP `financial-entry.service.ts:38` importa e `:156` mapeia `chequeEntryImmutable: chequeEntryImmutableError`; testes in-memory e -db afirmam 422 em reverse E delete ✓ · WOTS `frontend/src/pages/DashboardPage.tsx:216-217` `EmptyState title="Acesso não permitido"` quando `forbidden` ✓ → **as 11 FECHADAS nominais (R3) CONFEREM por presença no código da ref** → PASSA.
- PARCIAL (N=4): SLA-FIELD schema.prisma:2354 `sla_due_at` + adapter:313 `slaDueAt` ✓ / WorkOrderDelayBadge ainda por `scheduledFor` ✓ · 2A-NITS financial-title.repository.ts:56 `PERIOD_WRITE_BLOCKING_STATUSES = ["closing","closed"]` ✓ · ACCOUNT-ACTIVE entry.repository:66-67 e cheque.repository:60-61 `account_inactive` ✓ / title.service:353 "independe de is_active" ✓ · GATING-MOCKSHELLS DispatchConsole:9 TENANT_NAME fixo + :71-74 `can(...)`, Pedidos:31-33 ✓ / TablePage 0 `usePermissions`, Reports usa TablePage+ROWS ✓ → as 2 metades de cada PARCIAL conferem → PASSA.
- AUSENTES (N=6): SEGURANCA-FABRICADA PlatformSettingsPage.tsx:8/:43/:44 `<Toggle on />` ✓ · FILA-OS work_order_repository.dart:474 approvalRequest, :637 unableToStart; replay lista create/statusUpdate/assign ✓ · ESTOQUE-TECNICO 8 SKUs literais em prestador_repository.dart:38-87, `technician/stock` em src = 0 ✓ · MINHAS-OS home_screen.dart:120-123 filtra só tenantId ✓ · JOBS-PLACEHOLDER job.registry.ts:47-49/:53-55 ✓ · BAIXA-SEM-TELA: `financial-account` em frontend/src = 0 ✓ mas `git grep -n '/pay' a143d2c3 -- frontend/src` = **34** linhas (a prova da entrada diz "= 0") → a conferir.
- `git grep -h -o "/pay[A-Za-z_-]*" a143d2c3 -- frontend/src | sort | uniq -c` → 12 /payable · 14 /payable-source · 5 /payments · 1 /payee · 2 /payload(s); **0** chamadas a `/financial-titles/:id/pay` → a pendência BAIXA-E-CONTA-SEM-TELA é real; a prova escrita ("`grep -rn "/pay" frontend/src` = 0", repetida no §4.1 item 28 como "conferido") é literalmente falsa → nota dentro-do-bloco.
- Resíduo do guard dos painéis: entradas ABERTAS que citam `recent`/`roadmap` → só `P-O6R-B04` (alheia) e `P-KPI-PAINEL-NAO-RENDERIZA-SUMMARY` (critério = renderizar summary/description, não frescor de recent/roadmap); a única ABERTA com dono `B-GOV-GUARD-DERIVADOS` é `P-SAN3-FLIPS-DE-REGISTRO` (L8306-8313), cujo teste é sobre linha de status × fechamento de achado, não sobre os painéis → o "guard que falha quando recent/roadmap ficam atrás do history" não está em pendência aberta nenhuma; com RECENT e ROADMAP FECHADAS ele some do "o que está aberto?" (a classe que a junta do SAN2-1 reprovou) → REPROVA item 2.
- `git diff -U0 544ab67f a143d2c3 -- status-geral.md` → a v4 EDITOU a mesma linha ("73 → 103" → "72 → 105 contra a base — 31 flips e as 2 pendências do painel…") e manteve "(índice: 363 cabeçalhos, **103 FECHADAS, 260 ABERTAS**…)" → defeito nascido numa correção do próprio PR (§C7.4-bis); o §13 (CR2-06) afirma "status e log com FECHADAS 72 → 105" → REPROVA item 3.
- history B-O6R-06 @ref: pr 385 · mc 15ef3fbe… · ah e26eb9e5; `backfill_note` registra "snapshot_date 2026-09-08 -> 2026-09-11" com o motivo → redatação declarada ✓. `J-B-O6R-06-delta.md:1-6` → head do PR no ato = e26eb9e5; C1/C2/C3 head_medido e26eb9e5 ✓.
- `sync_replay_service.dart:580-586` → `b107BackendWorkOrderActionTypes = {create, statusUpdate, assign}` → approvalRequest/unableToStart fora ✓.
- `P-Ω4-3-REFATURAR-DELTA` (pend-ref L963-967): "A idempotência do faturamento é por (tenant_id, work_order_id, direction) … o 2º POST /invoice dá 409 already_invoiced … Item novo pós-faturamento permanece editável (invoiced_at NULL); só os já carimbados travam (item_invoiced 422)" → a própria pendência põe o obstáculo na idempotência (D-Ω4-C2) e mostra a D-Ω4-C1 compatível com faturar o delta; `grep D-Ω4-C2` em pendencias.md e PLANO_SAN3.md @ref → 0 ocorrências → o conflito real não está registrado → REPROVA item 2.
- `git show 0436d51c:…/00-critico-adversarial-r2.md` l.19-21 (CR2-01) → mediu o índice `add_invoicing:25-27` e `work-order-financial.service.ts:357-359 findActiveByWorkOrder → already_invoiced`, e escreveu "grep no plano: … D-Ω4-C1 0" → a atribuição à D-Ω4-C1 nasce no crítico e foi adotada pelo PR sem ler decisoes.md:607 (origem do C3-01; continua dentro-do-bloco: o REGISTRO é deste PR). l.49 (CR2-06) → apontou exatamente a frase "FECHADAS 73 -> 103" do status-geral (origem do C3-02).
- Limpeza: `git worktree remove --force …/j-san3-c3` → "removido"; `git worktree list` → só main, gov-descuido, gov-elenco, san3. Terreno: existe o diretório `.claude/worktrees/san2-r` em disco SEM worktree registrado → resíduo alheio, reportado, não varrido.

## Veredito
- Item 1 APROVADO · Item 2 REPROVADO (C3-01, C3-03) · Item 3 REPROVADO (C3-02) → **REPROVADO** (3 bloqueia dentro-do-bloco).
