# C1 validador-mestre — evidência (2ª instância) — B-SAN3-01 / PR #387

- Modelo: claude-opus-5[1m] (Opus 5)
- Objeto: bb540fb3 · base origin/main@02bd7dab · head PR 74f3f7c9
- Início: 2026-09-18
- A 1ª instância caiu (429) sem medição; nada dela é insumo.

## Terreno
- `git worktree add --detach .../j-bsan301-c1 bb540fb3` → HEAD `bb540fb3139031f40eea93ec5d7a47e987167bc2`, `status --porcelain` = 0 linhas, `core.autocrlf=true`.
- `npm ci` → ec=0 · `npm --prefix frontend ci` → ec=0 (próprios, sem junction).
- `git merge-base bb540fb3 origin/main` = `02bd7dab…` = `origin/main` (base bate). Delta `bb540fb3..74f3f7c9` = 10 `A` sob `agent-orchestration/omega/juntas/` (zero código) — R3 confirmada.

## Item 1 — diff × plano (medido em `git diff 02bd7dab bb540fb3`)
- `--name-only -- src prisma mobile infra .github package-lock.json frontend/package-lock.json migrations .env RBAC_MATRIX.md CLAUDE.md AGENTS.md` → **0** arquivos.
- `frontend/src` fora de `modules/work-orders/**` e de `operations/dispatches/{dispatches.service.ts,pages/OperationsDispatchesPage.tsx}` → **0**. `frontend/src/mocks` → **0**.
- Fora de `frontend/src|agent-orchestration|Kpis` (9): 4 `D` `jurado-san3c2-*` (`.claude`+`.agents`) = dívida A2 do #386 (emenda 2 (e′)); `frontend/package.json` (§5 do plano); `frontend/tests/work-orders-honest-errors.test.tsx` (teste novo, permitido); `frontend/tests/work-orders-row-actions.test.tsx` (emenda 2 (f)); `tests/e2e/critical-flows.spec.ts` (permitido); `tests/kpi-dashboard-charts.test.ts` (emenda 2 (e)).
- `frontend/package.json`: script `test:smoke` ganha exatamente `['tests/work-orders-honest-errors.test.tsx']`, remove `[]`; resto do JSON igual (python json.loads).
- `OperationsDispatchesPage.tsx` numstat `16 2`: estado `detailError`, `loadDetail` (mantém o item, `setDetailError(...)` quando `detail.dispatch` é nulo) e o `<div role="alert" data-state="error"><Alert …>` no painel. Nada fora de `loadDetail` + a renderização da mensagem (emenda (h)).
- `work-orders-row-actions.test.tsx`: 3 hunks, só o corpo do `Response` de F1/H1/H5 (+ comentário); nenhuma linha `assert` alterada (emenda (f)).
- `critical-flows.spec.ts`: 1 hunk `@@ -193,31 +193,156 @@` (o caso "com fallback seguro" → E1–E3 + `workOrderCodesOf`/`ensureWorkOrderId`); auxiliares de login intocados.
- `kpi-dashboard-charts.test.ts`: 1 hunk `@@ -481,3 +481,28 @@` (caso novo da rodada SAN3).
- **Veredito parcial item 1: VERDE** (nenhum arquivo fora do plano/emendas; nada proibido).

## Item 3 — registro (parcial 1)
- `node scripts/sync-agent-agents.mjs --check` (worktree em bb540fb3) → `[agents-sync] OK — 23 agentes, espelho consistente.` ec=0; `git ls-tree -r bb540fb3 .claude/agents .agents/agents | grep -c san3c2` → 0; `especialistas/` nos dois espelhos → 0 arquivos. **A3 paga.**
- `git cat-file -t 764e175d8d2ab030da34cd81bf8f18c6576645a4` → commit; `gh pr view 386` → `state MERGED · mergeCommit 02bd7dab2ffa29999920da8b7da345b6a5958b67 · headRefOid 764e175d8d2ab030da34cd81bf8f18c6576645a4` = os hashes completos gravados em `release.backfill_note` (latest) e `backfill_note` da entrada B-SAN3-01 (history json) e no fim da entrada do history.md. **A2 paga.**
- `status-geral.md` l.4383 emendada: "54 bloqueantes … *(v5 — 56 após a opção B do dono, §15 do plano; emenda de 2026-09-17, ressalva A1 do porteiro do #386)*". **A1 paga.** Parecer do porteiro versionado em `votos/SAN3-plano-opcao-B/00c-porteiro-pos-merge-386.md`; tabela da rodada 3 em `aposentadoria-especialistas.md`.
- `node scripts/kpi-freeze.mjs --check` → `em dia (snapshot 2026-09-17)` ec=0 · `node --check Kpis/app.js` ec=0 · `git diff --check 02bd7dab bb540fb3` ec=0.
- Índice: gerador do blob (`git -c core.autocrlf=false show bb540fb3:…`) numa cópia em `scratchpad/c1-indice-regen` (Python 3.13.14) → `381 cabecalhos / 370 IDs | FECHADA 105, ABERTA 276 | A 111 B 95 C 70`; `cmp` com o blob → ec=0, sha256 `1ff574e4692d3286…` igual. **Byte-idêntico.** `P-008` na seção FECHADAS (l.365); as 10 `P-SAN3-01-*` + `P-SAN3-PAINEL-TRILHA-DO-GATE` indexadas com dono "sim".
- `kpis-history.json`: 158 → 159 entradas, prefixo byte-a-byte igual (append-only); última = `B-SAN3-01`, pr 387, merge/approved `null`, `backend_tests 2996/2998`, `frontend_smoke_tests 1173`, `flutter 864`, `blocks 164`.
- `kpis-latest.json` (diff estrutural): `version B-O6R-06→B-SAN3-01`, `snapshot 2026-09-17`, `release.pr 387`, `merge_commit/approved_head null`; métricas `frontend_smoke 1126→1173`, `backend 2995/2997→2996/2998`, `blocks 163→164`, `flutter 864` carregado com nota, `mvp_demo 99`/`mvp_vendavel 88` intocados; `roadmap` só muda `as_of 2026-09-11→2026-09-17`.
- **Texto defasado dentro do próprio registro de KPI (achado A-C1-01, ajuste):** `release.summary` diz "Backend REEXECUTADO …: 2995/2997 (…), mesma forma e número" e a `notes[]` nova diz "npm test 2995/2997", enquanto a métrica publicada é 2996/2998; `kpis-history.md` (entrada B-SAN3-01) diz na divergência (1) que "a emenda (c) manda deixar as dívidas do #386 para o `B-SAN3-04a` — o `roadmap` **não** mudou neste PR" (o `roadmap.as_of` mudou e a mesma entrada, no fim, diz que o #386 foi pago aqui), "7 ABERTAS com dono" (são 10 novas `P-SAN3-*` abertas) e "Índice pelo gerador: 378 cabeçalhos / 367 IDs, 105 FECHADAS, 273 ABERTAS" (o gerador dá 381/370/105/276). Números das métricas corretos; a prosa ficou no estado do commit C/D.

## Item 2 — bateria (parcial 1: backend)
- Contêineres próprios: `j-bsan301-c1-pg` (`postgres:16`, `127.0.0.1:56401`, base `erp_jc1`) e `j-bsan301-c1-redis` (`redis:7-alpine`, `127.0.0.1:56402`); portas fora das faixas excluídas (`netsh … excludedportrange`: 56500-57306 etc.) e sem listener antes. `case "$DATABASE_URL"` conferido antes de qualquer comando de banco.
- `DATABASE_URL=postgresql://postgres:***@127.0.0.1:56401/erp_jc1 REDIS_URL=redis://127.0.0.1:56402` exportadas; `npx prisma generate` ok; `npx prisma migrate deploy` → "All migrations have been successfully applied"; `migrate status` → "107 migrations found … Database schema is up to date!"; `_prisma_migrations` finished = 107.
- `npm test` (runner: `CORE_SAAS_PERSISTENCE=memory — padrão do runner`, node v20.19.5, head bb540fb3, 09:37:12→09:42:14) → `[run-backend-tests] 283 arquivo(s) · 2998 teste(s) · pass 2996 · fail 0 · skipped 2` · `RUNNER_EXIT=0` (log `scratchpad/c1-backend-bb540fb3.log`). **= 2996/2998 publicado. VERDE.**
## Item 2 — bateria (parcial 2: frontend, KPI e vermelho-controle)
- No objeto `bb540fb3` (`VITE_USE_MOCKS` não exportada): `npm --prefix frontend run check` ec=0 (`tsc -b --noEmit`) · `npm --prefix frontend run build` ec=0 (`✓ built in 21.78s`) · `npm --prefix frontend run test:smoke` ec=0 → `# tests 1173 · pass 1173 · fail 0 · skipped 0` (**= 1173/1173 publicado; 1126 + 47**).
- `node --test --import tsx --test-reporter=tap tests/work-orders-honest-errors.test.tsx` (frontend/) → `# tests 47 · pass 47 · fail 0`, IDs L1-5, C1-7, D1-5, T1-3, M1-6, X1-9, R1-6, P1-4, G1, S1 (= 47, D-6 confirmada).
- `node --test --import tsx tests/approval-frontend-contract.test.ts` → 1/1. Os 3 guards de KPI (`kpi-achados-paridade`, `kpi-dashboard-charts`, `kpi-dashboard-contraste`) → `# tests 29 · pass 29 · fail 0` (charts 17).
- **Mutação do rótulo SAN3:** `sed` apaga `["B-SAN3", "SAN3"],` (app.js l.300) → `kpi-dashboard-charts` = `not ok 17 - painel: a rodada SAN3 tem barra própria …` · `# tests 17 · pass 16 · fail 1` ec=1; `git checkout -- Kpis/app.js` → 17/17 e árvore limpa. **Vermelho-controle do caso novo confirmado.**
- **Vermelho-controle do bloco no commit A** (`git checkout --detach 7b2ab102` no MEU worktree; `work-orders.state.ts` ausente): `# tests 47 · pass 4 · fail 43` ec=1; verdes só L4 D5 T3 X7; vermelhos os 43 listados pelo dev. **Contagem confere (43/47).**
- **Forma de cada vermelho (TAP):** comportamento em L1-3, L5, D1-4 (`code: 'OS-000101'`), T1-2, M1-6, X1-6, X8-9, G1, S1 = **26**; `ERR_MODULE_NOT_FOUND` (handler/state inexistentes) em **C1-C7** e R1-R6, `Element type is invalid … undefined` em P1-P4 = **17**. O registro diz "31 de comportamento + 12 de módulo": C2-C6 foram contados como comportamento, mas no commit A caem pelo import do handler antes de qualquer asserção.
- **Contraprova de comportamento para C2-C6** (medição própria): no commit A, só o handler do objeto copiado para `frontend/src/modules/work-orders/` sobre o service do head-base, `--test-name-pattern='^\[C[1-7]\]'` → `pass 2 (C1, C7) · fail 5`: C2/C3/C5/C6 `navigations expected 0 actual 1` (o service antigo devolvia `OS-FALLBACK` e o handler navegava), C4 `Missing expected rejection`. **Os testes do create pegam o defeito por comportamento.** Handler removido, worktree de volta a `bb540fb3`, `status --porcelain` = 0.
- **Veredito parcial item 2: VERDE**, com a nota A-C1-02 (classificação 31/12 imprecisa no registro: 26 de comportamento + 17 de módulo/export medidos no commit A; o comportamento de C2-C6 provado à parte).

## Item 2 — mutação do G1 repetida pela C1 (emenda 2 (n), D-11)
- Base (objeto): `--test-name-pattern='^\[G1\]'` → pass 1 · fail 0.
- **mutA (código):** `sed` troca as 6 linhas `return requireWorkOrder(response);` de `work-orders.service.ts` por `… ?? getMockWorkOrderDetail("new");` → G1 **fail 1**, `work-orders.service.ts: getMock* fora de isMockMode()` com `[74,106,133,144,174,234]`. Restaurado.
- **mutB (só comentário):** linha `// ?? getMockWorkOrderDetail(id) — so comentario` inserida na l.66 → G1 **pass 1** (comentário não alcança o mock — D-11 como declarada). Restaurado.
- **mutC (despachos):** 1ª `return requireDispatch(response);` (l.96) de `dispatches.service.ts` → `… ?? getMockDispatchDetail(dispatchId);` → G1 **fail 1**, `dispatches.service.ts … [96…]`. Restaurado; `status --porcelain` = 0; G1 de novo pass 1.
- **Veredito parcial: VERDE** (o G1 pega a reintrodução do `?? getMock…` nos dois services e ignora linha só de comentário).

## Item 3 — censo regenerado pela C1 (briefing §3)
- Script próprio `scratchpad/c1-censo/censo.mjs` (varre `frontend/src/**/*.{ts,tsx}` fora de `*.test.*`; corpo balanceado de `catch {}`/`.catch()` e `?? <expr com mock|fallback|seed|demo>`), rodado no MEU worktree em `02bd7dab` e em `bb540fb3` (checkout detached próprio, volta ao objeto com `status` = 0). Base: `FILES=599 OCCURRENCES=381 … ENTITY_NO_RETHROW=18` (meu critério de corpo é mais largo que o do plano — 381 × 350; a contagem total não é comparável, a classe A é).
- Classe A por grep de fabricante (`getMock*(`, `OS-FALLBACK`, `fallback-created`, `mock*Items[`/`.find`) nas duas fronteiras (`work-orders.service.ts`, `dispatches.service.ts`), fora de linha `import`/`//` — log `scratchpad/c1-censo/fronteira-getmock.txt`:
  - **02bd7dab:** fora de `isMockMode()`: work-orders l.28, 31, 47, 50, 62, 69, 83, 110, 121, 151, 211, 238, 240 (13 = #1-#13 do plano) · dispatches l.35, 38, 57, 64, 78, 89, 100 (7 linhas = #14, #15, #16×2, #17, #18×2). **Bate com as 18 entradas #1-#18 do §2.2**; a #19 é `work-orders/repository.ts:11` (`?? mockWorkOrders[0]`, também achada pelo meu script).
  - **bb540fb3:** toda ocorrência restante está numa linha `if (isMockMode())` ou dentro do bloco `if (isMockMode()) {` (l.159/186/215 sob as aberturas l.157/185/214) — o G1 verde confirma. **Fronteira fechada: 18 de 18.**
- Consumidores do contrato novo (`getWorkOrderTimeline`, `getWorkOrderFromApi`, `createWorkOrder`, `getDispatchFromApi`) em `frontend/src`: só `useWorkOrderDetail.ts:38-39`, `work-orders-create.handlers.ts:21`, `OperationsDispatchesPage.tsx:69` — todos dentro da fronteira; `tsc -b` verde.
- **Fora da classe do bloco, achado pela varredura de fabricante em todo `frontend/src`:** `frontend/src/pages/LogisticsPage.tsx` é ROTEADA (`App.tsx` `path="/logistics"` sob `PermissionGuard logistics:dispatch`) e `modules/logistics/repository.ts:4-10` devolve SEMPRE `mockAssets`/`mockQueues`/`mockWorkOrders` (`frontend/src/mocks/**`), sem `isMockMode()` nem chamada ao backend; origem `fb0ea65b` (2026-05-26, `git log --diff-filter=A`). No registro, `/logistics` só aparece em `P-WEB-ROTAS-SEM-PORTA` (falta de porta), não como tela que exibe OS inventada (grep `LogisticsPage|getLogisticsPanel|logistics/repository` em `pendencias.md`/`PLANO_SAN3.md`/`inventario/` → 0). Não é `catch` (fabrica sempre, não "quando o backend falha") → fora da propriedade do censo do plano; classe "tela de ficção" do `B-SAN3-06a`. **Achado A-C1-03 (nota, pre-existente).**
- Demais fabricantes fora da fronteira conferidos: `EventProvider`/`pollingClient` (`mockEvents` a cada 9 s) já é `P-WEB-EVENTBUS-MOCK`; `dashboard/repository.ts:41`, `navigation.service.ts:8`, `context/repository.ts:37`, `cloud-billing.service.ts` (`shouldUseMocks()`), `auth.service.ts:39` estão atrás do interruptor de mock; `useNavigationMenu.ts:49` usa o menu mock no erro só para calcular caminhos escondidos (`AppShell.tsx:43`, `governedPaths` vazio → nada escondido), não exibe dado.
- **Veredito parcial: VERDE** para a fronteira; 1 nota pre-existente (A-C1-03).

## Item 2 — e2e do §8.1 (cópia avulsa + spec rastreado)
- Identidade por função (sha256 de cada bloco balanceado, CR removido): cópia (`74f3f7c9:…/apoio/e2e-copia-avulsa.spec.ts`, 195 l.) × rastreado (`bb540fb3:tests/e2e/critical-flows.spec.ts`, 574 l.): E1 `99d58ac0c1ec` IGUAL · E2 `fc2aea18962d` IGUAL · E3 `7c8f83e29458` IGUAL · `workOrderCodesOf` IGUAL · `ensureWorkOrderId` IGUAL · `enableWorkOrdersFrontendContext` IGUAL · `loginAndActivateContext` DIFERE (o login ajustado: placeholders + "Entrar" + "Acessar"; no rastreado chama `loginAsTenantAdmin` com `getByLabel("Tenant ID")`). Subconjunto: 7 auxiliares e 10 casos só no rastreado. **Cópia = subconjunto idêntico por função (R6).**
- `npm run db:seed` com `DATABASE_URL=postgresql://postgres:***@127.0.0.1:56401/erp_jc1` → `SEED_EXIT=0`; `admin.demo@example.com` presente; `work_orders` = 0.
- `npx playwright test -c playwright.config.ts tests/e2e/_jc1-copia.spec.ts --reporter=list` (`E2E_API_PORT=3401`, `E2E_FRONTEND_PORT=5401` livres antes, `VITE_USE_MOCKS=false` pelo config, `CORE_SAAS_PERSISTENCE=prisma` pelo config, `REDIS_URL=redis://127.0.0.1:56402`) → `ok 1 E1 (39.1s) · ok 2 E2 (11.2s) · ok 3 E3 (8.2s) · 3 passed (1.3m) · EXIT=0` (log `scratchpad/c1-e2e/e2e-copia.log`). E1 exercitou o ramo VAZIO (0 OS no banco antes). **VERDE 3/3.**

## Item 3 — registro (parcial 2)
- Origem das pendências do §3.4 (blame em `02bd7dab`): `QuoteTab.tsx:85-87` → `5c5571b6` (2026-07-15) · `profiles.service.ts:95-99` → `11a1f533` (2026-07-26) · `useServiceQuoteReferences.ts:50-61` → `42522f95` (2026-07-13) · `DispatchCreateForm.tsx:32-42` e `OperationsDispatchesPage.tsx:98-102` ("Dados demonstrativos") → `5aa14ec8` (2026-06-10) · `repository.ts`/`pages/WorkOrdersListPage.tsx` nascem em `fb0ea65b` (2026-05-26) · `AppShell.tsx:80` `setUnread(0)` → `1a76c913` (2026-07-02), mas `AppShell.tsx:92-93` `setPendingApprovals(0)` → `78bbf4f9` (2026-07-09, #153) — a pendência `P-SAN3-01-SHELL-BADGES-ZERO-NO-ERRO` cita só `1a76c913` (nota A-C1-04: origem parcial; ambas anteriores ao bloco, a pré-existência se sustenta). Todas as datas < 2026-09-13 (início do bloco). **Datas conferem.**
- Donos: `B-SAN3-06a` (FORMS-SEM-CATCH, ALERTA-DADOS-DEMONSTRATIVOS), `B-SAN3-08` (ORCAMENTO-*), `B-SAN3-10` (E2E-LOGIN), `B-SAN3-KPI-TRILHA` (PAINEL), "fila pós-gate" (JURISDICAO, OS-LEGADO-MORTO — sugestão `B-WEB-FAXINA-OS-LEGADO`, SHELL-BADGES, CREATE-INVALID-DATE) — construto do §7.3 do `PLANO_SAN3.md` (o parecer do porteiro do #386 o aceita). `P-SAN3-01-DESPACHO-DETALHE-FABRICADO` nasce FECHADA (emenda (a)).
- `P-SAN3-01-CREATE-INVALID-DATE-MENSAGEM`: backend `work-order.validators.ts:120` emite `400 WORK_ORDER_INVALID invalid_date`; a tabela de `createErrorMessage` (nascida neste bloco) não trata → `ApiError.safeMessage` genérica. O registro declara escopo misto ("pre-existente quanto ao contrato; a tabela nasceu no B-SAN3-01"). Pela §C7.1-ter(a) a C1 declara **dentro-do-bloco, gravidade nota** (mensagem honesta, nada fabricado, digitado preservado; data malformada não sai do `<input type=date>` da tela) — já registrada com dono.
- Pós-commit B (`git diff --name-only a97a1351 bb540fb3 -- frontend src tests`) → só `tests/e2e/critical-flows.spec.ts` e `tests/kpi-dashboard-charts.test.ts`: o smoke 1173 medido pelo dev em `a97a1351` vale para o objeto (e eu o re-medi no objeto); o backend ganhou 1 caso em `3c763087` e o 2996/2998 é do `10eb7049` (sem código depois) — **coerente**.
- PR #387 (`gh pr view`): OPEN (draft), head `74f3f7c9`; `gh pr checks 387` → 7/7 `pass` (authority-portal, backend 6m48s, backend-postgres, docker, flutter, frontend, owner-portal). Corpo do PR: 47 casos/43 vermelhos, smoke 1126→1173/1173, backend 2996/2998 — **igual ao medido**.
- `roadmap` × history (script próprio): 12 blocos; com `merge_commit` hexa no history só B-O6R-01 (0a39824), -02 (99f1840), -05, -06, -07 (07a dc8168b + 07b fe2748c8) — todos `concluido`/`parcial`; nenhum `a_fazer` com merge; `as_of 2026-09-17` ≥ último merge `02bd7dab` (2026-09-13). **Coerente.**
- Higiene no diff de `frontend/src`: sem `console.log`, sem `TODO/FIXME`; strings técnicas só em identificadores/`data-state`.

## Item 2 — e2e rastreado (R6 — a C1 re-mede e declara)
- `npx playwright test -c playwright.config.ts tests/e2e/critical-flows.spec.ts --reporter=list` no objeto, mesmo terreno (cluster `j-bsan301-c1-pg`, seed aplicado, API 3401, web 5401, `REDIS_URL=redis://127.0.0.1:56402`) → **13 failed · 0 passed · EXIT=1**, cada caso ~45 s (timeout de teste); 13× `locator.fill: Test timeout … waiting for getByLabel('Tenant ID')` (+1 `page.waitForResponse` no do admin da plataforma). E1/E2/E3 do rastreado (l.206/240/292) morrem no mesmo login, antes da OS (log `scratchpad/c1-e2e/e2e-rastreado.log`).
- Evidência de origem (própria): `d5a4ed43` (2026-07-02 13:48 -0300, #111) remove `<Input label="Tenant ID" …/>` de `frontend/src/pages/LoginPage.tsx`; `git grep 'Tenant ID' 02bd7dab -- frontend/src` → 0 arquivos; o spec na BASE (`02bd7dab`) já pede `getByLabel("Tenant ID")` nas l.96/312/321. Hash por bloco base × objeto: 19 blocos iguais (inclusive `loginAsTenantAdmin`, `loginAsPlatformAdmin`, `activateFirstContext`), só o caso "com fallback seguro" saiu e E1-E3 + `workOrderCodesOf`/`ensureWorkOrderId` entraram. **A C1 declara `pre-existente`** (a classe nasce em 2026-07-02, 2,5 meses antes do bloco; o bloco não tocou os auxiliares de login) — já é `P-SAN3-01-E2E-LOGIN-DEFASADO`, dono `B-SAN3-10`, ALTA. Não reprova.
- O verde de E1-E3 vem da cópia avulsa (3/3, acima): mesmo corpo por hash, login ajustado.

## Limpeza
- `git worktree remove --force .claude/worktrees/j-bsan301-c1` → ec=0; `git worktree list | grep -c j-bsan301-c1` → 0.
- `docker rm -f j-bsan301-c1-pg j-bsan301-c1-redis` → ec=0; `docker ps -a | grep -c j-bsan301-c1` → 0. Portas 3401/5401 sem listener.
- `bsan301`: `status --porcelain` = 0, HEAD `74f3f7c9` (intocado). Nada na árvore principal nem na base viva. Resíduo alheio visto e não tocado: `j-bsan301-c2` (cadeira C2), `san2-r`.

## Veredito
**APROVADO** — 0 bloqueia · 1 ajuste (A-C1-01, dentro-do-bloco) · 5 notas (A-C1-02/04/05 dentro-do-bloco; A-C1-03/06 pre-existente). Voto: `C1-validador-mestre-voto.json`. Estado deste arquivo: FINAL.
