# R-GOV-PORTEIRO-PRE-MERGE - ciclo 2 - dossie dos achadores

> **Relatorio de ACHADORES** (D-JUNTA-SEPARACAO-DE-PAPEIS-TODO-FLUXO, §C7.4-bis). Descreve defeito,
> evidencia executada e a propriedade que falta. **Nenhum achador propoe correcao.** O plano do ciclo e
> `agent-orchestration/omega/planos/GOV-PORTEIRO-PRE-MERGE-ciclo2-plano-v1.md`, escrito por alcada separada.

**Arranjo da varredura:** 84 agentes - 7 dimensoes achadoras independentes, cada achado atacado por um
cetico instruido a derruba-lo com execucao, e um consolidador separado.

| | |
|---|---:|
| achados brutos | 76 |
| sobreviveram ao cetico | 40 |
| derrubados pelo cetico | 21 |
| perdidos (cetico morreu no limite de sessao) | 15 |

**Sobreviventes por gravidade:** bloqueante 5 - alta 16 - media 15 - baixa 4

## Os dois exploits executados

1. Um achador **produziu um `LIBERADO` valido com um `node -e`**, sem nenhum modelo envolvido.
2. Um achador **mutou o espelho de um guardiao para "aprove sempre" e o `--check` ficou VERDE**.

Ambos viram caso de teste permanente no plano do ciclo 2 (F-E prova 1 e F-A prova 1).

## Achados sobreviventes

### 1. [BLOQUEANTE] O recibo Sol/ultra do porteiro é AUTO-DECLARADO: produzi um `LIBERADO` válido com um `node -e`, sem nenhum modelo envolvido

- **classe:** `b-teatro` - **dimensao:** `espelho`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:89` - `tests/porteiro-pre-merge-governance.test.ts:9` - `CLAUDE.md:238` - `AGENTS.md:265`

### 2. [BLOQUEANTE] O gate executável do porteiro só aceita runtime=codex e model=gpt-5.6-sol: nenhum porteiro do Claude Code consegue produzir atestado válido, e o merge é o único caminho autorizado

- **classe:** `a-inexecutavel` - **dimensao:** `modelo`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:89` - `scripts/merge-authorized-pr.mjs:21` - `scripts/merge-authorized-pr.mjs:23` - `.claude/agents/porteiro-pos-merge.md:5` - `CLAUDE.md:239` - `CLAUDE.md:451`

### 3. [BLOQUEANTE] A independência do porteiro pré-merge é auto-declarada: o gate nunca cruza o agentId do porteiro com as identidades da junta — porteiro com o mesmo agentId do planejador OU do desenvolvedor libera o merge

- **classe:** `b-teatro` - **dimensao:** `lei-papeis`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:90` - `scripts/porteiro-pre-merge.mjs:69` - `scripts/porteiro-pre-merge.mjs:44` - `CLAUDE.md:355`

### 4. [BLOQUEANTE] `.filter(Boolean)` faz a junta que OMITE `planner`/`developer`/`origin` passar no gate: basta não nomear a alçada para nunca colidir com ninguém

- **classe:** `g-degradacao-silenciosa` - **dimensao:** `lei-papeis`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:44` - `scripts/porteiro-pre-merge.mjs:45` - `scripts/post-merge-finalize.mjs:13` - `scripts/post-merge-finalize.mjs:14` - `CLAUDE.md:356`

### 5. [BLOQUEANTE] A independência do porteiro nunca é conferida contra a junta — o código só confere contra a lista que o próprio porteiro escreveu, e o teste que diz cobrir isso passa verde

- **classe:** `b-teatro` - **dimensao:** `lei-porteiro`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:90` - `scripts/merge-authorized-pr.mjs:24` - `tests/porteiro-pre-merge-governance.test.ts:13` - `tests/porteiro-pre-merge-governance.test.ts:14` - `scripts/post-merge-finalize.mjs:13` - `CLAUDE.md:233` - `CLAUDE.md:353`

### 6. [ALTA] O guard do espelho de agentes é não-recursivo: reporta "23 agentes, espelho consistente" enquanto existem 25 — `especialistas/` fica fora, inclusive o guardião de interoperabilidade de modelos

- **classe:** `g-degradacao-silenciosa` - **dimensao:** `espelho`
- **arquivos:** `scripts/sync-agent-agents.mjs:79` - `scripts/sync-agent-agents.mjs:27` - `tests/agent-model-routing.test.ts:8` - `.claude/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md:1`

### 7. [ALTA] CLAUDE.md ordena ao Claude Code um modelo que o Claude Code não invoca, e afirma que o frontmatter que ele cita fixa esse modelo — o frontmatter diz outra coisa

- **classe:** `a-inexecutavel` - **dimensao:** `modelo`
- **arquivos:** `CLAUDE.md:233` - `CLAUDE.md:377` - `CLAUDE.md:379` - `CLAUDE.md:382` - `CLAUDE.md:451` - `.claude/agents/planejador-mestre.md:5`

### 8. [ALTA] §C2.8 e §C7.6 são byte-idênticos entre CLAUDE.md e AGENTS.md, e a tabela de interop não lista modelo como mecanismo que difere — declara o DoD 'idêntico'

- **classe:** `d-identidade-indevida` - **dimensao:** `modelo`
- **arquivos:** `CLAUDE.md:228` - `CLAUDE.md:374` - `AGENTS.md:255` - `AGENTS.md:401` - `AGENTS.md:529` - `AGENTS.md:540` - `EXECUTION_MODEL.md:145` - `EXECUTION_MODEL.md:287` - `EXECUTION_MODEL.md:480` - `EXECUTION_MODEL.md:615` - `comando-template.md:185` - `BUILD_ORDER.md:121` - `docs/claude-code-handoff/README.md:12`

### 9. [ALTA] O teste de roteamento de modelo depende do binário `rg` e falha na máquina do dono; a asserção que garante 'só estes dois papéis têm ultra' nunca executa, e nenhum workflow de CI roda governance:check

- **classe:** `e-sem-enforcement` - **dimensao:** `modelo`
- **arquivos:** `tests/agent-model-routing.test.ts:15` - `package.json:governance:check` - `.github/workflows/ci.yml:80`

### 10. [ALTA] O 'recibo' de modelo do porteiro é uma string auto-declarada: um JSON literal escrito sem nenhum modelo envolvido é aceito como prova de que rodou Sol/ultra

- **classe:** `b-teatro` - **dimensao:** `modelo`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:81` - `scripts/porteiro-pre-merge.mjs:89` - `tests/porteiro-pre-merge-governance.test.ts:9` - `.agents/agents/porteiro-pos-merge.md:14`

### 11. [ALTA] O `--check` do espelho de agentes é não-recursivo: os dois especialistas do porteiro pré-merge estão 100% fora da garantia de espelhamento e o comando diz OK

- **classe:** `b-teatro` - **dimensao:** `agentes`
- **arquivos:** `scripts/sync-agent-agents.mjs:88` - `scripts/sync-agent-agents.mjs:90` - `scripts/sync-agent-agents.mjs:98` - `scripts/sync-agent-agents.mjs:104` - `.claude/agents/especialistas/guardiao-enforcement-github-porteiro.md:1` - `.agents/agents/especialistas/guardiao-enforcement-github-porteiro.md:1` - `.claude/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md:1`

### 12. [ALTA] O único teste que executa a lei de roteamento de modelo falha na máquina do dono por dependência de `rg` fora do PATH, e nunca rodou em lugar nenhum (arquivo untracked)

- **classe:** `e-sem-enforcement` - **dimensao:** `agentes`
- **arquivos:** `tests/agent-model-routing.test.ts:15` - `package.json:35` - `agent-orchestration/omega/planos/GOV-PORTEIRO-PRE-MERGE-ciclo1-plano-v1.md:173`

### 13. [ALTA] O sync APAGA em silêncio qualquer papel criado do lado Codex, e o `agente-fabrica` — que a §C7.4 manda criar especialistas nos ciclos 1–2 — grava no único caminho que o espelho não cobre e não tem Bash para rodar o sync

- **classe:** `g-degradacao-silenciosa` - **dimensao:** `agentes`
- **arquivos:** `scripts/sync-agent-agents.mjs:127` - `scripts/sync-agent-agents.mjs:128` - `scripts/sync-agent-agents.mjs:129` - `.claude/agents/agente-fabrica.md:4` - `.claude/agents/agente-fabrica.md:7` - `.claude/agents/agente-fabrica.md:8` - `.agents/agents/agente-fabrica.md:14` - `CLAUDE.md:369`

### 14. [ALTA] O --check dos agentes declara "espelho consistente" sem nunca olhar a subpasta .claude/agents/especialistas/ — mutei o espelho de um guardião para "aprove sempre" e o gate ficou VERDE

- **classe:** `b-teatro` - **dimensao:** `sync`
- **arquivos:** `scripts/sync-agent-agents.mjs:79` - `scripts/sync-agent-agents.mjs:87` - `scripts/sync-agent-agents.mjs:95` - `scripts/sync-agent-agents.mjs:97` - `scripts/sync-agent-agents.mjs:109` - `.agents/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md:1` - `.claude/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md:1`

### 15. [ALTA] O único enforcement do espelho de agentes (tests/agent-model-routing.test.ts) está VERMELHO na máquina do dono: quebra com `spawnSync rg ENOENT` e derruba `npm run governance:check`

- **classe:** `g-degradacao-silenciosa` - **dimensao:** `sync`
- **arquivos:** `tests/agent-model-routing.test.ts:15` - `tests/agent-model-routing.test.ts:8` - `package.json:35` - `.github/workflows/ci.yml:81`

### 16. [ALTA] CLAUDE.md e AGENTS.md são BYTE-IDÊNTICOS exatamente no parágrafo que define o modelo — o único lugar do contrato onde a tradução por ferramenta é obrigatória

- **classe:** `d-identidade-indevida` - **dimensao:** `sync`
- **arquivos:** `CLAUDE.md:376` - `CLAUDE.md:387` - `AGENTS.md:403` - `AGENTS.md:414` - `AGENTS.md:530` - `AGENTS.md:537` - `scripts/sync-agent-agents.mjs:28` - `scripts/sync-agent-agents.mjs:59`

### 17. [ALTA] O verificador de atestado do porteiro só aceita `runtime: codex` + `model: gpt-5.6-sol`, enquanto o sync obriga o porteiro do lado Claude a ser `fable` — nenhum porteiro rodado no Claude Code pode produzir autorização válida

- **classe:** `a-inexecutavel` - **dimensao:** `sync`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:89` - `scripts/sync-agent-agents.mjs:28` - `scripts/sync-agent-agents.mjs:56` - `.claude/agents/porteiro-pos-merge.md:5` - `CLAUDE.md:233` - `CLAUDE.md:451` - `tests/porteiro-pre-merge-governance.test.ts:9`

### 18. [ALTA] Não existe nenhuma trilha de invocação: `agentId`, `role`, `runtime`, `model` e `reasoningEffort` são strings que o próprio artefato escreve — o 'recibo' do handoff Codex existe só na prosa

- **classe:** `b-teatro` - **dimensao:** `lei-papeis`
- **arquivos:** `.agents/agents/README.md:42` - `.agents/agents/README.md:44` - `scripts/porteiro-pre-merge.mjs:89` - `scripts/merge-authorized-pr.mjs:31` - `scripts/post-merge-finalize.mjs:18` - `.github/workflows/porteiro-pre-merge.yml:10` - `.github/workflows/porteiro-pre-merge.yml:48` - `agent-orchestration/codex/HANDOFF-CLAUDE-2026-08-20.md:86`

### 19. [ALTA] Sob Claude Code a lei é inexecutável como escrita: o porteiro roda em `fable` e `verifyAttestation` só aceita recibo `runtime=codex`/`model=gpt-5.6-sol`

- **classe:** `a-inexecutavel` - **dimensao:** `lei-porteiro`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:89` - `.claude/agents/porteiro-pos-merge.md:5` - `.claude/agents/porteiro-pos-merge.md:53` - `CLAUDE.md:233` - `CLAUDE.md:451`

### 20. [ALTA] O porteiro nunca verifica que o ruleset que ele leu se aplica a `main` nem que ele exige PR — um ruleset ativo apontado para outro branch satisfaz 'main usa ruleset ativo/strict'

- **classe:** `b-teatro` - **dimensao:** `lei-porteiro`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:23` - `scripts/porteiro-pre-merge.mjs:29` - `scripts/porteiro-pre-merge.mjs:34` - `scripts/configure-main-ruleset.mjs:21` - `CLAUDE.md:240`

### 21. [ALTA] A junta pode omitir planejador/desenvolvedor e todo o cruzamento de alçadas — no porteiro e no pós-merge — vira vazio; e não existe noção de PR crítico (5 votos unânimes) no gate

- **classe:** `b-teatro` - **dimensao:** `lei-porteiro`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:39` - `scripts/porteiro-pre-merge.mjs:44` - `scripts/post-merge-finalize.mjs:13` - `CLAUDE.md:336` - `CLAUDE.md:357`

### 22. [MEDIA] A obrigação de RODAR os scripts de sincronização existe só no AGENTS.md; o CLAUDE.md — dono das pastas-fonte — só as menciona

- **classe:** `c-divergencia-indevida` - **dimensao:** `espelho`
- **arquivos:** `CLAUDE.md:33` - `CLAUDE.md:36` - `AGENTS.md:33` - `AGENTS.md:150` - `package.json:35`

### 23. [MEDIA] O índice de agentes isolados apontado pelos dois contratos lista 5 papéis que não existem e omite 3 que existem — entre eles o `executor-pos-merge`, obrigatório por §C2.10

- **classe:** `f-contradicao` - **dimensao:** `espelho`
- **arquivos:** `.agents/agents/README.md:54` - `CLAUDE.md:38` - `AGENTS.md:153` - `scripts/sync-agent-agents.mjs:27`

### 24. [MEDIA] O 'OBRIGATÓRIO' Sol/ultra do planejador-mestre não tem nenhum enforcement: junta.planner é apenas uma string de identidade em todos os schemas

- **classe:** `e-sem-enforcement` - **dimensao:** `modelo`
- **arquivos:** `CLAUDE.md:379` - `AGENTS.md:406` - `scripts/porteiro-pre-merge.mjs:44` - `scripts/post-merge-finalize.mjs:13` - `scripts/configure-main-ruleset.mjs:26`

### 25. [MEDIA] O diretório .claude/agents/especialistas/ fica fora do sync e do --check: o espelho diverge do transform e o guard responde 'espelho consistente'

- **classe:** `g-degradacao-silenciosa` - **dimensao:** `modelo`
- **arquivos:** `scripts/sync-agent-agents.mjs:79` - `scripts/sync-agent-agents.mjs:87` - `scripts/sync-agent-agents.mjs:95` - `.claude/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md` - `.agents/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md`

### 26. [MEDIA] CLAUDE.md afirma que o modelo está fixado no frontmatter `model:` de `.claude/agents/planejador-mestre.md`; o frontmatter diz `fable`, não o que a frase afirma

- **classe:** `f-contradicao` - **dimensao:** `agentes`
- **arquivos:** `CLAUDE.md:377` - `CLAUDE.md:382` - `CLAUDE.md:383` - `.claude/agents/planejador-mestre.md:5` - `.claude/agents/porteiro-pos-merge.md:5` - `AGENTS.md:404` - `AGENTS.md:409`

### 27. [MEDIA] O --check de skills não roda em NENHUM lugar — nem CI, nem teste, nem npm script — enquanto o AGENTS.md afirma que o script "mantém os dois diretórios idênticos"

- **classe:** `e-sem-enforcement` - **dimensao:** `sync`
- **arquivos:** `scripts/sync-agent-skills.mjs:11` - `AGENTS.md:535` - `AGENTS.md:43` - `AGENTS.md:149` - `.github/workflows/ci.yml:81` - `agent-orchestration/controle/decisoes.md:779`

### 28. [MEDIA] O sync de skills apaga a árvore inteira do espelho e destrói conteúdo Codex-only em silêncio, reportando sucesso — contra a regra "a sincronização não apaga em silêncio"

- **classe:** `g-degradacao-silenciosa` - **dimensao:** `sync`
- **arquivos:** `scripts/sync-agent-skills.mjs:93` - `scripts/sync-agent-skills.mjs:98` - `scripts/sync-agent-skills.mjs:101` - `AGENTS.md:38` - `AGENTS.md:39` - `CLAUDE.md:33`

### 29. [MEDIA] 'Passes sequenciais do mesmo agente com nomes diferentes não satisfazem a regra' é inexigível: o gate compara strings e um sufixo novo passa; o reuso do mesmo TIPO de agente em fases diferentes não é tratado em nenhum documento

- **classe:** `e-sem-enforcement` - **dimensao:** `lei-papeis`
- **arquivos:** `CLAUDE.md:356` - `AGENTS.md:383` - `scripts/porteiro-pre-merge.mjs:45` - `agent-orchestration/omega/juntas/J-O6R-B01-ciclo2.md:12`

### 30. [MEDIA] A `agente-fabrica` acha defeito, escreve os critérios de veredito do votante que julgará esse defeito, e não é alçada listada em §C7.4-bis — nem tem restrição no próprio arquivo

- **classe:** `e-sem-enforcement` - **dimensao:** `lei-papeis`
- **arquivos:** `CLAUDE.md:346` - `CLAUDE.md:353` - `.claude/agents/agente-fabrica.md:6` - `.claude/agents/agente-fabrica.md:12` - `agent-orchestration/omega/reprovacoes/R-B-O6R-01-ciclo1.md:176` - `agent-orchestration/omega/reprovacoes/R-B-O6R-01-ciclo1.md:195` - `agent-orchestration/omega/juntas/J-O6R-B01-ciclo2.md:12` - `agent-orchestration/omega/juntas/J-JUNTA-MAPAS.md:15` - `agent-orchestration/omega/juntas/J-JUNTA-MAPAS.md:22`

### 31. [MEDIA] O executor do merge pode ser o planejador da junta: `assertMergeCandidate` só o compara com o porteiro, nunca com as alçadas da junta

- **classe:** `b-teatro` - **dimensao:** `lei-papeis`
- **arquivos:** `scripts/merge-authorized-pr.mjs:24` - `scripts/merge-authorized-pr.mjs:52` - `scripts/porteiro-pre-merge.mjs:69`

### 32. [MEDIA] Os agentes que a fábrica grava em `.claude/agents/especialistas/` — os votantes com VETO criados nos ciclos 1-2 — ficam FORA do sync e do `--check`, que reporta verde mesmo com o espelho divergente

- **classe:** `g-degradacao-silenciosa` - **dimensao:** `lei-papeis`
- **arquivos:** `scripts/sync-agent-agents.mjs:79` - `scripts/sync-agent-agents.mjs:87` - `scripts/sync-agent-agents.mjs:95` - `.claude/agents/agente-fabrica.md:8` - `.claude/agents/especialistas/guardiao-enforcement-github-porteiro.md:1` - `.agents/agents/especialistas/guardiao-enforcement-github-porteiro.md:1`

### 33. [MEDIA] O porteiro pré-merge é hoje 100% voluntário: `main` não tem nenhuma proteção remota e as ferramentas que implementam a lei nem estão no git

- **classe:** `e-sem-enforcement` - **dimensao:** `lei-porteiro`
- **arquivos:** `CLAUDE.md:231` - `CLAUDE.md:238` - `CLAUDE.md:242` - `scripts/porteiro-pre-merge.mjs:1` - `.github/workflows/porteiro-pre-merge.yml:1` - `.github/rulesets/main.template.json:1`

### 34. [MEDIA] Indisponibilidade do modelo do porteiro é resolvida pela lei com 'vira nota na ata' (segue em frente), enquanto o código falha fechado — a saída praticável é mentir o campo `model`

- **classe:** `f-contradicao` - **dimensao:** `lei-porteiro`
- **arquivos:** `CLAUDE.md:384` - `CLAUDE.md:386` - `AGENTS.md:411` - `scripts/porteiro-pre-merge.mjs:89` - `scripts/porteiro-pre-merge.mjs:81`

### 35. [MEDIA] Auto-atestação por repetição: veredicto negativo do porteiro não deixa rastro nenhum, e o último comentário publicado é o que vale

- **classe:** `b-teatro` - **dimensao:** `lei-porteiro`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:121` - `scripts/porteiro-pre-merge.mjs:125` - `scripts/porteiro-pre-merge.mjs:132` - `scripts/merge-authorized-pr.mjs:12` - `scripts/post-merge-finalize.mjs:8` - `CLAUDE.md:235`

### 36. [MEDIA] Com `integration_id: null` num check requerido, o porteiro aceita verde de qualquer fonte e ignora o vermelho da CI real com o mesmo nome de contexto

- **classe:** `g-degradacao-silenciosa` - **dimensao:** `lei-porteiro`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:32` - `scripts/porteiro-pre-merge.mjs:58` - `scripts/porteiro-pre-merge.mjs:59` - `.github/rulesets/main.template.json:26` - `scripts/configure-main-ruleset.mjs:20`

### 37. [BAIXA] O preâmbulo que o espelho promete injetar em todo papel tem 2 versões materialmente diferentes; a dos 2 especialistas omite a tradução de mecanismo Claude→Codex e a citação da própria lei de separação

- **classe:** `d-identidade-indevida` - **dimensao:** `agentes`
- **arquivos:** `scripts/sync-agent-agents.mjs:36` - `scripts/sync-agent-agents.mjs:37` - `scripts/sync-agent-agents.mjs:38` - `scripts/sync-agent-agents.mjs:39` - `scripts/sync-agent-agents.mjs:40` - `.agents/agents/especialistas/guardiao-enforcement-github-porteiro.md:5` - `.agents/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md:5`

### 38. [BAIXA] O README do espelho — declarado normativo pelo CLAUDE.md como sede do protocolo de agentes isolados — pode ser DELETADO sem que nada fique vermelho

- **classe:** `e-sem-enforcement` - **dimensao:** `sync`
- **arquivos:** `scripts/sync-agent-agents.mjs:27` - `scripts/sync-agent-agents.mjs:95` - `CLAUDE.md:38` - `AGENTS.md:154` - `.agents/agents/README.md:10`

### 39. [BAIXA] O campo `commands` do atestado — a prova de que o porteiro REEXECUTOU — aceita conteúdo vazio: só o comprimento do array é verificado

- **classe:** `b-teatro` - **dimensao:** `lei-papeis`
- **arquivos:** `scripts/porteiro-pre-merge.mjs:91` - `CLAUDE.md:234`

### 40. [BAIXA] A prova de 'fonte GitHub App comprovada' é tautológica no workflow: o app esperado é derivado do próprio check-run que o workflow acabou de criar

- **classe:** `b-teatro` - **dimensao:** `lei-porteiro`
- **arquivos:** `.github/workflows/porteiro-pre-merge.yml:50` - `.github/workflows/porteiro-pre-merge.yml:52` - `.github/workflows/porteiro-pre-merge.yml:53` - `scripts/porteiro-pre-merge.mjs:126` - `scripts/porteiro-pre-merge.mjs:138` - `CLAUDE.md:239`

## Achados DERRUBADOS pelo cetico (registrados para nao reaparecerem)

- **CLAUDE.md manda o Claude Code rodar `gpt-5.6-sol`/`ultra` — modelo que o Claude Code não invoca e que o próprio script do repo REJEITA no lado Claude**
  - refutado porque: O achado NÃO se sustenta como formulado. Derrubo os três pilares dele, cada um com comando executado.  (1) A propriedade alegada como ausente EXISTE e está viva. O achador afirma que "o par contrato-canônico ↔ artefato-executável pode divergir sem que nada detecte". Falso: `tests/agent-model-routing.test.ts:11` assere literalmente `model: fable` no lado Claude E o negativo `assert.doesNotMatch(claude, /^model: gpt-5.6-sol$/m)`, para os dois papéis; `scripts/sync-agent-agents.mjs:56-62` lança exceção fora disso; e `package.json:20` → `run-backend-tests.mjs` (readdirSync de `tests/*.test.ts`, linhas 63/118) coloca esse guard na suíte que roda na CI. Reexecutei as asserções: as quatro dão `true` para `planejador-mestre` e `porteiro-pos-merge`. O achador rodou o `throw` do sync e concluiu "nada detecta" sem procurar o teste que já detecta.  (2) Nas linhas 233/451 (porteiro) a identidade espelhada é CORRETA, não uma tradução faltante — é o oposto do que o achado classifica. `scripts/porteiro-pre-merge.mjs:89` exige `runtime==='codex' && model==='gpt-5.6-sol' && reasoningEffort==='ultra'`. Meu probe contra a função real: recibo Codex ACEITO; recibo Claude/fable REJEITADO; recibo Claude/opus REJEITADO. O porteiro é, por enforcement, um gate executado no Codex — então CLAUDE.md dizer que "ele roda em gpt-5.6-sol/ultra" é descrição factual, não instrução impossível. E `D-PORTEIRO-PRE-MERGE` (decisoes.md) diz, sem qualquer ressalva de lado, "executado em `gpt-5.6-sol` com raciocínio `ultra`" — os dois espelhos transcrevem fielmente a decisão do dono.  (3) A consequência alegada — "improvisa opus, registra na ata que rodou Sol/ultra e nada fica vermelho" — é o caminho que o repositório mais fecha. `merge-authorized-pr.mjs:21` chama `verifyAttestation`, e CLAUDE.md:240 restringe o merge a esse script. Um porteiro improvisado falha FECHADO com "recibo Codex Sol/ultra inválido". É degradação ruidosa, não silenciosa. A decisão ainda prevê explicitamente o caso: "A indisponibilidade real do modelo vira nota explícita" (espelhado em CLAUDE.md:386).  (4) A premissa central do achador — "a decisão restringe a substituição ao CODEX; o CLAUDE.md aplicou aos dois lados" — cai na leitura completa: a mesma decisão determina que "**frontmatters e espelhos registram o modelo**". Ou seja, o dono localizou o identificador nativo na camada de frontmatter, não na prosa. CLAUDE.md:382-384 diz exatamente isso e acrescenta "quem invoca não precisa lembrar" — mecanismo compensatório que o achador não considerou. O caminho operacional real (invocar o agente → frontmatter `fable` aplica) nunca executa o identificador Codex.  (5) Leitura fora de contexto em CLAUDE.md:386. "Chamada de `Agent`/`Workflow` que passe `model` diferente para esse papel contraria o contrato" tem como antecedente a cláusula imediatamente anterior ("Fixado no frontmatter `model:` de `.claude/agents/planejador-mestre.md`") — "diferente" = diferente do que o frontmatter fixa, isto é, diferente de `fable`. Isolada, a frase parece exigir Sol; em contexto, está correta.  RESÍDUO (por que "baixa" e não "nao-e-defeito"): permanece verdadeiro e medido que `fable` só aparece na prosa do CLAUDE.md dentro de IDs de decisão, e que a prosa nunca declara que o porteiro executa no runtime Codex. Quem parar na linha 233 ou 377 sem seguir o ponteiro da 382-384 fica com um quadro impreciso. Isso é imprecisão de redação na camada que o dono NÃO designou como registro de modelo — não é instrução inexecutável, não é divergência indetectável e não é caminho de degradação silenciosa. A gravidade "bloqueante" está inflada por três ordens de grandeza: nada trava, nada passa em falso, e o gate falha fechado.  (Fora de escopo, mas medido e registro por honestidade: `tests/agent-model-routing.test.ts:15` depende do binário `rg` via `execFileSync` e aborta com `spawnSync rg ENOENT` em shell sem ripgrep. Não afeta este achado — as asserções de frontmatter da linha 11 passam antes — mas é fragilidade do próprio guard. Não prescrevo correção.)
- **O gate de merge só aceita recibo `runtime='codex'`; um porteiro do Claude Code é rejeitado — e nenhum dos dois contratos diz isso**
  - refutado porque: O FATO MECÂNICO se reproduz, mas as três afirmações que sustentam o achado caem.  1) "nenhum dos dois contratos diz isso" — FALSO, medido. Os dois contratos espelhados exigem Sol/ultra para o porteiro, em duas passagens cada, e um terceiro documento normativo repete: CLAUDE.md:233 "Ele roda em **`gpt-5.6-sol` com raciocínio `ultra`**"; CLAUDE.md:451 "porteiro pré-merge independente em Sol/ultra"; AGENTS.md:260 e AGENTS.md:478 (texto idêntico); EXECUTION_MODEL.md:145 "Ele usa **`gpt-5.6-sol` com raciocínio `ultra`**"; EXECUTION_MODEL.md:615 "Agente porteiro novo (`gpt-5.6-sol`/`ultra`)"; decisoes.md:1583 "executado em `gpt-5.6-sol` com raciocínio `ultra`". O achador listou CLAUDE.md:230 e CLAUDE.md:451 nos próprios "arquivos" do achado — são exatamente as linhas que o contradizem. O probe dele (`sed -n '220,250p' CLAUDE.md | grep 'Codex'`) é o probe errado: o contrato nomeia o MODELO, não a ferramenta.  2) O campo `runtime` NÃO é a causa operante da rejeição, e portanto a moldura "condição não documentada" colapsa. Meu caso E isola isso: recibo com `runtime:'codex'` + `model:'fable'` + `ultra` é REJEITADO igualmente. Ou seja, `model === 'gpt-5.6-sol'` sozinho já fecha o portão — e essa é precisamente a condição escrita nos seis lugares acima. `runtime==='codex'` é redundância de cinto-e-suspensório sobre uma exigência documentada, não um requisito clandestino.  3) A exclusividade Codex do recibo é DELIBERADA e está escrita no plano normativo desta mesma branch, redigido por planejador independente: plano-v1:66 "`verify` aceita somente `LIBERADO` literal, identidade independente e **recibo Codex Sol/ultra**"; plano-v1:87 "**porteiro novo Codex Sol/ultra** publica parecer/status"; plano-v1:58 "Recibo registra agent id, role, **runtime**, modelo e esforço; papel portátil não é prova".  4) A classe "a-inexecutavel" está errada. A regra como escrita É executável — pelo Codex. Que o Claude Code não staffe este papel é diferença de MECANISMO ESPECÍFICO DA FERRAMENTA, o único caso em que D-INTEROP-CLAUDE-CODEX autoriza os contratos a divergirem. O `model: fable` em .claude/agents/porteiro-pos-merge.md:5 não é declaração de que o Claude executa o gate: R-GOV-PORTEIRO-PRE-MERGE-ciclo1.md:31-34 registra que `model: gpt-5.6-sol` naquele frontmatter foi REPROVADO por ser identificador inválido no runtime Claude sem gateway comprovado — `fable` está ali como identificador válido do arquivo canônico que o sync traduz. E o próprio arquivo, nas linhas 9-11 que o achador não leu (ele citou só a linha 5), diz "no espelho Codex ... exige **`gpt-5.6-sol`/`ultra`** passados explicitamente na invocação e **registrados no recibo**" — o recibo é atribuído ao lado Codex no mesmo parágrafo.  5) A consequência alegada ("bloqueante; hoje só uma ferramenta fecha um bloco") não é fato medido. Medi: NENHUM dos arquivos do gate existe em origin/main (`git cat-file -e origin/main:<path>` → ausente para os 6). Ruleset é `.github/rulesets/main.template.json` e o apply remoto exige junta crítica 5/5 + VETO antes de configurar (plano-v1 F3.2/F3.3; decisoes.md, emenda do ciclo 1). Nada trava merge hoje.  6) O caminho de degradação é FAIL-CLOSED, não silencioso: `verifyAttestation` lança e `assertMergeCandidate` (merge-authorized-pr.mjs:21) o chama antes da API de merge. E a "saída de menor atrito = falsificar `runtime`" não funciona (caso E): exigiria mentir em três campos, além de derrotar a prova de fonte `ERP_PORTEIRO_EXPECTED_APP_ID` (porteiro-pre-merge.mjs:127) e o VETO permanente do `guardiao-interoperabilidade-modelos-claude-codex` ("documento portátil ou espelho é tratado como se garantisse override que só a chamada real pode aplicar").  RESÍDUO QUE SOBREVIVE (baixa, classe f, não a): há tensão de redação entre D-FABLE-PARA-GPT-5-6-SOL (decisoes.md:1563 — "Onde a documentação ativa mandava usar Fable, o Codex passa a usar `gpt-5.6-sol`", que implica Fable permanecendo válido do lado Claude para o MESMO papel) e o §C2.8 incondicional ("roda em gpt-5.6-sol/ultra"), e nenhuma linha diz em palavras "este papel não é staffado pelo Claude Code". É lacuna de explicitude, fail-closed e rastreável — não é o gate bloqueante inexecutável descrito no achado.  NÃO MEDI: se o ruleset e o check requerido estão de fato aplicados no GitHub remoto (não consultei a API); a afirmação (5) apoia-se em git local + nos documentos de plano/decisão.
- **Os dois contratos afirmam, verbatim, que `main` usa ruleset ativo/strict sem bypass — a `main` real não tem ruleset nenhum nem proteção de branch**
  - refutado porque: O fato bruto do título reproduz (CLAUDE.md:240 e AGENTS.md:267 escrevem "`main` usa ruleset ativo/strict, sem bypass" e o repo vivo responde `[]` / "Branch not protected"), mas as TRÊS afirmações que sustentam o achado como defeito são mensuravelmente falsas, e é delas que dependem a classe (b-teatro), a propriedade-que-falta e a consequência.  (1) FALSO — "o único guard existente valida um ARQUIVO TEMPLATE no disco, nunca o estado vivo do GitHub". Existem DOIS guards de runtime sobre o estado VIVO: `scripts/porteiro-pre-merge.mjs:98` (`liveState` faz `gh api repos/${repo}/rulesets?includes_parents=true`) alimenta `requiredChecks()`, que aborta em `:26` (`ruleset ativo contém bypass actor`) e `:34` (`ruleset não exige atualização estrita da base`); e `scripts/merge-authorized-pr.mjs:40` refaz a busca do ruleset vivo por conta própria e reconstrói o snapshot. O teste do template não é "o único guard" — é um guard adicional sobre o desejado, não a única barreira.  (2) FALSO — "Não existe caminho pelo qual o contrato fique vermelho enquanto a `main` estiver desprotegida". Medido: com `rulesets: []` (o estado vivo de hoje), `buildSnapshot` LANÇA. Consequência real: HOJE, com a `main` desprotegida, o porteiro NÃO consegue emitir snapshot, `publish` não roda (e `:122` recusa fixture: `publish não aceita fixture`, com releitura viva em `:128`), e `merge-authorized-pr.mjs` aborta antes do `PUT /merge`. A superfície sancionada inteira está fechada justamente porque o ruleset não existe.  (3) FALSO — "É o oposto do fail-closed". O oposto: EXECUTION_MODEL.md:155 diz literalmente "Fonte confiável invisível ou não vinculável = VETO antes do bootstrap, nunca degradação para status forjável", e BUILD_ORDER.md:124 diz "ruleset ativo/strict sem bypass e merge CAS são **pré-condições**. Fonte GitHub App não comprovável bloqueia o bootstrap". Os dois documentos que o achador cita como cúmplices da falsa afirmação são exatamente os que qualificam o ruleset como PRÉ-CONDIÇÃO com cláusula de veto — não como estado consumado.  (4) Leitura fora de contexto: a cláusula está no meio de um parágrafo normativo em que toda oração vizinha é prescritiva ("merge só por `scripts/merge-authorized-pr.mjs`", "Mudança de qualquer campo invalida"). Presente do indicativo é o registro prescritivo padrão em texto normativo PT-BR; ler só essa oração como reporte factual de configuração é escolha do achador, não do texto.  (5) Erro de citação: a asserção do template está em `tests/porteiro-pre-merge-governance.test.ts:15`, não `:14` (`:14` é o teste de merge CAS).  (6) Não é achado oculto nem novo: a lacuna real é `GOV-C1-002` em `agent-orchestration/omega/reprovacoes/R-GOV-PORTEIRO-PRE-MERGE-ciclo1.md:40-47`, que já registra os MESMOS dois outputs `gh` (404 protection + `[]` rulesets). `grep -rn GOV-C1-002` retorna exatamente 1 ocorrência em todo o repo — ou seja, ninguém em lugar nenhum declarou a lacuna resolvida. Ela é item ABERTO do próprio ciclo que gerou esta branch, com remediação script (`scripts/configure-main-ruleset.mjs`) e ordem de bootstrap em `GOV-PORTEIRO-PRE-MERGE-ciclo1-plano-v1.md:86`: "executor distinto ativa/prova ruleset antes do merge ... Não há merge antecipado 'para instalar o gate'". Isso derruba a consequência "quando esta branch mergear, os contratos passam a afirmar proteção que não existe": a branch não está mergeada, e a sequência documentada exige ativar o ruleset ANTES do merge.  O QUE SOBREVIVE (e só isso): CLAUDE.md/AGENTS.md não carregam o qualificador "pré-condição/VETO" que EXECUTION_MODEL.md:155 e BUILD_ORDER.md:124 carregam, então quem ler apenas o contrato canônico recebe impressão mais forte do que o estado autoriza; e a `main` de fato aceita `gh pr merge` direto hoje, fora da superfície sancionada. É uma frouxidão de registro entre documentos espelhados sobre item já aberto — não "teatro alta" com "nenhum caminho para vermelho".  NÃO MEDIDO, e declaro: não consegui exercitar o caminho vivo `snapshot --pr <n>` fim-a-fim porque `gh pr list --state open` retornou `[]` — não há PR aberto. Medi o guard invocando `buildSnapshot` diretamente com `rulesets: []`, que é o mesmo code path que `liveState` alimenta.
- **A asserção que promete pegar 'Codex sem effort' é tautológica: passa igual para o transform real e para um transform mutante que não anexa reasoning_effort**
  - refutado porque: A OBSERVAÇÃO do achador reproduz, mas as duas afirmações que sustentam a classe "b-teatro/alta" são FALSAS, medidas.  O QUE REPRODUZ (fato): a asserção de tests/agent-model-routing.test.ts:22 é de fato tautológica. `.replace('reasoning_effort: ultra','')` remove a única ocorrência literal antes de a regex `/^reasoning_effort: ultra$/m` procurá-la (medi: o output REAL tem a string 1x, o MUTANTE 0x). Ela passa igual para o transform real e para o mutante. Isso é verdade.  O QUE NÃO SE SUSTENTA (o núcleo do achado): 1. "Falta a propriedade que o nome do teste promete: um espelho Codex que perca reasoning_effort faz a suíte ficar vermelha" — FALSO. Construí o mutante real (scripts/sync-agent-agents.mjs sem a linha 60) e rodei a suíte de verdade: ela FICA VERMELHA nos DOIS caminhos possíveis da mutação.    - Caminho A (edita o script e não regenera o espelho): tests/agent-model-routing.test.ts:8 executa `sync-agent-agents.mjs --check`, que sai 1 com "DIVERGE: .agents/agents/planejador-mestre.md / porteiro-pos-merge.md" → execFileSync lança → `not ok 1`.    - Caminho B (edita o script E regenera o espelho, que é o caminho que o achador supõe silencioso): o `--check` fica verde, mas tests/agent-model-routing.test.ts:12 (`assert.match(codex,/^reasoning_effort: ultra$/m)`, lendo o ARQUIVO em disco) falha com a mensagem literal "The input did not match the regular expression /^reasoning_effort: ultra$/m" → `not ok 1`. Medido end-to-end, não inferido. 2. "Nenhuma execução hoje distingue o transform correto do transform que esqueceu o esforço" — FALSO. `node scripts/sync-agent-agents.mjs --check` sozinho distingue: exit 0 (real) vs exit 1 (mutante). E o teste 1 distingue nos dois caminhos. 3. "o único teste que existe para isso continua verde" — FALSO em "único". O guard real é o teste 1 do MESMO arquivo (linhas 8, 12 e 16), e ele roda em CI: `npm test` → scripts/run-backend-tests.mjs varre `tests/` por `*.test.ts` (confirmei que `agent-model-routing.test.ts` está na lista) e .github/workflows/ci.yml:81 executa `npm test`. A linha 12 é avaliada ANTES da linha 15 (o `rg`), então a falha não depende do ripgrep.  MECANISMO COMPENSATÓRIO QUE O ACHADOR NÃO VIU: o par "espelho materializado em disco + `--check` de drift" (linhas 8 e 12) é justamente o enforcement executável dessa propriedade. A linha 22 é redundante, não é o guard.  DEGRADAÇÃO: nem o gap de ambiente degrada em silêncio — nesta máquina o teste 1 já falha com `spawnSync rg ENOENT` (linha 15), ou seja, falha FECHADO (vermelho), nunca verde. (Isso é um defeito distinto, de outra dimensão; não o avalio aqui.)  RESÍDUO REAL, MAS MENOR: sobra (a) uma asserção morta na linha 22, que não mede nada, e (b) um NOME de teste na linha 19 ("Codex sem effort") que anuncia uma cobertura que AQUELE teste não entrega. Cosmeticamente é a classe perseguida, mas sem a consequência afirmada: não existe caminho em que o espelho Codex perca `ultra` e a suíte siga verde. Logo a gravidade "alta" com consequência de "quebra silenciosa" está inflada — corrijo para "baixa" (asserção vazia + nome de teste que promete além do que aquele teste faz).  NÃO MEDI: se `rg` existe no runner do CI (ubuntu-latest); portanto não afirmo nada sobre a linha 16 em CI. As linhas 8 e 12 bastam e independem de `rg`.
- **A prosa concede exceção por 'indisponibilidade do modelo' que o gate executável não honra — e a única saída que o gate deixa é mentir o campo**
  - refutado porque: REFUTADO como formulado. Os fatos brutos reproduzem (o gate rejeita `reasoningEffort:'high'`, `model:'fable'` e o campo `nota`), mas as três pernas do achado caem:  1) LEITURA FORA DE CONTEXTO. CLAUDE.md:385 / AGENTS.md:412 está dentro do item §C7.6 cujo título é "Modelo do `planejador-mestre`"; o antecedente de "esse papel" é `planejador-mestre`, e o mecanismo citado na mesma frase é o frontmatter `model:` de `.claude/agents/planejador-mestre.md` — não o atestado do porteiro. A origem da cláusula (decisoes.md:1230-1231, D-PLANEJADOR-MODELO-FABLE) escopa de forma inequívoca: "a **junta** segue com o modelo disponível e o desvio vira nota explícita **na ata**". Grep executado nas duas decisões do porteiro (D-PORTEIRO-POS-MERGE 1264-1290 e D-PORTEIRO-PRE-MERGE 1578-1600), em §C2.8 (CLAUDE.md:231-241) e na DoD (CLAUDE.md:451) não retorna NENHUMA cláusula de exceção por modelo para o porteiro. Não há, portanto, prosa viva concedendo ao porteiro a exceção que o gate recusaria.  2) A PROPRIEDADE ALEGADAMENTE FALTANTE EXISTE — E JÁ FOI USADA. O achado diz que "a degradação prevista pelo contrato não tem representação no artefato". A prosa nomeia o artefato: "nota no registro da junta" / "nota explícita na ata". Isso é um arquivo markdown, e ele comporta a nota. Prova executada: `agent-orchestration/omega/juntas/J-O6R-B05-production-runtime-gates.md:9-13` é uma ata real que EXERCEU a exceção ("a cota do Fable esgotou na sessão e o plano foi escrito em Opus 5 ... a própria decisão prevê a exceção por indisponibilidade do modelo, 'que vira nota no registro da junta' — é esta nota"), inclusive registrando o carve-out (plano novo, não a revalidação §C7.4). A ausência de campo `nota` no `allowed` de porteiro-pre-merge.mjs:81 não é lacuna: o contrato nunca prometeu a nota ali.  3) A RECUSA É DESENHO DELIBERADO, NOMEADO E COBERTO. tests/porteiro-pre-merge-governance.test.ts:13 se chama literalmente "...e **receipt degradado** não liberam" e asserta o throw para `reasoningEffort:'high'` e `model:'gpt-5.6-terra'`. Suíte executada: 6/6 verde. O executável não "deixa de honrar" uma exceção — ele implementa uma regra escrita e testada de que recibo degradado não autoriza merge. Isso é coerente com D-PORTEIRO-POS-MERGE:1287-1289 ("sem comando executado não há parecer — se algo não pôde ser rodado, isso entra escrito").  4) CONSEQUÊNCIA INFLADA E DUPLICADA. O dano alegado ("a saída de menor atrito é preencher model:'gpt-5.6-sol' mesmo tendo rodado outro") é integralmente o achado 6 (auto-declaração), como o próprio achador reconhece entre parênteses. Medi: o atestado Sol/ultra auto-declarado sem prova retorna `true`. Mas esse incentivo é idêntico com ou sem a cláusula de prosa — a cláusula não o cria e removê-la não o remove. Atribuir aqui é contagem dupla. E "recusa sem explicar" também não se sustenta: a mensagem executada é `recibo Codex Sol/ultra inválido`, que nomeia exatamente o requisito violado.  RESÍDUO HONESTO (o que sobra, e por isso gravidade "baixa" e não "nao-e-defeito"): decisoes.md:1573 está dentro de um parágrafo que abre com "Nos dois papéis" e diz "A indisponibilidade real do modelo vira nota explícita...". Um leitor pode inferir que a exceção alcança o porteiro. Note-se, porém, que essa redação **omite deliberadamente** o "segue com o modelo disponível" que a versão original (1230) tinha, e acrescenta apenas uma restrição. É ambiguidade de redação de uma linha, com comportamento fail-closed e mensagem de erro explícita — não a contradição normativa com consequência operacional que o achado afirma.  NÃO MEDI / FORA DE ESCOPO: o fato de `verifyAttestation` exigir `runtime:'codex'` + `model:'gpt-5.6-sol'` enquanto `.claude/agents/porteiro-pos-merge.md` declara `model: fable` significa que um porteiro do lado Claude estruturalmente nunca produz atestado válido. Isso é real e medido, mas é o achado irmão de identidade-vs-tradução (classe d), não este. Não o importo para inflar esta gravidade.
- **`model: inherit`, token do Claude Code, é copiado verbatim para o frontmatter do espelho Codex, sob um comentário que afirma tradução allowlisted de modelo**
  - refutado porque: O texto bruto do achado reproduz (o `model: inherit` é de fato copiado verbatim e o script tem exceção explícita para ele), mas as TRÊS afirmações que sustentam o defeito caem sob execução.  (1) A PROMESSA CITADA É PARÁFRASE FABRICADA. O achador atribui ao cabeçalho a propriedade "o frontmatter emitido é portátil e todo modelo que sai dele foi traduzido". O texto verbatim de `scripts/sync-agent-agents.mjs:9-10` é: "frontmatter portátil (name + description + tradução **estritamente allowlisted** de modelo; só `tools:` sai)". "Estritamente allowlisted" é justamente o qualificador que NEGA a universalidade: a tradução é restrita a uma allowlist. O cabeçalho nunca afirma que todo modelo emitido foi traduzido. O achador construiu a promessa que depois refuta — e é essa promessa inventada que faz o achado parecer da classe "artefato afirma o que a execução não produz".  (2) O COMPORTAMENTO É O ESPECIFICADO, EM PLANO APROVADO. `agent-orchestration/omega/planos/GOV-PORTEIRO-PRE-MERGE-ciclo1-plano-v1.md:55` (F1 §2, verbatim): "`scripts/sync-agent-agents.mjs` mapeia somente esses dois papéis para a saída Codex com `model: gpt-5.6-sol` e `reasoning_effort: ultra`. **Os demais mantêm default da sessão.**" E o critério C1-001 (linha 38): "nenhum outro papel herda override". `.agents/agents/README.md:42-44` documenta que a invocação Codex usa `model`/`reasoning_effort` explícitos SOMENTE nos dois papéis cirúrgicos — logo, para os demais, não há override e vale o modelo da sessão. `model: inherit` enuncia exatamente isso. A consequência alegada ("não há o que herdar") não se sustenta: a sessão Codex tem modelo, e o próprio contrato prescreve herdá-lo nos papéis não-cirúrgicos.  (3) DIVERGÊNCIA COMPORTAMENTAL ZERO, MEDIDA. 20 dos 23 papéis não têm NENHUMA linha `model:` — nos dois lados (medido). "Sem linha" e "inherit" entregam a mesma instrução ao leitor Codex: sem override, use o modelo da sessão. O enquadramento do achador ("um é traduzido, um é copiado, três são proibidos") descreve mal o conjunto real: {2 traduzidos fable→gpt-5.6-sol+ultra} ∪ {1 que declara o default explicitamente} ∪ {20 que declaram o default por omissão} ∪ {qualquer override real → `throw` fail-closed}. `inherit` não é buraco na allowlist; é a ausência de override, que é o que a allowlist existe para exigir.  (4) A SOBREVIVÊNCIA DA LINHA `model:` É CORREÇÃO DE RAIZ DOCUMENTADA, não descuido. `agent-orchestration/controle/decisoes.md:1224-1231` (D-PLANEJADOR-MODELO-FABLE): o script "**apagava** a linha `model:` ao espelhar (ela era tratada como 'linha Claude-específica', junto de `tools:`)... `model:` **é regra de execução do papel** e agora sobrevive à sincronização — senão o contrato se perderia em silêncio a cada `sync`." Ou seja: remover/traduzir compulsoriamente a linha era o bug ANTERIOR, corrigido de propósito.  (5) EXISTE ENFORCEMENT EXECUTÁVEL que o achador não citou: `tests/agent-model-routing.test.ts` (rodado por `npm test` via `scripts/run-backend-tests.mjs`, que varre `tests/`, e por `npm run governance:check`). Seu subteste 2 passou aqui e prova o fail-closed: um papel fora da allowlist com `model: gpt-5.6-sol` lança "fora da allowlist".  GRAVIDADE: o papel afetado (`frontend-pixel-master`) não é alçada de autorização, veto, gate de merge ou dinheiro. Não há caminho de degradação silenciosa: as duas leituras possíveis de `inherit` convergem no mesmo comportamento dos outros 20 papéis. Rebaixo para nao-e-defeito.  RESÍDUO HONESTO (não é o achado sob ataque, e NÃO prescrevo correção): `.agents/agents/README.md:7-8` descreve o frontmatter do espelho como "frontmatter portátil (`name` + `description`)", sem mencionar `model:` — descrição defasada em relação aos TRÊS arquivos que hoje carregam `model:` (não só o do `inherit`). O README compensa parcialmente em 42-44, que documenta o Sol/ultra. Isso é outra propriedade, sobre outro arquivo, e não produz instrução errada.  NÃO MEDIDO, declarado: (a) o comportamento real do Codex ao ler literalmente o valor `inherit` — é externo ao repo e não pude executar; (b) se o runner de CI tem o binário `rg`. Incidental fora do meu escopo: `tests/agent-model-routing.test.ts:15` invoca `rg` por `execFileSync` e aqui falhou com `spawnSync rg ENOENT` (o `rg` desta sessão resolve como alias de shell, não binário spawnável); a asserção substantiva confere por grep — só os dois arquivos cirúrgicos têm `reasoning_effort: ultra`.
- **O sync rejeita os modelos legítimos do Claude Code (sonnet/opus/haiku) com uma mensagem que os chama de 'override de alto raciocínio'**
  - refutado porque: O MECANISMO reproduz, mas o ACHADO COMO FORMULADO não se sustenta: dois dos três pilares caem.  PILAR 1 (fato) — SUSTENTA. `transform()` lança para `model: sonnet|opus|haiku` com a string "override de alto raciocínio fora da allowlist" (scripts/sync-agent-agents.mjs:61-63). Reproduzi os 7 casos do achador + 3 meus, saída idêntica. E a escrita parcial também é fato: em sandbox, `runSync` escreveu `a-first.md` novo, deixou `c-last.md` antigo, já tinha apagado o obsoleto e abortou com stack cru (exit 1).  PILAR 2 (propriedade que falta) — REFUTADO. O achador afirma que fixar modelo é "o que o Claude Code permite e nenhuma decisão proibiu". O plano que rege exatamente esta mudança diz o contrário, em letra: `agent-orchestration/omega/planos/GOV-PORTEIRO-PRE-MERGE-ciclo1-plano-v1.md:55-56` — "`scripts/sync-agent-agents.mjs` mapeia somente esses dois papéis para a saída Codex ... **Os demais mantêm default da sessão.**" A allowlist implementada é exatamente {ausente, `inherit`} = "default da sessão". O guard NÃO tem categoria faltando: ele implementa a regra escrita. Reforço independente: `.claude/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md:47` define como critério de VETO "sync transforma uma diferença específica de ferramenta em configuração inválida na outra" — deixar `model: haiku` passar copiaria um id só-Claude para o frontmatter Codex, que é o defeito que o guardião veta. Ou seja, a permissividade que o achado pede criaria a violação. A recusa é fail-closed CORRETA, não brecha. (Nota: no HEAD commitado o script PRESERVAVA `model:` de qualquer papel; o mesmo diff que introduziu o throw reescreveu o comentário do cabeçalho (linha 10, "tradução estritamente allowlisted de modelo") — não sobrou contradição viva entre comentário e código.)  PILAR 3 (gravidade media) — INFLADO. (a) "Quebra qualquer papel Claude futuro que precise fixar modelo" descreve uma configuração que o plano proíbe, não uma capacidade que o contrato concede; hoje nenhum agente a usa — `grep '^model:' .claude/agents/*.md` dá só `inherit`(1), `fable`(2). (b) A escrita parcial não é degradação silenciosa: existe enforcement executável — `tests/agent-model-routing.test.ts:8` roda `sync-agent-agents.mjs --check`, e `npm test` (`scripts/run-backend-tests.mjs`, varredura de `tests/*.test.ts`) roda no job backend da CI (`.github/workflows/ci.yml:81`). No sandbox, após a escrita parcial o `--check` saiu 1 e, corrigido o frontmatter, reportou `DIVERGE` limpo e o re-run reparou. Estado parcial é detectável, vermelho e recuperável. (c) O próprio achador concede que o caso `porteiro-v2 model: fable` é fail-closed correto.  CLASSE ALEGADA ERRADA: "a-inexecutavel" = regra que a ferramenta não consegue executar. Nada aqui é inexecutável — os papéis Claude rodam (`--check` real: "OK — 23 agentes, espelho consistente."). É uma recusa projetada.  RESÍDUO QUE SOBREVIVE (menor e diferente do submetido): a string de diagnóstico. `haiku` é o modelo mais barato do roster e o operador recebe "override de alto raciocínio" — o texto afirma sobre a entrada algo que a entrada contradiz, e aponta o operador para "raciocínio/allowlist" em vez de "papéis não cirúrgicos mantêm o default da sessão / id só-Claude não pode ir para o espelho Codex". Isso é real, mas é redação de mensagem de erro, sem consequência de comportamento, com falha fechada e teste vermelho — baixa, não media, e não da classe alegada.
- **O identificador compartilhado D-FABLE-PARA-GPT-5-6-SOL e a branch ...-sol embutem o lado Codex: medido, nenhum mecanismo lê — é defeito de forma, mas é a forma que produziu a identidade indevida da prosa**
  - refutado porque: A afirmação central do achado é falsa como medida, e existe mecanismo compensatório executável que ele não viu.  1) FALSO NA LETRA. A "propriedade que falta" diz: "O nome canônico de uma decisão que vale para as DUAS ferramentas nomeia apenas o modelo de uma delas." O identificador `D-FABLE-PARA-GPT-5-6-SOL` contém OS DOIS identificadores: `FABLE` (modelo nativo do lado Claude, literalmente `model: fable` em .claude/agents/*.md:5) e `GPT-5-6-SOL` (lado Codex). O nome tem a forma `X PARA Y` — origem→destino. O corpo em decisoes.md:1564 lê: "Onde a documentação ativa mandava usar Fable, o Codex passa a usar `gpt-5.6-sol`". O identificador é compressão fiel dessa frase. Nomear uma regra de TRADUÇÃO pelos seus dois extremos é nomear a regra, não o mecanismo de um lado — é exatamente a propriedade que o achado alega faltar.  2) MECANISMO COMPENSATÓRIO NÃO VISTO, E É EXECUTÁVEL. O achado conclui "medido, nenhum mecanismo lê — é defeito de forma". Medido por mim: scripts/sync-agent-agents.mjs:56-59 IMPLEMENTA a transformação que o nome descreve. Linha 56-58 exige que a fonte Claude declare exatamente `model: fable`, senão `throw`; linha 59 faz `cleanedFm.replace(/^model:\s*fable\s*$/m, 'model: gpt-5.6-sol')`. Ou seja: "Fable → gpt-5.6-sol" é substituição literal de string, enforced, e FALHA FECHADO (throw). Linhas 61-63 também lançam para override de alto raciocínio fora da allowlist. O nome da decisão rotula um transform real e verificável. É o oposto de "forma sem mecanismo".  3) LINHAS CITADAS FORA DE CONTEXTO. .claude/agents/planejador-mestre.md:8 e porteiro-pos-merge.md:10 são listados como evidência de "identidade indevida". No contexto, são precisamente onde a TRADUÇÃO está correta e explícita: frontmatter `model: fable` (linha 5, lado Claude) + prosa "no Claude Code este papel usa o identificador nativo **`fable`**. O espelho Codex é gerado com **`gpt-5.6-sol`/`ultra`**".  4) A PROPRIEDADE NÃO É APLICADA SIMETRICAMENTE. `D-PLANEJADOR-MODELO-FABLE` (decisoes.md:1203) nomeia o modelo do lado CLAUDE e é citada em AGENTS.md:403 — o contrato do Codex, o lado que esse nome exclui. É a imagem espelhada exata da queixa, e o achado não a sinaliza. Propriedade que dispara numa direção e não na simétrica é preferência de nomenclatura, não classe de defeito. Precedente de nome bilateral já existe: `D-INTEROP-CLAUDE-CODEX` (decisoes.md:773).  5) O ADENDO DO `porteiro-pos-merge` É PRESERVAÇÃO DELIBERADA DOCUMENTADA. decisoes.md:1580, CLAUDE.md:232, AGENTS.md:259 e .claude/agents/porteiro-pos-merge.md:8 registram literalmente "identificador técnico legado preservado / para não quebrar referências". O perigo que ele descreve (renomear derruba sync-agent-agents.mjs:28 e porteiro-pre-merge.mjs:89) é justamente o motivo pelo qual a preservação foi escrita.  6) A METADE DA BRANCH É REGISTRO, NÃO NORMA. As 3 ocorrências de `governanca-porteiro-pre-merge-sol` estão em HANDOFF-CLAUDE-2026-08-20.md:72, log-execucao.md:3751 e status-geral.md:3930 — logs narrativos. Nenhuma regra normativa restringe nome de branch a termo neutro (CLAUDE.md §8.2 só prescreve prefixos feat/fix/chore) e §8.5 manda `--delete-branch` no merge: a string é transitória por contrato.  7) CONSEQUÊNCIA EMPRESTADA. A própria "consequência" atribui o dano a CLAUDE.md:377 e diz "é exatamente essa a cadeia que produziu os achados 2 e 3" — o prejuízo alegado pertence a outros achados. Sem a consequência emprestada, o conteúdo próprio deste achado é um nome de decisão em forma de mapeamento e um nome de branch apagado no merge, com zero leitura executável. O próprio achador concede: "não é defeito de fato hoje".  REDIRECIONAMENTO (reporto, não prescrevo correção): o acoplamento real da dimensão modelo está a duas linhas de onde o achador grepou e ele passou por cima. scripts/porteiro-pre-merge.mjs:89 exige `attestation.runtime === 'codex' && attestation.model === 'gpt-5.6-sol' && reasoningEffort === 'ultra'`, enquanto .claude/agents/porteiro-pos-merge.md:5 prescreve `model: fable` para o MESMO papel no lado Claude; `grep -rn -i 'fable' scripts/ .github/` retorna apenas sync-agent-agents.mjs:56-59 — não existe caminho de aceitação para recibo do lado Claude. Além disso, `grep -rn 'sync-agent-agents' .github/ package.json` não retorna NADA: o guard do espelho não está ligado ao CI, então nada fica vermelho sozinho quando o espelho diverge. São dois achados distintos deste, e os deixo como observação medida para outro planejador.
- **O contrato canônico do Claude Code ordena rodar `gpt-5.6-sol`/`ultra` — modelo que o Claude Code não invoca — e é byte a byte idêntico ao AGENTS.md justamente onde a decisão do dono exige tradução**
  - refutado porque: Os GREPS do achador reproduzem, mas a INFERÊNCIA e a CONSEQUÊNCIA não sobrevivem. Três refutações medidas:  (1) O PONTEIRO ESTÁ DENTRO DO PRÓPRIO BLOCO QUE ELE DIFFOU. O achador diffou `CLAUDE.md` 374-390 × `AGENTS.md` 401-417 e chamou de "identidade onde deveria haver tradução". As linhas 382-386 estão DENTRO desse intervalo e dizem: "Fixado no frontmatter `model:` de `.claude/agents/planejador-mestre.md` (e no espelho `.agents/agents/`), para valer **independente do modelo da sessão**; quem invoca não precisa lembrar." A identidade textual ali é CORRETA porque a frase resolve por CAMINHO, não por literal: no Claude Code resolve para `.claude/agents/` e no Codex para `.agents/agents/`. Isso é exatamente o "mecanismo específico da ferramenta" que o D-INTEROP autoriza a divergir — e ele diverge, nos arquivos, medido: `.claude/agents/planejador-mestre.md:5` e `.claude/agents/porteiro-pos-merge.md:5` = `model: fable`; `.agents/agents/…:4-5` = `model: gpt-5.6-sol` + `reasoning_effort: ultra`. Ele leu a prosa e não seguiu o ponteiro que estava na mesma tela.  (2) "A TRADUÇÃO EXISTE APENAS EM 2 ARQUIVOS DE AGENTE" É FALSO, E NÃO É SÓ DOCUMENTO — É UM TRADUTOR EXECUTÁVEL QUE FALHA FECHADO. `scripts/sync-agent-agents.mjs:56-57` LANÇA erro se o lado Claude não declarar exatamente `model: fable`; :59-60 faz a tradução para `gpt-5.6-sol` + `reasoning_effort: ultra`; :62-64 lança "override de alto raciocínio fora da allowlist" para qualquer outro papel. `node scripts/sync-agent-agents.mjs --check` → exit 0, "OK — 23 agentes, espelho consistente". `tests/agent-model-routing.test.ts` asserta o mapeamento e mata mutação nos dois sentidos. A tradução vive em 4 arquivos de agente + o script tradutor + o teste + `.claude/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md` (papel permanente cujos critérios de VETO são literalmente esta classe: "frontmatter contém identificador não aceito pelo runtime proprietário sem gateway comprovado" e "sync transforma uma diferença específica de ferramenta em configuração inválida na outra"). Isso também derruba a classe (e): há enforcement, e ele roda na CI — `npm test` (ci.yml:81) chama `scripts/run-backend-tests.mjs`, que descobre TODO `tests/*.test.ts` por readdir (:118-132), então os dois testes rodam no CI.  (3) A CONSEQUÊNCIA ALEGADA ESTÁ INVERTIDA: O CAMINHO É FAIL-CLOSED, NÃO SILENCIOSO. O desfecho (a) do achado — "passa `inherit`/default e registra na ata que cumpriu Sol/ultra, sem ninguém ver" — é impossível de duas formas medidas. Primeiro, quem invoca não passa modelo nenhum: o frontmatter liga o subagente a `fable` independentemente do modelo da sessão (é o que CLAUDE.md:383-384 afirma e o que os arquivos fazem). Segundo, recibo mentiroso não passa: `scripts/porteiro-pre-merge.mjs:89` rejeita qualquer atestação cujo `role/runtime/model/reasoningEffort` não seja exatamente `porteiro-pos-merge/codex/gpt-5.6-sol/ultra` (`fail('recibo Codex Sol/ultra inválido')`), e `scripts/merge-authorized-pr.mjs:4,21` chama `verifyAttestation` ANTES de mergear. Rodei `tests/porteiro-pre-merge-governance.test.ts` → 6/6 pass, incluindo o conjunto de mutação `{reasoningEffort:'high'}`, `{model:'gpt-5.6-terra'}`, `{agentId:'dev'}` — todos lançam. Ou seja: se alguém tentar o desfecho (a), o merge BLOQUEIA; nada fica verde em silêncio. E o desfecho (b) — "invoca fable e a ata diverge do contrato" — não diverge: `fable` É o contrato do lado Claude, fixado no frontmatter para o qual o próprio §C7.6 aponta.  O QUE SOBRA (e não inflo): `CLAUDE.md:233` (porteiro) e `CLAUDE.md:451` (checkbox da DoD "em Sol/ultra") nomeiam só o identificador Codex e, diferente de §C7.6, não carregam o ponteiro para o frontmatter. Mas o contexto executável torna essas duas linhas CORRETAS, não impossíveis: para o porteiro, o único recibo que autoriza merge é `runtime: codex` + `gpt-5.6-sol` + `ultra` (porteiro-pre-merge.mjs:89) — o gate pré-merge é, por construção do código, executado no runtime Codex. Resta uma imprecisão de prosa (as duas linhas não dizem ao leitor Claude que o binding do papel está no frontmatter), não uma ordem inexecutável nem degradação silenciosa. Gravidade "bloqueante" está inflada: nada está bloqueado, o espelho está consistente, o tradutor falha fechado e o gate rejeita recibo degradado.  NÃO MEDI / NÃO REFUTEI (registro honesto, é outro achado, em outro arquivo e na direção OPOSTA): existe tensão entre `.claude/agents/porteiro-pos-merge.md:5` (`model: fable`, lado Claude) e `scripts/porteiro-pre-merge.mjs:89` (só aceita `runtime: codex`) — um porteiro instanciado no Claude Code produziria recibo que o verificador recusa. Isso é o código sendo MAIS estrito que a doc, e é fail-closed (bloqueia o merge, não o falsifica). Não é o achado sob ataque e não o avalio aqui.
- **`tools:` é apresentado como o mecanismo da separação de alçadas, mas 14 achadores/vetantes têm `Bash` sem deny-list — inclusive os dois especialistas que declaram em prosa que seu Bash é 'somente para leitura'**
  - refutado porque: A premissa central do achado é falsa como medida, e a consequência alegada está executavelmente bloqueada.  (1) PREMISSA FALSA — nenhum artefato "apresenta `tools:` como o mecanismo da separação de alçadas". `grep -rn "tools:" CLAUDE.md AGENTS.md EXECUTION_MODEL.md` devolve VAZIO. As duas únicas menções normativas no repo dizem o CONTRÁRIO: `agent-orchestration/controle/decisoes.md:786` ("frontmatter portátil (name/description; `tools:`/`model:` do Claude removidos)") e `decisoes.md:1226` ("`tools:` continua saindo (é mecanismo sem equivalente no Codex)"). Isso é decisão registrada do dono, implementada em `scripts/sync-agent-agents.mjs:49` ("Remove `tools:` — a lista de ferramentas é mecanismo do Claude Code e não tem equivalente no Codex") e comprovada no espelho: `.agents/agents/especialistas/guardiao-enforcement-github-porteiro.md` não tem linha `tools:`. A §4-bis é uma regra que tem de valer nas DUAS ferramentas; um campo que uma delas não possui, por decisão registrada, nunca foi nem poderia ser o mecanismo dela. Sem promessa, não há teatro: a classe (b) exige um artefato que AFIRME uma garantia que a execução não produz.  (2) O MECANISMO DECLARADO EXISTE, É EXECUTÁVEL E FALHA FECHADO. `CLAUDE.md:356-357` declara o mecanismo: registro nominal em plano/ata/parecer/fechamento + "acúmulo invalida o fluxo". Executei os três guards e todos bloqueiam: `porteiro-pre-merge.mjs:44-45` (validateJunta) reprova identidade repetida entre origin/planner/developer/votantes; `porteiro-pre-merge.mjs:90` exige `independentOf` sem o próprio `agentId`; `merge-authorized-pr.mjs:24` reprova executor de merge não independente do porteiro. O cenário exato que o achado chama de "circular" — "o agente cujo parecer é a única autorização de merge pode executar o próprio merge" — foi rodado e deu BLOQUEADO.  (3) CONFUSÃO DE CAMADA + ARTEFATO ERRADO. `tools:` é allow-list de NOMES de ferramenta; deny-list é conceito da camada de permissions (settings), não do frontmatter. E o artefato citado, `.claude/settings.local.json:1`, não é artefato de governança: é untracked (`git ls-files --error-unmatch` → "did not match any file(s) known to git"), globalmente gitignorado (`git check-ignore -v` → `C:\Users\AMP/.config/git/ignore:1:**/.claude/settings.local.json`) e referenciado por ZERO markdown do repo. É arquivo por-máquina do dono; não entra em PR, não é lido por junta, não é corrigível pelo fluxo.  (4) GRAVIDADE INFLADA E RISCO MAL ATRIBUÍDO. O risco de merge fora do gate que EXISTE hoje vem do GitHub sem ruleset e sem branch protection (medido: rulesets → `[]`; protection → 404 "Branch not protected") somado a token com escopo `repo`. Isso é (a) independente de `tools:` — tirar Bash do porteiro não fecharia (qualquer humano/agente com o token merge), e o porteiro não consegue cumprir a própria missão sem Bash (`gh api`, `git show`, `scripts/porteiro-pre-merge.mjs`); (b) já fail-closed no caminho roteirizado — `porteiro-pre-merge.mjs:34-35` recusa montar snapshot sem ruleset ativo/strict exigindo `erp/porteiro-pre-merge` (rodado, exit=1); (c) já coberto por papel de VETO dedicado — `guardiao-enforcement-github-porteiro.md:44` ("`main` não tem protection/ruleset efetivo") e `:48` ("o merge permanece tecnicamente possível com parecer ausente"). A ausência de ruleset é outro achado, não prova de que `tools:` seja teatro.  O QUE SOBREVIVE (menor e diferente do reportado, honestidade): os FATOS de contagem reproduzem — 21 agentes têm `Bash`, 18 deles sem `Write`/`Edit`, e Bash escreve arquivo. Não existe nenhum guard executável sobre o campo `tools:` (o único teste que toca `.claude/agents` é `tests/agent-model-routing.test.ts`, que assere `model:` apenas). E as frases "`Bash` é permitido somente para leitura" (`guardiao-enforcement-github-porteiro.md:25`) e "**Não conserte nada**" (`porteiro-pos-merge.md:58`) são prosa não enforçada — mas são INSTRUÇÃO ao agente sob o cabeçalho "## Ferramentas e comandos permitidos", não afirmação de resultado produzido; e as identidades nos atestados são strings autodeclaradas. Isso é observação de higiene de baixa consequência, não "b-teatro/alta" sobre o gate de merge.  NÃO MEDIDO (declaro): não consegui medir se o frontmatter `tools:` do Claude Code aceita entrada com argumento (ex.: `Bash(git status:*)`); nenhum artefato do repo trata disso. Logo a premissa implícita do achador — de que um deny caberia em `tools:` — fica não verificada nos dois sentidos. Também não medi comportamento em sessão interativa com prompt de permissão (rodei em harness não-interativo, onde Bash arbitrário passou sem prompt).
- **`executor-pos-merge` — alçada obrigatória do §C2.10 e da DoD — está untracked nas duas árvores: a lei referencia um agente que não está no repositório**
  - refutado porque: O achado se sustenta apenas na parte trivial (o arquivo está untracked) e cai na parte que dá a ele a classe "a-inexecutavel" e a gravidade alta: a premissa de que "a lei referencia um agente que não está no repositório" é FALSA quando medida.  1) NENHUM contrato normativo nomeia `executor-pos-merge`. Grep repo-wide pelo token: as ÚNICAS ocorrências são os dois próprios arquivos untracked e o plano do ciclo 1. `grep -n executor CLAUDE.md` devolve 3 linhas (348/349 no HEAD, 453 na árvore) e todas dizem "executor pós-merge"/"executor distinto" — nunca o nome do agente. As linhas exatas que o achador citou provam o contrário do que ele afirma: CLAUDE.md:243 e AGENTS.md:270 dizem literalmente "Outro agente, também distinto do porteiro e das alçadas anteriores"; CLAUDE.md:453 diz "executor distinto". Isso é uma restrição de DISTINÇÃO sobre um papel, satisfeita por qualquer agente — não uma dependência de arquivo. A quarta citação, `.agents/agents/README.md:32`, sequer trata de pós-merge: é o item 5 do fluxo, sobre o protocolo de ciclos de reprovação.  2) O invariante que ele diz faltar nunca foi a regra deste repositório, e a violação já existe em `main` por desenho. O índice rastreado `.agents/agents/README.md` nomeia 5 papéis — `omega5p-planejador` (:49 em main), `omega5p-dev-backend` (:56), `omega5p-dev-frontend` (:57), `omega5p-dev-portal` (:58), `omega5p-avaliador` (:66) — e NENHUM tem arquivo no repo (`ls .claude/agents .agents/agents | grep -i omega5p` = vazio). A própria linha :32 que ele cita descreve o modelo oposto ao que ele pressupõe: `agente-fabrica` cria especialistas sob medida em tempo de execução.  3) A consequência alegada ("fechamento pós-merge impossível de executar conforme escrito") é refutada por precedente medido: `git show main:CLAUDE.md | grep -n executor` = ZERO ocorrências, e o fechamento factual pós-merge foi executado assim mesmo (commits 8240525 e adc0cb5, "backfill pós-merge"). Ou seja, o papel roda sem arquivo de agente hoje. Usar um agente distinto sem arquivo é CUMPRIMENTO da §4-bis (que exige distinção), não a "improvisação/acúmulo de alçada" que ele projeta.  4) A seleção do alvo é o inverso do próprio argumento dele. Cruzando artefatos citados LITERALMENTE em CLAUDE.md contra o git: `scripts/kpi-release.mjs` (citado em CLAUDE.md:247) tracked=NAO e `scripts/merge-authorized-pr.mjs` (citado em CLAUDE.md:241) tracked=NAO — os dois únicos artefatos que a lei nova NOMEIA e que faltam no índice; `.claude/agents/executor-pos-merge.md` é citado 0 vezes em CLAUDE.md. Ele elegeu justamente o artefato que a lei não nomeia.  5) Contexto que descaracteriza "brecha de governança": a branch está em voo, com 58 arquivos não commitados, incluindo CLAUDE.md, AGENTS.md, 4 scripts, 3 testes e o workflow do porteiro — todos untracked/modificados exatamente como o executor. Os dois espelhos existem simetricamente, não estão ignorados (`git check-ignore -v` exit=1) e constam do manifesto `GOV-PORTEIRO-PRE-MERGE-ciclo1-allowlist.txt`, além de o plano rastreado (commit 1ce26b2) prometê-los em :92/:133/:136 — isto é, há lista escrita contra a qual o "promessa × diff" do §C2.8 opera. Estado de trabalho não commitado, não defeito de lei.  FATO residual que MEDI e que NÃO é este achado (registro sem prescrever): `checkAllowlist` em scripts/porteiro-pre-merge.mjs:146-149 só falha para path fora do manifesto (`changed.filter(p => !manifest.has(p))`); path do manifesto ausente do diff não falha, embora o cabeçalho do arquivo diga "Manifesto exato do PR". Isso é uma propriedade de outro artefato, de outra classe, e não sustenta a gravidade alta atribuída ao executor. NÃO MEDI se algum agente humano/porteiro efetivamente roda `guard-allowlist` no CI (o workflow está untracked e não o executei).
- **O único índice normativo dos agentes (composição de junta por tipo de PR) existe só do lado Codex, cita 5 papéis que não existem em nenhuma árvore e omite os 3 agentes novos da lei**
  - refutado porque: O achado se sustenta só na parte que ele não usa para concluir. As três pernas que carregam o título, a classe e a consequência caem contra medição.  PERNA 1 — "o único índice normativo (composição de junta por tipo de PR) existe só do lado Codex" e "o Claude Code nunca vê a regra 'invariante financeiro ⇒ crítico obrigatório'": FALSO. Essa regra exata está em `comando-template.md:173-180`, arquivo NEUTRO na raiz, sob o cabeçalho "**Obrigatórios por gatilho (com VETO)**", com os seis gatilhos (RBAC/rota→`coordenador-de-acessos`+`inspetor-de-rotas`; **dinheiro→`critico-adversarial` (adversarial obrigatório)** na :176; secret/env/CORS/TLS→`agente-secops`; migration→`agente-dba-guardiao`; mapa→`avaliador-mapas`; tela→`cognicao-visual`+`master-teste-telas-rotas`). O `CLAUDE.md:9` e o `CLAUDE.md:202-203` declaram esse arquivo como o molde OBRIGATÓRIO de todo comando de bloco do lado Claude, e o `AGENTS.md:539` classifica a dupla `EXECUTION_MODEL.md` · `comando-template.md` como "**Companheiros (valem p/ ambos)** … idênticos". A mesma regra por risco reaparece em `EXECUTION_MODEL.md:279-292` ("### Composição da junta e gate posterior" → "Revisores **por risco**: `agente-secops` obrigatório … `agente-dba-guardiao` em todo PR com migração; `coordenador-de-acessos` em PR de RBAC/navegação"). O achador grepou apenas `CLAUDE.md` e `AGENTS.md` e concluiu ausência a partir de exit=1 num universo de busca de dois arquivos. Além disso o `CLAUDE.md:38` — contrato canônico, lado Claude — aponta explicitamente para "`.agents/agents/README.md`" como o lugar do protocolo de agentes isolados: o caminho físico sob `.agents/` é fonte única apontada pelo contrato canônico, não artefato exclusivo do Codex.  PERNA 2 — "a composição cita 5 papéis que não existem em nenhuma árvore": FALSO como enunciado. A seção "## Composição típica da junta por tipo de PR" (`README.md:98-104`) cita DEZ papéis e os dez existem nas duas árvores (medido). ZERO nomes `omega5p` nela. A citação que o achado atribui ao índice — «'rode `omega5p-avaliador` (VETO)'» — NÃO EXISTE no arquivo: a :100 diz "avaliador (VETO)", genérico. Os 5 nomes `omega5p` vivem só no catálogo de papéis (:23, :55, :62, :63, :64, :72), e 5 das 6 citações se autolimitam a uma rodada encerrada ("na rodada de Pátios", "da rodada Ω5P", "das fatias Ω5P", "(`/patios`)").  PERNA 3 — classe `c-divergencia-indevida`: FALSO. Os 5 arquivos existiram (#280) e foram apagados DELIBERADAMENTE e SIMETRICAMENTE nas duas árvores no commit fca4b32 (#312, "deleção dos 5 agentes efêmeros — FECHA a rodada Ω5P"). Não há divergência entre os espelhos: ambos medem 0/0 nos cinco. E o espelho é enforced — `tests/agent-model-routing.test.ts:8` executa `sync-agent-agents.mjs --check`, que retorna exit=0 "23 agentes, espelho consistente".  CONSEQUÊNCIAS ALEGADAS — as três caem. (a) "ata que nomeia alçadas que ninguém ocupou": a regra operativa de composição está nos neutros, não no catálogo; nenhum gatilho de junta depende dos nomes mortos. (b) "nenhuma junta sabe quando convocar os 2 guardiões": contrariado por lei vigente executável — `CLAUDE.md:347` / `AGENTS.md:374` / `EXECUTION_MODEL.md:306` mandam que os especialistas criados nos ciclos 1–2 "entram na junta seguinte e votam" — e pelo registro `R-GOV-PORTEIRO-PRE-MERGE-ciclo1.md:124-131`, que nomeia os dois com alçada e diz "Definições permanentes em `.claude/agents/especialistas/`, com papéis espelhados em `.agents/agents/especialistas/`". (c) `executor-pos-merge` não depende de tabela de README: é mandatado nominalmente em `CLAUDE.md:354-355`, na DoD `CLAUDE.md:453`, espelhado em `AGENTS.md:381-382`/`:480`, e `comando-template.md:189-192` exige registrar "executor pós-merge `<agente>`" em todo bloco.  RESÍDUO QUE SOBREVIVE (não é o achado): o catálogo de papéis do `README.md` tem 5 linhas mortas (:23, :55, :62-64, :72) e não lista `executor-pos-merge` nem os dois `especialistas/`; o `--check` não olha esse arquivo porque `KEEP = new Set(['README.md'])` (`sync-agent-agents.mjs:27`), então nada fica vermelho. É obsolescência de catálogo, visível igualmente às DUAS ferramentas (o `CLAUDE.md:38` designa o mesmo arquivo para o lado Claude), sem efeito sobre qual junta é convocada. Isso é "baixa", não "media" — e não é divergência indevida.  Observação metodológica: o próprio achado é uma instância da classe que ele persegue — afirma um resultado ("a composição cita papéis inexistentes"; "o Claude nunca vê a regra do crítico") que a execução não produz, com uma citação entre aspas que não está no arquivo.
- **`agente-devops-provisionador` acumula implementar (Write/Edit) e votar na junta numa mesma definição de agente — a §4-bis proíbe exatamente isso**
  - refutado porque: O núcleo factual do achado é verdadeiro e eu o reproduzi: `.claude/agents/agente-devops-provisionador.md:4` declara `tools: Read, Write, Edit, Grep, Glob, Bash` e o mesmo arquivo declara mandato de voto em :3 e :36 — e ele é o ÚNICO dos 23 agentes Claude que combina Write/Edit com mandato de voto. Mas o achado como FILED cai em quatro pontos independentes, três deles medidos.  (1) A EVIDÊNCIA CITADA NÃO REPRODUZ. O achador afirma ter rodado `sed -n '380,381p' CLAUDE.md` e colhido o texto da §4-bis. Rodei: CLAUDE.md:380-381 é o parágrafo do modelo do `planejador-mestre` (D-FABLE), assunto sem relação. A §4-bis está em CLAUDE.md:351-357. O texto citado literalmente é AGENTS.md:380-381. Ou seja, DUAS das cinco âncoras `arquivo:linha` do achado (CLAUDE.md:380 e :381) apontam para o parágrafo errado — ele citou o espelho Codex rotulando-o como o contrato canônico. Isto é exatamente a classe de defeito que este projeto persegue ("o artefato AFIRMA um resultado que a EXECUÇÃO não produz"), agora cometida pelo próprio achador.  (2) A CLASSE `c-divergencia-indevida` ESTÁ ERRADA — e é refutável por medição direta. `grep -rn "^tools:" .agents/agents/ | wc -l` → **0**; `grep -rln "^tools:" .claude/agents/*.md | wc -l` → **23**. O `tools:` é removido DELIBERADAMENTE pelo sync, com a razão escrita no código: `scripts/sync-agent-agents.mjs:49` — "Remove `tools:` — a lista de ferramentas é mecanismo do Claude Code e não tem equivalente no Codex" (repetido no cabeçalho, linha 10). O `diff .claude/agents/agente-devops-provisionador.md .agents/agents/agente-devops-provisionador.md` mostra corpo VERBATIM idêntico; a única diferença de conteúdo é o preâmbulo Codex + a ausência do `tools:`. `node scripts/sync-agent-agents.mjs --check` → "OK — 23 agentes, espelho consistente." Portanto não há divergência indevida entre os contratos espelhados: é precisamente a exceção de "mecanismo específico da ferramenta" que a regra de espelhamento autoriza.  (3) A PROPRIEDADE PEDIDA É ESTRUTURALMENTE IMPOSSÍVEL COMO GARANTIA — mecanismo que o achador não viu. Ele pede "que nenhuma definição de agente concentre Write/Edit e mandato de votar". Como o espelho Codex não carrega restrição de ferramenta NENHUMA (0/23, medido acima), TODO papel votante do Codex pode escrever — inclusive os dois VETO obrigatórios `agente-secops` e `agente-dba-guardiao`. Se a propriedade fosse a garantia de que a §4-bis depende, 100% da frota Codex estaria em violação permanente. Logo o conjunto de ferramentas não é, e não pode ser, o locus de enforcement da §4-bis. O locus real está escrito e vivo: CLAUDE.md:356-357 (= AGENTS.md:383-384) — "Plano, ata, parecer do porteiro e fechamento pós-merge registram nominalmente cada alçada; acúmulo invalida o fluxo".  (4) A CONSEQUÊNCIA ALEGADA É FALSA COMO ESCRITA, e a §4-bis não proíbe o que o título diz que ela proíbe. Lida na íntegra, CLAUDE.md:354-355 diz "Quem planeja não implementa nem aprova; quem escreve não analisa/vota **o próprio diff**" — a norma constrange o ATO condicionado a ter escrito aquele diff, não a CAPACIDADE declarada no arquivo de definição. Uma definição capaz de escrever só vira "quem escreveu" quando escreve. E a afirmação "nenhum revisor posterior tem como distinguir isso de uma composição legítima" é contradita pela cláusula de registro nominal acima: se o mesmo nome aparecer como implementador e como votante na ata, é visível.  GRAVIDADE INFLADA / NUNCA MATERIALIZOU. Levantei todas as 6 atas que nomeiam o agente (J-SAN-4, J-SAN-5, J-SAN-6, J-SAN-PROD-CODE, J-O6R-B01 e o prompt da rodada): em TODAS ele ocupa exclusivamente a cadeira de revisor/votante — em J-SAN-5 "devops-provisionador APROVADO", em J-O6R-B01 (2026-08-16) "6 correções" como cadeira de junta-5 sobre o PLANO. Não encontrei nenhuma ata que o nomeie como implementador. Além disso o próprio arquivo, nas linhas 42-43 que o achador leu mas não pesou, exige "junta de 5 unânime + PD registrado" para deploy de PRODUÇÃO — casando com a parada irredutível do §C7.5.  RESÍDUO QUE SOBREVIVE (e por isso marquei baixa, não "nao-e-defeito"): é fato medido que este é o único papel votante da frota Claude cujo tool set não é read-only, enquanto os 12 demais votantes/VETO são todos read-only, e dois papéis criados DEPOIS da lei (guardiao-fail-closed.md e inspetor-de-arnes-concorrente.md, mtime Aug 19) ganharam a cláusula textual "Achador/votante: não escreve a correção" (.agents/agents/README.md:80-81) que este nunca recebeu. É assimetria de higiene defensiva, não violação da §4-bis. Também confirmei que o único guard executável sobre definições de agente é tests/agent-model-routing.test.ts, que cobre só roteamento de modelo — nada cruza `tools:` com mandato. Isso é verdade, mas vale para a §4-bis inteira e para os 23 agentes, não é específico deste arquivo.
- **O sync exige `model: fable` no lado Claude, mas o contrato canônico CLAUDE.md manda o oposto — seguir o CLAUDE.md ao pé da letra quebra o próprio script com stack trace**
  - refutado porque: O achado sustenta apenas a observacao trivial (duas strings literais diferentes) e cai nos tres elos que dao a ele a gravidade alegada. (1) A premissa "seguir o CLAUDE.md ao pe da letra manda escrever gpt-5.6-sol no frontmatter Claude" e uma inferencia do achador, nao o que o texto diz: CLAUDE.md:382-383 APONTA para .claude/agents/planejador-mestre.md, e as linhas 8-9 desse mesmo arquivo declaram, em texto normativo, que no Claude Code o identificador nativo e `fable` e que o espelho Codex e gerado com gpt-5.6-sol/ultra. O contexto que o achador leu fora torna a linha correta. (2) A afirmacao central de "propriedade que falta" — "o sincronizador e hoje o UNICO artefato do repositorio que conhece o identificador do lado Claude" — e falsa e refutavel por grep: seis artefatos carregam a traducao (dois corpos de agente, o teste de roteamento, o HANDOFF, o plano do ciclo1 e a propria D-FABLE-PARA-GPT-5-6-SOL em decisoes.md:1562-1575). (3) A consequencia alegada — "relaxar o regex apaga a traducao sem que nada fique vermelho" — e falsa e eu a derrubei por drill de mutacao executado: relaxei exatamente esse regex e tests/agent-model-routing.test.ts:21 ficou vermelho com "Missing expected exception"; esse teste esta entre os 250 arquivos que `npm test` expande e que a CI roda em .github/workflows/ci.yml:81. Ou seja, existe mecanismo compensatorio executavel que o achador nao viu, e ele e mutation-guarded. Alem disso, CLAUDE.md:233/377 nao e identidade-sem-traducao: scripts/porteiro-pre-merge.mjs:89 REJEITA qualquer recibo cujo runtime !== 'codex' ou model !== 'gpt-5.6-sol' ou reasoningEffort !== 'ultra', e o recibo obrigatorio gerado pelo sync (linhas 65-68) exige o mesmo — o contrato descreve a execucao exigida, que e verificada, enquanto `model: fable` e o identificador-fonte consumido pelo transform. E o caminho de degradacao e fail-closed (throw, exit !=0, teste vermelho), o oposto do silencio que justificaria "bloqueante". Fica de pe apenas um residuo de precisao documental, de classe diferente da alegada (e, nao f): nenhum teste acopla a prosa do CLAUDE.md aos identificadores de runtime — medido, grep -rln "CLAUDE\.md" tests/ so retorna kpi-dashboard-charts.test.ts e porteiro-pre-merge-governance.test.ts, e neste a string e apenas um path de allowlist. Isso e baixa: a divergencia de prosa nao chega a produzir efeito porque o ponteiro do proprio CLAUDE.md leva ao arquivo que carrega a traducao e o guard falha fechado. Registro tambem, sem prescrever nada, um fato colateral que medi: tests/agent-model-routing.test.ts:15 invoca `rg` por execFileSync e nesta maquina Windows sai 'spawnSync rg ENOENT', deixando o teste 1 vermelho por motivo ambiental — defeito distinto e nao relacionado ao achado sob ataque.
- **Divergência de `tools:` na origem é invisível ao --check enquanto o preâmbulo gerado afirma que os poderes são idênticos aos do Claude Code**
  - refutado porque: O mecanismo que o achador descreve REPRODUZ (a cegueira do --check a `tools:` é fato), mas a leitura que transforma isso em "teatro" NÃO se sustenta. Quatro derrubadas, todas medidas:  1) A FRASE FOI LIDA FORA DE CONTEXTO. O achado ancora em scripts/sync-agent-agents.mjs:33 ("os poderes ... são idênticos aos do Claude Code") e trata "poderes" como allowlist de ferramentas. O próprio artefato define "poderes" como o que o CORPO VERBATIM carrega, em três lugares independentes: scripts/sync-agent-agents.mjs:9 ("MESMO corpo (o system-prompt do papel, VERBATIM — os poderes de VETO/adversarial não podem sofrer drift)"), agent-orchestration/controle/decisoes.md:786 ("corpo VERBATIM ... instruções e poderes de VETO/adversarial NÃO sofrem drift") e .agents/agents/README.md:8. E a linha SEGUINTE do próprio preâmbulo (scripts/sync-agent-agents.mjs:34-35) entrega explicitamente o nível de mecanismo ao Codex: "Onde o texto citar mecanismos do Claude Code (ferramenta Agent, caminhos .claude/, invocação de subagentes), use o equivalente do Codex". O parágrafo diz o oposto de paridade de ferramenta — é diretiva de ALÇADA/VETO ao leitor Codex.  2) A AFIRMAÇÃO TEM GUARD EXECUTÁVEL — o achador não o exercitou. Sob o referente correto (corpo), o --check FALHA em drift: mutei o corpo do critico-adversarial e obtive `[agents-sync] DIVERGE: .agents/agents/critico-adversarial.md`, EXIT_BODY=1. Logo a frase de :33 não é "não condicionada a nada verificável" como o achado alega — é condicionada exatamente ao que ela nomeia.  3) A FRASE AFIRMA PARIDADE, NÃO PLENITUDE — e no cenário degradado o erro apontaria para o lado errado. `grep -rn "^tools:" .agents/agents/` → 0 ocorrências nos 23 espelhos: o lado Codex nunca teve restrição de ferramenta. Se o critico do Claude caísse para `tools: Read`, o dano ("revisor de VETO vira revisor de releitura") ficaria INTEIRAMENTE no lado Claude, e nenhum texto em .agents/ o causa ou o esconde. O espelho não "afirma paridade total de poderes"; afirma equivalência ao lado Claude, seja ele qual for.  4) NADA É OCULTADO E NADA FOI PROMETIDO. `git ls-files .claude/agents` lista critico-adversarial.md entre 24 arquivos RASTREADOS — uma redução de `tools:` aparece como diff no PR; o --check nunca reivindicou validar capacidade do lado Claude (contrato declarado em scripts/sync-agent-agents.mjs:16: "só verifica; sai !=0 se divergir"). E `grep -rn "tools:" CLAUDE.md AGENTS.md` → ZERO ocorrências: nenhuma norma viva exige Bash/Grep para papel de veto. A propriedade "faltante" não é promessa contrariada por artefato — é guard novo sendo proposto. A remoção de `tools:` é, ainda, a exceção que a própria regra de espelhamento concede (mecanismo específico da ferramenta), documentada em decisoes.md:786.  O QUE SOBREVIVE, EM OUTRA ÂNCORA (não é este achado): CLAUDE.md:361 e CLAUDE.md:369 ("revisores independentes reexecutam e votam" / "Releitura não pega; só execução por um agente que não escreveu a correção pega") são regra normativa sem enforcement executável sobre as definições de agente — classe (e), não classe (b), e ancorada em CLAUDE.md, não no preâmbulo do sync nem no --check. Registro para roteamento; não é a correção deste achado e não a prescrevo.  NÃO MEDIDO (hipóteses da cadeia de consequência do achador, que ele também não mediu): (i) se o runtime do Claude Code de fato retira Bash quando `tools:` é estreitado; (ii) se o runtime do Codex concede toolset pleno a papel sem `tools:`. Ambas são semânticas de harness que não consigo executar aqui.  INCIDENTAL (fora do escopo do achado, medido): tests/agent-model-routing.test.ts:15 depende do binário `rg` e falhou nesta máquina com `spawnSync rg ENOENT` — subteste 1 vermelho por ambiente, não por drift.
- **A posição que mais acumula alçadas — o orquestrador — não existe na lei: a palavra tem ZERO ocorrências nos dois contratos, e a lista de alçadas de §C7.4-bis não a contém**
  - refutado porque: O fato literal reproduz (0 ocorrências de "orquestrador" em CLAUDE.md/AGENTS.md; §C7.4-bis não a lista), mas as quatro colunas que sustentam o achado caem na medição, e sem elas o defeito não é o que o título afirma.  (1) A classe está errada. §C7.4-bis TEM enforcement executável e ele DISPARA: scripts/porteiro-pre-merge.mjs:44-45 rejeita identidade repetida no conjunto [origin, planner, developer, votos]. Medi: `origin=='dev'` → throw "identidade repetida em alçadas incompatíveis da junta"; `developer` como votante → mesmo throw. O subteste em tests/porteiro-pre-merge-governance.test.ts:12 trava a regra e rodou verde (6/6). Somado a isso, .claude/agents/porteiro-pos-merge.md:38-40 manda um porteiro independente (Sol/ultra, sem alçada anterior) conferir "origem, planejamento, desenvolvimento, revisores/votantes, este porteiro" e bloquear em nome repetido — e §2/§3 do mesmo agente proíbem aceitar relato de terceiro, obrigando reexecução. Nomear a palavra "orquestrador" na lista não acrescentaria checagem nenhuma: a tupla já casa por identidade declarada, não por rótulo.  (2) A premissa "a posição não é enumerada, logo seu acúmulo é invisível" inverte como a lei está escrita. §C7.4-bis é escrita por FUNÇÃO e é universal ("ninguém atesta o próprio trabalho"; "Passes sequenciais do mesmo agente com nomes diferentes não satisfazem a regra"). A sessão que despacha, ao achar, OCUPA a alçada "achador/origem" e fica presa a ela — e a prática registra isso nominalmente: R-B-O6R-01-ciclo1.md:7 diz literalmente "**Achador:** orquestrador", sob cabeçalho que invoca D-JUNTA-SEPARACAO-DE-PAPEIS e declara "Não propõe correção". É o registro nominal que §4-bis exige, não a sua ausência.  (3) A consequência "o próximo elo recebe um registro que se declara conforme" é desmentida pela própria evidência citada. J-CHK-04C-EMENDA:28 registra que os dois erros de fato da pauta são do orquestrador e "foram apanhados pelos próprios votantes"; :127 registra a crítica de processo da dissidência como procedente. Isso é o mecanismo funcionando e sendo publicado, não invisibilidade.  (4) O "já usado como escape" é pré-lei. J-OMEGA3F-3B tem data 2026-07-15 (linha 3; commit 5a1b433 de 2026-07-15), 33 dias antes de D-JUNTA-SEPARACAO-DE-PAPEIS (decisoes.md:1430, 2026-08-17) e 36 antes do TODO-FLUXO (decisoes.md:1549, 2026-08-20). E o comportamento que ela documenta — reprovação do fid-avaliador consertada direto — é exatamente o que a lei nova fecha por função ("(b) quem achou é quem consertou? — se sim, o ciclo está contaminado"). Episódio anterior, já coberto pela lei que o achado chama de deficiente, não é prova de brecha viva.  (5) "Não existe na lei" é escopo de grep estreito. Grepei o corpo normativo vivo: decisoes.md tem 6 ocorrências e pendencias.md 5, incluindo uma cláusula que LIMITA a posição em decisão do dono (nível 1 de A1) — decisoes.md:1410 "mensagem de orquestrador não é consentimento do dono (§A1/§A5)" — um dever atribuído em decisoes.md:1569 e outro em pendencias.md:1674.  O que sobra, e sobra pequeno: as ocupações "redator da pauta/ata" e "decisor de escopo" não são slots enumerados, e medi um resíduo adjacente que o achador não citou nem alegou — com `origin` OMITIDO o buildSnapshot passa (o `.filter(Boolean)` da linha 44 descarta o campo ausente), o que vale para qualquer achador não declarado e não é específico da posição que orquestra. Isso é lacuna de redação com enforcement adjacente presente, não gate ausente: nada do que o achado alega como consequência (acúmulo invisível, ata autocertificada aceita sem reexecução, escape em uso) se sustenta sob comando executado. Bloqueante, não é. Baixa.  Não medi: se o porteiro humano-independente do lado Codex, na prática das últimas rodadas, chegou a reprovar alguma junta por essa tupla — só medi que o código e o teste que a implementam existem e disparam. Também registro como fato que scripts/porteiro-pre-merge.mjs e tests/porteiro-pre-merge-governance.test.ts estão UNTRACKED neste working tree (`git status --porcelain` → `??`), ou seja, o enforcement existe e executa mas ainda não está commitado nesta branch.
- **A cláusula fail-closed ('sem agentes isolados suficientes, a entrega bloqueia') só existe no AGENTS.md/Codex — o CLAUDE.md, contrato canônico, não a tem; e o caso real de esgotamento aconteceu do lado Claude e o fluxo seguiu**
  - refutado porque: O achado não se sustenta em nenhuma das três pernas que o compõem.  1. A PREMISSA CENTRAL É FALSA POR MEDIÇÃO. O achador afirma que a garantia só existe do lado Codex. `diff` dos blocos §4-bis dos dois contratos retorna IDENTICOS: CLAUDE.md:351-357 e AGENTS.md:378-384 são byte-idênticos e contêm exatamente a propriedade que ele diz faltar — "Passes sequenciais do mesmo agente com nomes diferentes não satisfazem a regra" e "acúmulo invalida o fluxo", mais "o porteiro e o executor pós-merge não participaram de alçada anterior" (CLAUDE.md:355). O caso de esgotamento só vira violação quando a alçada anterior absorve a seguinte — que é precisamente "acúmulo", já coberto. O achado nasceu de um grep enviesado: ele buscou o FRASEADO do Codex ("agentes isolados", "entrega bloqueia") dentro do CLAUDE.md, que expressa a mesma regra com outras palavras. Buscar o sinônimo errado não é ausência de regra.  2. A CLÁUSULA LITERAL EXISTE, E EM FONTE DE RANK SUPERIOR — o achador nunca a grepou. `grep -rn "a entrega bloqueia" --include=*.md .` mostra a frase em decisoes.md:1560, dentro de `D-JUNTA-SEPARACAO-DE-PAPEIS-TODO-FLUXO`, a decisão que o próprio CLAUDE.md:351-352 cita nominalmente. Pelo §A1 do CLAUDE.md (linhas 46-52), decisão do dono é rank 1 e os dois contratos são rank 2. Isso inverte a conclusão do achado: a frase fail-closed vincula o Claude Code com autoridade MAIOR que a do AGENTS.md. A tese "a versão canônica é a mais fraca das duas" está medida como falsa — o CLAUDE.md herda a cláusula da fonte que está acima dele.  3. A CLASSIFICAÇÃO (c) ESTÁ ERRADA NO MÉRITO: a frase do AGENTS.md:155 está dentro da seção cujo cabeçalho, em AGENTS.md:142, é literalmente "**Skills e subagentes do Codex (mecanismo — mesma FUNÇÃO do Claude Code):**" — a categoria em que a regra de espelhamento PERMITE divergir. Ela não introduz regra nova: cita a mesma decisão e a aplica ao mecanismo Codex, que adota arquivo de papel como system-prompt e não tem isolamento nativo de subagente. É reafirmação local de lei compartilhada, não lei exclusiva de um lado.  4. A EVIDÊNCIA DE CONSEQUÊNCIA É TEMPORALMENTE IMPOSSÍVEL E LIDA FORA DE CONTEXTO. O R-chk-p1-pr04c-a-ciclo3.md é datado 2026-08-14 e o merge #352 é de 2026-08-15 (`git log -1 7fada65`); a lei base é de 2026-08-17 e a ampliação de 2026-08-20. Um evento não pode ser prova de que uma lei três a seis dias posterior falha em vincular. Além disso o arquivo é registro histórico de REPROVAÇÃO, não norma viva, e o próprio texto que o achador cita continua, na seção "Estado": "Sem o veredito, o merge não acontece" — a acumulação foi declarada em ata e a medição foi despachada a outra parte antes do PR. Isso é a regra funcionando como detector, não sendo contornada; o achador citou as linhas 8-11 e parou antes da linha que desmonta a leitura dele.  Residual honesto, que NÃO é este achado: a assimetria de FRASEADO entre os dois arquivos é real (a frase literal aparece em AGENTS.md e não em CLAUDE.md). Mas assimetria de redação com identidade de norma, com a norma mais forte hospedada na fonte de rank 1 citada por ambos, não é "divergência indevida" nem tem a consequência alegada. Não medi se existe guard executável para a colisão de alçadas — isso é a classe (e), outro achado, e não sustenta este.
- **Nenhuma ata do repositório nomeia quem a redigiu, e nenhum teste ou script lê ata/reprovação real: a única checagem de acúmulo é unitária sobre fixture sintética**
  - refutado porque: O achado tem duas metades; a que sustenta a classe (e-sem-enforcement) e a gravidade (alta) é FALSA por execução, e a que sobra afirma uma exigência que a lei não faz.  1) A EVIDÊNCIA CITADA NÃO REPRODUZ. O achador reporta `grep -rln "omega/juntas\|docs/juntas" tests/ frontend/tests/` → "(sem saída)". Reexecutado literalmente, retorna `tests/vehicle-identity-schema.test.ts` (rc=0). O grep dele estava errado sobre o próprio resultado.  2) A AFIRMAÇÃO CENTRAL — "nenhum teste ou script lê ata/reprovação real: a única checagem de acúmulo é unitária sobre fixture sintética" — É FALSA. A checagem de acúmulo vive em `scripts/porteiro-pre-merge.mjs:39-46` (`validateJunta`) e roda sobre dado VIVO em dois caminhos de produção:  - `scripts/porteiro-pre-merge.mjs:106-111` (`liveState`) busca a ata da junta real no PR via `gh api repos/<r>/issues/<n>/comments`; sem o comentário `erp-junta-attestation:v1` ele falha ("comentário externo da junta ausente", linha 108).  - `scripts/porteiro-pre-merge.mjs:122`: `if (arg('--state')) fail('publish não aceita fixture')` — o caminho que publica o check REQUERIDO recusa fixture por construção; `publish` chama `buildSnapshot(await liveState(...))` na linha 128.  - `scripts/merge-authorized-pr.mjs:48-52` relê a ata real (`parseMarked(comments,'erp-junta-attestation:v1')`), reconstrói o snapshot (→ `validateJunta`) e só então executa o merge CAS na linha 53. O teste unitário sobre fixture é o teste do validador; o validador em si opera sobre a ata real.  3) A CONSEQUÊNCIA — "nenhum comando fica vermelho se a afirmação for falsa" — É FALSA, medida. Com `origin === planner` (acúmulo exato que ele diz não ser checado), o gate fecha: rc=1, "identidade repetida em alçadas incompatíveis da junta" (`porteiro-pre-merge.mjs:45`). Fail-closed: sem snapshot não há atestado, sem atestado o check requerido `erp/porteiro-pre-merge` nunca é publicado, e o ruleset (`.github/rulesets/main.template.json`, `strict_required_status_checks_policy: true`, `bypass_actors: []`) barra o merge.  4) "todos os casos usam a fixture sintética de :8; nenhum lê arquivo" É FALSO para 2 dos 6: `tests/porteiro-pre-merge-governance.test.ts:15` faz `readFileSync('.github/workflows/porteiro-pre-merge.yml')` e `readFileSync('.github/rulesets/main.template.json')` — artefatos reais do repo, não fixture.  5) A PREMISSA "a ata não registra as alçadas" É FALSA nas atas escritas sob a lei nova. `agent-orchestration/omega/juntas/J-O6R-B01-ciclo2.md:8` abre com "## Papéis do ciclo (`D-JUNTA-SEPARACAO-DE-PAPEIS`) — a ata é inválida sem isto" e uma tabela nominal (Acharam / Planejou / Desenvolveram / Votaram, linhas 12-16). Idem `J-O6R-B01-ciclo3.md` e 3 reprovações. Exatamente o que CLAUDE.md:357/361 exige.  6) O que sobra — o REDATOR não é nomeado — não viola regra nenhuma: CLAUDE.md:354-355 enumera as alçadas ("achador, planejador, desenvolvedor, cada analista/revisor, cada votante, porteiro pré-merge e executor pós-merge") e "redigir a ata" não está entre elas. O achador exige uma propriedade que a norma não contém e depois classifica a ausência dela como lei sem enforcement.  7) A CITAÇÃO DELE JOGA CONTRA ELE. `J-CHK-04C-EMENDA-deliberacao-j6r.md:28` diz: "Ambos os erros são do orquestrador que redigiu a pauta, e ambos foram apanhados pelos próprios votantes." A autoria FOI declarada e os votantes independentes PEGARAM os dois erros. É o mecanismo compensatório funcionando e sendo registrado — não uma parte não identificada passando sem checagem.  RESIDUAL que MEDI e que NÃO é o achado (registro sem prescrever correção): `scripts/porteiro-pre-merge.mjs:69` usa `state.juntaBlobOid || null` — o vínculo entre o JSON atestado e o markdown da ata é opcional e nunca verificado (snapshot sem `juntaBlobOid` passa com `"blobOid": null`). E `gh api repos/:owner/:repo/rulesets` retorna `[]`, e o PR #357 mergeado tem 0 ocorrências de `erp-junta-attestation:v1` — o gate está implementado e fail-closed no código, mas ainda não armado em produção (branch `docs/governanca-porteiro-pre-merge-sol` não mergeada). São achados distintos, com conteúdo distinto do que este achado afirma.
- **O preâmbulo carimbado em TODO agente espelhado manda o Codex 'adotar' o papel — que é emulação sequencial — três linhas antes de declarar emulação sequencial inválida**
  - refutado porque: O achado descreve como defeito VIVO exatamente o texto que este branch DELETOU, e inverte a ordem de leitura do parágrafo que cita.  1) O escape hatch de emulação sequencial EXISTIA e foi REMOVIDO neste diff. `git diff main -- scripts/sync-agent-agents.mjs` mostra as linhas apagadas: `Se você não puder criar subagentes isolados, **EMULE** este papel num passe adversarial próprio`. E `git diff main -- .agents/agents/README.md` apaga o cabeçalho `## Como o Codex usa estes papéis (protocolo de emulação da junta)` e o parágrafo `**Se não puder (caminho de emulação — sempre válido):** ... Emule assim, num único fluxo, **adotando um papel de cada vez** (carregue o arquivo do papel como se fosse o seu system-prompt naquele passe)`. `git grep -i "EMULE\b"` no repo inteiro retorna ZERO. O achado reporta como contradição residual o texto cuja remoção é o objeto do PR.  2) TODOS os marcadores de sequencialidade estavam no texto deletado — `num único fluxo`, `um papel de cada vez`, `naquele passe`, `passes de veto`. CLAUDE.md:356 define o ato proibido como `Passes sequenciais do mesmo agente com nomes diferentes`: dois elementos, (a) passes sequenciais e (b) mesmo agente. A frase remanescente `Adote as instruções abaixo como o seu system-prompt ao atuar como <name> na junta` não contém nenhum dos dois — nomeia UM papel, sem `passe`, sem `de cada vez`, sem `mesmo agente`. Dizer que o corpo abaixo é o system-prompt é descrição verdadeira da configuração de um subagente ISOLADO (é literalmente o que um subagente isolado recebe), não de troca de chapéu.  3) A moldura do achado ("três linhas antes") inverte a ordem real. O preâmbulo tem 6 linhas: `Adote as` é a linha 1 e a proibição é a linha 6 — a ÚLTIMA do mesmo bloco, imediatamente colada ao corpo do papel. A cláusula desambiguadora tem a palavra final, não a anterior.  4) O contexto do README que o achador citou mas não pesou: o TÍTULO da seção (:14) é `## Como o Codex usa estes papéis (agentes isolados obrigatórios)`; a frase reitora (:16) é `Invoque cada arquivo como a definição de um subagente isolado`; e (:19) `Sem agentes isolados suficientes, a entrega bloqueia; trocar o nome do papel não cria independência` — fail-closed explícito, que antecipa e mata a leitura do achado. Os passos 4/7/8 dizem `cada voto pertence a pessoa/agente distinto`, `novo agente`, `outro agente distinto`. `adote X` nos passos 1-3 é abreviação sob essa regra reitora.  5) A consequência alegada — "a instrução mais próxima do executor diz para ADOTAR" — é mensuravelmente falsa quanto à ordem de carregamento. O contrato-raiz que o Codex lê ANTES de abrir qualquer arquivo de papel é o AGENTS.md, e AGENTS.md:153-155 já diz `Invoque cada arquivo como a definição do subagente. A emulação sequencial pelo mesmo agente foi revogada por D-JUNTA-SEPARACAO-DE-PAPEIS-TODO-FLUXO; sem agentes isolados suficientes, a entrega bloqueia.` AGENTS.md:536 repete na tabela de paridade. O executor encontra a regra fail-closed primeiro, não o verbo `adote`.  6) Zero ocorrências permissivas: `grep -rni "emul"` no conjunto normativo vivo (CLAUDE.md, AGENTS.md, README do espelho, script, arquivo renderizado) devolve 6 ocorrências e as 6 são REVOGAÇÕES. Não há dois documentos dizendo coisas opostas — requisito da classe f-contradicao. Ambos dizem a mesma coisa; só compartilham um verbo abreviado.  7) A variante dos especialistas é ainda menos ambígua: `.agents/agents/especialistas/guardiao-interoperabilidade-modelos-claude-codex.md:8` diz `Adote as instruções abaixo como system-prompt DESTE PAPEL` — descrição do arquivo, não ordem ao orquestrador.  O QUE EU NÃO MEDI (honestidade): não observei uma sessão real do Codex; medi apenas o texto. E confirmei que NÃO existe enforcement executável do vocabulário — `grep -n "emul|adote|Adote|isolado|PREAMBLE|preamb" tests/agent-model-routing.test.ts tests/porteiro-pre-merge-governance.test.ts` devolve vazio, então nenhum teste ficaria vermelho se o escape hatch voltasse. Isso é uma lacuna de classe (e) sobre enforcement de redação, NÃO a contradição de classe (f) alegada, e não sustenta a consequência afirmada. Registro também que `node --test --import tsx tests/agent-model-routing.test.ts` falha no subteste 1 no meu shell com `spawnSync rg ENOENT` (ripgrep fora do PATH) — falha de ambiente, alheia a este achado; não obtive a suíte de governança verde localmente.
- **A lei do porteiro §C2.8 e a DoD §10 estão byte-a-byte idênticas nos dois contratos espelhados e dizem `gpt-5.6-sol` também no lado Claude, contradizendo o próprio agente Claude que diz `fable`**
  - refutado porque: REFUTADO no diagnóstico e na consequência. A evidência factual do achador reproduz (a identidade byte-a-byte existe, CLAUDE.md diz `gpt-5.6-sol`, o frontmatter Claude diz `fable`), mas a leitura dela está errada porque o achador não mediu o mecanismo que a §C2.8 realmente descreve.  (a) A identidade é CORRETA sob D-INTEROP-CLAUDE-CODEX, não uma cópia onde cabia tradução. A cláusula autoriza divergência "apenas quando forem estritamente específicas da ferramenta" (CLAUDE.md:25-27). O porteiro §C2.8 não é invocação de subagente — é um recibo externo `erp-porteiro-attestation:v1` validado por `scripts/porteiro-pre-merge.mjs:89`, que só aceita `runtime==='codex' && model==='gpt-5.6-sol' && reasoningEffort==='ultra'`. Esse mecanismo é ÚNICO e vale igual para quem quer que orquestre; logo os dois contratos devem enunciá-lo idêntico. Não há "vocabulário do lado Claude" a ser usado aqui: `gpt-5.6-sol` não é um argumento de invocação, é um VALOR DE CAMPO num schema que um guard confere.  (b) A propriedade que o achador diz faltar produziria justamente o defeito que ele persegue. Executei `verifyAttestation` com um recibo `fable`: REJEITADO com `recibo Codex Sol/ultra inválido`, tanto com runtime `claude-code` quanto `codex`. Se o CLAUDE.md fosse "traduzido" para mandar `fable`, o contrato canônico passaria a afirmar um resultado que a execução rejeita — artefato afirmando o que a execução não produz, com guard verde contra ele (`tests/porteiro-pre-merge-governance.test.ts:13` já exige throw para qualquer `model` diferente).  (c) A consequência alegada não se sustenta. O item de DoD (CLAUDE.md:451) não pede que o Claude Code INVOQUE Sol/ultra; pede que "porteiro pré-merge independente em Sol/ultra [tenha] emitido autorização literal". Isso se marca CONFERINDO um recibo publicado — `node scripts/porteiro-pre-merge.mjs verify --snapshot --attestation` — operação que qualquer das duas ferramentas executa sem invocar modelo nenhum. Nenhuma linha de CLAUDE.md manda o Claude Code spawnar o porteiro (grep completo de "porteiro" no arquivo, item 3 da evidência). Não existe, portanto, o "agente Claude que procura gpt-5.6-sol e não encontra" nem o checkbox impossível de marcar honestamente.  (d) A analogia de sustentação do achador não transfere. `tests/agent-model-routing.test.ts` governa o FRONTMATTER DE SUBAGENTE — mecanismo esse sim específico da ferramenta (`.claude/agents/` spawna, `.agents/agents/` é espelho) — e `scripts/sync-agent-agents.mjs:56-59` mostra que `model: fable` no lado Claude é o TOKEN-FONTE que o transform traduz. Dizer "o repositório sabe traduzir e não traduziu aqui" confunde dois mecanismos distintos. Correção factual adicional: esse teste NÃO passa nesta árvore como o achador afirma — falha com `spawnSync rg ENOENT` (ripgrep ausente do PATH). As assertions se sustentam quando conferidas à mão, mas a citação dele como "prova executada" não reproduz literalmente.  RESIDUAL QUE NÃO SUSTENTA ESTE ACHADO (registro, sem prescrição): existe uma tensão real entre `.claude/agents/porteiro-pos-merge.md:5` (`model: fable`) mais o cabeçalho :9-10 ("No Claude Code, o modelo nativo é **`fable`**") e o guard `scripts/porteiro-pre-merge.mjs:89`, que rejeita qualquer recibo que não seja codex/gpt-5.6-sol/ultra. Medi isso e ele aponta na direção OPOSTA à do achado: o mecanismo executável exige Codex/Sol, não `fable`. Portanto não pode ser usado para salvar a tese "CLAUDE.md deveria dizer fable" — se for defeito, é outro defeito, em outros arquivos, e cabe a outro achador levantá-lo.  Não medi (e digo que não medi): se o Claude Code de fato não consegue invocar `gpt-5.6-sol` — não executei nenhuma chamada de Agent com esse modelo; tomei isso do enunciado. Isso é irrelevante para a conclusão, porque §C2.8 não exige essa invocação do lado Claude.
- **Nada da governança do porteiro tem gate próprio na CI, e o único teste que amarra o papel do porteiro ao modelo certo falha por dependência de binário externo**
  - refutado porque: O sintoma reproduz, mas as DUAS afirmações que sustentam o achado são falsas quando medidas. O achado está classificado como "e-sem-enforcement"; o enforcement existe em três camadas independentes.  1) TÍTULO REFUTADO — "Nada da governança do porteiro tem gate próprio na CI". O achador grepou pela string literal "governance:" (o NOME do script npm) e concluiu ausência do MECANISMO. Existe um workflow dedicado, `.github/workflows/porteiro-pre-merge.yml` (96 linhas, 4 gatilhos: workflow_dispatch publica o atestado; pull_request_target/workflow_run/push INVALIDAM o check quando o head, o conjunto de checks ou a base mudam), e `.github/rulesets/main.template.json:31` põe `erp/porteiro-pre-merge` como required status check, com `strict_required_status_checks_policy: true`, `enforcement: active` e `bypass_actors: []`. Isso é gate próprio na CI, e fecha por padrão. O achador não cita nenhum dos dois arquivos — ambos untracked, como os testes que ele cita.  2) PROPRIEDADE QUE FALTA — REFUTADA POR MUTAÇÃO. Ele afirma que a asserção da linha 16 é "a única checagem executável que amarra o porteiro ao roteamento de modelo correto" e que "a garantia 'nenhum outro papel recebe Sol/ultra' não é exercida localmente". Falso nos dois casos. A linha 8 do mesmo teste roda `scripts/sync-agent-agents.mjs --check` sobre os 23 agentes, ANTES do `rg`, e não usa `rg`. Rodei três mutações adversariais e as três ficaram vermelhas na linha 8: (a) `reasoning_effort: ultra` injetado no espelho Codex de papel não-allowlisted -> `DIVERGE`, exit 1; (b) `model: gpt-5.6-sol` na origem `.claude` de papel não-allowlisted -> `throw ... override de alto raciocínio fora da allowlist`, exit 1; (c) arquivo órfão com Sol/ultra só no espelho -> `SOBRA no espelho`, exit 1. Além disso o teste 2 do MESMO arquivo (`ok 2`, PASSA localmente) exercita exatamente essa garantia via `transform()`, com `assert.throws(..., /fora da allowlist/)`. A varredura `rg` das linhas 15-16 é cinto-e-suspensório redundante, não a única checagem.  3) GRAVIDADE INFLADA / CONSEQUÊNCIA INVERTIDA. O defeito residual é real: `rg` aqui é uma FUNÇÃO bash que faz shim para claude.exe, não um binário — `command -v rg` resolve, `execFileSync('rg')` do Node dá ENOENT. Logo `npm test` fica vermelho nesta máquina. Mas isso é bug de portabilidade de uma asserção redundante, e FALHA FECHADO: vermelho, alto, em arquivo e linha nomeados (`tests/agent-model-routing.test.ts:15`). Não abre nenhum buraco de governança — nenhuma das três camadas de enforcement depende de `rg`. A classe perseguida pelo projeto ("artefato AFIRMA resultado que a execução não produz") não se aplica: aqui a execução produz MAIS garantia do que o achado reconhece.  NÃO MEDI: se `rg` existe no runner ubuntu-latest (sem rede). Ficou irrelevante para o veredito, já que a propriedade é enforçada sem `rg`.  Residual legítimo para outro agente tratar, em gravidade baixa: dependência de binário externo numa asserção redundante quebra a bateria local. Não prescrevo a correção (§4-bis).

## O que NAO foi medido (secao obrigatoria)

### dimensao `espelho`

1. NÃO medi se o binário `rg` existe no runner da CI (`ubuntu-latest`). Portanto não sei se o `assert.deepEqual` da allowlist de Sol/ultra em `tests/agent-model-routing.test.ts:15-16` é avaliado em ALGUM ambiente, ou em nenhum. Só medi a ausência de `rg` na máquina do dono (`which rg` → exit 1) e o `not ok 1 ... spawnSync rg ENOENT` local.

2. NÃO consegui executar o Codex nem verificar seu schema de frontmatter. Portanto NÃO medi se `.agents/agents/*.md` com `model: gpt-5.6-sol` / `reasoning_effort: ultra` produz efeito real de roteamento no Codex, ou se é prosa inerte. A própria decisão do dono (decisoes.md:1570-1572) admite: "não existe diretório, precedente nem schema confirmado neste repositório". Todos os meus achados sobre o lado Codex são sobre o TEXTO e sobre o validador em Node, nunca sobre o comportamento do runtime.

3. NÃO rodei a suíte completa (`npm test`, 250 arquivos em `tests/`). Rodei só `tests/agent-model-routing.test.ts` isolado e o `--check` dos dois scripts de sync. Portanto não sei se outros testes tocam o par de contratos por caminhos que meu `grep -rn 'CLAUDE\.md|AGENTS\.md'` não capturou (ex.: caminho construído dinamicamente por concatenação).

4. NÃO mutei nenhum arquivo do repositório para provar por execução que o `--check` do espelho de agentes ignora `especialistas/`. A prova é indireta mas dura: `--check` reporta 23 enquanto `find .claude/agents -name '*.md'` conta 25, e o código em `scripts/sync-agent-agents.mjs:79` usa `readdirSync` não-recursivo.

5. NÃO auditei o conteúdo das 11 skills nem dos 23 agentes linha a linha — só o inventário, o frontmatter dos dois papéis cirúrgicos e o `--check` de cada espelho. Divergência semântica DENTRO de um arquivo de skill sincronizado não seria detectada pela minha medição (nem pelo `--check`, que compara bytes, mas esse compara bytes e portanto pegaria).

6. `gh api repos/:owner/:repo/rulesets` retornou `[]` e a proteção de branch retornou 404 no momento desta auditoria (2026-08-21) com as credenciais desta máquina. NÃO verifiquei se algum ruleset de organização (fora do escopo do repositório) poderia estar aplicando proteção por outra via — `?includes_parents=true` não foi usado na minha consulta.

7. Sobre o achado nº 2 (recibo auto-declarado): provei que `verifyAttestation` aceita um objeto digitado à mão. NÃO investiguei o workflow `.github/workflows/porteiro-pre-merge.yml` a fundo para saber se existe alguma verificação de identidade do autor do comentário (GitHub App) que dificulte a falsificação na prática — só medi que a FUNÇÃO de verificação não tem essa amarração.

### dimensao `modelo`

1) NÃO MEDI se o runner da CI (`ubuntu-latest`) tem o binário `rg` no PATH. O achado 5 está medido apenas na máquina do dono (Windows/Git Bash), onde `rg` é função de shell e não binário spawnável, e `npm test` sai 1. Se a CI tiver `rg`, o guard fica verde remotamente e vermelho localmente — divergência que eu não consegui confirmar sem executar o workflow.

2) NÃO MEDI o comportamento real do harness do Claude Code ao invocar um subagente cujo frontmatter declara `model: fable` — assumi, do enunciado da tarefa, que `fable` é identificador válido do roster (sonnet|opus|haiku|fable|inherit). Todos os achados sobre o lado Claude tratam a PROSA (CLAUDE.md/companheiros) e o gate `verifyAttestation`, não a resolução do frontmatter pelo harness.

3) NÃO MEDI o comportamento real do Codex ao ler `model: gpt-5.6-sol` / `reasoning_effort: ultra` / `model: inherit` no frontmatter de `.agents/agents/*.md`, nem se o Codex sequer consome esse frontmatter (o próprio `.agents/agents/README.md:44` diz que "frontmatter sozinho não é recibo"). A afirmação "o lado Codex emite chamada válida" é HIPÓTESE baseada no mapeamento dado no enunciado e na existência do token no espelho — não em execução do Codex.

4) NÃO MEDI a escrita parcial do espelho quando `transform` lança no meio do laço de `runSync` (sync-agent-agents.mjs:111-114). Medi apenas que `transform` lança para sonnet/opus/haiku; a consequência "deixa o espelho em estado misto" é HIPÓTESE derivada da leitura do laço, não executada (não quis reescrever 23 arquivos rastreados do repositório do dono para provar).

5) NÃO MEDI nada contra o GitHub real: `porteiro-pre-merge.mjs publish`, `merge-authorized-pr.mjs` e `post-merge-finalize.mjs` foram exercitados só pelas suas funções puras exportadas (`verifyAttestation`, `buildSnapshot`, `assertMergeCandidate`, `validateFinalization`). Não confirmei o estado do ruleset de `main`, nem se o check `erp/porteiro-pre-merge` já está requerido, nem o `ERP_PORTEIRO_EXPECTED_APP_ID`. O achado 1 conclui "merge impossível pelo lado Claude" a partir do código dos dois pontos de verificação, não de uma tentativa de merge.

6) NÃO MEDI o conteúdo dos dois especialistas em `especialistas/` além do frontmatter e do preâmbulo divergente — não comparei os corpos linha a linha para dizer se os poderes de VETO já sofreram drift. Medi que o espelho difere de `transform(fonte)` e que `--check` não vê.

7) NÃO AUDITEI a rodada de skills (`scripts/sync-agent-skills.mjs`, `.claude/skills/` ↔ `.agents/skills/`) — está fora da dimensão de modelo, e o arquivo `codex-pr-workflow.md` aparece modificado nos dois lados sem eu ter medido paridade.

### dimensao `agentes`

MEDIDO E LIMPO (registro do que a tarefa pediu e não virou achado): o item 5 do meu mandato — a preservação do identificador legado `porteiro-pos-merge` para um agente que atua PRÉ-merge — está escrita nos DOIS contratos e no próprio arquivo do agente, não só no CLAUDE.md. Evidência: `grep -n "identificador técnico legado" CLAUDE.md` → linha 232; `grep -n "legado" AGENTS.md` → linha 259 (texto idêntico); `.claude/agents/porteiro-pos-merge.md:7-8` ("**Nome técnico legado:** `porteiro-pos-merge` foi preservado para não quebrar referências, mas a decisão `D-PORTEIRO-PRE-MERGE` (2026-08-20) moveu sua atuação para **antes do merge**"); `agent-orchestration/controle/decisoes.md:1581`; `.agents/agents/README.md` (linha da tabela de VETO). O espelho Codex carrega o mesmo aviso verbatim (`.agents/agents/porteiro-pos-merge.md:18`). Não achei brecha aqui.

O QUE NÃO CONSEGUI MEDIR:

1. **Comportamento na CI.** Não tenho acesso ao GitHub Actions desta branch. Não sei se `rg` (ripgrep) existe no runner `ubuntu-latest`; se existir, `tests/agent-model-routing.test.ts` pode passar lá mesmo falhando aqui — o que tornaria o enforcement verde num lugar que o dono não olha e vermelho no lugar que ele olha. Não sei se `npm test` está verde na CI hoje. Medi apenas: (a) o teste falha nesta máquina com `spawnSync rg ENOENT`; (b) o arquivo está untracked, logo nunca esteve num commit e nunca rodou na CI até agora; (c) nenhum workflow em `.github/workflows/*.yml` cita `governance`, `agent-model-routing` ou `sync-agent-agents` por nome — mas `npm test` chama `scripts/run-backend-tests.mjs`, que expande `tests/*.test.ts` em JS, então o teste ENTRARIA na suíte assim que fosse commitado. Não executei a suíte inteira (`npm test`) para confirmar o efeito — levaria minutos e exige Postgres.

2. **O runtime respeita `tools:`?** Não instanciei um subagente com `tools: Read, Grep, Glob, Bash` para verificar empiricamente que ele consegue escrever/comitar/mergear. Provei duas premissas separadamente (Bash escreve arquivo sem Write/Edit; `.claude/settings.local.json` não tem nenhuma entrada `deny`; `gh` 2.89.0 está no PATH), mas a composição delas dentro de um subagente é inferência, não medição direta. É a única parte do achado sobre `Bash` que não é 100% fato executado.

3. **O runtime do Codex aceita `model:`/`reasoning_effort:` em `.agents/agents/*.md`?** Não há runtime Codex nesta máquina. O `decisoes.md:1570-1572` reconhece que "não foi criada chave de frontmatter sem schema nem adapter `.codex/agents/*.toml`: não existe diretório, precedente nem schema confirmado neste repositório" — ou seja, o próprio registro admite que o frontmatter do espelho pode não ser lido por ninguém, e que só a chamada real aplica o override. Não consegui verificar se algum orquestrador Codex de fato passa `model="gpt-5.6-sol"`/`reasoning_effort="ultra"`, nem se algum recibo `agent_id · role · runtime · model · reasoning_effort` já foi emitido — não encontrei nenhum recibo desses gravado em `agent-orchestration/`, mas não fiz varredura exaustiva de todas as atas.

4. **`fable` é resolvível nesta conta?** Não invoquei nenhum subagente com `model: fable` para confirmar que o identificador resolve de fato no Claude Code desta instalação. Tratei-o como válido por constar da lista que o mandato me deu. Se não resolver, os achados 2 e 3 pioram (nenhum dos dois lados teria roteamento efetivo), mas isso é hipótese não medida.

5. **Histórico de execução das juntas.** Não auditei as atas em `agent-orchestration/omega/juntas/` nem `docs/juntas/` para verificar se algum ciclo já registrou nominalmente um agente que não existe no repositório (os 5 papéis `omega5p-*` do índice, por exemplo) ou se algum achador já apareceu depois como corretor. Está fora da minha dimensão (definições de agente), e o volume de atas é grande demais para medir no tempo desta auditoria — mas é onde as consequências dos achados 6, 7, 8 e 10 se materializariam, e recomendo que alguém meça.

6. **`.agents/skills/` × `.claude/skills/`.** Fora da minha dimensão. Note apenas que `scripts/sync-agent-skills.mjs` é um script irmão do que audito e não verifiquei se sofre do mesmo defeito de não-recursividade.

NADA FOI ALTERADO NO REPOSITÓRIO. Todas as mutações rodaram em cópias isoladas no scratchpad (`sb2`, `sandbox`), removidas ao final; `git status .claude .agents scripts` continua idêntico ao estado inicial da branch. Sondas preservadas em C:\Users\AMP\AppData\Local\Temp\claude\c--Users-AMP-Documents-GitHub-ERP-Techsolutios\dc4293a7-881e-40e2-bf60-b8ed2751af16\scratchpad\ (probe-sync-scope.mjs, mutation-e2e.mjs, mutation-fabrica.mjs, preamble-classes.mjs) para quem quiser reexecutar.

### dimensao `sync`

1) NÃO medi se `rg` (ripgrep) existe no runner `ubuntu-latest` do GitHub Actions. Medi apenas que na máquina do dono ele NÃO existe (`which rg` → exit 1) e que por isso `tests/agent-model-routing.test.ts` falha com `spawnSync rg ENOENT` e `npm run governance:check` sai 1. Logo, NÃO posso afirmar se o único enforcement do espelho de agentes está verde ou vermelho na CI — só que está vermelho localmente. Não rodei nenhum workflow do GitHub (sem acesso/execução de CI nesta sessão).

2) NÃO medi se o runtime do Codex aceita de fato as chaves de frontmatter que `sync-agent-agents.mjs:59-60` INVENTA no espelho (`model: gpt-5.6-sol`, `reasoning_effort: ultra`) nem se `fork_turns: "none"` (linha 66) é sintaxe válida ali. Não há runtime Codex neste ambiente. O script escreve essas chaves sem nenhuma validação de aceitação — se o Codex as ignorar, o espelho continua declarando o roteamento e o `--check` continua verde. HIPÓTESE NÃO MEDIDA, deixada explicitamente fora dos achados.

3) NÃO medi se o Claude Code honra `model: fable` no frontmatter de um subagente (não invoquei nenhum agente para inspecionar o modelo efetivo). O contrato exige que o roteamento valha "independente do modelo da sessão" (CLAUDE.md:383-384) e nenhum artefato do repositório observa o modelo efetivamente usado — mas isso é hipótese, não fato medido.

4) NÃO rodei a suíte completa `npm test` (250 arquivos). Provei a INCLUSÃO de `tests/agent-model-routing.test.ts` no conjunto via `expandTestFiles('tests')` e rodei o subconjunto `governance:check`. Portanto não posso afirmar o estado global de `npm test` nesta árvore (que tem 58+ arquivos não commitados de outro bloco em andamento).

5) NÃO consegui provar QUEM criou `.agents/agents/especialistas/*.md`. FATO medido: `transform()` não produz o preâmbulo que esses arquivos têm (5 linhas quebradas, sem a citação de D-JUNTA-SEPARACAO-DE-PAPEIS-TODO-FLUXO e sem a linha de tradução de mecanismos), e nenhum script do repo escreve em subpastas de `.agents/agents/`. HIPÓTESE não medida: que tenham sido escritos à mão. A conclusão que sustento é só a verificável — o `--check` não os cobre.

6) NÃO testei a mutação do lado ORIGEM dentro de `especialistas/` (`.claude/agents/especialistas/*.md`) nem a deleção de um arquivo de origem em subpasta. Pela leitura do código (`readdirSync` não recursivo nas linhas 79/87/95/109) o resultado seria o mesmo verde do drill B/E, mas NÃO executei esses dois casos específicos.

7) NÃO auditei a consistência histórica dos números publicados como prova de paridade (decisoes.md:779 diz "28 arquivos" de skills, decisoes.md:797 diz "36 arquivos"; HANDOFF-CLAUDE-2026-08-20.md:97 diz "22 agentes", hoje são 23). Registrei apenas a contagem atual medida (11 skills / 36 arquivos; 23 agentes verificados de 25 existentes); não investiguei se as divergências históricas têm justificativa registrada.

PROTOCOLO DE RESTAURAÇÃO (cumprido, sem git checkout/stash/clean): backups por `cp` em C:/Users/AMP/AppData/Local/Temp/claude/.../scratchpad/bk antes de cada mutação; restauração por `cp` de volta; conferência por `md5sum` de cada arquivo e por md5 do conjunto da árvore `.agents/skills` (5fe57906d830bb3663c682781655ce4e, idêntico antes e depois). Estado final verificado: `sync-agent-agents.mjs --check` = OK/exit 0, `sync-agent-skills.mjs --check` = OK/exit 0, contagens 26/36/25 inalteradas, `git status --porcelain` das árvores tocadas igual ao inicial. Único efeito colateral: mtimes de `.agents/skills/**` foram atualizados pela re-execução do sync em modo cópia (conteúdo byte a byte idêntico, comprovado por md5).

### dimensao `lei-papeis`

1) NÃO medi o estado vivo do GitHub. Não executei `gh api` nesta sessão (sem garantia de autenticação e sem autorização para chamada de rede), então não sei se o ruleset de `main` está de fato ativo/strict, se `erp/porteiro-pre-merge` está realmente na lista de checks requeridos, nem se alguma PR já carregou um comentário `erp-junta-attestation:v1` real. TODOS os meus achados sobre o gate são sobre o CÓDIGO do gate (executado localmente contra os módulos reais do repo), não sobre a configuração viva. Consequência importante: `git log main --oneline -- scripts/porteiro-pre-merge.mjs` retornou VAZIO — todo esse gate só existe na branch `docs/governanca-porteiro-pre-merge-sol`, não mergeada. Não sei se os buracos que provei são exploráveis hoje ou se são buracos de um mecanismo ainda não ativado.

2) NÃO encontrei — e portanto não pude medir — nenhum log/task-history de invocação de subagentes VERSIONADO no repositório. Não sei se o harness do Claude Code ou do Codex guarda recibos de invocação fora do repositório (transcripts, task history do provedor). Minha afirmação é estritamente: não existe, no repositório, nenhuma trilha que ligue um `agentId` declarado a uma invocação ocorrida, e nenhum script/teste consulta trilha alguma. Se essa trilha existir fora do repo, ela não está ligada a nenhum gate.

3) NÃO executei nada do lado Codex. Não tenho a ferramenta. Tudo que afirmo sobre o comportamento do Codex vem da leitura de `AGENTS.md`, `.agents/agents/**` e dos scripts — não de execução. Em particular, não pude verificar se uma invocação Codex de fato emite `fork_turns`/`model`/`reasoning_effort` ou se isso permanece autodeclaração.

4) NÃO rodei a suíte completa (`npm test`). Executei apenas `npx tsx --test tests/porteiro-pre-merge-governance.test.ts` (6/6 pass), `node scripts/sync-agent-agents.mjs --check` (OK, 23 agentes) e a probe própria em `scratchpad/probe1.mjs`, que importa `scripts/porteiro-pre-merge.mjs` e `scripts/merge-authorized-pr.mjs` REAIS (não cópias). Não descarto que exista, em algum arquivo da suíte que não li, alguma checagem de acúmulo de papel — mas `grep -rln "agent-orchestration" tests/` e `grep -rln "omega/juntas|docs/juntas" tests/` sustentam que nenhum teste lê ata ou reprovação real.

5) NÃO medi a intenção do dono quanto ao orquestrador. É possível que a ausência da palavra nos dois contratos seja deliberada (o orquestrador seria "o dono da sessão", fora da lei de agentes). Reporto o FATO de que a palavra não aparece e o FATO de que os registros o mostram ocupando alçadas — não decido qual leitura é a correta.

6) HIPÓTESE NÃO MEDIDA que deixo explícita e NÃO contei como achado: se o campo `origin` do atestado da junta pretende ser o "achador", isso não está documentado em lugar nenhum. `grep -rn "erp-junta-attestation"` encontra apenas CONSUMIDORES (scripts/porteiro-pre-merge.mjs, scripts/merge-authorized-pr.mjs, scripts/post-merge-finalize.mjs, scripts/configure-main-ruleset.mjs e a fixture do teste) — nenhum produtor, nenhum esquema escrito, nenhuma documentação. Não pude medir a semântica pretendida do campo, e por isso não afirmei que o "achador" está ausente do esquema; afirmei apenas o que medi (ele não está na lista de identidades cruzadas de `validateJunta`).

### dimensao `lei-porteiro`

1. NÃO executei o workflow `.github/workflows/porteiro-pre-merge.yml`. Ele é untracked, não existe no remoto e `workflow_dispatch` só roda a partir do branch default. Todo o achado 10 (tautologia do `ERP_PORTEIRO_EXPECTED_APP_ID`) e a afirmação de que o bootstrap `in_progress` fica pendurado vêm de LEITURA do YAML + leitura de `publishCommand`, não de execução — marquei como hipótese.

2. NÃO tentei mergear nada, nem `gh pr merge`, nem `git push origin main`, nem `git push --dry-run`. A afirmação de que esses caminhos estão abertos hoje é inferida de medições read-only conclusivas (`rules/branches/main` = `[]`, `rulesets` = `[]`, `branches/main.protected` = false, `branches/main/protection` = 404) e do fato de que o token tem escopo `repo`. Não é a mesma coisa que ter mergeado — mas o GitHub não tem outra fonte de proteção além dessas duas.

3. NÃO executei `scripts/porteiro-pre-merge.mjs snapshot|publish` contra o remoto, nem `merge-authorized-pr.mjs`, nem `configure-main-ruleset.mjs plan|apply`. Todos escrevem no GitHub (comentários, check-runs, rulesets, merge) ou dependem de um PR aberto com junta publicada — não há nenhum. Ataquei as FUNÇÕES EXPORTADAS (`buildSnapshot`, `verifyAttestation`, `assertMergeCandidate`, `checkAllowlist`) com fixtures adversariais, que é o mesmo código de decisão que os comandos usam. As probes ficaram em `C:/Users/AMP/AppData/Local/Temp/claude/c--Users-AMP-Documents-GitHub-ERP-Techsolutios/dc4293a7-881e-40e2-bf60-b8ed2751af16/scratchpad/probe.mjs`, `probe2.mjs`, `probe3.mjs`; nada foi escrito no repositório (`git status --porcelain` inalterado, 13 untracked antes e depois).

4. NÃO medi se `rg` (ripgrep) existe no runner `ubuntu-latest` da GitHub Actions. Portanto não sei se `tests/agent-model-routing.test.ts` passaria na CI. O fato medido é só o local: falha com `spawnSync rg ENOENT`.

5. NÃO li integralmente o corpo dos agentes `.claude/agents/porteiro-pos-merge.md` (só frontmatter + grep de `recibo|runtime|model|attestation|LIBERADO`) nem o `.agents/agents/porteiro-pos-merge.md`. A afirmação de que o agente do lado Claude nunca é instruído a emitir o JSON `erp-porteiro-attestation:v1` vem de grep sem ocorrência de `attestation`/`runtime` no arquivo — é forte, mas é ausência por grep, não leitura linha a linha.

6. NÃO auditei a semântica real do GitHub para `required_approving_review_count: 0` combinado com `require_last_push_approval: true` no template (`.github/rulesets/main.template.json:14-17`). Suspeito que a aprovação de último push seja inócua com zero reviews exigidas, mas não medi e por isso não virou achado.

7. NÃO reexecutei a suíte backend completa (`npm test`) — só os três arquivos de teste da governança. Não sei o estado geral vermelho/verde do repositório nesta branch.

8. Os achados 1 e parte do 2 já constam do ciclo 1 (`agent-orchestration/omega/reprovacoes/R-GOV-PORTEIRO-PRE-MERGE-ciclo1.md:33-48`, GOV-C1-001 e GOV-C1-002). Reporto assim mesmo porque MEDI hoje que continuam verdadeiros: a autoria escreveu ferramenta, não mudou o fato remoto, e a ferramenta não entrou no git.

### dimensao `satelites`

1) NÃO medi se o binário `rg` existe no runner `ubuntu-latest` do GitHub Actions. Portanto não sei se `tests/agent-model-routing.test.ts` fica verde ou vermelho na CI — só provei que ele falha por `spawnSync rg ENOENT` nesta máquina e que a dependência não está declarada em `package.json`. O achado nº 3 é fato quanto ao escopo do teste (medido lendo o arquivo inteiro) e quanto à falha local; é HIPÓTESE quanto ao comportamento na CI.

2) NÃO medi o que o harness do Claude Code faz ao receber `model: "gpt-5.6-sol"` numa chamada de `Agent`/`Workflow` — se erra alto, se cai em `inherit` em silêncio, ou se recusa a criação. Não consegui provocar a chamada nesta sessão. A inexecutabilidade do identificador vem do enunciado de modelos disponíveis (sonnet|opus|haiku|fable|inherit), não de execução minha. O caminho de degradação do achado nº 1 é, nessa parte, HIPÓTESE.

3) NÃO validei `.github/workflows/porteiro-pre-merge.yml`, `scripts/configure-main-ruleset.mjs` nem `scripts/post-merge-finalize.mjs` contra a API real do GitHub — não abri PR, não autentiquei `gh`, não consultei rulesets. Toda a evidência sobre atestado/CAS veio de execução local das funções exportadas (`verifyAttestation`) e de leitura do código.

4) NÃO li integralmente `agent-orchestration/codex/log-execucao.md` (3.746+ linhas) nem `agent-orchestration/docs/status-geral.md` (3.950 linhas). Amostrei topo, cauda e todas as ocorrências dos termos-alvo. Ambos são registro append-only datado — VERIFIQUEI E DESCARTEI como não-achado: as menções a "KPIs pós-avaliação humana", "KPIs duplos" e `mobile/flutter_app/Kpis` neles são entradas de 2026-06/07 narrando o que era verdade na época, e `D-KPI-DUPLA-REVOGADA` (decisoes.md, seção "Histórico preservado (§A5)") preserva-as explicitamente.

5) NÃO auditei um a um os ~200 comandos em `agent-orchestration/codex/comandos/B-*.md`. Amostrei B-106, B-107, B-107K, B-108, B-108K, B-110, B-152H: todos são comandos de bloco datados, com a lei da época — VERIFICADOS E DESCARTADOS pelo mesmo motivo (registro histórico, não normativo). Mesma conclusão para `docs/juntas/*.md` e `agent-orchestration/omega/juntas/*.md` (atas de votação: `J-CHK-P1-PR03:18` diz "Verde da junta = merge (§C7.1)", mas é o registro do que a junta decidiu naquela data).

6) VERIFICADOS E CORRETOS (não são achados, registro para não serem re-auditados): `EXECUTION_MODEL.md` §§8/10 e `comando-template.md` já descrevem porteiro pré-merge + fechamento factual distinto; `Kpis/README.md:36` já marca o painel Flutter como histórico apagado; `agent-orchestration/omega/lista-saneamento.md:4-5` e `prompt-rodada-saneamento-infra.md:14` já trazem o banner pré-merge; `.claude/agents/porteiro-pos-merge.md` e `planejador-mestre.md` têm `model: fable` correto e corpo pré-merge correto; `Kpis/kpis-latest.json:79` tem `dual_kpis: false`; `diff -r .claude/skills .agents/skills` retorna 0 (espelhos idênticos, sem path da outra ferramenta vazando); `node scripts/sync-agent-agents.mjs --check` → "OK — 23 agentes, espelho consistente"; `README.md` da raiz não enuncia processo (é doc de como subir a app) e `BUILD_ORDER.md:42` cita a política antiga em moldura explicitamente histórica.

7) NÃO consegui medir se algum agente/orquestrador de fato já tentou invocar `gpt-5.6-sol` a partir do Claude Code e o que aconteceu — não há trilha de invocação com modelo registrado em `agent-orchestration/` que eu pudesse conferir. O contrato exige o registro (`D-FABLE-PARA-GPT-5-6-SOL`: "frontmatters e espelhos registram o modelo, e o corpo registra o esforço"), mas eu não localizei nenhum parecer/ata com recibo de modelo executado para verificar.

## Lacuna de arranjo desta propria varredura

A dimensao `satelites` (lei velha ainda viva em EXECUTION_MODEL.md, comando-template.md, skills) perdeu
**todos** os seus ceticos no limite de sessao. Por regra deste projeto, achado sem cetico nao conta: os
achados dessa dimensao foram **descartados**, nao herdados. Essa superficie segue **NAO AUDITADA** e o
plano do ciclo 2 (§5) manda revarre-la depois da implementacao e antes do voto da junta.
