{
  "name": "@erp-techsolutions/owner-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Owner-portal (PWA público ISOLADO do módulo Pátios) — consulta placa+Renavam. Build separado, zero backend embutido.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
